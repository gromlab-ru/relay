{
  "name": "@gromlab/relay-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Relay: CLI, сервер, MCP и веб-интерфейс для local/workspace",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gromlab-ru/relay.git"
  },
  "type": "module",
  "packageManager": "pnpm@11.18.0",
  "engines": {
    "node": ">=22.13",
    "pnpm": ">=11.18.0"
  },
  "scripts": {
    "dev": "node --import ./apps/server/scripts/environment.mjs ./node_modules/turbo/bin/turbo run dev --filter=@gromlab/relay-server --filter=@relay/web",
    "dev:app": "pnpm run dev",
    "dev:server": "node --import ./apps/server/scripts/environment.mjs ./node_modules/turbo/bin/turbo run dev --filter=@gromlab/relay-server",
    "dev:web": "turbo run dev --filter=@relay/web",
    "dev:ui": "pnpm run dev:web",
    "dev:cli": "node --conditions=tasks-source apps/cli/scripts/dev.mjs",
    "dev:mcp": "pnpm run build:mcp && node --import tsx apps/mcp/src/main.ts",
    "playground": "pnpm --filter @relay/playground --silent run tasks",
    "start": "node --import ./apps/server/scripts/environment.mjs ./node_modules/turbo/bin/turbo run start --filter=@gromlab/relay-server",
    "start:cli": "node apps/cli/dist/cli/main.js",
    "start:mcp": "node apps/mcp/dist/main.js",
    "build": "pnpm run release:check && turbo run build",
    "build:cli": "turbo run build --filter=@gromlab/relay-cli",
    "build:mcp": "turbo run build --filter=@gromlab/relay-mcp",
    "build:server": "turbo run build --filter=@gromlab/relay-server",
    "build:web": "turbo run build --filter=@relay/web",
    "build:core": "turbo run build --filter=@relay/core",
    "build:contracts": "turbo run build --filter=@relay/contracts",
    "lint": "turbo run lint",
    "lint:web": "turbo run lint --filter=@relay/web",
    "typecheck": "turbo run typecheck",
    "typecheck:web": "turbo run typecheck --filter=@relay/web",
    "test": "turbo run test",
    "test:cli": "turbo run test --filter=@gromlab/relay-cli",
    "test:mcp": "turbo run test --filter=@gromlab/relay-mcp",
    "test:server": "turbo run test --filter=@gromlab/relay-server --filter=@relay/server-runtime",
    "test:core": "turbo run test --filter=@relay/core",
    "bench:graph": "node --conditions=tasks-source --import tsx scripts/graph-storage-benchmark.mts",
    "test:contracts": "turbo run test --filter=@relay/contracts",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "docs:check": "pnpm --filter @gromlab/relay-cli run docs:check",
    "skills:install": "npx --yes skills@1.5.26 experimental_install",
    "skills:build": "node --conditions=tasks-source --import tsx scripts/skill-bundle/build.mjs",
    "skills:check": "node --conditions=tasks-source --import tsx scripts/skill-bundle/check.mjs",
    "skills:test": "tsx --tsconfig packages/server-runtime/tsconfig.json --conditions=tasks-source --test scripts/skill-bundle/*.test.mjs",
    "check": "pnpm run release:check && pnpm run release:test && pnpm run format:check && pnpm run skills:check && pnpm run skills:test && pnpm run docs:check && turbo run lint typecheck test",
    "clean": "turbo run clean",
    "package:check": "pnpm run release:check && turbo run package:check --filter=@gromlab/relay-cli --filter=@gromlab/relay-server --filter=@gromlab/relay-mcp && node scripts/smoke-relay.mjs",
    "package:check:cli": "turbo run package:check --filter=@gromlab/relay-cli",
    "package:check:server": "turbo run package:check --filter=@gromlab/relay-server",
    "package:check:mcp": "turbo run package:check --filter=@gromlab/relay-mcp",
    "release:version": "node scripts/release/relay.mjs version",
    "release:check": "node scripts/release/relay.mjs check",
    "release:test": "node --test scripts/release/*.test.mjs",
    "release:notes": "node scripts/release/relay.mjs notes",
    "release:publish": "node scripts/release/relay.mjs publish"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "agent-browser": "0.37.1",
    "esbuild": "^0.28.2",
    "prettier": "^3.6.2",
    "tsx": "^4.20.5",
    "turbo": "2.10.12",
    "zod": "^4.1.0"
  },
  "prettier": {
    "trailingComma": "all",
    "printWidth": 100
  }
}
