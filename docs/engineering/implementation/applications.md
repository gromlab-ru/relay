# Реализация приложений

## Прогресс карточек приложений: 22 сентября 2026

Экран `product-applications` показывает бизнес-прогресс и общий прогресс через внутренний
`application-progress`. `domains/board-tasks/useApplicationTaskProgress` читает сводки
собственной доски по slug: порции по 100, одна версия, до трёх попыток при BOARD_CHANGED.
Адаптер накапливает только четыре счётчика, не кеширует весь список. Бизнес-задачи
определяются по productLinks, выполненные — по done. Сложность чтения линейна числу
задач доски; специализированного серверного агрегата пока нет. SSE и явное обновление
после операций канбана инвалидируют проекцию. REST/SDK и хранение не изменены.

Lint/typecheck/build Web прошли. Проверены обе темы, четыре ширины, пустота/0/100%,
105 задач, отсутствие дублей, SSE в обе стороны и сетевой отказ/повтор; axe — 0 нарушений.
Браузерная проверка и ограничения — в
[досье](../../work/application-card-progress/README.md).

## Карточка доски: 22 сентября 2026

`task-card` использует общее представление TaskCardPreview для оригинала и drag-копии;
только TaskCard подключает sortable. `task-kanban` измеряет исходный DOM в onDragStart:
initial rect dnd-kit в этот момент может быть null. Удалены запасные размеры 288×100,
которые в браузере заменяли фактические 256×168,8. Placeholder получает размеры
активной карточки; transform применяет только перенос, без масштабирования.

Первая цель читается через `domains/product/useProductTargetPreviews` на уровне
колонки: уникальные ID, порции до 100 через действующий адаптер, SWR и SSE.
Число остальных целей берётся из TaskSummary; список всех целей и полные описания
не запрашиваются. REST, SDK и хранение не изменены. Прогресс приёмки и отношения
используют существующие поля сводки. Новых TSX и архитектурных юнитов нет.

Lint, typecheck и сборка Web прошли. В браузере проверены совпадение внешней и внутренней
геометрии при drag/drop, мышь/клавиатура, отмена и фокус после сохранения, обе темы,
четыре ширины, сеть/конфликт/SSE и чтение 101 цели. Физический touch не проверен;
эмуляция подтверждает ручку 44px и синтетические события удержания/отмены.
Фактические сценарии и точка продолжения — [досье](../../work/board-card-design/README.md).

## Проектные фичи и сценарии: 21 сентября 2026

Страницы общего продукта используют принятый дизайн первого этапа: полный Markdown,
якорные разделы, задачи с прогрессом, зависимости и реализации приложений. Общий виджет
`product-requirement` получает содержание от страниц; `product-dependencies` поднят
из страницы имплементации без копии кода. ProductContributions читает адресный список
по target (5 реализаций); фича показывает компактные сценарии (10 с продолжением).

Статусы project readiness, каталога, графа и дерева поднимаются от задач через активные
имплементации к их проектному kind:id; прямые задачи учитываются дополнительно.
FI дополнительно агрегирует активные SI своей фичи и приложения, проектная фича —
все свои проектные сценарии. Статус и состав задач обходят один предметный граф.
Список задач требования также включает задачи приложений. Убрано прежнее сообщение
о ручном переподтверждении готовности.
Подробности и фактические проверки — [досье второго этапа](../../work/project-requirement-progress/README.md).

## Страницы реализаций и готовность по задачам: 21 сентября 2026

В Web переработана страница реализации приложения: полное исходное требование,
собственный вклад, открытый список задач с общим прогрессом и отдельные зависимости.
Якорная навигация и одноколоночная мобильная версия поддерживают длинные описания.
Дерево приложения использует ту же динамическую готовность из Core; ручные селекторы
убраны из редактора вклада и состава. Хранение и ревизии текстов сохранены.

Расчёт и владельцы — [продуктовая модель](product.md), сценарии —
[Web](../../product/applications/web/PRODUCT.md#реализации-приложений).
Core 75/75, HTTP 33/33, Web lint/typecheck/build; браузерные проверки обеих тем,
четырёх ширин, SSE, продолжения, сети и конфликтов — в
[журнале](../../work/application-implementation-progress/JOURNAL.md).

## Удаление сущностей: 21 сентября 2026

Реализованы Core/Contracts/REST/SDK и Web для задач, фич, сценариев, приложений,
реализаций и документов. У каждого вида свой каскад; внешние записи сохраняются
со снятием ссылок. Предпросмотр показывает весь состав, запись проверяет версию и
сохраняет отдельную квитанцию. Общий WAL обеспечивает восстановление до чтений.
[Устройство и совместимость](../entity-deletion.md), [сценарии](../../product/ENTITY-DELETION.md).

В браузере временной базы проверены все шесть точек удаления и переходы, конфликт,
сеть/повтор, отмена и черновик задачи, вложенный Escape, обе темы и четыре ширины.
Исправлены начальный фокус, закрытие родительского окна по Escape и контраст подтверждения.
Актуальные результаты автоматических проверок: [журнал](../../work/entity-deletion/JOURNAL.md).

## Обсуждения и история карточки: 21 сентября 2026

После пользовательской приёмки визуальная часть переработана. ActivityFeed — внутренний
компонент TaskActivity: последовательные группы по дням и общий Mantine Timeline.
ActivityEntry показывает строку журнала (32px для обычного события на широком экране),
DiscussionMessage после дополнительного замечания — сообщение без аватара, с компактной
тональной шапкой метаданных и отдельным содержанием (заголовок/Markdown).
Полные данные загружаются адресно; технические ID полей не выводятся в обзоре.
Обе ленты используют полную ширину таба; ограничение 880px и центрирование удалены.
MarkdownView получил необязательный compact-режим для сообщений и подробностей.
Plain-text изменения показываются как «до → после», Markdown — полностью в сравнении.

Реализованы общие Contracts/Core, REST/SDK, CLI и MCP, три таба Web под названием задачи.
Публикация сохраняет заголовок/Markdown/автора/роль, не меняя ревизию карточки; Web использует
«Оператор». История включает значения до/после, обратные связи и блокеры, критерии, граф.
Хранение v5, раздельные ленты, восстановление WAL и старые события описаны в
[контракте](../../reference/TASK-ACTIVITY.md).

Проверены полные Core 62/62, Server Runtime 33/33, CLI 35/35, MCP 9/9,
скилл 7/7, сборки и typecheck затронутых пакетов.
Web typecheck/lint/build прошли после удаления прежних мёртвых объявлений ProductPage.
В браузере подтверждены публикация, история до/после, подгрузка, сеть/повтор, локальный SSE,
конкурентное изменение содержания, пустое обсуждение, клавиатура и сохранение Markdown
с историей отмены между табами. Просмотрены обе темы и 1440/1024/768/390;
[актуальный журнал](../../work/task-discussions-history/JOURNAL.md).

Последующие разделы фиксируют состояние предыдущих работ; их исходный блокер ProductPage
устранён в этой итерации.

## Критерии приёмки и компактные поля: 21 сентября 2026

Критерии находятся в acceptanceCriteria файла задачи v4. Core предоставляет адресное
чтение и изменение, ревизии, идемпотентность и метаданные выполнения; блокировка done
не меняет blocked/ready. Старые v1–v3 читаются без записи, следующая мутация обновляет
формат атомарно. Движок сущностей принимает критерии при создании, общий каталог
не включает их полные тексты. REST/CLI/MCP используют один сервис, SDK регенерирован.

Web: task-acceptance — вложенный юнит task-editor; строки и форма — внутренние компоненты.
domains/board-tasks владеет адресными запросами, SWR и SSE. Список раскрывает Markdown,
сохраняет порядок и число загруженных строк; форма сохраняет черновик и требует явной
сверки ревизии. Собственные записи обновляют базовую ревизию основного редактора.

ui/themes задаёт стандартным полям sm (36px), MarkdownField — шрифт темы sm, отступы
10px и начальные четыре строки. Поля настроек используют общий размер. Глобальный
сброс полей наследует только семейство шрифта, сохраняя размер и line-height Mantine.

Состояние проверок и ограничения обновляются в досье:
[критерии](../../work/task-acceptance-criteria/README.md),
[поля](../../work/compact-form-fields/README.md). Общие lint/typecheck имеют исходный
блокер product-page.tsx; адресный lint и отдельная Vite-сборка проверяются отдельно.

## Цели карточки по типу доски: 21 сентября 2026

`task-editor` адресно читает доску самой задачи через `useBoard`; `task-context`
сохраняет компоновку выбора и предлагает только «Фичи» / «Сценарии». Продуктовая
доска выбирает общие цели, приложение — активные имплементации своего applicationId,
инфраструктура не показывает блок. При переносе выбор перемонтируется по boardId,
редактор заголовка/Markdown сохраняет черновик. По уточнению пользователя удалён
переход «Все связи и контекст задачи» и пустая оболочка раздела на инфраструктуре.
Сохранённые цели различаются по `requirementKind`: Puzzle/violet для фичи,
Route/teal для сценария, в том числе внутри имплементаций. Локальный CSS окрашивает
иконку и лёгкую заливку; по последнему уточнению боковая полоса удалена.

Core проверяет область целей при create/update и смене доски; несовместимые ссылки
не удаляются автоматически. Старые данные читаются без миграции, правка содержания
не требует исправления старых целей. Каталог продукта получил необязательный
`implementationTarget=feature|scenario` до total/пагинации, SDK сгенерирован из OpenAPI.
Хранение v3 и форма `productLinks` не изменены.

Core 56/56 и HTTP 30/30 успешны; адресный Web lint и Vite-сборка успешны.
Общие lint/typecheck блокирует прежний `product-page.tsx` с неиспользуемыми объявлениями.
Браузер подтвердил области целей, сохранение и снятие, подгрузку 20→23, клавиатурное
переключение, предпросмотр, пустое приложение, инфраструктуру, сеть/повтор, конфликт,
смену доски и сохранность черновика. Просмотрены обе темы и ширины 1440/1024/768/390;
axe диалога на 1440 light и 390 dark: 0 нарушений и 0 непроверенных результатов.
Финальная сверка после удаления ссылки, замены иконки фичи на Puzzle и удаления боковой полосы
повторила все четыре ширины и обе темы; рамки целей равномерные 1px, на 390 нет переполнения.
Прямые цели/имплементации различимы, инфраструктура не оставляет пустого раздела.
Финальный axe: 0 нарушений / 0 incomplete; MCP 7/7, CLI канбана 1/1.
Точные команды и ограничения — [досье](../../work/task-board-targets/README.md).

## Обязательные подзадачи: 21 сентября 2026

`task-relations` переработан в плоский список подзадач с выбором доски и задачи;
переключатель направлений и группировка удалены. Домен передаёт фильтр `parentId`
в общий список задач, Core применяет его до пагинации. Запись использует существующий
`parentId` ребёнка и его ревизию. Формат хранения v3 сохраняется.

Core объединяет прямых детей и зависимости в вычисляемые блокеры. Добавление ребёнка
к готовому родителю и завершение с блокерами защищены общей блокировкой. Новые рёбра
проверяются на смешанные циклы; прежние графы доступны для чтения и исправления.
Из `task-modal` удалено подключение хлебных крошек по дополнительному поручению пользователя.
SDK сгенерирован по актуальному OpenAPI. Пройдены целевые Core 14/14 и HTTP 3/3,
полные Core 52/52 и Server Runtime 30/30, CLI-проверка канбана 1/1 и MCP 7/7,
typecheck Core/Server Runtime/Project Runtime, адресный Web lint, Vite-сборка и сборка CLI/MCP.
Общие typecheck и Web lint блокируют исходные неиспользуемые объявления в `product-page.tsx`.

Браузером подтверждены добавление ребёнка, блокировка/разрешение завершения родителя,
прогресс, отсутствие крошек внутри карточки и подгрузка 40→42. Axe диалога — без нарушений
и непроверенных результатов. Пользователь сообщил о самостоятельной проверке и поручил
закончить работу; оставшиеся агентские браузерные сценарии остановлены.
Область проверок и ограничения: [досье](../../work/task-card-mechanics/README.md).

## Группировка досок Web: 21 сентября 2026

`compositions/layouts/project/ui/project-navigation` отделяет группы загруженных досок
по `kind`: продукт → приложения → инфраструктура, Mantine Divider без текста между
группами. Один компонент обслуживает сайдбар и мобильный Drawer. Порядок REST и постраничное чтение по 50 через
`domains/boards` сохранены; `applicationId` уже присутствует в дисковом и Web-контрактах.
Изменение принято пользователем. Web lint/typecheck/build успешны; просмотрены обе темы
и четыре ширины, проверены клавиатурный переход и восстановление каталога после отказа.
Полный axe открытого Drawer обнаружил два замечания к landmark общей шапки;
на настольной странице нарушений нет. Дальнейшие проверки остановлены по поручению
пользователя. Подробности и непроверенные сценарии: [досье](../../work/board-navigation-groups/README.md).

## Хлебные крошки Web: 21 сентября 2026

Добавлены метаданные маршрутов и общий `compositions/widgets/page-breadcrumbs`.
Каркас проекта и служебные состояния используют Mantine Breadcrumbs;
длинные пути сокращаются по доступной ширине с меню родителей. Имена записей читаются
через существующие адресные SWR-хуки сущностей и досок, без полного снимка продукта.
Канбан учитывает высоту новой полосы. REST и хранение не изменены.
В работе над подзадачами крошки внутри модалки задачи удалены по поручению пользователя.

Реализация проверена: lint/typecheck и производственная сборка успешны. Браузерная
приёмка охватила проектные маршруты, local/workspace, обе темы и ширины 1440/1024/768/390,
клавиатуру, черновики, контекст возврата, отказы сети и обновление названий по SSE.
Axe редактора и диалога задачи — без нарушений; открытое меню потребовало ручной сверки
ARIA. Единичный AbortError при пакетном обходе не воспроизведён адресно, причина не установлена.
Подробные результаты и ограничения: [досье](../../work/web-breadcrumbs/README.md).

## Последовательные маршруты Web: 20 сентября 2026

[Карта страниц и переходов](../../../apps/web/ROUTING.md) принадлежит фронтенду.
После [исходного аудита](../web-routing.md) реализации и их `/edit` вложены в приложение;
короткие адреса совместимы. Общий ProductLayout обслуживает все страницы продукта,
граница `product-snapshot` подключает полный снимок только соответствующей ветке.
Адресное чтение сценария/реализации и редактор вклада не запрашивают полный снимок.
Редакторы фич/приложений зарегистрированы явно, мёртвый маршрут работы удалён.
Исправлены вход product с/без слеша, возвраты редакторов, query/hash задач и SPA-ссылки Markdown.

Web lint/typecheck/build, docs:check и адресные браузерные сценарии успешны.
Проверены черновики, offline/повтор, конкурентная запись, обе темы и четыре ширины;
axe страницы сценария и редактора реализации — без нарушений.
Область, артефакты и ограничения: [журнал](../../work/web-routing-consistency/JOURNAL.md).

## Очистка разделов: 20 сентября 2026

Удалены числовая доска, прежние страницы паспорта/знаний/работы и их Core/REST/CLI/MCP-контракты.
Обзор, планы, релизы и история Web — «В разработке». SDK содержит 81 операцию;
блокировки, ревизии, история действующих сущностей, миграции продукта/графа, workspace
и режимы сохранены. Web lint/typecheck/build успешны, Core 46/46, HTTP Runtime 29/29,
CLI 33/33, MCP 7/7, standalone Server 10/10. Браузер подтвердил заглушки, удалённые
URL, действующие продукт/канбан/граф, сохранность черновика и конфликт через SSE;
axe после исправления h1 — 0 нарушений. Проверены npm-архивы CLI/Server/MCP вместе,
local/workspace, docs:check, skills:check и 7/7 тестов скилла.
[Текущий журнал](../../work/legacy-cleanup/JOURNAL.md). Сведения ниже относятся к предыдущим этапам.

**Сверка: 19 сентября 2026 года.** Прочитаны README приложений, карта пакетов,
маршруты и продуктовый Web-адаптер, инициализатор Playground. Это статическое
основание карты, не новая проверка всех пользовательских сценариев.

При актуализации локальных README выполнены lint, typecheck и сборка Web, а также
чтение справки CLI и JSON-ошибка отсутствующего конфига. Результаты и ограничения:
[журнал библиотеки](../../work/documentation-library/JOURNAL.md). Браузерная приёмка
и проверка установленной поставки в этой работе не выполнялись.

| Приложение | Техническое состояние                                                                              | Проверки при изменении                                        |
| ---------- | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- |
| CLI        | Отдельный пакет; local Core или HTTP, workspace HTTP; представления терминала отделены от операций | `test:cli`, текст/JSON, установленный архив                   |
| Server     | Отдельная точка запуска; runtime в общем пакете, встроенный Web в дистрибутиве                     | `test:server`, local/workspace, SSE, установленный архив      |
| Web        | React SPA, Mantine, SWR; маршруты проекта и продукта, постоянный продуктовый адаптер               | `lint:web`, `typecheck:web`, `build:web`, браузерная приёмка  |
| MCP        | Отдельный Streamable HTTP-сервис поверх Server/REST SDK                                            | `test:mcp`, discovery, повтор, конфликт, бюджет               |
| Playground | Два ТЗ, workspace-конфиг и неразрушающий init через CLI; отдельное наполнение                      | Чтение реестра; повторный init только в согласованной области |

Команды таблицы запускаются через `pnpm run` из корня. Общая проверка установленной
поставки — `pnpm run package:check`. Факт выполнения записывается в журнал, а не выводится
из наличия скрипта.

## Источники владельцев

- CLI: [README](../../../apps/cli/README.md), [продуктовый контракт](../../product/applications/cli/README.md).
- Server: [README](../../../apps/server/README.md), [runtime](../../../packages/server-runtime/README.md).
- Web: [README](../../../apps/web/README.md), [маршруты](../../../apps/web/src/app/router/app-router.tsx).
- MCP: [README](../../../apps/mcp/README.md), [контракт](../../product/applications/mcp/README.md).
- Playground: [README](../../../apps/playground/README.md), [init](../../../apps/playground/init.mjs).

## Ограничения и оговорки

### Настройки проекта: 20 сентября 2026

Добавлены Core schema/storage и сервис ревизионной записи projectSettings в конфигурации.
Server разрешает slug вместе с ID/ключом, проверяет занятость адреса в каталоге;
GET/PUT context/settings представлены в OpenAPI и SDK (97 операций, 427 моделей).
Web domains/project разделяет постоянный ID и путь по slug. Экран project-settings
и вложенная форма general-settings созданы генератором; шапка открывает настройки
шестерёнкой, кнопка создания задачи удалена. Навигационные ссылки используют
useProjectBasePath, запросы/подписки/черновики сохраняют постоянный ID.
Форма использует Mantine useForm, черновик sessionStorage и явное разрешение конфликта.
Текущие проверки и ограничения: [досье](../../work/project-settings/README.md).

### Множественные доски: 19 сентября 2026

Добавлены Core-модель доски, собственные папки `boards/<slug>/`, создание системных
досок при init, slug приложения и восстановление составного создания приложения/доски.
REST предоставляет каталог с продолжением и чтение по slug; SDK обновлён генератором.
Web имеет домен `domains/boards`, вложенную навигацию и экран `project-board`.
Исходный этап пустых досок описан
в [журнале](../../work/multiple-boards/JOURNAL.md).

### Канбан отдельных досок: 19 сентября 2026

Реализованы короткие ID, префиксы, пустое либо заполненное создание, Markdown,
перенос с сохранением ID, зависимости и обратное чтение. Есть REST/SDK, CLI `task`
и MCP `board_task_*`. Web переработан после отклонения первой UI-версии: полноширинная
доска, постоянный маршрут задачи, центральный просмотр/редактор, сортировка и подгрузка
по механике прежнего канбана. Измеренная подгрузка 40→44 сохранила DOM первой карточки
и scrollY 3311; после local CLI/SSE — 45 без сброса объёма. При создании зафиксирован
один pushState, неизменный DOM доски и отсутствие skeleton редактора.
Полный перечень проверок и оставшиеся ограничения: [досье](../../work/board-kanban/README.md).

Последующая UI-итерация меняет только существующий `project-board`: отдельная шапка,
колонки по высоте окна, стабильный scrollbar gutter, компактные карточки. `task-kanban`
владеет локальным снимком порядка на время DnD и записи; подтверждённые данные по-прежнему
приходят через `domains/board-tasks`. Overlay использует измеренную геометрию, а дополнительная
невидимая запись задаёт точный beforeId на границе загруженной страницы. REST не изменён.
Проверены мышь/клавиатура, задержка записи 4 секунды, отказ с восстановлением положения,
подгрузка 40→44 с сохранением DOM и scrollTop 3353. Проверки этой итерации — в досье;
приёмка пользователем и новая страница задачи ещё впереди.

Следующее уточнение пользователя сохраняет модальное окно вместо отдельной страницы.
`task-modal` управляет размером; `task-editor` — единым обзором и черновиком; вложенный
`task-context` — постоянными продуктовыми связями и чтением контекста. Новый маршрут
`boards/:boardSlug/:taskId` сохраняет pathless-владельца экрана; `/tasks/:id` совместим
для старых ссылок, смена доски канонизирует URL без закрытия окна. Поиск связей имеет
описание по запросу; межзадачные и продуктовые записи постоянные. Выбор подзадачи —
два компактных селектора без выбора типа; dropdown задач ищет по названию/ключу,
исключая done/cancelled через серверный completion=unfinished до пагинации.
Глобальная тема не добавляет внешний контур текстовым полям/селекторам: фокус меняет
цвет существующей рамки. CodeMirror подсвечивает рамку владельца при focus-within.
Закрытая карточка после DnD обновляется через адресный GET/mutate, обычное открытие
перечитывает кеш. Это исправляет старый статус при revalidation без SWR-подписчика.
Статус/доска выбираются
через компактные меню без формы и блока «Детали». Типа эпика нет. MarkdownField
лениво загружает CodeMirror; внутри ввода нет вкладки предпросмотра.

В Web сохранились внутренние имена прототипа; маршрут `product/work/:workId` удалён.
Граница продуктовой интеграции описана в
[реализации продукта](product.md). Скрытые страницы прежнего жизненного цикла удалены.

Playground не содержит реализации продуктов из ТЗ. Его init сохраняет существующие
конфигурации и валидирует проекты. Текущее содержимое локальных `.relay` не
инвентаризировалось и не очищалось. Правило ignore не снимает отслеживание уже
закоммиченных конфигураций; состав Git и политика данных учитываются отдельно.

Старые инструкции и отчёты у владельцев читаются с учётом даты. При расхождении
с продуктовым контрактом агент фиксирует проблему в досье, а не выбирает молча удобную версию.
