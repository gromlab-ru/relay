# Журнал предметной интеграции связей

## 22 сентября 2026 — начало и область

- Поручение: подготовить бекенд и пути Web/CLI/MCP к полному графу контекста.
- Прочитаны обязательный протокол, стандарт линков, правила библиотеки, концепция,
  интерфейсный стандарт, инструкции Core/REST/CLI/MCP/Web и применимые frontend skills.
- `git status --short --branch`: чистая ветка `refactor/application-rebuild`;
  `git rev-parse --short HEAD`: `90d7b26`. Исторические отчёты не считаются текущими проверками.
- Статически найдена существующая интеграция нового хранилища через `owned-relations`
  и общую `Workspace.storageSession`. Старые адаптеры в этих функциях возвращаются без записи.
- Найден устаревший Web-потребитель `product_context` в материалах задачи; этот endpoint
  выбирает документы только по `fields.links`. Новый `fields.relations` там не учитывается.
- Первоначальная архитектура и проверки записаны в README; дальнейшие выводы — в карте операций.

## 22 сентября 2026 — интеграция и первые проверки

- Проверены все точки предметной записи в `application`: ProductService, реализации,
  общий mutate канбана, обсуждения и каскад. Содержание, статусы и критерии сами по себе
  новых отношений не требуют. Снятая реализация сохраняет прежние линки по действующей модели.
- Добавлены SI → part-of → FI, запрет изменения предметных рёбер через публичный граф
  и явное `storage reconcile-relations`. Старые базы используют штатную явную миграцию.
- Первый запуск `node --conditions=tasks-source --import tsx --test packages/core/test/product-relations.test.ts`:
  4/5 сценариев прошли, включая отказ после продуктовой записи и восстановление публикации.
  В пятом тесте неверно передан компактный DTO контекста низкоуровневому writer:
  отсутствует description, лишняя revision. Исправлен тестовый ввод; повтор предстоит.

## 22 сентября 2026 — сквозной этап

- Адресный повтор Core: 16/16, включая шесть каскадов. Полный Core: 132/132.
- Web переведён с `useProductContext` на полный `getFullContext` с адресным содержимым
  узла. Обозначены диагностические операции; удалена ложная подпись «Добавлена вручную»
  по `source=graph`. Typecheck и lint прошли. В первом typecheck исправлено неверное
  имя SDK-метода `getGraphContext` на существующий `getFullContext`.
- Сборка CLI/MCP/Web/Server Runtime с зависимостями через Turbo прошла (8 пакетов).
- MCP: 10/10. REST: 33/34, новый `RELATION_MANAGED` ошибочно отображался HTTP 500;
  добавлен в карту конфликтов HTTP 409. CLI: 35/36, отсутствовал отдельный заголовок
  новой команды в CLI.md; справочник дополнен. Адресные повторы этих проверок предстоят.
- Пользователь отменил ошибочные сообщения об инициализации и подтвердил продолжение
  исходной задачи. До отмены CLI создал `.relay` в корне репозитория (проект `lFPzW9RI`,
  только системные сущности); это не тестовая база данной работы. Её данные не используются
  для проверок. В Playground появились сторонние изменения; эта работа их не изменяет.

## 22 сентября 2026 — браузер и SSE

- REST-повтор 2/2 и документационные CLI-тесты 5/5 прошли после исправлений.
- Собственный Server PID 104516, база `.artifacts/relations-web-p1jFGh`, URL
  `http://127.0.0.1:43077`; headless-сессия `tasks-web-relations-90d7` с конфигом Web.
  Первые попытки запуска: `/tmp/opencode` недоступен для записи; shell-background
  прерван лимитом инструмента. Запуск перенесён в `.artifacts` через отдельный launcher.
- В браузере полный граф цели находит документ через `references` задачи: 5 узлов/4 ребра,
  адресный Markdown открыт. На 1440 px светлая тема читаема; axe — 0 нарушений.
- Прямая CLI-запись открепления вызвала SSE: граф стал 4 узла/3 ребра. Найден дефект
  потребителя — открытый текст уже недостижимого документа оставался видим. Выбор текста
  теперь допускается только для узла текущего полного контекста. Типы узлов получили русские подписи.

## 22 сентября 2026 — итоговые сценарии и передача

- Финальный Core-прогон: 133/133. Дополнительный сценарий связал документ со всеми
  девятью видами, проверив полный обход с обоих концов. Сохранена совместимость старой
  неактивной SI без FI: родитель не выдумывается, требование применяется к активной SI.
- Общий typecheck Core/Server Runtime/CLI/MCP/Web: 13 задач Turbo успешно, включая сборки.
  Скилл собран и проверен, 19 файлов; сборщик также штатно обновил игнорируемую копию Playground.
- Web: повтор после исправления SSE подтверждает исчезновение недостижимого полного
  текста. В сетевых адресах наблюдались только полный `graph/context` и адресный `entities/get`.
- Через форму Web добавлена цель-фича, затем снята сценарная цель; CLI подтвердил точные
  сохранённые рёбра. Offline не сбросил выбор; конфликт после CLI-правки не затёр данные.
  После явной сверки удалось сохранить. Создание Web-задачи сразу создало `part-of` к доске.
- Контекст просмотрен в светлой/тёмной темах на 1440/1024/768/390; клавиатурный Enter
  открывает текст. Axe контекста — 0 нарушений/0 неполных проверок. В диагностической
  форме 390 — 0 нарушений и одна неполная проверка контраста; текст просмотрен на снимке.
  Новая русская ошибка при отзыве предметного ребра проверена в UI.
- После пересборки статических файлов свой Server перезапущен из-за 404 нового хеша;
  последний PID 122229 остановлен, сессия `tasks-web-relations-90d7` закрыта.
- Удалены неиспользуемые frontend-схема старого контекста и helper, собиравший варианты
  из полного ProductState. Потребителей у них нет; актуальные адресные варианты целей сохранены.
- Продуктовая модель, стандарт, Web-контракт, техническое покрытие, справочники и скилл
  актуализированы. Изученные модели продукта/документов и правила канбана остаются верными:
  их предметные ограничения и расчёты не заменяются графом. Карта владельцев — ANALYSIS.md.
- `pnpm run docs:check`: пять исходных ссылок на отсутствующий `STORAGE-REFACTORING.md`.
  `git ls-tree HEAD -- STORAGE-REFACTORING.md` подтверждает отсутствие в исходном HEAD.
  Адресная проверка четырёх документов нового досье: успешно, 6 ссылок до итоговой сводки.
  Общий провал не выдан за успешную проверку библиотеки.
- Финальная адресная проверка досье: 4 документа / 7 ссылок, успешно. Prettier изменённых
  отслеживаемых файлов и `git diff --check` успешны. Новые файлы отформатированы отдельно.
  Последний Web build (включая typecheck) и lint после очистки старого пути прошли;
  `skills:check` подтверждает актуальную сборку. Точка передачи и индекс обновлены.

## 23 сентября 2026 — исправление принадлежности фичи и продукта

- Пользователь обнаружил отсутствие фича → продукт и продукт → проект. Предыдущая
  трактовка этих отношений как ненужных «связей изоляции» была ошибочной; это реальные
  обязательные предметные принадлежности. Предыдущая приёмка не покрывала их.
- План: явные `part-of` в группах feature-links/product-links после предметной записи;
  инициализация и migrate записывают продукт → проект даже для паспорта revision=0;
  reconcile восстанавливает обе ступени существующей базы без изменения данных сущностей.
- Владельцы и постоянные адреса уже определены единственным продуктом Workspace
  (`product:passport`) и `config.projectId`; новый параметр выбора родителя и миграция
  дисковой схемы не требуются. Отдельные рёбра сохраняются Core, чтение не пишет их.
- Снятие этих обязательных линков отдельной операцией не предусмотрено: удаление фичи
  отзывает её связи штатным каскадом, продукт/проект не удаляются. Повтор и отказ используют
  общую транзакцию и квитанции; независимые диагностические рёбра сохраняются.
- Рабочее дерево сверено. Продолжаются собственные изменения предыдущего этапа;
  сторонние изменения Playground сохраняются.
- Добавлен `syncProductRootRelations`; `syncProductRelations` сохраняет feature-links
  и согласует product-links. Вызовы root-согласования добавлены в migrate и reconcile,
  потому что пустой паспорт не входит в обычный список заполненных продуктовых записей.
  `EntityEngine.get.references` теперь отражает обоих фиксированных родителей.
- Адресная регрессия: 21/21. После обновления ожиданий полного графа во внешних
  интерфейсах полные прогоны прошли: Core 137, REST 34, CLI 36, MCP 10 — всего 217 тестов.
- Сборка 7 пакетов и typecheck четырёх владельцев с зависимостями (11 задач) успешны;
  `skills:build`/`skills:check` успешны. Новых схем API/хранилища и фронтенд-кода нет.
- На собственной старой базе CLI-согласование добавило ровно два недостающих ребра;
  человеческий вывод показал обе ступени. В Web пройден путь фича → продукт → проект,
  обратные отношения видны. Своя сессия закрыта, Server PID 201579 остановлен.
- Зафиксировано отдельное расхождение подписи выбранного незаполненного паспорта:
  заголовок пуст, но каталог и строка связи имеют заглушку. Это не объявлено исправленным.
- Финальные проверки этого исправления: Prettier затронутых файлов, `git diff --check`
  и `skills:check` прошли. Новое досье: 4 документа / 7 ссылок, успешно.
  Полный `docs:check` повторно показал те же 5 исходных ссылок на отсутствующее ТЗ,
  новых ошибок ссылок нет. Точка продолжения и действующие правила актуализированы.
