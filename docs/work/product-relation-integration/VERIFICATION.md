# Проверки предметных связей

Дата: 22 сентября 2026. Исходный коммит `90d7b26` и незакоммиченные изменения этой работы.
Пользовательские базы не использовались как испытательный корпус.

## Автоматические проверки

| Проверка                                                                                                                                                        | Результат                                                 |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| `node --conditions=tasks-source --import tsx --test packages/core/test/*.test.ts`                                                                               | Финальный прогон: 133/133                                 |
| `pnpm --filter @relay/server-runtime test`                                                                                                                      | 33/34; выявлена карта HTTP-кода новой ошибки              |
| `pnpm --filter @relay/server-runtime exec node --import tsx --test test/entities.test.ts`                                                                       | После исправления HTTP 409: 2/2                           |
| `pnpm --filter @gromlab/relay-cli test`                                                                                                                         | 35/36; отсутствовал заголовок новой команды в справочнике |
| `pnpm --filter @gromlab/relay-cli exec node --import tsx --test test/documentation.test.ts`                                                                     | После исправления: 5/5                                    |
| `pnpm --filter @gromlab/relay-mcp test`                                                                                                                         | 10/10                                                     |
| `pnpm exec turbo run typecheck --filter=@relay/core --filter=@relay/server-runtime --filter=@gromlab/relay-cli --filter=@gromlab/relay-mcp --filter=@relay/web` | 13 задач успешно, включая сборки зависимостей             |
| `pnpm --filter @relay/web lint`, `pnpm --filter @relay/web build`                                                                                               | Успешно; обычное предупреждение Vite о chunk более 500 kB |
| `pnpm run skills:build`, `pnpm run skills:check`                                                                                                                | 19 файлов, актуальная переносимая сборка                  |

Новый `product-relations.test.ts` проверяет связи всех девяти видов, SI/FI, оба
направления документов, замену и снятие линков, перенос задачи, обратное related,
стабильность ID, отказ между шагами, recovery публикации, повтор и независимые рёбра.
Тесты единого хранилища дополняют шесть каскадов и миграцию прежних данных.
REST проверяет три входа записи: product, entities и канбан. CLI/MCP проверяют
предметные записи и последующий полный граф без ручной синхронизации.

## Web: ручной сценарий

Собственная headless-сессия `tasks-web-relations-90d7`, `apps/web/agent-browser.json`.
Временная база `.artifacts/relations-web-p1jFGh`. Скрипты запуска и снимки в `.artifacts`
игнорируются Git и не являются частью поставки.

- Материалы цели задачи: сценарий → задача → документ с новым `references` виден в
  полном графе; Markdown загружается адресно. Наблюдались запросы `graph/context`
  и `entities/get`, старого `product/context` нет.
- CLI-открепление документа при открытом Web обновляет граф через SSE (5/4 → 4/3).
  Исправлено и повторно проверено скрытие открытого текста недостижимого документа.
- Web-сохранение второй цели создаёт отдельное `implements`. Web-снятие сценарной
  цели отзывает только нужное ребро; фича и альтернативный путь к сценарию сохраняются.
- Offline при сохранении оставляет выбранные флаги. После внешнего изменения задачи
  устаревшая ревизия отклоняется, выбор сохраняется; явное повторное открытие формы
  позволяет сверить актуальное состояние и сохранить.
- Кнопка создания Web создаёт задачу вместе с `part-of` к доске; проверено CLI-чтением.
- Открытие полного текста с клавиатуры (focus → Enter) работает.
- Защита предметного ребра показана русской ошибкой; форма создания явно диагностическая.
- Светлая и тёмная темы, 1440/1024/768/390: просмотрены снимки, текст и кнопки читаемы,
  длинное содержание доступно вертикальной прокруткой.
- Axe контекста: 0 нарушений / 0 неполных проверок. Диагностическая форма на 390:
  0 нарушений, 1 неполная проверка контраста из-за наложения; визуально текст читаем.
  `errors` и `console` в итоговой проверке пусты.

При обновлении production-сборки свой Server перезапущен: его статические маршруты
не знали новых хешированных файлов (наблюдался HTTP 404). Это не отказ API контекста.
Сессия закрыта, свой последний Server PID 122229 остановлен.

## Документы и ограничения

`pnpm run docs:check` обнаруживает пять исходных ссылок на отсутствующий
`STORAGE-REFACTORING.md`: инженерный документ хранилища, README/PLAN/VERIFICATION его
досье и README исследования Dolt. Файл отсутствовал на исходном срезе; его содержание
не восстанавливалось предположениями. Новые ссылки досье проверяются отдельно.

Форматирование изменённых файлов и `git diff --check` прошли. Адресная проверка
четырёх документов нового досье прошла: 7 ссылок. После удаления неиспользуемого
старого frontend-кода повторены Web typecheck/build/lint, результат успешный.
Старые базы требуют явного `storage migrate`; уже единые после обновления правил —
`storage reconcile-relations`. Пользовательские данные автоматически не согласовывались.
Нагрузочный прогон и будущая визуализация canvas в эту проверку не входят.

## Исправление иерархии продукта — 23 сентября 2026

По замечанию пользователя добавлены реальные обязательные отношения фича → продукт
и продукт → проект. Прежние проверки выше их не покрывали.

- `node --conditions=tasks-source --import tsx --test packages/core/test/*.test.ts`: **137/137**.
- `pnpm --filter @relay/server-runtime test`: **34/34**.
- `pnpm --filter @gromlab/relay-cli test`: **36/36**.
- `pnpm --filter @gromlab/relay-mcp test`: **10/10**.
- Сборка CLI/MCP/Server Runtime и зависимостей: 7 задач успешно; typecheck четырёх
  владельцев с зависимостями: 11 задач успешно. Сборка и проверка скилла: успешно.

Проверены init с незаполненным паспортом, создание фичи в ранее инициализированной базе,
прямое сохранённое ребро до чтения контекста, повтор, заполнение паспорта, переименование
всех трёх звеньев, удаление фичи с сохранением корня продукта, явная миграция прежней базы
и reconcile старых баз с фичей и без фич. Reindex не создаёт недостающие связи.
Тексты/ревизии сущностей при reconcile сохраняются, повтор не дублирует рёбра.

На прежней собственной базе `.artifacts/relations-web-p1jFGh` CLI-согласование добавило
ровно 2 ребра (`added=2, updated=0, removed=0`). Человеческий `graph context FEATURE-1`
показал всю цепочку. В собственной headless-сессии `tasks-web-product-root-20260923`
проверены переходы из фичи в продукт, из продукта в проект и обратная группа «Включает».
Сессия закрыта, свой Server PID 201579 остановлен. API/CLI/MCP-контракты не изменены.

Наблюдение Web: у незаполненного паспорта в прежней тестовой базе верхний заголовок
исследователя пуст, хотя каталог и строки отношений показывают «Продукт (паспорт не заполнен)».
Это отдельное расхождение карточки выбранного узла; оно не мешает двум проверенным связям.
