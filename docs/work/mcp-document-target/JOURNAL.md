# Журнал восстановления MCP

## 22 сентября 2026 — диагностика

- Прочитаны протокол, стандарт интерфейсов и инструкции владельцев MCP/Contracts.
- По стеку и коду обнаружен `documentRelationSchema.target` без `.describe()`.
  `createTools` передаёт вложенные схемы в `documentToolSchema`, который отклоняет
  недокументированные параметры. Проверку сохраняем, исправляем исходный контракт.
- Сверены ветка и рабочее дерево: текущая разработка библиотеки документов содержит
  эту новую схему; остальные существующие изменения сохраняются.
- Проверки ещё не выполнялись.

## 22 сентября 2026 — исправление и проверки

- В исходный контракт добавлено описание `target`; проверка обязательных описаний
  сохранена. Валидация данных и набор инструментов не изменены.
- `pnpm run build:mcp`: Contracts и Core собраны, `project-runtime` не собран из-за
  несогласованных с REST SDK новых `sort=updated`, `documentKind` и полей проекта.
  Это ограничение текущей смежной разработки, не ошибка добавленного описания.
- Первый запуск тестов с `tasks-source` без TSX-конфига остановился на декораторах
  Server. С `TSX_TSCONFIG_PATH=packages/server-runtime/tsconfig.json` команда
  `node --conditions=tasks-source --import tsx --test apps/mcp/test/mcp.test.ts`
  прошла: 9/9. Тесты импортируют MCP из существующего dist, зависимости — из исходников;
  проверяют discovery, вызовы, читаемое содержимое отдельно от structuredContent,
  повтор, конфликт, изоляцию, бюджеты и пагинацию.
- Дополнительно через `node --conditions=tasks-source --import tsx --input-type=module`
  запущен именно `apps/mcp/src/server.ts`: конфиг `apps/playground/relay.workspace.json`,
  случайный свободный порт. Настоящий SDK-клиент подключился, получил 66 инструментов;
  рекурсивно проверены русские описания всех свойств, включая 8 вхождений `target`.
  `projects_list` вернул успешный structuredContent и непустой текст. Клиент и MCP закрыты.
- Продуктовый контракт MCP, `docs/reference/MCP.md` и источники скилла не требуют
  изменения: новое действие или аргумент не вводились, восстановлено соблюдение
  обязательных описаний. Текущее состояние отмечено в реализации приложений.
- `pnpm run docs:check`: 207 документов / 1029 ссылок и 17 файлов скилла / 64 ссылки,
  ошибок нет. Prettier для четырёх изменённых документов и `git diff --check` прошли.
