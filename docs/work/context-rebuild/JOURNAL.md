# Журнал изменений

> Исторические записи перенесены 19 сентября 2026 года. Их утверждения о состоянии
> файлов расходились с рабочим деревом при новой инвентаризации. Сохранены как свидетельство
> прежней работы; текущее состояние — в [новом досье](../documentation-library/README.md).

Записи идут от новых к старым. Фиксируем цель, решения, затронутые материалы,
фактические проверки и точку продолжения; актуальное описание продукта ведём отдельно.

## 2026-09-19 — Пересборка продуктового контекста

- Цель: дать новому агенту короткое и достоверное понимание Relay вместо чтения
  разрозненного видения и постраничных спецификаций прототипа.
- Прочитаны все прежние материалы `docs/product`, корневые план и журналы продукта;
  содержание сопоставлено со справочником продукта, схемами и операциями Core,
  CLI/MCP, навигацией и адаптером постоянных данных Web.
- Переписаны обзор Relay, описание продукта и готовности. Повторяющиеся материалы
  страниц объединены в `entities/product/INTERFACE.md`; вместо старого плана подключения
  ядра и уже закрытых вопросов добавлены `LIFECYCLE.md` и `DIRECTION.md`.
- Разделены текущее поведение, согласованное развитие и предложения. Зафиксированы
  принцип плана для самостоятельной цели любого размера и области работы без
  обязательной фичи. Инфраструктура остаётся обсуждаемым разделом, не готовой возможностью.
- Явно описана граница: новые продуктовые записи ещё не входят в контекст задач,
  контрольные точки и состав релизов; старые паспорт, требования и знания не объединены.
- Обновлены точки входа в корневом README, `docs/README.md`, README Web и ссылки
  исторических материалов. Корневой `AGENTS.md` требует чтения контекста перед работой,
  актуализации старых описаний и записи результатов в этот журнал после каждой доработки.
- Проверки: `pnpm run docs:check` — 71 документ и 505 ссылок без ошибок,
  также проверены 49 ссылок скилла. `pnpm run skills:check` — сборка скилла актуальна.
  Prettier сначала обнаружил форматирование таблиц в шести файлах; после исправления
  проверка всех изменённых Markdown прошла. `git diff --check` — без ошибок.
- Продуктовое поведение не изменилось; тесты приложения и браузерная приёмка не запускались.
  Работа ведётся в `refactor/application-rebuild`, пользовательская правка `.gitignore`
  сохранена; коммит и отправка этой переработки не выполнялись.
- Переработка документации завершена. Следующая предметная работа: согласовать открытые
  решения в `DIRECTION.md` перед интеграцией планов, задач, релизов и инфраструктуры.
  Перечень не является поручением автоматически реализовать все предложения.

## 2026-09-19 — Протокол документации и объединение веток

- Цель: сделать `docs/product` кратким и актуальным контекстом Relay для следующих
  участников; убрать обязательное создание тематических веток.
- В корневом `AGENTS.md` оставлены русский язык и протокол чтения, актуализации
  продуктовых описаний и ведения этого журнала. Старые разделы протокола удалены.
- По поручению пользователя сохранены только `main` и `refactor/application-rebuild`
  локально и в origin. После `git fetch origin --prune` проверено: все остальные ветки
  уже входили в `refactor/application-rebuild`, дополнительных слияний не потребовалось.
  Рабочая ветка отправлена в origin; `main` не изменена.
- Проверки: отсутствие невлитых локальных и remote-tracking веток перед удалением;
  после удаления — `git branch -a`, `git ls-remote --heads origin`, `git diff --check`.
- Продуктовое поведение не изменилось. Пользовательская правка `.gitignore` сохранена.
  Изменения протокола и журнала пока не закоммичены.
- Следующий шаг: прочитать существующие материалы `docs/product` и переписать их
  кратко и согласованно. Эта переработка ещё не выполнена. Обсуждавшиеся связи планов,
  задач и релизов с продуктом и раздел инфраструктуры не выдавать за реализованные.
