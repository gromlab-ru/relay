# Журнал критериев приёмки

## 21 сентября 2026 — проектирование и начало реализации

Пользователь согласовал название «Критерии приёмки» и поручил реализацию.
Прочитаны протокол, документы владельцев, текущие модели и потребители.
В текущей задаче критериев нет; blocked влияет на подбор работы оркестратором,
поэтому критерии получают отдельные счётчики и проверку завершения.
Хранение принадлежит агрегату задачи, UI — вложенной области редактора.
Рабочее дерево проверено git status --short --branch: чистое.
План, совместимость и проверки зафиксированы в README. Реализация ещё не проверена.

### Общая модель и первые проверки

Добавлены вложенный массив v4, адресные операции Core, страницы/полное чтение,
метаданные выполнения и отдельная проверка done. REST использует тот же Core.
SDK обновлён закреплённым генератором с собственного API 4793.
Тест обнаружил применение Zod-default к отсутствующим полям частичного изменения:
исправлены схемы update, чтобы изменение summary не очищало Markdown.
Целевые проверки критериев прошли 3/3; прежняя проверка миграции требует ожидаемой v4
вместо v3 и обновлена. Полный набор ещё впереди.
Попытка разместить временный сервер в /tmp/opencode отклонена правами окружения;
проверочный сервер и его изолированные данные размещены в .artifacts.

### Сквозная реализация и проверки

Core 59/59, Server Runtime 31/31, MCP 8/8 и CLI канбана/критериев 2/2 прошли.
Добавлены проверки OpenAPI обоих маршрутов, страниц и полного чтения, конфликтов,
изоляции, метаданных, сохранности Markdown и восстановления прежнего pending.
Общий каталог сущностей исключает внутренний массив критериев из своего DTO;
создание через движок принимает тот же набор критериев атомарно.

В браузере подтверждены раскрытие, отметка, добавление, сохранение обычных переносов
и Markdown, отказ сети с сохранением черновика после закрытия/открытия окна,
внешнее изменение по API с предупреждением и сохранённым вводом, явное применение
новой ревизии, запрет done и успешное завершение после выполнения всех критериев.
Проверка общей сборки выявила прежние неиспользуемые объявления product-page.tsx:
общие lint/typecheck не прошли, адресный lint прошёл, tsc с отключёнными проверками
unused и отдельный vite build прошли. Это не заменяет строгий общий typecheck.

Во время реализации пользователь уточнил глобальную плотность полей Mantine.
Работа и проверки этого изменения — [отдельное досье](../compact-form-fields/README.md).
После регенерации/сборки SDK Vite временно терял модули при очистке dist; после
завершения сборки полная перезагрузка восстановила страницу. Проверки UI выполняются
по свежей загрузке, а не по переходному HMR-состоянию.

### Финальная проверка и передача

- `node --conditions=tasks-source --import tsx --test test/*.test.ts` в Core: 59/59;
  в Server Runtime: 31/31. Включены обе OpenAPI-проекции критериев.
- `pnpm run build:cli`, `pnpm run build:mcp` успешны;
  `pnpm --filter @gromlab/relay-mcp run test`: 8/8.
- CLI `node --import tsx --test test/board-tasks.test.ts`: 2/2, отдельно текст и JSON.
  Совместный запуск с documentation.test.ts: 6/7; неизменённая проверка переносимости
  требует references/RECORDS.md, отсутствующий в действующем bundle ещё до этой работы.
- `pnpm --filter @relay/core run typecheck`, аналогичные Project Runtime и Server Runtime
  прошли. Первая параллельная попытка typecheck пересеклась с очисткой dist при сборке;
  после завершения зависимостей последовательный повтор успешен.
- Адресный eslint изменённых домена, карточки, темы, Markdown и настроек прошёл.
  `pnpm --filter @relay/web run typecheck` после финальных изменений сообщает только
  прежние неиспользуемые Link, ArrowLeft и hasBack в product-page.tsx.
  Общий lint также останавливается на прежних unused в этом файле.
- `pnpm --filter @relay/web exec tsc --noEmit --noUnusedLocals false --noUnusedParameters false`
  и `pnpm --filter @relay/web exec vite build` прошли. Это ограниченная проверка типов;
  строгий общий typecheck не объявляется успешным. Сборка предупреждает о чанке >500 КБ.
- `pnpm run skills:build`, `pnpm run skills:check`, `pnpm run docs:check` успешны:
  17 файлов пакета, 179 документов и 933 ссылки библиотеки без ошибок.

Браузер tasks-web-acceptance-0921 на временном проекте Q7mbDegk подтвердил:
20→23 критерия; новая запись local CLI отобразилась без reload, сохранив загруженный
объём 24. Пробел переключает чекбокс; Ctrl+Enter сохраняет Markdown формы критерия.
Отметка при несохранённом заголовке задачи сохраняет ввод и не создаёт ложный конфликт.
Удаление с подтверждением обновляет счётчик. При ready/review невыполненные критерии
не препятствуют работе, done отклоняется; после всех отметок done сохраняется явно.

После обновления общих полей проверены обе темы и ширины 1440/1024/768/390.
Просмотрены снимки ~/.agent-browser/tmp/screenshots/screenshot-1789986016551.png,
1789986017133, 1789986017725, 1789986018291, 1789986036739, 1789986037321,
1789986037900, 1789986038899 (тот же префикс и расширение).
На 390px окно/документ имеют ширину 390, горизонтального переполнения нет.
Axe диалога после исправления контраста: 0 violations / 0 incomplete, включая
пустую форму и ошибку обязательного заголовка; финальная консоль без ошибок приложения.
Реальное мобильное устройство не проверялось.

Собственные процессы API 302153 (4793), Vite 309339/309276 (5174) остановлены;
сессия браузера закрыта. Данные и технические скрипты остались в .artifacts вне Git.
Канонические документы задач/Web/REST/CLI/MCP и состояние реализации обновлены;
инструкции оркестратора дополнены и распространяемый скилл пересобран.

Финальный контроль после записи передачи: docs:check — 179 документов / 934 ссылки,
skills:check — 17 файлов, Prettier всех изменённых авторских файлов и git diff --check
успешны. Сгенерированный SDK и пакет скилла не форматировались вручную.
