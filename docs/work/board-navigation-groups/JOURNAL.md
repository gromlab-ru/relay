# Журнал группировки досок

## 21 сентября 2026 — исследование и допуск к реализации

- Сверены ветка и чистое рабочее дерево; прочитаны действующие инструкции и связанные
  работы. Цель выделена отдельно от исторического внедрения множественных досок.
- `packages/contracts/src/entities/board.ts` хранит `kind` и `applicationId`;
  `domains/boards/config/boards.schema.ts` проверяет эти поля на Web-границе.
  `project-board.screen.tsx` уже использует тип для описания и ссылки на приложение.
- Сайдбар в `project-navigation.tsx` выводит единый список в порядке API. Один компонент
  обслуживает постоянный сайдбар и Drawer. Поэтому достаточно изменить локальную
  проекцию списка, без расширения схем и повторного запроса приложений.
- В README зафиксированы владелец, прочитанные инструкции, контракты, границы
  пагинации, критерии и план проверок. Выбран Mantine Divider с подписью «Приложения».

## 21 сентября 2026 — реализация

- В существующей навигации подготовлена проекция ссылок из системных досок и приложений;
  сохранён серверный порядок внутри группы. Перед первым приложением показывается
  единственный подписанный Divider, только когда есть системные доски.
- Использованы существующий predicate и публичный Mantine API, CSS и новые юниты
  не потребовались. Проекция сохраняет ID-ключи, адреса и выделение открытой доски.
- Актуализированы продуктовый контракт Web, отличие порядка каталога от навигации
  в правилах досок и техническое состояние. Проверки пока не завершены.

## 21 сентября 2026 — первые проверки и уточнение пользователя

- Web lint/typecheck/build успешны на первоначальном варианте. Собственный сервер
  4801 запущен на свежей базе `.artifacts/board-navigation-check`; `/tmp/opencode`
  недоступен для создания каталога. Первый launcher остановился по таймауту оболочки,
  отдельный nohup-запуск работает (PID 1188172).
- В agent-browser `tasks-web-board-groups-0921` подтверждены две системные доски без
  разделителя. Созданы два приложения публичным API: SSE добавил их в нижнюю группу.
  JSON отдельно подтвердил `kind`, `applicationId` и прежний порядок REST.
- Переход на Web-доску клавишей Enter успешен. Axe в светлой/тёмной настольных темах:
  0 нарушений. Слишком ранняя проверка во время анимации Drawer дала дубли landmark;
  следующий клик после Escape попал на уходящий overlay. Требуется повтор после
  наблюдаемого завершения перехода; эти попытки не считаются проверкой мобильного меню.
- Пользователь прислал скриншот и уточнил: разделитель должен быть без текста.
  Убраны label/labelPosition, актуализированы требования и состояние реализации.
  Финальные проверки относятся к этому варианту.

## 21 сентября 2026 — окончательный порядок групп

- Пользователь уточнил компоновку: «Продукт», линия, доски приложений, линия,
  «Инфраструктура». Предыдущая идея размещения обеих системных досок сверху отменена.
- Навигация теперь сохраняет исходный серверный порядок; линия появляется только
  на переходе между `kind` соседних загруженных досок. Сравнение идёт после объединения
  страниц, поэтому граница страницы внутри приложений не создаёт лишний разделитель.
  Без приложений между продуктом и инфраструктурой остаётся одна линия.
- Убраны фильтрация/перестановка и ненужный импорт predicate. Для линии применён общий
  цвет рамки темы. Продуктовый контракт, инженерное состояние и план актуализированы.
- Проверки прежнего порядка не подтверждают окончательный вариант; браузерный пакет
  был прерван поступившим уточнением. Следующий шаг — адресная финальная приёмка.

## 21 сентября 2026 — проверки окончательного варианта

Проверялось незакоммиченное состояние поверх `de6a60a`, производственная сборка
Web и свежая временная база `.artifacts/board-navigation-check`, проект `noWdS3Ag`.
Браузер: собственная headless-сессия `tasks-web-board-groups-0921`, сервер 4801.

- Финальные команды `pnpm run lint:web --output-logs=errors-only`,
  `pnpm run typecheck:web --output-logs=errors-only` и
  `pnpm run build:web --output-logs=errors-only` успешны. Проверка типов также
  выполнила свежую сборку; последующая явная сборка использовала её Turbo-кеш.
- `pnpm exec prettier --check` финального TSX успешен. Ранее форматирование
  продуктовых документов, инженерного состояния, индекса и досье проходило;
  последующие записи остановки не проверялись форматтером.
- Условный Divider после удаления подписи стал достаточно коротким для сворачивания
  Prettier в строку. Декоративная линия получила `aria-hidden="true"` и общий цвет
  рамки; итоговый формат согласован с требованиями JSX и форматтером.
- После пересборки статический проверочный сервер требовал перезапуска: прежний
  экземпляр отдавал старую точку входа с удалёнными именами bundle. Он остановлен,
  новый экземпляр с PID 1200996 отдаёт актуальную сборку. Это проблема проверочного
  запуска при замене dist, не заявляемое исправление механизма поставки.
- Подтверждён порядок «Продукт», Web, API, «Инфраструктура» с двумя линиями без текста.
  Просмотрены снимки обеих тем на 1440/1024/768/390. На 390 ширина документа равна
  ширине viewport; Enter на «Инфраструктура» открыл её и закрыл мобильное меню.
- Перехват `**/boards?*` с отказом показал «Не удалось загрузить доски».
  После снятия перехвата кнопка «Повторить» восстановила каталог и группировку.
- Полный axe настольной страницы окончательного варианта: 0 violations,
  0 incomplete. В открытом Drawer повторно обнаружены `landmark-no-duplicate-banner`
  и `landmark-unique` на общей шапке, в том числе после завершения анимации.
  Предварительное предположение о влиянии только анимации не подтвердилось.
  Причина и исправление вне этой локальной правки не исследованы.
- Пакетные команды ожидания исчезновения Drawer не дали надёжного ожидания overlay;
  отдельная команда `wait --fn '!document.querySelector(".mantine-Drawer-overlay")'`
  успешно дождалась его удаления. Повторное открытие, темы и переходы работали.
- Машинный GET каталога отдельно подтвердил два системных `kind` с `applicationId: null`
  и два `application` с реальными ID приложений. REST-порядок не менялся.
  Создание приложений и SSE проверены на предыдущем варианте; повтор после последнего
  изменения разметки не выполнялся. Вариант без приложений с одной линией подтверждён
  чтением условия, отдельная браузерная проверка окончательного варианта не выполнена.

Локальные снимки в `/home/gromov/.agent-browser/tmp/screenshots/`:

- `screenshot-1789970028521.png`, `screenshot-1789970039072.png` — 1440, светлая/тёмная;
- `screenshot-1789970039214.png`, `screenshot-1789970039131.png` — 1024, светлая/тёмная;
- `screenshot-1789970057843.png`, `screenshot-1789970132539.png` — 768, светлая/тёмная;
- `screenshot-1789970151669.png`, `screenshot-1789970133118.png` — 390, светлая/тёмная.

Артефакты локальные, в Git не входят. Автотесты фронтенда не добавлялись.

## 21 сентября 2026 — приёмка и остановка по поручению пользователя

- Пользователь подтвердил: «все нормально, проверки останавливайте, добавляйте логи».
  Дополнительные сценарии и команды проверок остановлены, результат принят.
- К моменту остановки открыты настройки проекта, но поля ещё не менялись. Проверки
  черновика, конкурентного изменения и большого списка с продолжением не выполнены;
  их прохождение не заявляется. docs:check и git diff --check также не запускались.
- Актуализированы досье, индекс и инженерное состояние. Справочник BOARDS.md,
  README Web и карта маршрутов сверены: их изменения не нужны, поскольку поля,
  серверный порядок, адреса, границы списка и архитектурные владельцы сохранены.
- Сессия закрыта командой `pnpm exec agent-browser --config apps/web/agent-browser.json
  --session tasks-web-board-groups-0921 close`. По PID и команде подтверждён собственный
  сервер 1200996; ему отправлен SIGTERM. Пользовательские 4700/5173 не затрагивались.
- Код и документы остаются в рабочем дереве без коммита. Следующий шаг — новое
  поручение пользователя; автоматическое продолжение проверок не запланировано.
