# Журнал множественных досок

## 19 сентября 2026 года

- Пользователь согласовал структуру досок, адреса по slug, файловый контейнер каждой
  доски и отдельный следующий этап полной переработки задач. Уточнил, что миграция
  не нужна, затем поручил реализацию. Системные доски создаются при init проекта.
- Исследование: существующая доска является выборкой всех задач; отдельной сущности
  нет. Приложение не имеет slug. Init публикует конфигурацию и создаёт прежнее хранилище.
  Продуктовая операция публикует один JSON; для приложения и доски требуется
  восстановление составной записи, а не только общая блокировка.
- План и критерии записаны в README до реализации. Существующие приложения без slug
  не мигрируются; рабочие пользовательские данные не удаляются.
- Пользователь выбрал `p2p-rental` местом приёмки и отдельно разрешил однократную
  адаптацию его локальных JSON без мигратора. Перед изменением создана резервная копия
  `.artifacts/p2p-rental-before-boards-20260919.tar.gz`. Двум приложениям добавлены slug
  `web` и `api`; ID, содержание, связи, ревизии и история сохранены. Созданы четыре доски.
- Реализованы Core, REST и SDK, навигация, отдельный экран доски с пустыми колонками
  и поле slug в форме приложения. Новые доски не используют прежние карточки, DnD и
  редактор задач: полная новая модель задач остаётся следующим этапом.
- По уточнению пользователя прежняя доска возвращена в навигацию отдельным пунктом
  «Прежняя доска» для сравнения функционала. Её удаление — отдельный последующий шаг
  после подтверждения пользователя; маршрут `/board` и прежние задачи сохранены.
- Промежуточные проверки: Core 29 тестов успешно; Web lint/typecheck/build успешно.
  Серверные тесты обнаружили отсутствующий scoped-маршрут `boards`, отображение
  предметного NOT_FOUND как 500 и устаревшие счётчики OpenAPI. Исправлены маршрутизация,
  таблица HTTP-ошибок и контрактные проверки; три затронутых HTTP/OpenAPI-теста пройдены.
- Браузерная проверка `p2p-rental`: открытие по UUID проекта, порядок четырёх досок,
  переход в `web`, светлая тема 1440, снимок и axe (0 нарушений). URL с ключом проекта
  вместо UUID в Web не поддерживается существующим выбором проекта; REST ключ принимает.
- Дополнены CLI (`--slug` и человеческое чтение адреса) и MCP (описание аргумента),
  пересобраны SDK и скилл. Публичные справочники и документы владельцев обновлены:
  продуктовые правила досок, Web, хранение/REST, CLI, MCP, архитектура, карточки реализации.
- `pnpm run typecheck` прошёл для всех пакетов. В общем `pnpm run test` прошли Core 29,
  server-runtime 36, MCP 6, project-runtime 2 и контрактные проверки. Исправлена найденная
  нехватка `--slug` в справочнике CLI; отдельный повтор `pnpm --filter @gromlab/relay-cli run test`
  завершился успешно: 88 тестов. Общий прогон остановлен существующим тестом
  `apps/server/test/dev-server.test.ts` (default): фикстура не содержит workspace-конфиг,
  который уже выбирался `apps/server/scripts/environment.mjs` до начала работы.
  Файл выбора конфигурации в этой работе не менялся. Остальные 9 тестов приложения Server прошли.
- Последние Web lint/typecheck/build успешны. Сборка сообщает предупреждение о крупном
  основном чанке. Временные сбои HMR при очистке SDK dist исчезают после восстановления
  сборки и reload; проверки выполнялись на стабильном состоянии после сборки.
- На `p2p-rental` выполнены чтение новых досок, переход на прежнюю доску, Back/Forward,
  reload, неизвестный slug с русской ошибкой, мобильное меню и его закрытие после перехода.
  Снимки сохранены для 1440/1024/768/390 в светлой и тёмной темах. Axe обнаружил отсутствие
  клавиатурного фокуса прокрутки; добавлен фокусируемый Mantine Box с ролью region.
  Повторный аудит: 0 нарушений. ArrowRight прокручивает колонки (наблюдаемый scrollLeft=40).
- На временном проекте `.artifacts/boards-check-JYRnNj` и собственном сервере 4791
  браузер создал приложение `board-check`: проверены зарезервированный slug, сохранение
  черновика после reload, отказ сети без потери ввода и успешный повтор после восстановления.
  Перечитаны `board.json` и пустой `tasks/`; новая доска появилась в навигации автоматически.
  Прямое создание `cli-check` через local CLI стало видно в браузере по SSE. JSON-квитанция
  создания и отдельный текстовый `product get` проверены: читаемый slug и полный Markdown.
  Конкурентность и восстановление прерванной записи проверены Core/HTTP-тестами;
  браузерная приёмка новой модели задач не проводилась, поскольку она ещё не реализована.
- Финальные проверки документов: `pnpm run docs:check` — 139 документов, 1010 ссылок,
  ошибок нет; ссылки распространяемого скилла — 54 без ошибок. `skills:check` подтверждает
  актуальность 16 файлов пакета, `git diff --check` успешен. Навигация из индекса работы,
  продуктового контракта и справочника проверена. Форматирование изменённых файлов проверено.
- Собственная сессия `tasks-web-boards-20260919` закрыта, собственный сервер 4791
  (PID 905457) остановлен. Пользовательский dev-workspace оставлен работающим.
  Точка приёмки — четыре доски `p2p-rental` и отдельная «Прежняя доска».
