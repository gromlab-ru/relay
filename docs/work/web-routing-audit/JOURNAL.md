# Журнал карты страниц и аудита роутинга

## 20 сентября 2026 — начало исследования

- Зафиксирована цель пользователя: карта страниц фронтенда с назначением и выявление
  нестыковок роутинга. Реализация исправлений не входит в поручение.
- Прочитаны обязательные документы и инструкции Web, применимые skills, профиль и правила
  маршрутизации. Инженерная карта будет описывать фактическое поведение, а не заменять
  продуктовые требования.
- `git status --short` не показал изменений; ветка `refactor/application-rebuild`, HEAD
  `c1c353f102d0ffa65681439da8286a58f3c647fe`.
- Начальная сверка: дерево URL находится в `app/router/app-router.tsx`; сценарии и
  реализации продукта подключены отдельно от `ProductLayout`; редактор использует
  общий `:collection/:entityId/edit`; доска имеет общего pathless-владельца для трёх адресов.
  Это наблюдения для последующей проверки, не итоговые диагнозы.

## 20 сентября 2026 — уточнение фокуса и подготовка проверки

- Пользователь дополнительно попросил найти разрывы логической последовательности:
  новую ветку URL вместо продолжения пути родителя. Это включено отдельным пунктом аудита.
- Статически обнаружены основания проверить вход `product` без завершающего слеша,
  потерю query задачи и возврат реализации к фиче вместо исходного приложения.
- Web lint и typecheck прошли; production-сборка прошла (повтор `build:web` использовал
  свежий результат Turbo). Исходники приложения не изменялись.
- `/tmp/opencode` доступен только на чтение текущему пользователю; создание временного
  каталога отклонено. Для изолированной базы и артефактов используется `.artifacts`
  внутри рабочего проекта. Пользовательские базы не используются.

## 20 сентября 2026 — карта и адресные сценарии Web

- Повторное уточнение пользователя закрепило область: только Web-приложение. Сервер
  и временная база использованы как окружение браузера; их устройство не является
  предметом карты. Исходники приложения не менялись.
- Составлена [инженерная карта](../../engineering/web-routing.md): страницы, назначения,
  меню, query/history state, совместимые адреса и разрывы последовательности.
- Главный разрыв воспроизведён: приложение `applications/WEB` → реализация
  `implementations/WEB-FI-1`; `state.returnTo` содержит приложение с якорем, но ссылка
  «К фиче» ведёт на `features/FEATURE-1`, активных ссылок подраздела в меню нет.
  Кнопка редактирования реализации не меняет URL.
- Подтверждены браузером: `product` без слеша → повреждённый URL/404; `product/` → паспорт;
  `boards` → продуктовая доска; короткий сценарий → вложенный адрес; несовпадающая
  фича отклоняется; `product/work/PRODUCT-1` → «Работа не найдена».
- Для сценария с `?q=Поиск&status=none` сняты фактические ссылки возврата и редактирования:
  обе без query. Для прямой задачи с `?q=Аудит&blocked=1` поиск пустой, фильтр выключен,
  закрытие убирает query. Короткий адрес задачи с query/hash канонизируется без них.
- Внутренняя Markdown-ссылка из фичи на доску создала новый Document; установленный
  маркер `window` исчез. Подключённого потребителя MarkdownLinkProvider в исходниках нет.
- Остальные выводы (раздельные ветки ProductLayout, возврат редактора через старый якорь,
  общий `:collection/:entityId/edit`) основаны на чтении кода. Запись продуктовых форм
  и полная проверка Back/Forward не выполнялись.
- Окружение: Node 24.18.1, свежая production-сборка Web, отдельный сервер 4798,
  база `.artifacts/relay-routing-audit-0920`, local-проект `project-9ymz` / `Lamz5Rsr`,
  собственная headless-сессия `tasks-web-routing-0920`. Изображение
  `.artifacts/relay-routing-audit-0920/implementation.png` просмотрено: родительское
  приложение отсутствует в заголовочной цепочке и активном подразделе меню.
- Axe для страницы реализации: 0 violations, 0 incomplete; `errors` пустой.
  Это адресная проверка, не приёмка всех страниц, тем, размеров, ошибок и черновиков.
- Ограничения инструментов: первый фоновый запуск завершился по timeout оболочки;
  повторный отдельный процесс успешно обслуживал браузер. При подготовке задачи
  ошибочный служебный запрос к `/tasks` дал 404, задача создана штатной CLI-командой.
  Одна `eval`-команда в batch имела ошибку quoting и повторена отдельной командой.
  Первое ожидание несовпадающего родителя застало незавершённое чтение; после reload
  ожидаемое состояние подтверждено. Эти попытки не считаются успешными проверками.
- Браузерная сессия закрыта. Команда собственного процесса проверена через `ps`,
  сервер PID 87935 остановлен SIGTERM. Временные артефакты игнорируются Git.
- Продуктовые контракты Web и PRODUCT проверены: существующий плоский адрес реализации
  действительно согласован; предложенная вложенность остаётся предложением. Тексты
  требований сохранены, добавлена навигационная ссылка на фактические расхождения.

## 20 сентября 2026 — завершение

- `pnpm run docs:check`: 164 файла, 875 ссылок, ошибок нет; поставляемый скилл —
  16 файлов и 59 внутренних ссылок, ошибок нет.
- Prettier первоначально запросил форматирование таблиц новой карты. После
  `pnpm exec prettier --write docs/engineering/web-routing.md` проверка всех семи
  изменённых/новых документов прошла. `git diff --check` успешен.
- Проверена навигация: инженерный индекс → карта → источники/контракт → досье/журнал;
  обратная ссылка из Web-контракта ведёт на фактическое состояние. Предложения
  не объявлены действующими требованиями.
- Аудит завершён. Следующее действие — пользовательское решение по структуре страниц
  реализации и отдельная работа над исправлениями. Коммиты не создавались.
