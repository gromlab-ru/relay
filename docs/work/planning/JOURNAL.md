# Журнал работы над разделом «Планы»

## 24 сентября 2026 — переносимость справочников и навигация библиотеки

Сборка скилла обнаружила ссылки новых справочников на рабочее досье, не входящее в
распространяемый пакет. Сведения об этапе разработки оставлены у инженерного владельца
и в досье; точные справочники связаны с переносимыми CLI/MCP/прогрессом. Повторные
`skills:build` и `skills:check` прошли: 22 файла, установленная копия Playground обновлена.

Пять прежних битых ссылок на `STORAGE-REFACTORING.md` заменены явной отметкой недоступного
источника и навигацией к действующему STORAGE. Glob и `git log --all -- ...` не нашли
исходный файл. Старое ТЗ и результаты его выполнения не выдумывались; исторические
выводы сохранены. Это необходимо для полноценной проверки библиотеки при передаче.

## 24 сентября 2026 — Web подключён, первый браузерный сквозной сценарий

Композиции переключены на серверные домены: отдельные страницы, полный прогресс,
реальные области, документы, история и снимок. Ревизия страницы этапов сопровождает
строки редактора, порядок меняется по полному списку. Общий журнал получил Markdown-
пояснение, хранящееся массивом строк. В карточке задачи показан текущий план.
Генерация трёх TSX записана в `apps/web/GENERATION.md`. После исправления
exactOptionalPropertyTypes события типизация/сборки/lint: 14/14 задач Turbo прошли.
SDK: 159 операций, 744 модели.

Временная база `.artifacts/planning-backend-jheqdS`, проекты a/b. Сервер: PID 1597231,
127.0.0.1:34957, запускатель `.artifacts/planning-backend-preview.mts`. Своя headless-
сессия `tasks-web-planning-backend-0924`; каждый вызов с явным конфигом Web. Начальные
вызовы без повторения --config попадали на about:blank; единая конфигурация решила запуск.

Через UI создан PLN-16 `yL33zYKx`, этап и включение настоящей PRODUCT-9. Пройдены abort
записи, сохранение ввода/requestId, reload и повтор, скрытый поиском выбор, панель задачи,
Escape, начало и итог. Ключ повтора создания — `f08b37cb-19bf-456a-b7fe-c3a56d8f7a06`.
Прямой local CLI изменил описание: SSE обновил фон, сохранив ввод «Мой несохранённый
заголовок» и ревизию. Сохранение вернуло конфликт; ввод остался. Для продолжения теста
черновик отброшен явно.

Создан REL-15 `DTOMsj8P` с готовым и незавершённым планами. Пройдены скрытый выбор,
планирование 1/2 и недоступность выпуска при 1/2. Через UI прикреплён DOC-1. После
явного снятия незавершённого плана релиз выпущен из редактора. Снимок содержит план,
этап, задачу, проект и документ. После CLI-правок задачи/документа и повторного открытия
задачи снимок после reload сохранил прежние тексты и 1/1. Текущий план показывает
расхождение; переход к нему, Back и Forward работают. История релиза раскрылась.

A11y плана: 0 нарушений WCAG, один incomplete у декоративного разделителя. Страница
прочитана визуально: `/home/gromov/.agent-browser/tmp/screenshots/screenshot-1790269830418.png`.
Матрица размеров/тем, большие списки, рестарт и окончательный прогон ещё впереди.

## 24 сентября 2026 — REST, SDK и интерфейсы агента

Добавлены контроллеры plans/releases и два прогресса, scoped-routing, OpenAPI и SDK
(159 операций после добавления task-candidates), Backend local/HTTP, команды CLI и
предметные MCP-инструменты. Адресные интеграционные сценарии REST, CLI и MCP прошли.
Типизация Core/Server/CLI/MCP и их зависимости: 11/11 задач Turbo прошли.

Полный предварительный прогон: Core 153/156 (три прежних предположения о девяти видах
и обязательном rename); REST 34/36 (фиксированное число 115 операций); CLI 35/38
(число видов, пока не обновлённая справка, запуск source-command вне pnpm run);
MCP 11/12 (пока отсутствовали новые инструменты в справочнике). Проверки новых сценариев
прошли. Ожидания видов/числа операций и справочники актуализированы; повтор полного
прогона требуется. CLI нужно запускать штатно через pnpm run test.

Ранее одновременная сборка зависимостей и адресные тесты вызвали отсутствие dist во время
чтения; повтор после завершённой сборки подтвердил REST/CLI. Discovery MCP выявил потерю
описаний после unwrap/removeDefault; описания восстановлены и адресный MCP-сценарий прошёл.

Начато подключение Web: новые домены planning/releases, SWR, ограниченное чтение страниц
и сохраняемый ключ неподтверждённого запроса в infra/tasks-api. Композиции ещё используют
прежние источники; следующий шаг — формы, каталоги, этапы, задачи и просмотр снимка.

## 24 сентября 2026 — первый предметный срез интеграции

Добавлены Contracts, кодеки единого хранилища, PlanningService/ReleasesService,
самодостаточные снимки, каталог новых видов и защита используемых задач/областей при
удалении. Линки записываются вместе с явными вызовами `replaceOwnedRelations`;
группы этапа не пересекаются с канбаном. Прогресс использует `taskCompletions`.

`build:contracts` и типизация Core прошли после исправления двух ошибок типов.
Первые предметные тесты обнаружили применение defaults к частичному update и строгий
разбор pagination с лишним `ref`; исправлены сохранение отсутствующих полей и внутреннее
выделение параметров страницы. Повторный адресный прогон `core/test/planning.test.ts`:
4/4 прошли (около 6 с). Проверены перенос/принадлежность/страницы/связи, снимок после
изменений и перезапуска, отказ до связи, прерывание после записи сущности и recovery,
совместимость старого канбана и явная миграция. Это ещё не проверка внешних интерфейсов.

Попытка с TMPDIR=/tmp/opencode остановилась до тестов из-за EACCES каталога. Для тестов
использован существующий локальный `.artifacts` как TMPDIR; временные базы убирает fixture.
Следующий шаг — REST/OpenAPI/SDK, Backend и команды CLI/MCP. Собственных серверов нет.

## 24 сентября 2026 — начало постоянной интеграции

Изучены четыре корневых ТЗ, протокол, правила библиотеки, контекст продукта, стандарт
линков/связей/контекста, инструкции всех затронутых владельцев и реальный код. Подтверждены
локальные UI-источники, шесть прогрессов, общая транзакция Workspace и отдельные группы
связей. Пользователь выбрал отдельный будущий этап запрета исполнения, одну текущую
принадлежность с историей и самодостаточный снимок, затем поручил реализацию.

`git status --short --branch`: чистая `refactor/application-rebuild`, HEAD `db8b8b8`.
Создан [план интеграции](INTEGRATION.md): владельцы, конкретизация пограничных правил,
хранение, обязательные связи и проверки. Прочитаны skills Unit Architecture,
React Reference и REST API Codegen, профиль Web и инструкции данных/форм/состояния.
Новая модель расширяет хранилище; прогресс переиспользует `taskCompletions`.
Проверок реализации и собственных серверных процессов на этой точке ещё нет.

## 24 сентября 2026 — начало исследования

Пользователь предложил ознакомиться с Relay перед обсуждением планирования и раздела
«Планы» в UI. Прочитаны обязательный протокол, правила библиотеки, концепция продукта,
стандарт интерфейсов, стандарт линков/связей/контекста, инструкции Web/Core, архитектура
и досье очистки прежней модели.

`git status --short --branch` показал чистое рабочее дерево ветки
`refactor/application-rebuild`; HEAD — `bff35fe` (`fix: черновая визуализация сущностей`).

Действующий `product/DIRECTION.md` уже фиксирует: план описывает самостоятельную цель
изменения даже при одной задаче, фича не принадлежит единственному плану, область может
быть общепроектной или адресной, завершение плана не всегда требует релиза приложения.
Эти принципы используются как исходный контекст; точная модель ещё требует обсуждения.

`apps/web/src/compositions/screens/plans/plans.screen.tsx` содержит только `StatePanel`
с заголовком «Планы» и текстом «В разработке». В исходниках обнаружен `WorkPlan`
у виджета продуктовой работы; его использование и происхождение ещё проверяются.
Статическое чтение не является браузерной приёмкой.

Создано досье отдельной цели: история удаления прежней модели не заменяет новую работу
над планированием. Программный код и пользовательские базы не менялись.

## 24 сентября 2026 — сверка исходников и уточнение пользователя

Проверены маршрут `plans`, навигация, `PlansScreen`, реестр видов сущностей,
`boardTaskSchema`, каталоги Core и поиск предметных операций в Core/Contracts,
server-runtime/rest-sdk, CLI и MCP. План и этап не представлены в действующем контракте;
поля участия задач в планировании отсутствуют. `parentId` имеет тип ссылки на другую
канбан-задачу, а не на этап.

Остатки прототипа найдены в `product-work-list`, `PRODUCT_WORK_SCHEMA` и демоданных.
Внешних потребителей `ProductWorkList` поиском в `apps/web/src` не обнаружено,
`create-product-view.ts` возвращает пустой `work`. Реальный раздел планирования нужно
строить на текущих задачах и общем хранении, а не считать прототип готовым механизмом.
Подтверждённые факты перенесены в инженерную сводку реализации.

Во время исследования пользователь уточнил основной процесс: сначала описываются
продукт, фичи, приложения и их вклады; затем ставятся продуктовые задачи и задачи
приложений; перед разработкой определяется план, его этапы и задачи к закрытию на
каждом этапе. Планов может быть несколько. Уточнение записано в `product/DIRECTION.md`
и `product/workflows/README.md`; кардинальность участия задачи, параллельность и правила
завершения оставлены открытыми вопросами, а не принятыми по предположению решениями.

Проверенные документы `product/README.md`, `product/CAPABILITIES.md`, `product/GLOSSARY.md`,
`product/ENTITIES.md`, `product/entities/product/MODEL.md`, `product/entities/tasks/KANBAN.md`
и `product/applications/web/README.md` не требуют правок в рамках этой сверки: существующие
сущности и ограничения не менялись, планы по-прежнему находятся в разработке.
Продуктовые правила задач целиком не проходили повторную приёмку; обнаруженное описание
плана не используется для автоматической реализации несогласованных правил.

Первый `pnpm run docs:check` завершился ошибкой на пяти ссылках на отсутствующий
`STORAGE-REFACTORING.md`: инженерный документ хранилища и четыре места в досье его
работ. Новые досье и индекс проверку ссылок не нарушили. `git ls-files --error-unmatch
STORAGE-REFACTORING.md` подтвердил, что файл отсутствует среди отслеживаемых.
Восстановление прежнего ТЗ хранилища не входит в обсуждение планирования.

## 24 сентября 2026 — основание начала работ и связь с релизом

Пользователь уточнил: план — это план работ и обоснование, чтобы начать работу;
наличие задачи само по себе не даёт такого основания. При создании релиза в него
нужно включать планы, по которым выполнялась работа, со сделанными результатами,
всеми задачами и необходимыми описаниями. Пользователь также обозначил открытый
вопрос: подготовка релиза сама является работой и нуждается в планировании.

Уточнения перенесены в направление продукта и сквозной процесс. В качестве предложения
для обсуждения сформулирована связка записи релиза с обычным планом его подготовки
и выпуска. Объединение сущностей, кардинальность, включение планов целиком либо по
этапам, переходы состояний и фиксация состава не объявлены принятыми решениями.
Нужные предметные линки и отдельные связи Core отмечены в точке продолжения.

До последнего уточнения пользователя выполнены повторный `pnpm run docs:check`
(те же пять исходных ошибок ссылок), `pnpm exec prettier --check` для всех шести
изменённых документов (успешно) и `git diff --check` (успешно).
Навигация вручную сверена по цепочке индекс работ → досье → направление продукта /
сквозной процесс → состояние реализации → журнал и точка продолжения.
Исполнение приложения, браузер и тесты кода не запускались: программный код не менялся.

## 24 сентября 2026 — остановка работ, продолжение только обсуждения

Пользователь явно уточнил, что сейчас требуется только обсуждение, без планирования
и ведения работ. Формализация и дальнейшие проверки остановлены; точка продолжения
сохраняет это ограничение. Ранее записанные варианты не являются утверждённым заданием
на разработку. Изменения ограничены документацией; программный код не изменялся.

## 24 сентября 2026 — поручение сохранить два черновика ТЗ

После обсуждения пользователь поручил записать в корень отдельные ТЗ на прогресс
и на планы. Это новое разрешение на документирование после прежней остановки работ.
Основа обсуждения: два типа плана, включая релизный; релизы в Web являются представлением
релизных планов. Для прогресса нужны предметные эндпоинты и разные типизированные ответы,
а общий механизм вычисления и обновления предложен как техническая основа.

В предшествующем обсуждении статически прочитаны текущие расчёты Core, адаптеры Web,
списки задач, обновление карточек, единое хранилище и SSE. Найдены общий каскад
`productTaskStatuses`, два запроса счётчиков продуктовой цели, клиентский обход доски
для прогресса приложения, чтение всех задач перед пагинацией и полное обновление каталога
карточек при записи. Выполнение этих сценариев и измерение производительности не проводились.

Перед записью повторно прочитаны обязательные правила и текущие документы. Рабочая копия
остаётся на `bff35fe`, ветка `refactor/application-rebuild`; исходные изменения направления,
сквозных процессов, сводки реализации, индекса и досье planning сохранены. Ведётся прежнее
досье обсуждения планирования; связанный черновик прогресса выделяется в самостоятельный
корневой документ. В ТЗ будут отделены основания пользователя, предложения реализации
и вопросы, на которые пользователь ещё не ответил. Проверки документов запланированы;
реализация кода и изменение баз не входят в это поручение.

## 24 сентября 2026 — корневые черновики и актуализация контекста

Созданы `PROGRESS-SPEC.md` и `PLANS-SPEC.md`. В первом зафиксированы пользовательская
группа progress и отдельные предметные ответы; адресный пересчёт и производные сводки
обозначены как техническое предложение. Второй использует два типа плана, раскрывает
включённые планы и собственные работы релизного плана, обратное чтение из задачи
и предлагаемый снимок выпуска. Кардинальности, запуск, параллельность и переход старых
данных оставлены открытыми вопросами. Овервью исключён из реализации.

В оба документа включены владельцы, сценарии, интеграция продуктовых линков с явной записью
Core, совместимость, границы списков и критерии приёмки. Обновлены навигация направления
продукта и индекс работ; в техническую сводку перенесены результаты статического исследования
прогресса. Непроверенная производительность не объявлена подтверждённой.

Концепция продукта, сквозные процессы, действующие правила продукта/канбана и справочники
API/CLI/MCP/хранилища проверены на применимость: черновики не меняют их действующие контракты,
а направление связывает их с будущим поведением. Источники и сборка продуктового скилла
не менялись. Новые отношения и пользовательские записи сейчас не создавались.

Базовый `pnpm run docs:check` до добавления черновиков подтвердил пять прежних ошибок:
ссылки на отсутствующий `STORAGE-REFACTORING.md`. Первая проверка `pnpm exec prettier --check`
всех семи затронутых документов и `git diff --check` прошла. Штатный сборщик списка документов
не включает произвольные корневые Markdown, поэтому для обоих ТЗ запланирована также
адресная проверка тем же `checkDocumentation`. Заключительные результаты будут записаны отдельно.

## 24 сентября 2026 — проверка черновиков и завершение документирования

Оба корневых ТЗ перечитаны полностью. Проверены взаимные ссылки, переходы из направления
продукта и индекса работ, соответствие пользовательскому обсуждению и явные границы
неутверждённых предложений. Уточнена формулировка о MVP: выпуск ограниченного объёма
не означает готовности всего описанного продукта, а не запрещает сам выпуск MVP.

Проверки на `bff35fe` с документальными изменениями:

- `pnpm run docs:check` после записи ТЗ — те же пять исходных ошибок ссылок на отсутствующий
  `STORAGE-REFACTORING.md`, новых ошибок в библиотеке не сообщено.
- Адресный вызов штатного `checkDocumentation` через `node --input-type=module -e`
  для двух ТЗ, направления, сводки реализации, индекса работ и README/JOURNAL досье —
  успешно: 7 документов, 109 ссылок. Корневые ТЗ включены явно.
- `pnpm exec prettier --check PROGRESS-SPEC.md PLANS-SPEC.md docs/product/DIRECTION.md docs/engineering/implementation/README.md docs/work/README.md docs/work/planning/README.md docs/work/planning/JOURNAL.md` — успешно.
- `git diff --check` — успешно.

Изменены только документы. Тесты приложения, сборка, браузерные сценарии и нагрузочные
замеры не запускались; будущие критерии приёмки не объявлены выполненными. Собственных
процессов нет, коммиты не создавались. Поручение записать черновики выполнено;
следующий шаг — согласование открытых решений. Итоговая точка продолжения обновлена.

## 24 сентября 2026 — начало корректировки ТЗ прогресса для передачи в разработку

После обсуждения пользователь выбрал отдельный контроллер прогресса, предметные обработчики
и собственный расширяемый контракт для каждого вида сущности. Недостающие сведения получают
запросом прогресса составляющей либо расширением кода и контракта текущего обработчика.
URL из разговора является примером; маршрутизация должна соответствовать реальному API Relay.
Производительность отложена: универсальный движок, сохранённые сводки, адресный пересчёт,
новые индексы и смена СУБД не являются обязательной основой этой реализации.

Последнее поручение уточняет объект изменения: `PROGRESS-SPEC.md`, несмотря на предшествующее
упоминание `PLANS-SPEC.md`. Цель — подготовить ТЗ прогресса другому агенту, не реализовывать код.
Владельцы и проверки остаются прежними: Core, Contracts, Server/SDK, CLI/MCP, Web;
документальные ссылки, форматирование и `git diff --check`. Прогресс читает предметные
обязательства, не обходит контекст и не создаёт линков или связей. Миграция данных не требуется
для текущей документальной работы.

Сверены рабочая копия и HEAD `bff35fe`, ветка `refactor/application-rebuild`: ранее изменённые
документы и оба неотслеживаемых ТЗ сохранены. Прочитаны текущий черновик, досье, направление,
инженерная сводка и инструкции владельцев. План: переписать требования без прежней обязательной
оптимизации, согласовать навигационные описания, проверить документы и сохранить передачу.

## 24 сентября 2026 — ТЗ прогресса переработано под предметные контракты

`PROGRESS-SPEC.md` заменяет прежнее техническое предложение: выбран отдельный контроллер,
обработчики Core и контракты по видам. Зафиксированы сценарий раскрытия до задачи, причины,
различие фактического выполнения и колонки, обязательность незавершённой инфраструктурной
зависимости и согласование старых полей с новым расчётом. Автоматическое перемещение карточек
не объявлено принятым. Точные URL/DTO оставлены разработчику в рамках предметных требований.

Убраны обязательные новые индексы, инкрементальный движок, сохранённые сводки, миграция
и нагрузочные бюджеты. Допустим расчёт при чтении; точность, согласованность и явные границы
списков остаются критериями. Планы и выпуски подключаются после реализации их модели,
а не блокируют прогресс существующих сущностей. ТЗ планирования не менялось.

Обновлены направление продукта, инженерная сводка и навигация работ. Действующие описания
продукта, канбана, API и хранения не переписаны как будто новая функциональность уже готова:
изменение семантики и необходимость их актуализации при внедрении явно заданы в ТЗ.
Концепция, стандарт линков и интерфейсные правила остаются применимы; прогресс не читает
контекст и не создаёт отношения. Источники скилла не затронуты. Далее — проверка документов.

## 24 сентября 2026 — проверки корректировки и передача

Полностью перечитано обновлённое ТЗ: выбранный подход, нефиксированный URL, поэтапное
раскрытие, собственные схемы и отложенная оптимизация согласованы между разделами.
Проверен переход индекс работ → досье → ТЗ → продуктовые правила / ТЗ планирования.

Фактические проверки на `bff35fe` с документальными изменениями:

- `pnpm run docs:check` — пять прежних ошибок ссылок на отсутствующий
  `STORAGE-REFACTORING.md`; новых ошибок библиотека не сообщила.
- Адресный `checkDocumentation` через `node --input-type=module -e` для `PROGRESS-SPEC.md`,
  `PLANS-SPEC.md`, направления продукта, инженерной сводки, индекса работ и README/JOURNAL
  досье — успешно: 7 документов, 108 ссылок.
- Первичная проверка Prettier нашла только форматирование новой таблицы `PROGRESS-SPEC.md`.
  Выполнен `pnpm exec prettier --write PROGRESS-SPEC.md`, повторный `--check` всех шести
  изменённых документов прошёл.
- `git diff --check` — успешно.

Программный код и базы не изменены; тесты приложения, сборка и браузер не запускались.
Коммиты не создавались, собственных процессов нет. Точка продолжения обновлена для передачи
агенту разработки. Результаты проверок документации не являются проверкой будущего прогресса.

## 24 сентября 2026 — последующая реализация

По отдельному поручению пользователя выполнена реализация шести видов прогресса.
Код, решения и фактические проверки ведутся в [отдельном досье](../progress/README.md).
Точка передачи этого исследования связана с актуальным результатом; модель планирования
остаётся отдельной работой.

## 24 сентября 2026 — начало UI/UX-реализации

Новое поручение: начать разработку планов с особым упором на красоту, понятность и
соответствие дизайну Relay; сначала UI/UX, затем логика. Решения пользователь доверил
разработчику. Исходное дерево чистое, HEAD `41237e0`, ветка `refactor/application-rebuild`.

Прочитаны обязательные правила, `PLANS-SPEC.md`, профиль и инструкции Web, необходимые
разделы React Reference / Unit Architecture и локальные шаблоны генерации. Подтверждены
графитовые токены, Mantine, общий каркас и заглушки планов/релизов. Блокирующий протокол
Web выполнен; архитектура, этапы и проверки записаны в PLAN.md до первой правки кода.

Выбран UI-прототип с типизированными примерами и явной отметкой демонстрационного режима.
Это позволяет оценить составной сценарий до предметного API, которого сейчас нет.
Общий widget обслуживает оба каталога; домен примеров владеет данными, вложенные области
не импортируют родителя. Изменения хранятся только в текущей вкладке по ID проекта.
Необходимые отношения Core перенесены в точку продолжения предметного этапа.

## 24 сентября 2026 — визуальная основа

Штатным генератором созданы planning-workspace, plan-catalog, plan-card, plan-detail,
plan-overview, release-content, plan-stages, plan-task, task-picker и plan-form.
Каталог, подробности и состав выпуска собраны на общей теме: графитовые поверхности,
тональные смысловые акценты, типографическая иерархия, вертикальные этапы.
Адресные планы подключены к существующим screens и router. Модель примеров находится
в отдельном domains/planning-demo, постоянные данные Relay не менялись.

Первая проверка `pnpm --filter @relay/web run typecheck` прошла. На временном проекте
`.artifacts/planning-preview-6KbR4n` запущен API 4786 и Vite 5186. Собственная headless-сессия
agent-browser — `tasks-web-planning-0924`. Каталог и план открыты, скриншоты 1440 просмотрены:
общий ритм согласован с Relay, этапы и сводка читаются отдельно. Найден лишний focus-контур
у заголовка каталога; исправление включено в следующий этап. Кнопки редактирования ещё
не подключены: первая оценка касалась визуальной структуры, не готовности сценариев.

Вызовы agent-browser должны включать один и тот же `--config apps/web/agent-browser.json`:
без него следующий вызов попал в about:blank. Повтор с одинаковым config восстановил
ожидаемую именованную сессию. Скриншоты CLI сохраняются в локальном каталоге
`/home/gromov/.agent-browser/tmp/screenshots/`, несмотря на переданный относительный путь.

## 24 сентября 2026 — взаимодействия и первая сквозная проверка UI

Подключены локальное сохранение по проекту/вкладке, проверка версии формата, Markdown
массивами строк, редактор с черновиком, создание/изменение/порядок этапов, подбор задач
с фильтрами и сохранением скрытого выбора, состав выпуска. Начало не перемещает задачи;
завершение требует итог, отмена — причину. Фиксация релиза отключена с объяснением будущего
снимка Core. Общие серверные правила этим демонстрационным доменом не вводятся.

На временном проекте создан «Проверка интерфейса планирования», добавлен этап и две
задачи с разных досок. Выбор сохранился при поиске и закрытии окна. После запуска
и reload план активен, обе задачи остались в `todo`; JSON хранит Markdown массивами.
Редактор сохранил название, описание и Markdown после Escape и перезагрузки.
Симуляция QuotaExceededError при записи набора оставила форму открытой и старое имя
плана неизменным; после восстановления хранилища повтор успешно сохранил новый заголовок.
Отмена сохранила причину. Создание релизного типа из общей формы выдало `REL-002`.
В релиз добавлен третий план; скрытый поиском выбор сохранился, reload подтвердил состав.

Браузер выявил одинаковые React-key у подробностей и редактора одной записи: после
редактирования могли оставаться лишние DOM-узлы. Ключи разделены по роли, повторная
проверка сохранения показала один h1 и ноль диалогов после закрытия. Исправлены контраст
синих подписей и заголовка следующего шага, семантика групп и повторный banner в Modal
через публичный Mantine attributes. Axe каталога/плана — 0 нарушений; форма создания
релиза на 390 в тёмной теме — 0 нарушений и 0 incomplete. Ручная проверка декоративных
разделителей и контраста дополняет аудит. Внешняя ширина 390 не переполняется.

`lint:web`, `typecheck:web`, `build:web` прошли после исправления двух ошибок label формы.
После последующих локальных улучшений предстоит заключительная проверка. Автотесты Web
не добавлялись. UI-проверки не подтверждают серверные конфликты, SSE или снимки планов.

## 24 сентября 2026 — завершение UI/UX-этапа

Финальный интерфейс использует компактную полосу прототипа, общие поверхности Relay,
раздельные карточки собственных работ и включённых планов. На мобильном экране сводный
прогресс виден над целью, действия помещаются в строку, полная сводка доступна по якорю.
Раскрытие этапа сохраняется при изменении состава; список этапов и задач имеет продолжение.

Фактическая браузерная приёмка собственной headless-сессией:

- Каталог и подробности: светлая/тёмная темы, ширины 1440, 1024, 768, 390; скриншоты
  просмотрены, DOM-замер во всех сочетаниях подтвердил отсутствие горизонтального переполнения.
- Релизный состав отдельно просмотрен на 1440 в обеих темах и на 390; axe финального
  релизного плана в обеих темах — 0 нарушений, 0 incomplete. Каталог и рабочий план —
  0 нарушений, ручная оценка дополнила incomplete для декоративных точек и обрезанных
  горизонтальной прокруткой подписей фильтров.
- Новый рабочий/релизный план, название/Markdown, ошибка обязательного названия и фокус,
  Escape/reload черновика, добавление этапа, выбор задач разных досок, скрытый выбор,
  запуск без смены колонок, отмена с причиной, включение плана в релиз и reload — успешно.
- Потеря сети сохраняет редактор и черновик при закрытии/открытии. Отказ записи
  QuotaExceededError не закрывает форму и не меняет подтверждённое содержимое; повтор успешен.
- «Начать с пустого каталога» и восстановление примеров проверены через UI. Пустой поиск
  и неизвестный ID имеют отдельные объяснения. Повреждённый JSON не подменяется примерами.
- Временный набор из 25 планов показал 12 → 24 → 25 карточек. Исходный набор восстановлен.
  Back/Forward восстановили фильтр `status=draft` и адрес плана. Enter на summary раскрыл этап,
  фокус остался на SUMMARY. Клавиатурное закрытие форм работает.
- После исправления ключей повтор редактирования дал один h1 и ноль оставшихся диалогов.
  Заключительные `errors` пусты; в console только штатные сообщения Vite/React DevTools.
  В просмотренном сетевом журнале нет POST/PUT; планирование не вызывает API записи проекта.

Ключевые локальные скриншоты в `/home/gromov/.agent-browser/tmp/screenshots/`:
`screenshot-1790249098466.png` — светлый каталог 1440;
`screenshot-1790248604914.png` — тёмный каталог;
`screenshot-1790249670249.png` / `screenshot-1790249671417.png` — релиз в обеих темах;
`screenshot-1790249673493.png` — итоговая мобильная шапка плана;
`screenshot-1790248949335.png` — мобильное создание релизного плана.
Артефакты локальные и не входят в Git.

Заключительные проверки кода поверх `41237e0`:

- `pnpm run lint:web` — успешно.
- `pnpm run typecheck:web` — успешно, включает фактическую production-сборку зависимостей/Web.
- `pnpm exec turbo run build --filter=@relay/web --output-logs=errors-only` — успешно,
  подтверждён актуальный результат сборки из кеша после предыдущей команды.
- Prettier изменённых исходников/документов и `git diff --check` — успешно.
- `checkDocumentation` для 16 затронутых документов — успешно, 240 ссылок на момент проверки.
- `pnpm run docs:check` — только пять прежних ссылок на отсутствующий `STORAGE-REFACTORING.md`;
  новых ошибок библиотека не сообщила. Это исходный блокер полной проверки документации.

Актуализированы Web-контракт, карта возможностей, локальные README/ROUTING/GENERATION,
инженерное покрытие, направление и досье. Действующие Contracts/Core/API/CLI/MCP,
хранилище и skill не менялись; оснований пересобирать скилл или менять их справочники нет.
Собственная сессия закрыта; SIGTERM получили только процессы API 4786 и Vite 5186,
`ss` подтвердил отсутствие слушателей этих портов. Коммиты и публикация не выполнялись.
UI/UX-этап завершён; следующая предметная интеграция описана в точке продолжения.

## 24 сентября 2026 — убрать выделенный план из каталога

Пользователь попросил удалить «План в фокусе»: достаточно состояния «В работе».
Продолжается то же досье и рабочая копия поверх `41237e0`; прежние изменения сохранены.
План правки: в существующем `plan-catalog` удалить общий выделенный блок, вычисление
выбранного плана и его неиспользуемые стили; уточнить Web-контракт и инженерное состояние;
проверить каталоги планов/релизов, фильтр «В работе», темы и адаптивность, затем
lint/typecheck/build и документы. Новых юнитов и TSX нет; применяются ранее прочитанные
профиль Web и правила React Reference / Unit Architecture. Продуктовые данные,
машинные контракты, линки и связи Core не затронуты: меняется только композиция каталога.

Из общего каталога удалены выделенный блок (включая его релизное представление),
автовыбор записи, вычисления прогресса для блока, неиспользуемые импорты и 156 строк CSS.
Сразу после заголовка идут фильтры и карточки. Уточнены Web-контракт, PLAN и техническое
состояние; README/ROUTING/GENERATION и предметные справочники остаются верными:
маршруты, генерация, действия и хранение этой правкой не менялись.

Проверки на той же рабочей копии поверх `41237e0`:

- `pnpm exec turbo run lint typecheck build --filter=@relay/web --output-logs=errors-only` —
  все 5 задач успешны, Web заново проверен и собран.
- Agent-browser `tasks-web-planning-no-focus`, временная база
  `.artifacts/planning-preview-kSspCJ`, API 4786/Vite 5186: в планах 6 карточек и
  0 блоков фокуса; фильтр «В работе» даёт 2 карточки. В релизах 1 карточка,
  выделенного выпуска нет. Обе темы и 1440/1024/768/390 без горизонтального переполнения.
- Скриншоты 1440/390 в обеих темах просмотрены. Каталоги планов и релизов: axe —
  0 нарушений, 0 incomplete. `errors` пуст, console содержит только сообщения разработки.
- Prettier и `git diff --check` прошли. Общий `docs:check` — пять прежних ссылок
  на отсутствующий `STORAGE-REFACTORING.md`; новых ошибок нет. Адресная проверка
  изменённых документов проходит отдельно.

Человеческое представление и машинный состав сверены в браузере; доменный формат
и данные не изменялись. Свою сессию и оба процесса закрыли, `ss` подтвердил освобождение
портов. Новые локальные логи: `.artifacts/planning-no-focus-*.log`. Правка завершена.

## 24 сентября 2026 — релизы выделяются из планирования

Пользователь отменил релизный тип плана: план — только работа. Релиз становится отдельной
сущностью со статусом; при создании выбираются планы, чтобы заранее запланировать выпуск.
Продолжается UI-этап. Ветка `refactor/application-rebuild`, HEAD `41237e0`, прежние
незакоммиченные изменения сохранены. Прочитаны обязательные правила, текущие владельцы
Web, модель и хранилище прототипа, добавлен раздел доменных границ Unit Architecture.

До реализации зафиксированы владельцы, модель, совместимое чтение, требуемые связи
будущего Core и проверки в PLAN.md. Выбраны отдельные модель/хранилище releases-demo
и самостоятельная проекция releases. Плановый UI не импортирует релизный UI.
Канонические ТЗ будут разделены: PLANS-SPEC — работа, RELEASES-SPEC — состав и статус выпуска.

## 24 сентября 2026 — отдельный релиз реализован в UI

Пользователь попросил закончить релиз, сохранив текущий вид панели задачи из этапа.
Дополнительная переработка визуального шума не выполнялась. Плановый каталог, форма
и подробности теперь содержат только работу: релизные поля/выбор/действия удалены.
Панель чтения задачи и основные этапы сохранены.

Создан `domains/releases-demo`: отдельный локальный формат, собственные статусы
planned/released/cancelled, выбранные planIds и снимок результата. Создание релиза
показывает выбор планов сразу; незавершённые планы допустимы для планового выпуска.
Новый экран и вложенные области созданы генератором, release-content перенесён к релизам.
Восстановление v1 сохраняет оригинал: прежние собственные этапы/задачи становятся
обычным планом подготовки с постоянным детерминированным ID. Плановый формат v2
не содержит kind/version/includedPlanIds. Новые формы читают подходящие старые черновики.

ТЗ разделены на PLANS-SPEC и RELEASES-SPEC; направление, прогресс и Web-контракты
приведены к новой модели. Предварительные typecheck и lint/typecheck/build прошли.
В браузере создан «Октябрьский выпуск» из активного и завершённого планов: сохранён
самостоятельный статус planned, плановое хранилище не записывалось. Выбор сохраняется
при поиске, Escape и reload. Выпуск неполного состава отклонён с сохранением формы;
после исключения активного плана зафиксирован released с локальным снимком и датой.
Reload подтвердил snapshot и Markdown массивами; axe формы и результата — 0 нарушений.
В отдельной сессии проверен v1: 6 записей/27 задач читаются, тип релиза исчез из каталога,
старая REL-099 стала отдельным релизом с прежним ID и добавленным планом подготовки;
после сохранения исходная строка v1 побайтово не изменилась. Финальные проверки продолжаются.

## 24 сентября 2026 — проверка самостоятельных релизов и завершение

Проверки на временном проекте `.artifacts/planning-preview-ocQGIT`, ID `XcXQUqXb`:

- В форме нового плана нет радио выбора типа, версии и релизных опций. В каталоге
  читаются только планы работ. Подробности плана больше не подключают UI релизов.
- Новый релиз с активным и завершённым планами сохраняется как planned. Статус
  отменён/перепланирован через форму; сравнение сохранённого JSON подтвердило, что
  планы и задачи не изменились. Выбор нескольких планов сохраняется при поиске и reload.
- Пустой состав отклонён. Попытка released с активным планом показала предметную ошибку
  и сохранила форму. После исключения активной работы сохранён released со снимком
  выбранного плана. После изменения текущего названия плана снимок сохранил прежнее имя.
- QuotaExceededError при записи релиза оставил ввод в форме и не дал ложного успеха;
  повтор после восстановления storage сохранил запись, закрыл диалог, оставил один h1.
- Каталог и подробности релизов проверены в обеих темах при 1440/1024/768/390:
  горизонтального переполнения нет. Просмотрены снимки широкого/мобильного интерфейса
  и формы. Axe формы на 1440 и 390, каталога и подробностей — 0 нарушений, 0 incomplete.
  Консоль содержит только сообщения Vite/React DevTools, errors пусты в обеих сессиях.
- Совместимое чтение v1 проверено отдельной сессией: ID/ключ релиза, тексты, выбранные
  планы и собственные прежние работы сохранены. Первое новое сохранение не переписало v1.
  Локальное сохранение Markdown проверено отдельно от отображения: массивы строк.

Ключевые локальные скриншоты в `/home/gromov/.agent-browser/tmp/screenshots/`:
`screenshot-1790255701341.png` — создание релиза;
`screenshot-1790256817870.png` — тёмный каталог;
`screenshot-1790256857863.png` — светлая страница;
`screenshot-1790256855064.png` — мобильная страница;
`screenshot-1790256921852.png` — мобильная форма.

Заключительные проверки:

- `pnpm exec turbo run lint typecheck build --filter=@relay/web --output-logs=errors-only` —
  успешно, 5/5 задач, включая новую сборку Web.
- Prettier и `git diff --check` — успешно.
- `checkDocumentation` для всех 21 изменённых/новых Markdown, включая корневые ТЗ, —
  успешно, 283 ссылки.
- `pnpm run docs:check` — только пять исходных ссылок на отсутствующий
  `STORAGE-REFACTORING.md`, без новых ошибок.

Проверен граф владельцев: releases → planning-demo через публичное API, обратной
зависимости нет; дочерние композиции используют фасеты и не импортируют предков.
Серверные контракты, Graph/Core, CLI/MCP, реальная база и источники скилла не менялись;
их новые предметные реализации не объявлены готовыми. Документы владельцев и ТЗ
актуализированы. Панель задачи сохранена по последнему поручению пользователя.

Обе собственные сессии закрыты, API 4786 и Vite 5186 остановлены адресными SIGTERM;
`ss` подтвердил освобождение портов. Изменения не закоммичены, исходные предыдущие
правки сохранены. UI-этап завершён, точка продолжения обновлена.

## 24 сентября 2026 — краткое ТЗ интеграции с бекендом

Пользователь поручил создать в корне короткое базовое ТЗ подключения готовых разделов
к бекенду. План: описать постоянные сущности, предметные операции, связи Core,
подключение Web и критерии приёмки со ссылками на существующие ТЗ; проверить ссылки
и форматирование. Сверены PLANS-SPEC, RELEASES-SPEC и точка продолжения.
Рабочая копия поверх `41237e0` содержит предыдущие изменения, они сохраняются.
Это документальное поручение: реализация, данные и окружение не меняются.

Создан `PLANS-RELEASES-INTEGRATION-SPEC.md`: пять коротких разделов о модели/хранении,
операциях, связях и надёжности, API/Web и приёмке. Подробные правила не продублированы,
даны ссылки на существующие ТЗ. Обновлены направление, индекс работ и точка продолжения.
PLANS-SPEC, RELEASES-SPEC, PROGRESS-SPEC и инженерное состояние остаются верными:
задание описывает следующий этап, текущий бекенд не объявляется реализованным.

Проверки: адресный `checkDocumentation` — 5 документов, 90 ссылок, успешно;
Prettier и `git diff --check` прошли. `pnpm run docs:check` сообщил только пять
прежних ссылок на отсутствующий STORAGE-REFACTORING.md. Новый документ перечитан,
навигация из направления и досье проверена. Код, базы и скилл не менялись;
серверы, браузеры и тесты приложения не запускались. Поручение выполнено.
