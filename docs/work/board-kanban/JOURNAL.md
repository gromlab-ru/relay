# Журнал канбана задач

## 2026-09-19 — финальные проверки перед коммитом

- Компактная форма подтверждена браузером: открытие без popup, выбор доски,
  поиск названия «депозита» и ключа INFRA-44, выбор стрелками/Enter, продолжение
  списка с 40 до 44 задач. PRODUCT-14 (done) и PRODUCT-4 (cancelled) не выдаются.
  Выбранная PRODUCT-3 записана как дочерняя; существующие связи не скрыты.
- Статусный дефект проверен до и после: ранее API review / UI in-progress,
  после исправления оба перехода review↔in-progress совпадают в UI и API.
  Адресная мутация кеша дополнена проверкой при обычном открытии; создание сохраняет
  использование первоначального ответа без дополнительного GET.
- У полей computed outlineStyle=none; фокус меняет единственную рамку. Автофокус
  селектора при открытии формы удалён. CodeMirror использует рамку focus-within.
  Исправлен обнаруженный недостаточный контраст оранжевой метки «На проверке».
- У динамического Combobox исправлены устаревший aria-activedescendant и доступ
  клавиатурой к прокрутке. Popup остаётся внутри диалога. Финальный axe: диалог без
  раскрытого списка 0/0; открытый dropdown 0 нарушений и 2 incomplete, описанные в README.
- Полные Core 37/37, Server Runtime 38/38, CLI 89/89, MCP 7/7 успешны; root typecheck,
  Web lint/build, сборка скилла и проверка документации прошли. Из-за расширенного
  состава накопленного коммита дополнительно проверены прежние короткие ID,
  CLI/HTTP совместимость и восстановление хранилища полными наборами этих пакетов.
- Состав Git сверён: изменения префиксов, коротких ID, Core/REST/CLI/MCP, Web,
  документации и generated SDK/скилла относятся к накопленной работе канбана.
  Пользователь поручил фиксацию и push. После fetch origin текущая ветка имела 0/0.
  Собственные браузер и процессы 354205/355767 закрыты, пользовательские не затронуты.

## 2026-09-19 — статус после DnD и фокус полей

Воспроизведён новый дефект пользователя: открыть WEB-1, закрыть, перенести клавиатурой
в review, открыть снова — API возвращает review, карточка показывает «В работе».
Причина: общая SWR-revalidation не перечитывает закрытую карточку без подписчика,
а useBoardTask запрещает чтение устаревшего кеша при монтировании. План: адресно
обновлять полную карточку по ID после DnD, затем обновлять проекции доски.

Пользователь также попросил убрать автоматическое открытие dropdown при добавлении
связи и двойную рамку полей во всём проекте. Автофокус больше не открывает селектор;
владелец общего оформления ui/themes оставляет одну рамку поля с изменением цвета
при фокусе. CodeMirror следует тому же правилу. Читаемость клавиатурного фокуса сохраняется.
Для динамического dropdown отдельно проверяется ARIA и клавиатура при смене результатов.

## 2026-09-19 — компактные селекторы связи

Пользователь отклонил большой список/предпросмотр, согласовал кнопки типов и два
селектора; уточнил, что dropdown задач сужается вводом ключа/названия и не содержит
готовых/отменённых. Фильтрация переносится до серверной пагинации, чтобы первая
страница завершённых задач не давала ложное пустое состояние. Рабочее дерево сверено;
предыдущие изменения сохранены. План и границы записаны в README.

## 2026-09-19 — итог проверки и точка продолжения

- Финальный сценарий выбора: выбрана доска «Продукт», PRODUCT-3 и дочерний тип;
  после смены на «Инфраструктура» выбор сброшен, типы скрыты и сохранение недоступно.
  Затем выбрана INFRA-1, тип «Связана с»; API подтвердил related на инфраструктурную задачу.
- Обратный список сценария показал WEB-1 и открыл её карточку. Документ «Правила
  доступности» прочитан в том же диалоге (dialogs=1). В мобильном выборе фокус получает
  поле поиска, ширина документа 390px. Ошибок JavaScript не наблюдалось; axe мобильного
  диалога с продуктовым выбором — 0 violations/0 incomplete.
- Финальные Web lint/typecheck/build прошли. Сборка сохраняет известный большой
  основной чанк; CodeMirror вынесен в динамические чанки. Полный корневой test не запускался.
- Сборщик скилла сначала отклонил превышение бюджета 220 строк. Инструкции сжаты,
  skills:build/check прошли (17 файлов); docs:check — 144 документа/1040 ссылок без ошибок.
  Проверены и обновлены продуктовые, инженерные, CLI/MCP-справочники и направление развития.
- Собственная сессия закрыта; через ss/ps проверены процессы 280737/241088 и отправлен
  SIGTERM. Рабочее дерево оставлено без коммита. Продолжение — пользовательская оценка UI.

## 2026-09-19 — доска перед выбором связанной задачи

По новому уточнению пользователя форма связи перестроена: сначала доска, затем
поиск/выбор задачи только этой доски, после — уже существующий тип связи.
При смене доски выбранная задача сбрасывается. Наличие одной связи не запрещает
выбрать ту же карточку для другого типа; повтор того же типа отмечается отдельно.
Каталог досок поддерживает продолжение и ошибку загрузки. Владелец — task-relations,
данные через существующие фасеты domains/boards и domains/board-tasks.

## 2026-09-19 — реализация и проверки наполнения

- Core сохраняет до 100 целей реализации, проверяет вид и проект, отклоняет дубли;
  чтение версии 1 не пишет файлы, следующая атомарная запись публикует v2.
  Обратный список productTarget доступен через API/CLI/MCP. SDK получен закреплённым
  генератором с собственного сервера 4793: 93 операции, 401 модель.
- CodeMirror загружается по входу в редактирование. Начальный текст фиксируется ref:
  обычное обновление React не пересоздаёт редактор и не сбрасывает undo/выделение.
  Сочетание Ctrl/Cmd+Enter перехватывается React capture до штатного Mod-Enter CodeMirror.
  В браузере первоначальный fill contenteditable вставлял вместо замены; для проверки
  использованы реальная фокусировка, Ctrl+A и keyboard inserttext.
- Для обратных списков создан штатным генератором compositions/widgets/product-tasks,
  потребители — фича, сценарий и контракт приложения. Виджет получает ID, данные через
  domains/board-tasks, загружает по явному раскрытию. Команда записана в GENERATION.md.
- Пройдены root typecheck (18 задач Turbo, включая Web build), Web lint; Core канбан
  7/7, HTTP/OpenAPI 4/4, CLI канбан 1/1, MCP 7/7 после удаления типа эпика.
- Браузер tasks-web-content: постоянный выбор общего сценария и реализации Web,
  чтение/сохранение Markdown, Ctrl+Enter, тот же DOM CodeMirror при разворачивании.
  Принудительный сетевой отказ сохранил черновик после закрытия/повторного открытия.
  Внешняя API-правка вызвала конфликт, ввод остался; явная сверка и сохранение успешны.
- Меню статуса отклонило done при блокерах и сохранило прежнее значение; выбор
  in-progress и перенос product→web прошли сразу, URL стал boards/web/c6DBNw7n.
  Выбор существующей PRODUCT-2 как дочерней сохранил parentId=c6DBNw7n и обратную связь,
  новых карточек при связывании не создано.
- Просмотрены снимки 1440/1024/768/390 и тёмной темы. На 390 documentWidth=390.
  axe диалога в светлой/тёмной настольной теме: 0 violations, 0 incomplete.
  Снимки находятся в .artifacts/web/task-content-*.png; более ранние отражают промежуточный UI.
- Временные данные: .artifacts/kanban-ui-JVplwr, проект eUELe6ZZ. Пользовательские
  проекты не наполнялись. Финальная документация и закрытие своих процессов — следующий шаг.

## 2026-09-19 — дочерняя задача как связь

По уточнению пользователя из карточки убраны отдельные кнопки/форма создания
подзадачи. Единый раздел «Связи с задачами» добавляет существующую карточку с типом
«Дочерняя задача» или «Родительская задача», наряду с зависимостями и обычной связью.
Направление каждого типа объясняется относительно текущей задачи.

## 2026-09-19 — уточнения во время реализации карточки

Пользователь подчеркнул приоритет гармоничного UI/UX, отменил отдельный тип «Эпик»
и запросил немедленную смену статуса/доски выбором значения. Затем указал, что селекты
и сам блок «Детали» не вписываются. Тип удалён из модели и всех интерфейсов;
остаются обычное родительство и декомпозиция. Справа компактные строки без заголовка,
рамки и подложки; цветная метка статуса и название доски открывают меню.
Выбор сразу сохраняется, отказ оставляет подтверждённое значение и сообщение рядом.

## 2026-09-19 — начало наполнения карточки

Пользователь согласовал предложенный единый обзор, множественные продуктовые связи,
тип эпика и двухрежимный Markdown. Сверены текущие Core board-task/service/storage,
продуктовая модель и Web task-editor/markdown-field: продуктовая связь пока только
локальный выбор, родительство/зависимости уже постоянные, редактор — textarea с вкладками.
План владельцев, совместимости и проверок записан в README до реализации.

## Завершение рабочего окна по подтверждению пользователя

- Пользователь оценил результат положительно и поручил завершить работы; наполнение
  карточки оставлено на следующий этап. Дополнительная реализация остановлена.
- Реализованы два размера модалки, сохраняемые панели вкладок, чтение реального
  продуктового контекста с временным выбором, поиск/предпросмотр и группировка связей.
  Новый task-context создан штатным генератором, команда записана в GENERATION.md.
- Адрес изменён по уточнению пользователя на boards/:boardSlug/:taskId. Проверены
  обычное открытие, старый адрес и перенос между досками с неизменным DOM окна.
- В проверках исправлены контраст заголовка предупреждения, ограничения отступов
  развёрнутого окна и чтение подписи выбранного контекста до открытия соответствующей вкладки.
  Собственные изменения связей/положения обновляют базовую ревизию черновика без ложного конфликта.
- Подробные фактические результаты браузерной проверки и ограничения сведены в README.
  Финальные lint/build/typecheck, docs:check и diff-check успешны. Сохранено известное
  предупреждение размера чанка. Продуктовые/инженерные документы актуализированы;
  серверные REST/CLI/MCP-контракты не менялись, продуктовая привязка явно предварительная.
- Закрыта сессия tasks-web-modal. Через ss/ps установлены собственные PID 138936/139180
  и отправлен SIGTERM. Пользовательские процессы и данные не затронуты, коммиты не делались.

## Уточнение маршрута в ходе рабочего окна

Пользователь уточнил канонический адрес: `boards/:boardSlug/:taskId`, поскольку у доски
используется slug. Полный путь включает проект. Старый `/tasks/:id` для новых задач
перенаправляется, числовой legacy остаётся совместимым. При переносе меняется slug
адреса, стабильный ID задачи сохраняется; окно не должно перемонтироваться.

## 19 сентября 2026 — согласовано рабочее окно задачи

- Пользователь положительно оценил анимацию доски. Для следующего этапа уточнил:
  сохранить модалку, добавить разворачивание и прокрутку вместо отдельной страницы.
- Согласованы вкладки описания, контекста и связей. Постоянные данные задачи и межзадачные
  связи используют API. Продуктовые привязки ещё не поддержаны Core; их UI-выбор будет
  явно временным в sessionStorage, с реальным чтением требований и документов.
- Изучены существующий редактор/черновик, продуктовые схемы и операция getProductContext.
  План и владельцы зафиксированы перед реализацией. Библиотеки и серверные схемы не меняются.

## 19 сентября 2026 — результат UI-итерации доски

- Переработаны шапка, фильтры, поверхности колонок и типографика карточек. Колонки
  прокручиваются независимо и занимают остаток высоты окна. Мобильная шапка компактнее.
- Владелец жеста удерживает снимок всех загруженных колонок, локально переносит задачу
  и сохраняет результат до сверки после записи. Overlay повторяет оформление и размеры
  карточки, не регистрируя второй sortable. Мышь может захватывать поверхность карточки;
  клавиатура и сенсорное удержание используют ручку.
- В браузере обнаружены и исправлены две геометрические проблемы: появление scrollbar
  меняло ширину текста; flex-контейнер сжимал кнопку подгрузки до 2px. Добавлены стабильное
  место под scrollbar и запрет сжатия кнопки/хвоста колонки.
- Дополнительная запись за видимой страницей используется как beforeId. Проверенный
  GET после сортировки INFRA-39: 38,40,39,41,42,43,44. Это подтверждает сохранение
  правильного порядка за границей видимых 40 карточек.
- Задержка записи 4 секунды: UI уже показывал новую колонку при saving=true; после ответа
  место сохранилось. При искусственном сетевом отказе показана предметная ошибка,
  карточка вернулась в исходную колонку. После эксперимента fetch восстановлен.
- Подгрузка 40→44 сохранила DOM и scrollTop 3353; открытие/закрытие задачи также сохранило
  доску. Мышь и клавиатура проверены, снимки всех четырёх ширин и обеих тем просмотрены.
  Reduced motion проверен через media; axe — 0 violations, 1 incomplete, ограничения
  перечислены в README. Реальное устройство с touch и конкурентная запись не проверялись.
- Финальные Web lint/build/typecheck, docs:check и diff-check успешны. Сохранено известное
  предупреждение размера чанка. Обновлены продуктовый Web/KANBAN, инженерное состояние,
  локальный README Web и досье; REST/CLI/MCP-контракты остаются верными, поскольку не менялись.
- Закрыта собственная браузерная сессия, проверены PID и остановлены собственные 4793/5174.
  Данные пользователя и его процессы не затронуты. UI готов к оценке, не объявлен принятым.

## 19 сентября 2026 — новая согласованная UI-итерация

- Изучены скриншот, текущие компоненты, схемы задач и `ProductQueries.context`.
  Сборка контекста продукта уже различает общие требования, контракты приложений
  и документы с причинами включения; привязки задач к продукту пока отсутствуют.
- Пользователь поручил приступить к предложенной доработке доски. Границы и план
  зафиксированы в README. Страница задачи и продуктовые связи отложены отдельно.
- Найдены предпосылки рывков: ожидание записи/refresh без локальной проекции порядка,
  overlay шириной 284px с отдельной разметкой и независимые обновления колонок во время жеста.

## 19 сентября 2026 — остановка и передача по требованию пользователя

- Пользователь повторно не принял UI/UX, потребовал закончить и передаёт доработку
  другому агенту. Реализация остановлена, текущий результат не объявляется завершённым.
- Последние браузерные проверки подтвердили добавление связи, запрет done с блокером,
  перенос WEB-1→PRODUCT-4 при постоянном `/tasks/E6l06xIg`, сохранение адреса после reload.
  Offline-отказ сохранил ввод; после закрытия и повторного входа черновик восстановился.
  Внешняя правка CLI не затёрла форму; явное применение новой ревизии позволило сохранить.
- Исправлены найденные при проверке мобильные обрезания кнопки редактирования, контраст
  ссылок и дублирующий banner модального окна через публичный compound API Mantine.
  Web typecheck/lint/build пройдены после исправления. Просмотрены снимки 390/768/1024
  и тёмной темы 1440. Ширина документа на телефоне 390, горизонтальная прокрутка ограничена
  доской. Последние axe-аудиты — 0 нарушений; incomplete-контраст отмечен отдельно.
- При проверке обновлённой static-сборки возник пустой экран: собственный сервер,
  запущенный до пересборки, отдавал 404 новым хешированным assets. После перезапуска
  только собственного сервера страница проверена заново. Пользовательский dev не трогали.
- Итог проверок и ограничения сведены в README. Полный root test и заключительное
  общее ревью/форматирование не выполнялись. Все изменения оставлены незакоммиченными.
- Сессия `tasks-web-kanban-20260919` закрыта. По `ps` проверен собственный PID 1217926
  (`.artifacts/kanban-server.mts`) и отправлен SIGTERM. Сервер 4792 остановлен;
  пользовательские 4700/5173 оставлены без изменений.

## 19 сентября 2026 — проверка переработанного взаимодействия

- Создание пустой задачи проверено в браузере: ровно один `history.pushState` на
  `/projects/kUZ84THO/tasks/8YQN1JBG`; DOM-узел доски сохранился, skeleton редактора — 0.
  После сохранения заголовка и Markdown число переходов осталось 1, тот же DOM доски.
- Клик по фону закрыл окно: диалогов 0, возврат на исходный маршрут, DOM доски сохранён.
  Сделан снимок `.artifacts/web/kanban-v2-modal-1440.png`; чтение Markdown отделено от формы.
- DnD: клавиатурой задача перенесена inbox→ready, ревизия выросла до 3. Мышью изменён
  порядок в ready: PRODUCT-3 перед PRODUCT-1. Результат перечитан из API.
- Подгрузка инфраструктурной колонки: 40→44 карточки, минимальное число в DOM во время
  операции — 40, первый DOM-узел сохранился, scrollY остался 3311. После прямого local
  CLI-создания и SSE — 45 карточек, тот же первый узел и та же прокрутка.
- Браузерный axe на доске: 0 нарушений, требующие ручной оценки generic aria-label
  уточнены ролью group; контраст обрезанной пятой колонки дополнительно проверяется визуально.
- После сборки runtime новые HTTP-тесты канбана и 7 MCP-тестов успешны. Первая попытка
  этих тестов дала 404 из-за устаревшего dist server-runtime, а не из-за маршрутизации;
  пересборка обязательна для проверок межпакетного compiled-контракта. CLI — 89/89.

## 19 сентября 2026 — уточнение создания и эталон прежней доски

- Пользователь поручил продолжить и уточнил: сразу создавать пустую задачу с номером;
  поля не обязательны, CLI/API заполняют их одним запросом. Отказались от промежуточного
  маршрута создания и возврата на доску после отправки формы.
- Пользователь указал прежний канбан как эталон DnD и подгрузки. Прочитаны sortable-карточка,
  алгоритмы коллизий/клавиатуры/вставки, CSS и useGetBoard/getBoardSlice. Подтверждены
  keepPreviousData, сохранение count, ограниченное перечитывание при BOARD_CHANGED,
  marker через псевдоэлемент без сдвига и overflow-anchor:none у кнопки продолжения.
- Перед переработкой обновлены владельцы: общий технический DnD в ui, новый домен отвечает
  за серверный кеш; композиция — полноширинный layout и канонический маршрут задачи.

## 19 сентября 2026 — UI/UX не принят, возврат к проектированию

- Дополнительное замечание пользователя: окно не закрывается кликом по фону.
  Причина — явное `closeOnClickOutside={false}`. Убрано отключение штатного поведения
  Mantine; закрытие использует прежний onClose, черновик не удаляется.

- Пользователь указал на широкие поля, недостаточную рабочую площадь, мерцание создания
  и неприемлемую адресацию открытой задачи через `?task=`. До дальнейшей переработки
  запрошено обсуждение качественного UI/UX. Продолжение реализации интерфейса приостановлено.
- Чтение кода подтвердило документный контейнер `ProjectPage` с max-width 88rem и
  отступами до 2rem. При успешном создании смена React key размонтирует `TaskModal`,
  затем GET показывает новое состояние загрузки. Дополнительно работают refresh и SSE.
  Это статическая диагностика; профилирование каждого визуального скачка не проводилось.
- Прочитаны официальные документы Linear о досках и создании задач. Предложение
  переработки сохранено в UX.md и отделено от текущей реализации.
- Браузерная проверка ограничена открытием доски и окна задачи на временном проекте
  с коротким projectId; сделан снимок 1440px. Полная UX-проверка не заявляется.
- Попытка старта собственного сервера сначала завершилась из-за ошибочного пути
  `src/index.ts`; исправлено на фактический `src/bootstrap.ts`, сервер запущен на 4792.
  Продуктовые демоданные в рамках этой проверки не менялись.

## 19 сентября 2026 — разрешение реализации

- Пользователь выбрал префикс приложения при создании, `PRODUCT`, центральную модальную
  задачу и изменение ключа при переносе со стабильным ID. Запрошены короткие ID 6–8
  символов во всём проекте и понятный оркестратору граф блокеров.
- Найдены разрозненные генераторы Core: UUID проекта, префиксные ID продукта,
  жизненного цикла, комментариев и отчётов. Переход генерации отделён от чтения старых
  записей; существующие связи должны сохраниться. План и владельцы обновлены до правок.
- Реализованы Core и REST новой модели: задачи, ключи, перенос с долговечным намерением,
  граф связей, обратное чтение, блокеры и пагинация. Новые ID централизованы (8 символов),
  прежние схемы расширены для совместимого чтения. Префикс приложения хранится при создании.
- Core: 33 теста успешно, включая четыре новых сценария канбана. Server-runtime:
  36 существующих тестов успешно; новые HTTP-сценарии ещё предстоит добавить.
- SDK сгенерирован штатным генератором 5.2.4: 93 операции. Подключены локальный и HTTP
  backend, команды `boards`/`task` и MCP `board_task_*`. Typecheck CLI и MCP успешен.
- Через локальный `pnpm run create ui-unit` созданы шесть UI-юнитов: task-kanban,
  его task-column/task-card, task-modal и его task-editor/task-relations. Web typecheck
  успешен; два замечания lint по доступности и подготовке prop исправляются перед браузером.

## 19 сентября 2026 — начало обсуждения

- Прочитаны протокол, правила документации, концепция продукта, единый стандарт,
  контракты досок и задач, направление развития и точка продолжения этапа досок.
- `git status --short --branch`: ветка `refactor/application-rebuild`, изменения
  предыдущего этапа присутствуют; они не изменялись в рамках обсуждения.
- Пользователь уточнил: сначала обсуждение колонок, анимаций, CLI/MCP и карточки.
  Реализация отложена до предметных решений, подготовлено предложение в README.
- Действующие документы досок и задач пока остаются верными: они явно отделяют
  пустые новые доски от прежней модели. Предложение не заменяет их требований.
- Следующий шаг: согласовать колонки, минимальные поля, ключи и семантику связей.
- Проверки досье: Prettier успешен; `pnpm run docs:check` — 141 документ,
  1016 ссылок без ошибок; `git diff --check` успешен. Ссылки из индекса к предложению
  и от предложения к действующим владельцам проверены. Проверки кода не выполнялись:
  изменения ограничены документацией обсуждения.
