# Журнал: единое хранилище сущностей

## 22 сентября 2026 — сохранение технического задания

- Основание: пользователь потребовал немедленно сохранить в корне проекта документ
  со всеми договорённостями и завершить текущую работу на сохранении ТЗ.
- Создан `STORAGE-REFACTORING.md`: ID-хранение, изменяемые ключи, JSON-индексы,
  Git-коллизии, отдельно сохраняемые связи, компактные наборы владельцев, общий журнал,
  восстановление, полный контекст одним вызовом и критерии приёмки.
- Последние уточнения имеют приоритет: `CONTEXT-UI.md` — прототип; основной контекст
  охватывает всю достижимую компоненту, циклы сохраняются при конечном обходе.
- Статические выводы по коду относятся к `5c7265b`; исторический benchmark не объявлен
  свежей проверкой. Реализация рефакторинга и пересборка Playground не выполнялись.
- Проверка до записи: `git status --short --branch` — чистая ветка
  `refactor/application-rebuild`; целевой корневой файл отсутствовал.
- Действующие справочники сущностей и графа описывают текущую реализацию. Новый контракт
  явно помечен целевым ТЗ; их синхронизация с кодом включена в критерии выполнения.
- `pnpm run docs:check`: 218 документов и 1096 ссылок, ошибок нет; дополнительно
  проверены 17 файлов и 64 внутренние ссылки скилла.
- `git diff --check`: успешно.
- Первичная адресная проверка Prettier обнаружила оформление таблицы в ТЗ;
  представления интерфейсов оформлены списком с тем же содержанием.
- Повторные проверки Prettier для всех четырёх изменённых документов и `git diff --check`
  прошли. ТЗ сохранено, точка продолжения актуальна; поручение по сохранению завершено.

## 22 сентября 2026 — ознакомление с техническим заданием

- Основание: пользователь поручил прочитать корневой `STORAGE-REFACTORING.md`.
  Документ прочитан полностью вместе с обязательным протоколом, правилами библиотеки,
  контекстом продукта, стандартом линков и текущим досье.
- Ключевой контракт ТЗ: один вызов возвращает всю достижимую компоненту сохранённых связей
  в обоих направлениях, включая циклы и параллельные рёбра. Полнота не подменяется первой
  страницей или глубиной прототипа `CONTEXT-UI.md`.
- Остальные опорные требования: постоянные ID и изменяемые ключи с алиасами, единое
  JSON-хранение, адресные индексы, компактные наборы связей владельцев, журнал операций,
  восстановление, Git-коллизии и прогретый контекст без повторного чтения постоянных файлов.
- Применимость стандарта связей подтверждена: предметные линки и связи Core сохраняются
  отдельно, бекенд явно согласует оба шага. Чтение ТЗ не выполняет предметных мутаций.
- `git status --short --branch` и `git rev-parse --short HEAD`: ветка
  `refactor/application-rebuild`, HEAD `5c7265b`; прежние незакоммиченные изменения — индекс
  работ, корневое ТЗ и досье. Сверка реализации и нагрузочные проверки в это чтение не входили.
- ТЗ, `docs/product/README.md`, `docs/development/LINKS-AND-CONTEXT.md` и индекс работ
  сохраняют верное назначение: требования и план реализации не изменены. Обновлены только
  точка продолжения и журнал ознакомления.
- Проверки документации: `pnpm run docs:check` — 218 файлов и 1097 ссылок без ошибок;
  дополнительно 17 файлов и 64 ссылки скилла. Адресная проверка Prettier для README и журнала
  досье и `git diff --check` прошли. Переходы из досье к ТЗ и журналу проверены чтением.

## 22 сентября 2026 — начало реализации

- Пользователь поручил приступить к реализации. Подтверждены прежняя ветка и исходные
  незакоммиченные документы; инструкция не разрешает коммиты или публикацию.
- Прочитаны инструкции Core, Contracts и обоих runtime, стандарт интерфейсов, действующие
  репозитории, каталог/резолвер, мутации канбана/продукта и графа. Выявлены независимые WAL,
  полные `all()` и история/квитанции внутри предметных записей.
- Зафиксированы этапы в PLAN и физические решения у инженерного владельца. Начинается
  общая основа; прежние базы не переключаются на новый формат до готовности адаптеров
  и явного возобновляемого переноса.
- Добавлены схемы Contracts и общий слой `storage/entity-store`: кодеки, ID-пути,
  сегментированный адресный индекс, пакетный WAL, операции/квитанции, надгробия и пространства
  ключей. Связи сохраняются отдельными наборами владельца/slot внутри общей операции.
- Добавлен низкоуровневый полный контекст по индексам; корни разных видов, циклы, петли
  и параллельные рёбра не требуют изменений обхода.
- Первая проверка `pnpm --filter @relay/contracts build` прошла. Core typecheck сначала
  обнаружил несовместимый вызов `omit` у объединения схем и два неиспользуемых импорта;
  исправлены типизация общего кодека и импорты, повторный Core typecheck прошёл.
- `node --conditions=tasks-source --import tsx --test test/entity-store.test.ts` из Core:
  19/19 успешно. На 1000 прогретых вызовах счётчики чтения сущностей, связей, истории и
  сегментов не увеличились. Индекс 10 000 адресов читает одну ветвь, старый снимок сохраняется.
- При последующей сверке устранены два непокрытых первой проверкой случая: история
  противоположного конца связи теперь восстанавливается из адресов операции, а конфликт
  внешней записи проверяется и до создания WAL. Добавлены соответствующие сценарии,
  проверка больших наборов и конкуренции независимых процессов; результаты ещё ожидаются.
- Дополнительные проверки: 23/23 успешно, Core typecheck прошёл. Полный
  `pnpm --filter @relay/core test` — 114/114 успешно. Первые две ошибки расширенного теста
  оказались ошибками самого сценария: порядок неразличимых параллельных рёбер проверялся
  как значимый и локальная переменная затеняла фабрику записи; исправлены проверки.
- Нагрузочная база 10 000 сущностей / 10 000 связей на ext4 создана через новый слой:
  холодный полный контекст 1792,45 мс, средний из 100 повторов 20,06 мс; повторные чтения
  постоянных сущностей, связей и операций — 0. Это промежуточный замер до увеличения листа
  индекса с 256 до 1024 записей. Локальный корпус: `.artifacts/entity-scale-10000-btt4E5`.
- По измерениям публикация файлов внутри фазы WAL распараллелена с ожиданием всех начатых
  записей при ошибке. Сохраняется порядок: сущности → связи → операция → индексы → версия.
  Проверка принадлежности ключей ветви кешируется на неизменяемом сегменте; увеличен лист
  до 1024 записей при прежнем пределе 1 МиБ. Повторные 23 теста и typecheck прошли.
- Большой корпус `.artifacts/entity-scale-100000-Ixa0gD` продолжается после лимитов терминала:
  два прогона завершились по 600 с, следующий по 1800 с. Созданы 100 000 сущностей и как
  минимум 70 000 связей по последнему сообщению прогресса; полный замер ещё не завершён.
  Повторы используют исходные квитанции; незавершённый WAL восстанавливается при открытии.
- CPU-профиль контрольного корпуса 500 записей показал преобладание ожидания I/O.
  Попытка отдельного tmpfs-прогона в `/tmp/opencode` остановилась на EACCES до создания базы;
  права не менялись, приёмочный корпус продолжает использовать ext4 рабочего каталога.
- Пользователь попросил сообщить результаты тестов по завершении. Подтверждены 114 тестов;
  нагрузочный прогон 100 000 пока не объявляется пройденным.
- Большой прогон завершён: 100 000 сущностей и 100 000 связей, ext4, Node.js 24.18.1.
  Полный холодный контекст 10 729,07 мс, средний повтор 239,60 мс (100 вызовов), reindex
  50 753,72 мс, recovery 94,54 мс. Постоянные файлы при чтении контекста не открывались.
  Сохранён JSON-отчёт; время последнего продолжения не считается общим временем наполнения.
- После проверки встроенных кодеков и явного перехода версии данных прошли все 116 тестов
  Core (25 новых), типы и сборки Core/Contracts. Уточнены проверки идентичности файла,
  неизменности автора/времени создания и проверки кодека до включения записи в пакет.
- Пользователь указал на неудовлетворительную задержку и спросил, перестраивается ли индекс
  при запуске. Подтверждено по коду и сценарию: обычное открытие использует сохранённый индекс;
  50,75 с относятся к отдельной явной операции reindex, а 10,73 с — к полному холодному чтению.
- Найден дефект холодного пути: внутри цикла каждого ребра создавались схемы Zod. Схемы
  вынесены в константы, лист получил кешируемый Map, добавлено типизированное чтение без
  предварительного глубокого копирования. Контроль целостности и изоляция ответа сохранены.
- После первых исправлений холодный замер снизился до 5 274,52 мс. CPU-профиль отдельного
  чтения подтвердил затраты на копирование, работу с индексом и GC; затем устранено лишнее
  копирование перед разбором выбранного узла/ребра и добавлены измерения фаз.
- `--read` на готовом корпусе: полный холодный ответ 4 612,70 мс, индекс/декодирование
  3 577,36 мс, обход/сбор 425,84 мс, сортировка/подготовка кеша 608,51 мс; средний повтор
  243,52 мс. Heap после холодного чтения 464,62 МиБ. Корпус не наполнялся и не перестраивался.
  Повторные 25 адресных тестов и Core typecheck прошли.
- Создан постоянный отчёт VERIFICATION; актуализирован технический владелец и точка
  продолжения. `docs/product/README.md` и стандарт линков остаются верными: бизнес-сценарии
  не изменены, новый слой поддерживает явную отдельную запись связей, чтение их не создаёт.
  Публичные справочники пока описывают прежние подключённые репозитории; их перевод впереди.
- Пользователь изменил приоритет: продолжить реализацию и восстановить работу; проверку
  производительности выполнять позже. Нагрузочные прогоны и дальнейшая оптимизация
  холодного пути остановлены. Следующий этап — рабочие предметные адаптеры, совместимость
  и функциональные проверки сквозных операций.
- Подключена общая сессия к Workspace под прежней проектной блокировкой: вложенные вызовы
  репозиториев образуют одну операцию, первичная квитанция проверяется до предметного сценария.
  Добавлены адаптеры продукта, реализаций, технических scope, досок, задач, настроек, графа
  и обсуждений. Исторические события/квитанции читаются из общего журнала по индексным указателям.
- Предметные сервисы после записи явно вызывают синхронизацию своих групп связей. Проектная
  изоляция productId сама по себе не превращается в искусственное ребро. Вычисляемая готовность
  обновляет карточки индекса перед публикацией составного действия.
- Добавлен явный StorageService.migrate: сохранение ID, метаданных, scope, истории и квитанций,
  перенос подтверждённых владельцев документных связей, атомарное переключение маркера формата.
  Исторические резервы без известных ID сохраняются отдельно, без выдумывания удалённых сущностей.
- Потребовалось настроить self-import в typecheck Core и запуск тестов с tasks-source:
  смешивание исходного и собранного Workspace стало некорректным после появления приватных
  механизмов сессии. После исправлений сборки Contracts/Core и typecheck Core прошли.
- Два функциональных сценария `unified-workspace.test.ts` прошли: полный перенос небольшой
  предметной базы и работа пустого проекта после перехода. Проверены документы и прежние ID
  связей, комментарии, история, повтор, изменение фичи/задачи, постоянный путь после переноса,
  настройка проекта и первичное заполнение паспорта. Первый запуск теста обнаружил неверную
  подготовку: SI создавалась без обязательной FI; исправлен сценарий подготовки, правило сохранено.
- Перевод ещё не завершён: каскадное удаление, полное чтение через публичные интерфейсы,
  обслуживание и обнаружение изменений подключаются следующими шагами. Нагрузочные прогоны
  после изменения приоритета не выполнялись.
- Подключены все шесть предметных каскадов нового формата и публичный полный контекст:
  Core, REST, SDK, CLI, MCP. Версия полного контекста пригодна для записи графа; диагностика
  graph list сохраняет прежнюю пагинацию. Добавлены storage migrate/reindex и новый формат init.
- Server использует лёгкий сигнал версии вместо таймерного чтения всего продукта/канбана.
  Изменения файлов проверяются по сохранённым контрольным суммам: внешняя правка отмечает
  индекс устаревшим, явный reindex восстанавливает работу. Потеря заголовка индекса не блокирует обслуживание.
- Полная регрессия Core на этом этапе: 124/124 успешно. Затем добавлены проверки новой
  инициализации с нестандартным storageDir и восстановления отсутствующего заголовка;
  все 10 сквозных сценариев единой базы прошли. MCP — 9/9; адресная проверка CLI — 12/12.
  REST-сценарии прошли, счётчик посещённых OpenAPI-маршрутов скорректирован на добавленный
  endpoint; повторные оба OpenAPI-сценария прошли.
- SDK сгенерирован закреплённым 5.2.4 из изолированного экспорта OpenAPI: 103 операции,
  509 моделей. Экспорт больше не зависит от пользовательского сервера на порту 4700.
- Обнаружено стороннее незакоммиченное досье `docs/work/dolt-storage-evaluation`; к этой
  реализации оно не относится. Проверка Web выполняется по отдельному плану досье.
- Дополнительно проверены квитанции после удаления сущности: внешний EntityEngine фиксирует
  собственный исходный запрос до разрешения сущности, вложенные действия остаются в одной
  операции. Прямой API задач также повторяет квитанцию по сохранённому адресу надгробия.
  Все 11 сквозных сценариев нового формата прошли, сборка и typecheck Core прошли.
- Web: lint, typecheck и production build прошли. Создан отдельный Server на временной
  базе `.artifacts/storage-web-db-z3iEUw`, отдельная headless-сессия
  `tasks-web-storage-20260922`. Через UI проверены изменение названия/Markdown, отображение
  связанного документа, SSE от локального CLI, HTTP 409 с сохранённым черновиком, сетевая
  ошибка и повтор после восстановления связи. Точный Markdown подтверждён отдельным CLI JSON.
- Проверены светлая/тёмная темы при 1440/1024/768/390, открытие карточки с клавиатуры и Escape.
  a11y: 0 нарушений, 0 непроверенных пунктов, 38 успешных проверок; ошибок исполнения страницы нет.
  Скриншоты сохранены локально agent-browser. Команда fill для CodeMirror дописала текст,
  поэтому полная замена проверена штатным выделением Ctrl+A и вводом; это ограничение проверки,
  не принятое изменение приложения. Смена набора флагов agent-browser первоначально сбросила
  его вкладку; далее все вызовы использовали одинаковую конфигурацию.
- Проверочный браузер закрыт, свой Server PID 4128039 остановлен. Пользовательские процессы
  не перезапускались. Новых TSX и фронтенд-автотестов не создавалось.
- Финальная функциональная регрессия: Core 127/127, Server Runtime 33/33, CLI 36/36,
  MCP 9/9, Project Runtime 2/2 — всего 207 тестов успешно. Все относящиеся сборки и
  typecheck прошли. В прежних тестах обновлены ожидания ID-путей, автоматических предметных
  связей и нового полного ответа; отдельный тест старого конфига явно использует legacy-инициализацию.
- Проверка выявила реальный пропуск SSE для изменения конфигурации: сигнал единой базы
  дополнен её текущими настройками. Повторные сценарии замены конфига и восстановления
  после ошибки прошли. Внешняя правка ID-файла проверена через ошибку устаревшего индекса
  и последующий явный reindex; полное сканирование по таймеру не возвращалось.
- SDK diff проверен: добавлен FullContext и его две операции, расширен предел бюджета;
  существующие параметры пути не превратились в any. Скилл пересобран, skills:check прошёл.
  docs:check: 224 документа / 1160 ссылок, 18 файлов скилла / 73 ссылки, ошибок нет.
  Форматирование затронутых авторских файлов и git diff --check прошли; generated SDK
  обновлялся только генератором.
- Последняя проверка сопровождения после финальной сводки: skills:build/skills:check
  успешно; docs:check — 224 документа / 1161 ссылка и 18 файлов скилла / 73 ссылки,
  ошибок нет. git diff --check успешно. Функциональный этап завершён; оставшиеся ограничения
  полного ТЗ и отложенная производительность перечислены в README и справочнике STORAGE.
