# Журнал компактной истории

## 23 сентября 2026 — исследование и согласование

- Прочитаны протокол, правила библиотеки, продуктовый контекст, стандарт связей и интерфейсов,
  инструкции Core, Contracts и Playground. Границы владельцев проверены по архитектуре;
  Unit Architecture не распространяется на внутреннюю структуру Core, новые фасеты не нужны.
- Статическим чтением подтверждено: прежние `boards` остаются после init → migrate;
  `openWorkspace` повторно создаёт `tasks`; кеш продукта сохраняется вне общего игнорируемого
  индекса. Текущий журнал содержит полные снимки файлов и производные указатели событий.
- Пользователь согласовал краткую историю без старых Markdown-текстов, сохранение комментариев
  и квитанций, группировку записей в сегменты и укрупнение индекса истории.
- До реализации проверено рабочее дерево. Существующие незакоммиченные изменения относятся
  к предыдущим работам; общий сброс и замена файлов целиком недопустимы.
- Проверки пока ограничены чтением кода, JSON, `git status`, `git check-ignore`, подсчётом
  содержимого базы. Это не подтверждает готовность нового формата.

## 23 сентября 2026 — общий слой и допуск интерфейсов

- Добавлен внутренний Journal в `storage/entity-store`: ограниченные сегменты, локальный
  поток, вычисляемый адрес записи, укрупнённые индексы истории и отдельный поиск квитанций.
  Прямая инициализация и явная миграция подключаются к общей публикации WAL.
- Первый прогон Core: typecheck и 25/25 тестов `entity-store.test.ts` прошли, включая
  прерывания на всех стадиях, повтор, конкуренцию процессов и reindex. Нужны дополнительные
  проверки ротации, миграции и предметных интерфейсов.
- Для Web прочитаны полный React Reference и Unit Architecture, профиль `apps/web/README.md`,
  колокация/зависимости, UI/Mantine и правила JSX/TypeScript. Владелец отображения — существующий
  внутренний `activity-change` композиции `task-activity`; схема ответа принадлежит
  `domains/board-tasks`. Новые юниты и зависимости не требуются. План: перед сравнением
  значений показать явное «Содержимое изменено» при `contentOmitted`; проверить lint,
  typecheck/build и собственную браузерную сессию на временной базе.
- SDK остаётся у существующего `packages/rest-sdk`; его исходники обновляются только
  закреплённым генератором по OpenAPI после изменения Contracts.

## 23 сентября 2026 — сквозные проверки и демо-база

- Сквозной прогон: Core 145/145, MCP 10/10. В CLI один старый тест предполагал обязательное
  существование `tasks/`; заменён проверкой его отсутствия после init и последующего чтения.
  Повторный CLI-прогон — 36/36. REST был прерван общим Turbo после ошибки CLI;
  отдельный повтор завершился 34/34, включая SSE, OpenAPI и `contentOmitted`.
- Новый тест конфликта независимых потоков прошёл; отдельный набор компактной истории — 9/9.
  Typecheck владельцев, Web lint/typecheck/build и генерация SDK прошли. SDK изменил только
  модель изменения истории. Скилл собран и проверен.
- `docs:check` возвращает пять исходных ссылок на отсутствующий `STORAGE-REFACTORING.md`.
  Новых ошибок ссылок не найдено. Источник не подменяется вымышленным документом.
- Демо перенесено штатным CLI `storage migrate`. Перед этим под общей блокировкой сохранена
  локальная копия `.artifacts/p2p-history-backup-BiyDgt` (без производных индексов/runtime).
  81 файл / 1 289 916 байт стал одним сегментом / 509 434 байт; 81 исходная квитанция и
  контрольные суммы 147 файлов текущих сущностей, связей, ключей и конфигурации совпали.
  `validate`: valid=true, 74 публичные сущности, 4 доски, 1 задача. Старые каталоги исчезли.
- Дополнительный reindex обнаружил прежнюю особенность: обновление только производных
  карточек создавало пустую `storage-maintenance`-запись. Добавлена отдельная публикация
  индексного пакета без записи в историю и регрессия. В демо уже записанное фактическое
  обслуживание сохранено: 81 исходная запись + одна пустая служебная. Повторная сверка
  различает новые действия и потерю первоначальных квитанций.
- Работающий пользовательский dev-server успешно прочитал перенесённый продукт через REST.
  В собственной браузерной сессии проверено раскрытие истории клавиатурой: факт изменения
  текста, переход статуса и полный Markdown комментария отображаются раздельно и корректно.

## 23 сентября 2026 — завершение

- Финальная проверка миграции добавила сверку прежних операций с file-hashes: потеря или
  внешняя замена файла останавливает переход до публикации. Для прямого init исправлена
  канонизация configPath при обращении через symlink. Обе ситуации покрыты регрессиями.
- Последний полный Core — 148/148, typecheck и сборка Core прошли; CLI пересобран.
  Остальные фактические прогоны и границы собраны в [отчёте](VERIFICATION.md).
- Web проверен на 1440/1024/768/390 в обеих темах: краткие изменения, полные комментарии,
  клавиатура, SSE после локальной CLI-записи, ошибка сети, сохранность черновика при смене
  табов и параллельной публикации, успешный повтор. Свои браузер и Server закрыты;
  отсутствие процесса подтверждено проверкой PID.
- На демо повторены reindex, сверка и validate; данные и 81 исходная квитанция сохранены,
  новые пустые операции обслуживания не появляются. Проверены человеческая история и JSON,
  повтор миграции и чтение работающим Server. Кеш и пустые прежние каталоги очищены штатно.
- Актуализированы STORAGE, TASK-ACTIVITY, CLI/MCP, продуктовые правила задач/Web,
  инженерное состояние, решение и источник скилла. Стандарт связей остаётся верным:
  меняется физическая публикация истории, явные вызовы Core и предметные линки сохранены.
- Досье и индекс обновлены; дальнейшее действие — приёмка пользователем.
- Отдельно закреплён запрет добавления исторических значений через readonly-снимок.
  Дополнительный набор компактной истории прошёл 11/11; typecheck и сборки Core/CLI повторены.
  Финальный docs:check подтвердил ровно те же пять исходных ошибок ссылок.
