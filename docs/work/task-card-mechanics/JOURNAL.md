# Журнал механик карточки задачи

## 21 сентября 2026 — исходная модель и уточнение направления

- Пользователь начал обсуждение типов карточек, затем явно отложил их и описал
  декомпозицию продуктового сценария на задачи реализации на других досках.
  Текущий предмет обсуждения — родительство и зависимость завершения.
- Прочитаны протокол разработки, правила библиотеки, контекст продукта, единый стандарт,
  инструкции Web/Core/Contracts, досье канбана и действующие документы задач и Web.
- `git status --short --branch` показал чистое рабочее дерево в
  `refactor/application-rebuild`; `git rev-parse --short HEAD` — `48affd3`.
- Статически проверен `packages/contracts/src/entities/board-task.ts`: у задачи есть
  `parentId` и `dependencies`, отдельного типа карточки нет. Это согласуется с канбаном:
  родительство и зависимости различаются, только `done` удовлетворяет зависимость,
  завершение подзадач не закрывает родителя автоматически.
- Подготовлено предложение: объединить добавление родительства и обратной зависимости
  родителя в одно пользовательское действие добавления обязательной подзадачи. Причина —
  пользователю нужен одновременно состав работы и проверяемое условие завершения.
  Решение о поведении по умолчанию и жизненном цикле пары связей ещё не принято.
- Проверены `docs/product/entities/tasks/README.md`, `docs/product/entities/tasks/KANBAN.md`
  и `docs/product/applications/web/README.md`: действующие правила остаются верными;
  содержание не менялось, поскольку новая механика пока обсуждается. Код, хранение
  и публичные контракты не изменялись.
- Перед паузой сохранена точка продолжения в README. Следующий шаг — согласование
  механики обязательных подзадач и явного завершения продуктовой задачи.
- Проверки документации после добавления досье: `pnpm run docs:check` — 173 документа,
  911 ссылок и 59 внутренних ссылок скилла без ошибок; `pnpm exec prettier --check`
  для двух файлов досье и индекса работ — успешно; `git diff --check` — успешно.
  Ссылки из индекса к досье и далее к правилам задач и Web проверены чтением.

## 21 сентября 2026 — упрощение выбора связи

- Пользователь показал текущую форму с вариантами «Зависит от», «Связана с»,
  «Родительская задача», «Дочерняя задача», «Блокирует» и отметил их избыточность.
- По общему контракту это три отношения, показанные в пяти направлениях:
  `depends-on`, `related`, `parent`. Скриншот показывает именно выбор направления
  относительно открытой карточки, а не пять самостоятельных видов отношений.
- Уточнено предложение: основные действия принадлежат блокам подзадач и зависимостей;
  родитель доступен отдельной строкой, обратное направление «Блокирует» вычисляется,
  обычная контекстная связь остаётся второстепенным действием. Это уменьшает число
  решений при добавлении задачи реализации. Добавление обязательной подзадачи
  по-прежнему предлагается как одно составное действие; пользователь его ещё не утвердил.
- Прежнее предложение о переключателе обязательности не является утверждённым
  требованием; набор элементов формы будет определён после согласования сценария.
- Действующие документы канбана и Web остаются верными: текущая реализация не менялась.
  Точка продолжения обновлена; следующий шаг — согласовать упрощённую организацию связей.
- После уточнения повторно пройдены `pnpm run docs:check` (173 документа, 911 ссылок;
  скилл — 59 внутренних ссылок), Prettier для досье и индекса, `git diff --check`.

## 21 сентября 2026 — поручение на реализацию

- Пользователь уточнил: сам механизм подзадач блокирует закрытие родителя; существующий
  раздел становится «Подзадачами», без переключателя типов и группировки списка.
  Предложения об опциональной обязательности и двойной записи связей заменены этим правилом.
- Прочитаны оба обязательных Web-skill и применимые разделы, профиль приложения,
  реализация формы, доменного доступа, Core и общих схем; правила SDK и Server runtime.
  Допуск к реализации зафиксирован планом в README. Новые TSX и архитектурные границы
  не нужны; сетевой доступ остаётся в домене, общие правила завершения — в Core.
- Исходный Core учитывает только `dependencies` и проверяет родительство отдельно.
  Поэтому переименование UI недостаточно: нужно добавить прямых детей в блокеры,
  запретить новые смешанные циклы и добавление незавершённого ребёнка готовому родителю.
- Для списка выбран фильтр `parentId` на сервере до пагинации. Он сохраняет честные
  счётчики и продолжение, даже когда у задачи много других отношений.
- Формат хранения не меняется. Старые дети читаются через прежний `parentId`; обычные
  зависимости и контекстные связи остаются в общем графе. Чтение старых графов сохраняется,
  проверка смешанных циклов применяется к новым рёбрам, чтобы разрешать исправление старых.

## 21 сентября 2026 — реализация подзадач и первые проверки

- Core вычисляет блокеры по прямым детям и явным зависимостям без дублирования;
  новые смешанные циклы запрещены, готовый родитель защищён от добавления незавершённого
  ребёнка. Добавлен фильтр `parentId` до пагинации. Формат хранения не изменялся.
- В существующем Web-владельце убраны выбор направления и группировка; список прямых
  детей использует прежний постраничный доменный хук. Запись меняет только родительство
  ребёнка с его ревизией; собственный черновик родителя не требует изменения ревизии.
- Добавлены содержательные Core-проверки завершения/отмены/повторного открытия,
  готового родителя, перепривязки, конфликтов, повторов, смешанных циклов, старого графа,
  пагинации и конкурентного завершения. HTTP проверяет фильтр, блокировку и OpenAPI.
- Первый целевой Core-прогон: 13/14. Прежний тест цикла сначала встретил новый запрет
  привязки к готовому родителю. Исправлены предусловия теста: родитель возвращён из `done`,
  чтобы проверить именно цикл. Повторный прогон — 14/14; HTTP — 3/3.
- Пользователь дополнительно поручил удалить хлебные крошки из карточки после подзадач.
  Исследован `task-modal`: единственное подключение `ProjectBreadcrumbs embedded` удаляется
  без изменений маршрутов. Дополнение внесено в план до правки.
- Первая попытка направить лог проверочного API в `/tmp/opencode` получила отказ прав
  файловой системы. Повторный запуск использует локальный `.artifacts/subtasks-api.log`.
- SDK обновлён `@gromlab/rest-api-codegen@5.2.4` с OpenAPI собственного API на 4793:
  81 операция, 381 модель; изменился только сгенерированный файл контрактов. Сервер
  PID 1266543, проект `XisFeVWY`, данные `.artifacts/subtasks-check-fzVsA4`.
- Из окна `task-modal` удалено подключение `ProjectBreadcrumbs`; требования Web,
  канбана и карта маршрутов обновлены. Общая навигация каркаса остаётся у своего владельца.

## 21 сентября 2026 — общие проверки реализации

Проверки относятся к рабочей копии на базе `48affd3` после реализации подзадач и удаления
крошек. Их результаты перенесены в журнал при завершении этой же работы.

- `pnpm --filter @relay/core exec node --conditions=tasks-source --import tsx --test test/*.test.ts`
  — полный Core 52/52.
- `pnpm --filter @relay/server-runtime exec node --conditions=tasks-source --import tsx --test test/*.test.ts`
  — полный Server Runtime 30/30.
- `pnpm --filter @relay/core run typecheck`,
  `pnpm --filter @relay/server-runtime run typecheck`,
  `pnpm --filter @relay/project-runtime run typecheck` — успешно.
- В `apps/web`:
  `pnpm exec eslint src/compositions/screens/project-board/ui/task-modal src/domains/board-tasks/types/board-tasks.type.ts`
  и `pnpm exec vite build` — успешно. Последняя команда собирает Web без общего typecheck.
- `pnpm run typecheck` и `pnpm run lint:web` не прошли из-за неиспользуемых `Link`,
  `ArrowLeft`, `eyebrow`, `backState`, `backLabel`, `hasBack` в
  `apps/web/src/compositions/widgets/product-page/product-page.tsx`. Файл не менялся
  в этой работе; исходное состояние подтверждено `git show HEAD:apps/web/src/compositions/widgets/product-page/product-page.tsx`.
- `pnpm exec turbo run build --filter=@gromlab/relay-cli --filter=@gromlab/relay-mcp`
  — 7/7 задач. После сборки
  `pnpm --filter @gromlab/relay-cli exec node --import tsx --test test/board-tasks.test.ts`
  — 1/1; `pnpm --filter @gromlab/relay-mcp exec node --import tsx --test test/mcp.test.ts`
  — 7/7.
- Первая попытка CLI-проверки использовала старый `dist` и завершилась
  `ERR_MODULE_NOT_FOUND` для `@relay/core/dist/application/record-request.js`.
  Пересборка устранила ошибку. Попытка MCP с `--conditions=tasks-source` встретила
  `Parameter decorators only work when experimental decorators are enabled`;
  успешный запуск после сборки выполнен без этого условия.
- Изменённые исходники и документы отформатированы Prettier; новых TSX не создано.

## 21 сентября 2026 — браузерная проверка и ограничения

Использованы собственные API 4793, Vite 5174, сессия `tasks-web-subtasks-0921` и временный
проект `XisFeVWY` в `.artifacts/subtasks-check-fzVsA4`; это не пользовательские данные.

- Через форму к `PRODUCT-1` добавлена `WEB-1`, в том числе выбором кандидата клавиатурой.
  Показан прогресс «1 из 2 готово». Завершение родителя отклонено с сообщением о
  незавершённых подзадачах или зависимостях. DOM подтвердил две строки подзадач
  и отсутствие хлебных крошек в карточке.
- После завершения ребёнка показано «2 из 2 готово»; родитель успешно переведён в `done`.
- `agent-browser --session tasks-web-subtasks-0921 a11y --selector '[role="dialog"]'`
  — 0 violations, 0 incomplete, 24 passes.
- Сохранены `.artifacts/subtasks-1440-light.png`, `subtasks-1440-dark.png`,
  `subtasks-1024-light.png`, `subtasks-1024-dark.png`. Просмотрены тёмный снимок 1440×1000
  и светлый 1024×900; наличие остальных снимков не приравнивается к их визуальной оценке.
- В `PRODUCT-3` проверена пагинация: «Загружено 40 из 42 подзадач», после «Ещё подзадачи»
  — «0 из 42 готово»; в DOM 42 строки и нет крошек.
- Одна попытка browser back после перехода к ребёнку вернула `about:blank`; ожидание
  истекло. Родитель повторно открыт прямым URL, основной сценарий завершён.
  Корректность навигации назад этой попыткой не подтверждена.
- Для проверки восстановления создана `INFRA-2` (`lwSKJnMM`, «Проверка сети и конфликта»).
  В форме `PRODUCT-2` ожидание кандидата после ввода доски и поиска истекло через 25 секунд.
  Причина не установлена: фактический выбор доски не подтверждён. Сетевой отказ и конфликт
  ревизии после этого не имитировались; успешного результата такого сценария нет.

## 21 сентября 2026 — завершение по поручению пользователя

- При возобновлении сверены протокол, документация, досье и рабочее дерево. Записи README
  об ожидающей генерации SDK и удалении крошек устарели: обе операции уже выполнены.
  Обновлены итог работы, индекс и состояние реализации.
- До нового поручения проверено окружение: `ss -ltnp '( sport = :4793 or sport = :5174 )'`
  не показал слушающих процессов; curl к 4793 получил отказ соединения, браузерная сессия
  была пустой. Старые PID не использовались для остановки, API и Vite не перезапускались.
- Пользователь сообщил: «все проверил сам, пишите логи и заканчиваем». Это зафиксировано
  как его самостоятельная проверка и поручение завершить работу. Перечень проверенных
  им сценариев не сообщался; оставшиеся браузерные сценарии агент не продолжает.
- `pnpm exec agent-browser --session tasks-web-subtasks-0921 close` — сессия закрыта.
  Временные данные, скрипт, логи и снимки оставлены в игнорируемом `.artifacts`.
- В прежнем досье канбана добавлена ссылка на последующее изменение родительства и формы;
  исторические результаты не переписаны. Индекс больше не представляет крошки внутри
  карточки как текущее поведение.
- Продуктовые документы задач и Web, `docs/reference/KANBAN.md` и `apps/web/ROUTING.md`
  уже описывают новый контракт; дополнительная смена требований не понадобилась.
  Сводка технического состояния дополнена фактическими проверками и их ограничениями.
- Изменения оставлены в рабочей копии без коммита и push. Типы карточек отложены;
  возобновление разработки — по отдельному поручению.
- Финальные проверки документов: `pnpm run docs:check` — 173 файла и 914 ссылок без
  ошибок; скилл Relay — 16 файлов и 59 внутренних ссылок без ошибок. Prettier check
  всех изменённых отслеживаемых файлов, нового типа формы и файлов досье — успешно;
  `git diff --check` — успешно. Проверены переходы индекс → досье → правила задач/Web
  и техническое состояние. Новых прогонов приложения после поручения пользователя нет.
