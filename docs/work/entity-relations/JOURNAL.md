# Журнал исследования связей

## 20 сентября 2026 — завершение этапа хранения

- Уточнён смысл счётчиков на реальном результате: пакет из восьми основных связей,
  добавление и удаление временной связи дают 10 событий/ревизию 3/8 активных отношений.
  Объяснение добавлено в действующий GRAPH.md; переиндексация их не увеличивает.

- После последних защитных правок повторены 13 адресных тестов графа/хранилища и полный
  набор CLI 90/90 — успешно. Typecheck Core, Project Runtime, Server Runtime, CLI, MCP,
  Web прошёл; lint Web и production build успешны. Ранее полный Core 55/55 и Server 45/45
  покрыли общие операции, OpenAPI и SSE на новой схеме.
- Финальные измерения повторены на готовых корпусах после проверок кеша и восстановления;
  таблица STORAGE.md и JSON-артефакты содержат актуальные результаты.
- Скилл пересобран из источников, skills:check и 7/7 тестов сборщика прошли. Пять проверок
  документации CLI подтвердили справку новых команд и переносимость пакета. docs:check:
  159 документов, 1094 ссылки, ошибок нет. Рабочий и существующий staged diff прошли --check.
- Повторный публичный graph reindex на p2p-rental подтвердил 8 активных связей, 10 событий,
  ревизию 3. Проверочные процессы завершены, собственная браузерная сессия закрыта;
  действующие серверы пользователя сохранены. Изменения v2 и новые исходники находятся
  в рабочем дереве, подготовленный пользователем индекс не изменялся.
- Общий механизм не переносит старые предметные поля автоматически и не назначает
  бизнес-последствия произвольным типам. Эти границы, а также стоимость старого каталога,
  ограничения файлов и условия замеров явно описаны в действующей документации.

## 20 сентября 2026 — результаты v2 и реальный playground

- Раздельная схема завершена: текущие записи/надгробия, события, адресные квитанции,
  WAL, сегментированные индексы и ограниченный кеш. Индексы исключаются своим .gitignore;
  их потеря восстанавливается, потеря постоянной записи не принимается за удаление.
- Полные проверки на рабочем дереве поверх 3827e1c: Core 55/55, Server 45/45; сборки
  Core, Project Runtime, Server Runtime и Web успешны, lint/typecheck Web пройдены.
  После последних защитных проверок будут повторены адресные тесты и CLI.
- Финальные замеры на готовых наборах выполнены последовательно: 10 000/100 000 связей,
  холодный контекст 110/662 мс, повторный 13–18/15–21 мс, запись одной связи 64/66 мс.
  Подробности, перестроение, восстановление и условия — STORAGE.md. Исходные JSON занимают
  23,12/231,08 МиБ с измерительными записями; общий предел 16 МиБ отсутствует.
- На p2p-rental до записи создан архив `.artifacts/p2p-rental-before-graph-v2-20260920.tar.gz`.
  Через публичный CLI добавлены PRODUCT-10 (`40NDd9yf`), WEB-4 (`NZHbkrtc`), API-2 (`j2hFkPUq`)
  с явным назначением «Обкатка Relay» и восемь отношений contains/verifies/depends-on/references
  к SCENARIO-8, WEB-SI-8, API-SI-8 и реальным документам. Готовность продукта не подтверждалась.
- CLI и HTTP с profile=context/depth=6 вернули для WEB-4 одинаковые 22 узла/36 отношений.
  Пять реальных запросов к API p2p-rental заняли 43/20,29/22,41/19,59/20,27 мс с разбором JSON.
  Досье данных: `.artifacts/p2p-graph-v2-result.json`, человеческий вывод — p2p-graph-v2-context.txt.
- Собственная headless-сессия `tasks-web-graph-v2-p2p` открыла настоящий playground через
  действующие серверы пользователя. Добавлена временная связь RYL3KIBl, проверена после reload
  и удалена в Web; CLI подтвердил два события истории. Основные восемь связей оставлены для оценки.
  Ошибок JS нет, сессия закрыта. Снимок: `.artifacts/web/screenshot-1789909603244.png`.
- На отдельной копии `.artifacts/p2p-graph-migration-fxO6c5` подготовлена фикстура v1:
  публичный graph migrate сохранил восемь связей, восемь событий и квитанцию побайтно по содержимому;
  контрольная прерванная запись восстановилась, повтор вернул исходную квитанцию.
- Реальная пагинация обнаружила конфликт --version графа с глобальным флагом Commander:
  CLI использует --snapshot-version, REST сохраняет version. Добавлена проверка выполнения
  следующей страницы, а не только наличия подсказки. Реализация обсуждена с пользователем:
  обход выполняет Core, CLI/API/Web получают один результат; тип отношения задаёт смысл связи.
- Пользователь подготовил предыдущий срез в Git index во время работы. Стадирование,
  сброс индекса и коммиты этой работой не выполнялись; v2 находится в рабочем дереве.

## Хранение v2 и нагрузочная проверка

- Реализованы отдельные current/history/requests, метаданные с контрольной суммой индекса,
  журнал составной записи, надгробия отозванных ID и сегментированные производные индексы.
  Контекст читает тела только рёбер страницы; квитанция и история одной связи адресные.
- Добавлены локальные graph migrate/reindex. V1 читается без записи; прерванный перенос
  завершается перед операциями Core, исходник сохраняется как v1-backup.json.
- Новые проверки: база более 16 МиБ, число адресных чтений, точный повтор до/после
  миграции, три границы прерывания транзакции, изменение исходника/целевого файла,
  потеря индекса и восстановление истории. Восемь новых тестов прошли, typecheck Core пройден.
- Первый реальный прогон 10 000 связей показал 104 мс на холодный контекст и 15–18 мс
  на повторный; открывались две текущие связи без истории. Выявлена лишняя синхронизация
  производных файлов. Публикация переработана: WAL сохраняется до группового fsync
  постоянных файлов и каталогов; кеши остаются восстановимыми. Проверки прерывания повторены.
- Прогон 100 000 был остановлен на своей базе до замеров и продолжен с восстановлением
  журнала. Финальные прогоны 10 000/100 000 выполняются в .artifacts, результаты ещё ожидаются.
- Пользователь добавил обкатку p2p-rental. Прочитаны инструкции playground, каталог графа
  и реальные SCENARIO-8/правила финансов. В базе 126 узлов и 203 прежних отношения;
  relations.json v1 отсутствует. Старые пустые пользовательские задачи не переопределяем;
  для наглядного сценария будут отдельные явно помеченные карточки проверки Relay.

## Начало переработки хранения v2

- Пользователь согласовал раздельное хранение и поручил реализацию. Прочитаны обязательные
  документы, правила Core, текущий репозиторий, сервис, файловая атомарность и транзакции продукта.
- Рабочее дерево содержит предыдущий срез, изменений пользователя не удаляем.
- До реализации зафиксированы схема, явная миграция с совместимым чтением, восстановление,
  индексы, границы внешнего редактирования и нагрузочная приёмка в STORAGE.md.

## Уточнение о ёмкости JSON-хранилища

- Пользователь спросил, обязательно ли хранить связи одним документом и достаточно ли
  16 МиБ для крупного проекта. Сверены актуальные досье, техническая карточка и рабочее дерево.
- Один файл — упрощение исполняемого среза, не свойство универсального графа. В нём
  растут не только актуальные рёбра, но и история с копиями состояния и квитанции.
  Гарантии ёмкости крупного проекта нет; нагрузочных измерений не проводилось.
- Рекомендация: отдельные текущие записи связей (при необходимости каталоги по префиксу ID),
  отдельная история и квитанции, восстанавливаемые индексы входящих/исходящих отношений,
  общий журнал составных транзакций. Ограничение размера относится к записи/сегменту,
  а не ко всему графу. Одно разбиение файлов без индексированного чтения не решает масштабирование.
- Требуется измерять адресные чтения, запись, построение индекса и восстановление после сбоя
  на согласованных объёмах. Это рекомендация следующего изменения; текущий код не менялся.
- Действующие GRAPH.md и engineering/implementation/relations.md остаются верны:
  они явно описывают текущий агрегат и отсутствие замеров, а не обещают масштабирование.
- Проверки уточнения: Prettier двух файлов досье, docs:check (157 документов,
  1088 ссылок) и git diff --check — успешно.

## Итог проверок и завершение среза

- Core: сборка и 46/46 тестов прошли на текущем коде. Project Runtime/Server Runtime
  собраны; typecheck Core, Project Runtime, Server Runtime, CLI, MCP и Web прошли.
- Server: в полном прогоне прошли 43 проверки, включая новый SSE-тест. Две OpenAPI
  проверки выявили обязательность default-поля description в выходной схеме запроса.
  GraphMutation/GraphQuery/GraphHistoryQuery переключены на входные схемы; оба OpenAPI
  теста после исправления прошли. SDK повторно сгенерирован и собран из актуальной схемы.
- CLI: после сборки актуального исполняемого пакета полный набор 90/90 прошёл, включая
  новый агентский сценарий. После уточнения ошибки некорректного JSON повторены сборка,
  тест graph и typecheck — успешно. Ранее новый тест запускался со старым dist и закономерно
  не находил команду; это исправлено сборкой, не изменением предметной логики.
- Web: lint, typecheck и production build прошли. Production-страница relations открыта
  через собственный Relay Server, загружены отношения, ошибок JS нет. После последнего
  обновления SDK typecheck повторён успешно.
- skills:build/check и 7/7 тестов сборщика прошли. Бюджет основного руководства сохранён:
  подробности вынесены в GRAPH.md; скилл обновлён генератором, результат вручную не менялся.
- docs:check — 157 документов, 1088 ссылок; скилл — 17 Markdown-файлов, 63 внутренние
  ссылки, ошибок нет. Форматирование изменённых файлов и git diff --check пройдены.
- Собственная браузерная сессия закрыта. Серверы 4793 и 5193 остановлены по проверенным
  PID; `ss` подтвердил отсутствие слушателей этих портов. Пользовательские процессы не затрагивались.
- Текущие ограничения описаны в engineering/implementation/relations.md: проекции старых
  отношений, агрегат 16 МиБ, отсутствие canvas, отдельного MCP и снимков редакций требований.
  Это рабочий сквозной срез, не декларация готовности всех возможностей PROPOSAL.md.

## Сквозные CLI и Web проверки

- Реальный CLI на временном проекте `.artifacts/entity-graph-project` создал фичу,
  сценарий, приложение, реализации, задачу и зависимость; прикрепил контекстный документ.
  `graph context scenario:...` в JSON нашёл все ожидаемые ID и путь до документа;
  текстовый ответ содержит Markdown и «Почему включено». Повтор записи не создал дубликат.
- Проверен HTTP backend CLI: graph list получает ту же базу через собственный сервер 4793.
- Web сгенерирован закреплённым генератором, команды записаны в apps/web/GENERATION.md.
  Реализованы поиск выбора, форма, направления, пути, продолжение и ссылки из карточек.
- Собственная headless-сессия agent-browser `tasks-web-entity-graph-0920`, Vite 5193:
  создан произвольный тип example-for, проверен после reload и удалён через UI.
  Внешняя запись CLI обновила граф; сохранение старой формы вернуло конфликт и сохранило
  пояснение. После явного обновления версии сетевой отказ был имитирован route --abort;
  повтор после unroute сохранил связь. Выбор работает ArrowDown/Enter/Tab.
- Проверены ширины 1440, 1024, 768, 390, светлая/тёмная темы; горизонтального переполнения
  не обнаружено. Снимки находятся в `.artifacts/web/entity-graph-*` и screenshot-1789901062096.png,
  screenshot-1789901102584.png. Первоначальный a11y нашёл недостаточный контраст статуса;
  цвет исправлен, повторные аудиты обеих тем — 0 нарушений. Необработанных ошибок браузера нет.
- Полный Core: 46 тестов пройдены. Server: 42 из 44 прошли; два OpenAPI-теста ожидали
  старое число операций. Счётчики обновлены с 103 до 109 и добавлены реальные запросы
  новых операций в проверку схем, а также SSE-тест прямой графовой записи. Повтор следует.
- Каталог дополнен областью продукта и явным списком настоящих lifecycle-ссылок;
  sessionId не интерпретируется как ссылка на запись Relay. Идемпотентность нормализует автора
  независимо от local/HTTP. Требуются итоговые проверки после этих правок.

## Реализация Core и подключение транспорта

- Добавлены схемы entity-graph, атомарный репозиторий relations.json, каталог существующих
  сущностей, универсальный сервис и отдельная политика контекста приложения.
- Добавлены REST и OpenAPI, SDK пересобран закреплённым генератором с собственного
  сервера на 4793. CLI и local/HTTP backend получили graph list/context/link/update/unlink/apply/history.
- Core typecheck и четыре новых теста пройдены. Тест обнаружил зависимость хеша версии
  от порядка полей до/после кодека; версия исправлена нормализацией через схему.
- При добавлении схем обнаружено совпадение имени domain/graph.ts со старым валидатором:
  прежний файл восстановлен без изменений, новые схемы находятся в entity-graph.ts.
- Core/project-runtime собраны; typecheck CLI и server-runtime пройден.
- [Контракт графа](../../reference/GRAPH.md) описывает фактическую физическую схему,
  продолжение, проекции прежних связей и ограничения первого среза.

## Начало реализации

- Пользователь поручил реализацию и фактическую проверку CLI/Web. Досье продолжено.
- Сверено дерево: изменения только в документах обсуждения, ветка синхронна upstream.
- Прочитаны правила затронутых владельцев и необходимые skills; план допуска Web
  и сквозной контракт сохранены в PLAN.md до правок реализации.
- Для первого исполняемого среза выбран один атомарный агрегат новых рёбер с лимитом,
  адаптер существующих связей сохраняет единственность фактов без неявной миграции
  пользовательских данных. Граница перехода будет явно показана в интерфейсах.

## Уточнение: универсальное ядро и политики приложений

- Пользователь определил границу: ядро поддерживает любые связи сущностей и вложенности;
  ограничения вроде единственной фичи сценария устанавливает приложение (Web/API).
- Исправлено прежнее предложение о предметных ограничениях внутри движка: ядро обеспечивает
  техническую целостность, политики приложения проверяются на его путях записи атомарно.
  Вложенность — рёбра с необязательной областью иерархии; глубина не задаётся схемой.
- Добавлен контракт полного графа для карт: узлы, включая изолированные, рёбра, версия,
  продолжение; пакет явных изменений вместо перезаписи по частичному отображению.
- В текущем файле предложения отсутствовал абзац контекстных ссылок, ранее отражённый
  в журнале; уточнение пользователя вновь явно внесено в актуальное предложение.
- Действующие контракты и код не меняются. Проверки уточнения: Prettier трёх файлов,
  `pnpm run docs:check` (153 документа, 1065 ссылок) и `git diff --check` — успешно.

## Уточнения после первоначального предложения

- Пользователь указал, что задача может исправлять приложение без фичи/сценария либо
  настраивать будущую инфраструктуру. В предложение добавлены независимая область
  работы и отсутствие обязательной цепочки сущностей.
- Пользователь уточнил, что связи устанавливаются агентами/операторами и документ может
  прикрепляться только для контекста, без предметной принадлежности задаче. Зафиксирован
  предлагаемый тип `references`, необязательное пояснение, отсутствие транзитивного
  переноса области и различение явного материала с автоматически подобранными документами.
- Это уточнение обсуждаемой модели; действующие PRODUCT.md, KANBAN.md и техническая
  архитектура остаются верными, поскольку код и контракты не меняются.
- Для этих изменений выполнены `pnpm run docs:check`, проверка форматирования трёх
  файлов досье и `git diff --check` — успешно. Проверка документации: 153 файла,
  1065 ссылок, ошибок нет. Код и исполняемые интерфейсы не менялись.

## 20 сентября 2026 — начало

- Основание: пользователю нужна восстанавливаемая цепочка от продуктовой сущности
  до реализации, задач, документов, плана и выпуска; запрошены варианты для JSON-базы.
- Прочитаны обязательные документы, справочники продукта и канбана, направление развития.
- `git status --short --branch`: чистое дерево, ветка `refactor/application-rebuild`,
  впереди upstream на один коммит. Исходные изменения пользователя отсутствуют.
- По справочникам уже есть постоянные ID, productLinks, связи задач и контекст документов;
  выводы требуют сверки с кодом. Новая модель релизов пока не объединена с продуктовой.

## 20 сентября 2026 — сверка и предложение

- Прочитаны `domain/board-task.ts`, `application/board-tasks/service.ts`,
  `storage/board-tasks.ts`, `application/product/queries.ts`, `application/project/relations.ts`;
  проверены ссылки планов и релизов в `domain/project.ts`.
- Подтверждено: зависимости и родительство проверяются независимо, блокеры вычисляются;
  обратная выборка productTarget включает только явные ссылки; контекст продукта
  подбирает документы с reasons. Чтение задач сканирует файлы всех досок.
- Пользователь уточнил необходимость универсального механизма и немедленного
  восстановления связей агентами. В предложение добавлены общая адресация, реестр типов,
  профили контекста, объясняющие пути и явная полнота/продолжение ответа.
- [Сравнены три варианта](PROPOSAL.md); рекомендован гибрид с первым этапом общего
  графа чтения. Отдельно описаны JSON-транзакции, единственность владельца факта,
  вычисляемые документы и неизменяемая история поставки. Решение пользователя ещё требуется.
- Сверены PRODUCT.md, KANBAN.md, engineering/ARCHITECTURE.md и implementation/product.md:
  предложение не меняет их действующие контракты. DIRECTION.md получает только ссылку
  на обсуждение; предложение не превращено в согласованные требования.
- Это статическое исследование: новые операции не реализованы, поведенческие тесты
  и замеры масштабирования не выполнялись.

## 20 сентября 2026 — уточнение целевой архитектуры

- Пользователь отверг привязку проектирования к кустарной текущей реализации:
  требуется конечный универсальный способ организации и добавления связей.
- Рекомендация пересмотрена: единый канонический реестр всех предметных связей,
  включая обязательную структуру. Гибрид сохранён только как сравниваемый вариант.
- В целевой модели едины сервис записи, реестр сущностей/типов, транзакции, история,
  индексы и профили агентского чтения. Поля-ссылки в DTO — проекции, не вторые источники.
- Переход существующих данных вынесен в отдельный последующий этап; реализация
  по-прежнему не начата, предложение ожидает согласования.

## 20 сентября 2026 — проверки предложения

- Форматирование пяти изменённых Markdown-файлов выполнено через `pnpm exec prettier --write`.
- `pnpm run docs:check` — успешно: 153 документа, 1065 ссылок; скилл — 16 файлов,
  62 внутренних ссылки, ошибок нет.
- `git diff --check` — успешно. Проверена навигация: индекс работ → досье → предложение
  и журнал; из DIRECTION.md доступно явно помеченное обсуждение.
- Изменены только документы. Предложение не подтверждено исполнением: ядро, JSON-схемы
  и публичные интерфейсы не менялись. Следующий шаг — согласовать целевую модель.
