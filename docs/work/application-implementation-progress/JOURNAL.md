# Журнал

## 21 сентября 2026 — исследование и проектирование

- Подтверждено чистое дерево на `refactor/application-rebuild`.
- Текущая страница показывает только описание вклада; задачи скрыты за кнопкой,
  редактор позволяет назначать готовность вручную. Дерево получает её из product state.
- Выбран единый расчёт в Core, а не отдельное вычисление по видимой странице задач.
  Общая готовность проектных требований остаётся предметом следующего этапа.
- Зафиксированы UI/UX, владельцы, совместимость и проверки до реализации.

## 21 сентября 2026 — реализация первого прохода

- Core накладывает статусы по явным productLinks задач на реализации в state,
  продуктовом каталоге и каталоге сущностей. Индекс не кеширует готовность задач;
  ревизия реализации и сохранённое описание не меняются. Общая проектная readiness
  пока сохраняет прежнюю семантику второго этапа.
- Созданы генератором внутренние implementation-details и implementation-dependencies.
  Страница показывает полное исходное требование и вклад, задачи открыты сразу,
  зависимости отделены от контекста. Дерево использует серверный статус и новые подписи.
- Прогресс читает два полных счётчика на одной версии канбана (limit=1), без загрузки
  всех карточек. Сам список сохраняет порции по 40. Ручной статус удалён из редактора.
- Предметные правила и контракт Web обновлены. Следующий шаг — проверки и визуальная доработка.

## 21 сентября 2026 — проверки и расширение проверки формы

- Web typecheck и Vite build прошли; lint выявил одно тернарное выражение в JSX,
  значение вынесено перед разметкой. Core build/typecheck прошли, полный набор 75/75.
- Новый интеграционный тест проверяет state, адресное чтение, оба каталога и
  неизменность хранения. Историческое ожидание ручного статуса заменено на none
  без задач; проверка прежней проектной readiness сохранена.
- Найден второй ручной статус в scope-editor. По прочитанным правилам форм React
  Reference он заменён вычисляемым индикатором; ненужные props удалены у потребителя.
- Первый запуск временного API потребовал TSX_TSCONFIG_PATH server-runtime для
  декораторов; затем исправлено имя метода графа read в проверочном скрипте.
  Рабочие данные не затронуты; создаётся изолированная база в .artifacts.

## 21 сентября 2026 — браузерная и машинная приёмка

- На временном проекте rrZ278Ax, база `.artifacts/implementation-check-GyLNqf`,
  проверены REST-переносы задач и SSE: 1/3 → 3/3, статус «Реализовано»;
  возврат задачи из done при открытом дереве сразу меняет фичу и сценарий на «В работе».
  Отдельный сценарий без задач показывает «Не реализовано», другой с done — «Реализовано».
- Пагинация: 41 задача, 40 done и одна review; до подгрузки показаны 40 строк,
  прогресс 40/41 и статус «В работе», после — 41 и нет кнопки продолжения.
  Проверены шесть зависимостей с переходом 5 → 1. Соседние связи индуцированного
  графа отфильтрованы по корню, чтобы не представлять их прямыми зависимостями.
- Переход к задаче открывает окно канбана; Back возвращает в реализацию. Переход
  по якорю задач переводит фокус на соответствующий раздел. Сохранение проверено Enter.
- Ошибка POST не стирает черновик; reload восстанавливает название. Внешняя запись
  вызывает конфликт, блокирует сохранение, сохраняет текст. «Оставить мой ввод» +
  сохранение работают, после успеха черновик очищен. Селекторов готовности нет.
- Ошибка GET задач отличается от пустоты; после восстановления возвращается прогресс.
  Первая слишком широкая маска сетевого отказа задела Vite-модуль и дала таймаут;
  повтор с маской только API подтвердил нужный сценарий.
- UI уточнён после скриншота: полное описание первым, вклад и задачи ниже; быстрые
  якоря вместо повторной полосы родителей, ссылка на доску в контексте и пустом списке.
  Контраст зелёного статуса исправлен. Axe: 0 нарушений и 0 incomplete на
  1440/1024/768/390 в обеих темах; отдельно страница фичи также 0/0.
- Скриншоты `.artifacts/implementation-{1440,1024,768,390}-{light,dark}.png`,
  `implementation-tree-light.png`, `implementation-tasks-light.png`. Снимки body
  включают пустую область за пределами внутреннего скролла; видимый интерфейс оценён
  по viewport и отдельным снимкам. До искусственных сетевых отказов ошибок JS не было.
- `pnpm --filter @relay/core run test`: 75/75; Core build/typecheck успешны.
  `pnpm --filter @relay/server-runtime run test`: 33/33, включая HTTP/OpenAPI/SSE.
  `pnpm run lint:web` и `pnpm run typecheck:web` (включает Vite build) успешны.
- Финальная сверка добавила вычисляемые статусы в версию краткого каталога: изменение
  отображаемой готовности теперь меняет и версию ответа. CAS продуктовых текстов
  сохраняет прежнюю независимость от перемещения задач.
- После форматирования и параллельных сборок Vite HMR временно потерял модульные
  стили/экспорты. Финальная проверка перенесена на production build через собственный
  Vite preview и свежую сессию `tasks-web-implementation-final-0921`: стили и прямой
  URL восстановлены без изменения механизма приложения. Проверка мобильного якоря
  выявила перекрытие заголовка закреплённой шапкой; добавлен scroll-margin-top.
- Финальный production build успешно собран. На 390px обе темы показывают оформленные
  строки задач; начало раздела после перехода находится на y=80 при шапке до y=64.
  Axe 0/0, ошибок страницы нет. Скриншоты финального состояния:
  `.artifacts/screenshot-1790012684816.png` и `screenshot-1790012685849.png`.
- Проверены граф зависимостей и фасеты: новые компоненты внутренние для product-entity,
  данные через публичные API доменов, межслойных обратных импортов нет. Схемы SDK,
  маршруты apps/web/ROUTING.md, правила текста и документация Playground сохраняют
  верность: транспорт, URL и пользовательская память Playground не менялись.
- Справочник PRODUCT.md входит в bundle.json скилла; завершающий шаг включает
  штатные skills:build и skills:check, а не ручное редактирование готового пакета.
- Обе собственные браузерные сессии закрыты; API и Vite/preview остановлены.

## 21 сентября 2026 — завершение

- `pnpm run skills:build` обновил распространяемый пакет и локальную копию Playground;
  `pnpm run skills:check` подтвердил актуальность 18 файлов и переносимость ссылок.
- `pnpm run docs:check`: 189 документов, 975 ссылок, ошибок нет; ссылки пакета также верны.
  Форматирование изменённых файлов и `git diff --check` прошли.
- Порты 4795/5175 свободны. Первый этап приложений готов; следующий этап проектных
  требований сохраняется отдельной целью. Коммит и публикация не выполнялись.
