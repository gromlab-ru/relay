# Журнал

## 21 сентября 2026 — незавершённые сценарии при готовой FI

Пользователь показал API-FI-1 done при двух дочерних SI со статусом none. Причина:
в task-progress были только рёбра «имплементация → проектная цель», без агрегации
SI внутри FI. Зафиксирован полный порядок агрегации и общий обход для статусов/задач,
включая проектные сценарии в готовности проектной фичи. Начальное рабочее дерево чистое;
это исправление поверх сохранённого пользователем предыдущего результата.

- Построен единый предметный граф обязательных частей. FI → SI индексируется парой
  applicationId/featureId; проектная фича также включает описанные проектные сценарии.
  Статусы вычисляются снизу вверх, список задач обходит тот же граф без повторов.
- Регрессии покрывают 0/2, 1/2, 2/2, отсутствие прямых FI-задач, отмену и возврат SI,
  разделение приложений/фич, снятие SI, добавление сценария и версии пагинации,
  все каталоги и граф. Два прежних теста, закреплявших независимость FI от сценариев,
  адаптированы: проверка самостоятельного вклада теперь относится к SI, а родитель
  единственного проектного сценария наследует его статус. Некорректный кириллический
  requestId в новой фикстуре заменён допустимым техническим ключом.
- Адресные 8 тестов и Core typecheck прошли. Следующий шаг — полные проверки и
  браузерная фикстура со структурой пользовательского скриншота.
- Полный Core: 82/82; HTTP/OpenAPI/SSE: 33/33; Core typecheck, сборки Contracts/Core/
  Server Runtime/SDK, Web lint/typecheck/build успешны. SDK получен закреплённым 5.2.4
  со сборки tsc; в diff только пять описаний productTarget, без изменения типов.
- На production preview воспроизведена структура скриншота: API-FI-1 с выполненной
  собственной задачей и двумя SI без задач — partial и 0/2. Создание задач SI в done
  даёт 1/2 partial, затем 2/2 done; возврат API-SI-2 в работу снова даёт FI partial
  через SSE. Проектный FEATURE-1 также partial при 1/2; FI-страница содержит все три
  задачи и прогресс 2/3, без повторов. REST readiness подтверждает те же состояния.
- Проверены 1440/1024/768/390 и обе темы, axe 0/0, console/errors пусты. Скриншоты
  `.artifacts/screenshot-1790020179652.png` (исходный случай) и
  `screenshot-1790020317746.png` / `screenshot-1790020318329.png` (унаследованные задачи).
- Начальная версия 0e456f3, ветка refactor/application-rebuild. Временный проект
  7btzXH7s находится в `.artifacts/scenario-cascade-check-VnTdry`, скрипт
  `.artifacts/scenario-cascade-server.mjs`, метаданные `scenario-cascade-check.json`.
  Собственные API 4798, preview 5178 и сессия tasks-web-scenario-cascade-0921 остановлены.
- Финальная сверка: skills:build/check успешны, тесты скилла 7/7; docs:check —
  191 файл, 982 ссылки без ошибок. Форматирование и git diff --check прошли.
  Порты 4798/5178 свободны. Исправление полного сценарного каскада завершено;
  изменения оставлены незакоммиченными поверх 0e456f3.

## 21 сентября 2026 — уточнение каскада пользователем

После приёмки страниц пользователь обнаружил FEATURE-1 со статусом none при готовой
имплементации API. Причина — ошибочное исключение реализаций из проектной readiness
на предыдущем этапе. Уточнён контракт: задача → имплементация → её проектная цель.
Наличие задачи в любой активной ветке поднимает цель как минимум до partial;
полная готовность требует готовности всего объявленного состава. Прямые задачи
сохраняются как дополнительная ветка, но их отсутствие не блокирует каскад.
План исправления и UI-пояснений записан в README до реализации.

- Реализован подъём статусов в общем task-progress; state и оба каталога используют
  один расчёт. productTarget выбирает прямые задачи и задачи активных реализаций
  до пагинации, без дублей. Смена активного состава инвалидирует версию такой выборки.
- Core build/typecheck и все 79 тестов успешны. Регрессия воспроизводит отсутствие
  прямых задач, API inbox/done, незапущенный Web, завершение всего состава,
  снятие Web, конфликт страницы, отмену API и снятие последней связи.
- Пояснения Web и действующие product/reference/engineering/skill исправлены:
  прежнее утверждение об исключении имплементаций из готовности было ошибкой.
- Браузер воспроизвёл случай пользователя на временном проекте OCNLunxJ:
  нет прямых задач, API готов, Web без задач — FEATURE-1 и SCENARIO-1 в работе,
  список содержит API-1. Создание и завершение WEB-1 подняло обе цели до done через SSE;
  возврат API-1 в работу при открытом дереве поднял partial без reload.
- В обеих темах и на 1440/1024/768/390 axe 0/0, ошибок JS нет. Снимки:
  `.artifacts/screenshot-1790018269297.png`, затем в agent-browser/tmp/screenshots
  1790018342026/1790018342657/1790018343407. Web lint/typecheck/build и HTTP 33/33 успешны.
- Уточняется также русское описание productTarget в Contracts/OpenAPI. Прочитаны
  инструкции Contracts/SDK и REST API Codegen (CLI): существующий SDK регенерируется
  закреплённым 5.2.4, форма запроса не меняется. Последующая сборка проверит потребителей.
- Первая генерация с tsx-сервера выявила потерю decorator metadata и нежелательный any
  в параметрах пути. Генерация повторена со сборки tsc: в итоговом SDK меняются только
  пять описаний productTarget; типы и операции сохранены. Правило источника OpenAPI
  уточнено в README владельца SDK. Contracts/Core/Server Runtime/SDK сборки и Web typecheck успешны.
- Исправление завершено. Скилл пересобран и проверен, тесты скилла 7/7,
  docs:check — 191 документ, 981 ссылка без ошибок. Все собственные серверы,
  preview и браузер закрыты; порты 4797/5177 свободны. Рабочее дерево сохраняет
  принятые страницы и исправленный каскад, коммит не выполнялся.

## 21 сентября 2026 — приёмка первого этапа и проектирование второго

- Пользователь принял UI реализаций приложений и поручил такие же проектные страницы.
- Сверено рабочее дерево: результаты первого этапа на месте, коммита нет.
- Найденные отличия: общая фича пока агрегирует ручные подтверждения/сценарии,
  сценарий использует простой текстовый экран; задачи скрыты. У фичи длинная лента
  полных сценариев, а реализации зависят от полного snapshot-provider.
- Зафиксированы общий компонент чтения требования, подъём зависимостей и компактные
  сценарии. Готовность переходит на прямые типизированные цели задач продуктовой доски.
- Определены владельцы, контракт совместимости, границы списков и программа проверки.

## 21 сентября 2026 — реализация

- Расчёт Core обобщён в task-progress: ключ содержит kind:id, проектная readiness
  получает статус прямых задач. Индекс накладывает динамические статусы всех трёх видов
  после чтения кеша. Счётчики участия приложений сохранены, stale больше не меняет готовность.
- Создан генератором product-requirement, поднят product-dependencies; страницы используют
  единый дизайн, сценарии фичи стали компактными с продолжением. ProductContributions
  читает только активные реализации выбранной цели по 5, не требует полного snapshot.
- Открытый ProductTasks и хук прогресса обобщены для приложения/продуктовой доски.
  Проектное дерево показывает серверный статус даже у фичи без сценариев.
- Ожидаемое поведение обновлено в product; следующие шаги — проверки и точные справочники.

## 21 сентября 2026 — первые проверки

- Web typecheck/lint прошли. Core build/typecheck и все 78 тестов прошли; три старых
  ожидания ручной готовности актуализированы под явные задачи, проверки хранения и повторов сохранены.
  Новые проверки покрывают обе проектные цели, отмену, удаление задачи, оба каталога,
  overview/context, неизменность текста и CAS-версии, независимость одноимённых ID разных видов.
- HTTP/OpenAPI/SSE: 33/33. Собран production Web, временный API 4796 и preview 5176.
  База `.artifacts/requirement-check-BL9nlW`, проект lWjDyIxR; рабочие данные не затронуты.
- В браузере открыты фича и сценарий, полные тексты и задачи на месте. Связь сценария
  открывает APP1-SI-1, Back возвращает в сценарий. Пагинация реализаций 5→1 и зависимостей
  5→1 работает; axe сценария 0 нарушений/0 incomplete.
- Сценарии дополнительно упорядочены по числовому ключу, чтобы порядок не зависел от ID.
  Убрано устаревшее предупреждение о ручном переподтверждении. Product/reference/engineering
  и инструкции оркестратора/исполнителя актуализированы под готовность по прямым задачам.

## 21 сентября 2026 — браузерная приёмка

- Одна собственная headless-сессия tasks-web-requirements-0921, production preview,
  обе страницы на 1440/1024/768/390 в light/dark: просмотрены все 16 скриншотов,
  axe в каждом случае 0 нарушений и 0 incomplete. Переполнения по горизонтали нет.
  До искусственных сетевых отказов console/errors пусты.
- Через REST завершены PRODUCT-2/3: открытый сценарий получил 3/3 и «Реализовано» без reload.
  Общее дерево показывало фичу done при 1 готовом сценарии из 12. Возврат PRODUCT-2
  в review при открытом дереве обновил фичу/сценарий до «В работе» через SSE.
- Сценарии на странице фичи отсортированы SCENARIO-1…10; подгрузка добавила 11/12,
  кнопка исчезла. В сценарии с 41 задачей сначала 40 строк и прогресс 40/41, после
  подгрузки 41 строка; статус остаётся «В работе» из-за review.
- У фичи без задач/сценариев и сценария без задач показано «Не реализовано»;
  пустые реализации и зависимости объяснены отдельно. Якорь задач на 390 переводит
  фокус к разделу и оставляет y≈80 ниже закреплённой шапки.
- Проверены сеть и сохранность редактора сценария: ошибка POST сохраняет название,
  reload возвращает черновик; внешняя правка вызывает выбор версии без потери ввода.
  «Оставить мой ввод» и сохранение клавишей Enter работают, результат открыт по прежнему ID.
- Ошибка GET задач не отображается как отсутствие задач. Ошибка адресного каталога
  реализаций сохраняет описание и показывает повтор; после восстановления возвращаются
  APP1-SI-1 и остальные. Первая широкая маска сетевого отказа задела product/entities
  и закономерно закрыла контекст; уточнённая маска только общего entities проверила нужный блок.
- Основная серия из 16 снимков лежит в `/home/gromov/.agent-browser/tmp/screenshots/`
  от `screenshot-1790014806905.png` до `screenshot-1790014846268.png`:
  фича light (1440→390), фича dark (390→1440), сценарий dark (1440→390),
  сценарий light (390→1440). В `.artifacts` сохранены первые широкие снимки
  screenshot-1790014486350.png, screenshot-1790014531363.png и пустота 1790014939243.

## 21 сентября 2026 — финальная сверка

- Применимый граф юнитов сверён: product-requirement → product-dependencies /
  product-contributions / product-tasks, затем домены → infra. Прежних глубоких
  импортов implementation-dependencies нет. Общий просмотр не зависит от snapshot-provider;
  фича сохраняет существующую полную загрузку для сценариев/документов.
- Схемы REST/SDK, URL в ROUTING.md и инструкция запуска Playground сохраняют верность:
  форма транспорта и хранения не менялась, новая готовность описана в PRODUCT.md.
- Финальные `pnpm run lint:web`, `pnpm run typecheck:web`, `pnpm run build:web`
  успешны; вывод сохранён в `.artifacts/requirement-final-web.log`.
- `skills:build`, `skills:check`, `skills:test` успешны (7/7), скилл обновлён штатным
  сборщиком вместе с локальной копией Playground. `docs:check`: 191 файл, 980 ссылок,
  ошибок нет; форматирование и diff-check успешны.
- Финальный мобильный список сценариев и якорь проверены на актуальном production build:
  `.artifacts/screenshot-1790015454556.png`, axe 0/0. Сессия tasks-web-requirements-0921
  закрыта, API 4796 и preview 5176 остановлены. Второй этап завершён, изменения обоих
  этапов остаются в рабочем дереве без коммита и публикации.
