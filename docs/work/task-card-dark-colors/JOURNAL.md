# Журнал

## 22 сентября 2026 — исследование и план

Проверены скриншоты, task-card, его потребители и тема. Фон целей использует Mantine
`violet-light` и `teal-light`; violet — стандартная насыщенная шкала, teal — проектная
приглушённая зелёная. В тёмной схеме эти заливки имеют несопоставимую заметность.
Текст уже использует светлые оттенки второй ступени. Решение — локально смешивать
светлые оттенки третьей ступени с `--tasks-surface`, не менять палитру всего приложения.
Геометрия, подписи и иконки сохраняются. План и критерии записаны до реализации.

## 22 сентября 2026 — исправление

Обе тёмные заливки используют 18% светлого оттенка и 82% поверхности карточки.
Светлые варианты и цвета текста оставлены прежними. Актуализированы продуктовый
контракт Web и состояние реализации. Lint, typecheck и build Web прошли; сборка
сообщает предупреждение о chunk >500 kB. Границы владельцев и контракты сохранены.

## 22 сентября 2026 — приёмка и завершение

- Проверочная база создана отдельно: `.artifacts/card-colors-check-v7X73Q`, API 4798,
  Vite 5178, headless-сессия `tasks-web-colors-0922`. При подготовке `/tmp/opencode`
  оказался недоступен для записи логов; использована `.artifacts`. Запуск TSX потребовал
  tsconfig server-runtime для декораторов; фикстура исправлена добавлением обязательного summary.
- В браузере просмотрены light/dark × 1440/1024/768/390: фича, сценарий с `+1`, задача
  без цели; обрезка текста и геометрия сохраняются. Тёмные фоны RGB (58,53,72) и (57,62,59)
  заметно светлее поверхности (32,32,32). По вычисленным CSS-цветам контраст текста
  7,0557:1 и 7,7561:1; зелёная метка больше не выглядит почти чёрной.
- Axe: 1440 dark и 390 light — 0 violations, 0 incomplete. Клавиатурное открытие задачи
  Enter и закрытие Escape успешны; console содержит только Vite/React DevTools,
  errors пуст. Сетевые сбои, конкуренция и сохранение черновика повторно не проверялись:
  изменение ограничено двумя CSS-фонами; прежние проверки не объявляются новыми.
- Машинная проверка отдельно: GET `/api/v1/board-tasks?board=product&limit=40` вернул
  ok, три задачи, правильные feature/scenario productLinks, total=3, nextOffset=null.
  Справочник `docs/reference/KANBAN.md` сверён: контракт и пагинация остаются верными.
- Скриншоты агентского браузера сохранены в его локальном каталоге
  `/home/gromov/.agent-browser/tmp/screenshots/`: dark — `screenshot-1790054690883.png`,
  `screenshot-1790054709711.png`, `screenshot-1790054709757.png`, `screenshot-1790054709801.png`;
  light — `screenshot-1790054718968.png`, `screenshot-1790054719207.png`,
  `screenshot-1790054719262.png`, `screenshot-1790054719333.png`. Это локальные артефакты вне Git.
- `pnpm run docs:check`, Prettier изменённых CSS/Markdown и `git diff --check` успешны.
  Сессия закрыта, собственные процессы API/Vite остановлены. Пользовательское окружение
  на 4700/5173 не использовалось для записи и не останавливалось.
- Финальный status обнаружил новые параллельные изменения Contracts/Core, включая
  document-library. Они сохранены; проверки Web выше относятся к состоянию на момент
  их запуска, а не подтверждают последующие изменения другой работы.
