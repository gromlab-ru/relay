# Журнал исправления маршрутов Web

## 20 сентября 2026 — начало

- Пользователь поручил перенести карту во фронтенд и приступить к исправлению маршрутов.
  Зафиксирован план по результатам аудита; изменения ограничены Web и его документацией.
- Рабочее дерево содержит только документы собственного аудита, HEAD `c1c353f`.
  Дополнительно прочитаны правила архитектурных границ, строгий style guide и генерация TSX.
- Выбрана общая продуктовая ветка с отдельной границей полного снимка: объединение URL
  не должно заставлять адресную страницу загружать весь продукт. Реализации адресуются
  внутри приложения, редактор получает `/edit`; короткие адреса сохраняются для совместимости.

## 20 сентября 2026 — дерево маршрутов и переходы

- Генератором созданы `product-snapshot` (ui-unit) и `relay-markdown-link` (ui-component).
  Первый адаптирован в маршрутную границу с lazy-фасетом; прежний ProductOutlet перенесён
  с его фасетом целиком внутрь границы. Общий ProductLayout теперь обслуживает все страницы.
- Реализация и `/edit` вложены в приложение, короткие адреса сохранены. Ссылки дерева,
  вкладов фичи и областей документов обновлены. Родитель проверяется до канонизации.
- Убраны общий шаблон редактора и маршрут мёртвой работы. Вход в продукт обрабатывается
  до подключения снимка; новые возвраты сценария используют прямые адреса.
- Query задачи становится частью ссылки, канонизация и local-вход сохраняют hash/state.
  Markdown-адаптер подключён в общей проектной области через существующий UI-провайдер.
- Карта сохранена в `apps/web/ROUTING.md`; продуктовый контракт обновлён, прежний
  инженерный аудит явно обозначен исходным историческим срезом.
- Проверки ещё не выполнены; следующий шаг — форматирование, типы и браузер.

## 20 сентября 2026 — первые проверки

- TypeScript и сборка прошли. Первый lint выявил две вычисляемые JSX-тернарности
  и неявный children у ссылок; исправлено по правилам проекта, повтор lint успешен.
  docs:check обнаружил две исторические ссылки на перенесённый ProductOutlet;
  заменены ссылками на текущее расположение с сохранением привязки к исходной версии.
- При первом открытии после повторной сборки сервер отдавал 404 для нового asset:
  перезапуск собственного сервера обновил зарегистрированную статику. Один CDP navigate
  завис; собственная сессия закрыта и запущена заново, сценарий задачи прошёл.
- В agent-browser подтверждены вход product с/без слеша; приложение → реализация → edit;
  активное меню «Приложения»; черновик после Back/Forward/reload; отказ сохранения offline
  без потери ввода и успешный повтор; сохранение возвращает в просмотр.
- Markdown-ссылка из сохранённого вклада открыла фичу при неизменном performance.timeOrigin.
  Короткий сценарий и edit сохранили query/hash; сохранение вернуло в сценарий, отмена
  редактора, открытого из фичи, — к фиче #scenarios. Старый /implementations/:ref/edit
  канонизирован; неверные родители отклонены; work и произвольный редактор дают 404-страницу.
- Короткая задача и legacy ?task канонизированы с query/hash. Закрытие прямой ссылки
  и reload сохраняют фильтры; открытие задачи клавишей Enter, Back/Forward успешны.
- При сверке дополнительно сохранён query у контекстных ссылок родителей и устранён
  двойной слеш ссылки реализации при входе в приложение с завершающим слешем.

## 20 сентября 2026 — завершение

- На окончательном коде успешны `pnpm run lint:web`, `pnpm run typecheck:web`,
  `pnpm run build:web`; после исправления ссылок успешны `pnpm run docs:check`
  и `git diff --check`. Форматирование изменённых файлов проверено Prettier.
- Конкурентная запись во временную реализацию через REST вернула тот же ID и ревизию 3.
  SSE показал конфликт в редакторе: локальный заголовок сохранён, запись заблокирована.
  «Оставить мой ввод» и последующее сохранение успешны, возврат в просмотр выполнен.
- Подтверждены возврат реализация → редактор → просмотр → исходное приложение с якорем,
  ссылка реализации из приложения с завершающим слешем, query родительской ссылки сценария,
  local-вход без проектного префикса и совместимость старого якоря сценария.
- Явные редакторы фичи/приложения открывают соответствующие формы. Создание сценария
  вернуло непосредственно на вложенную страницу SCENARIO-2. Полный снимок продукта
  при прямом входе в редактор реализации отсутствует в сетевых запросах.
- Визуально просмотрены восемь снимков редактора: 1440/1024/768/390, светлая и тёмная темы.
  Управление доступно, на 390 горизонтального переполнения нет. Локальные снимки
  находятся в `~/.agent-browser/tmp/screenshots/screenshot-1789936222868.png`,
  `1789936222921`, `1789936222981`, `1789936223045`, `1789936237112`, `1789936237448`,
  `1789936237519`, `1789936237590` (последние имена с теми же префиксом и расширением).
  CLI сохранил их в собственный каталог вместо указанного пути; в Git они не входят.
- Axe 4.12.1: редактор реализации в тёмной теме — 39 passes, 0 violations/incomplete;
  сценарий в светлой — 34 passes, 0 violations/incomplete. В финальной браузерной
  сессии console/errors пусты. Проверка клавиатуры: задача открыта Enter, Back/Forward
  восстановили модалку и query.
- Обновлены технические владельцы и продуктовый контракт. Проверенные правила хранения,
  API и пагинации остаются верны: изменения затрагивают Web-адреса и контекст переходов.
  Браузерная проверка выполнена в local на временной базе; полный регресс workspace,
  всех документов и редактора состава в эту адресную приёмку не входил.
- Собственная сессия закрыта, сервер PID 165021 остановлен. Пользовательские процессы
  не затронуты. Работа завершена; коммитов и публикации нет.
