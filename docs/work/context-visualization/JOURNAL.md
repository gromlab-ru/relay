# Журнал визуализации контекста

## 23 сентября 2026 — исследование и допуск

Прочитаны обязательный протокол, правила документации, продуктовая концепция,
стандарт линков/связей, интерфейсный стандарт и Web AGENTS. Выполнены четыре пункта
допуска Web: skills, архитектура, профильные инструкции, план до первой правки.

Текущий исследователь использует `GET /graph` с глубиной и страницами; существующая
кнопка полного контекста в материалах задачи использует другой контракт
`GET /graph/context`. Для визуализации принят ограниченный запрос из CONTEXT-UI:
он предоставляет полные сведения рёбер и явные границы, не загружая всю компоненту
до первого изображения. Полный контракт остаётся самостоятельной операцией.

Проверены текущие незакоммиченные изменения, включая диагностическое назначение
редактора связей и новые доменные хуки; они сохраняются. Node.js v24.18.1,
pnpm 11.18.0. Новая цель выделена в собственное досье.

## Реализация первого среза

Добавлены `@xyflow/react` 12.11.6 и ELK.js. Генератором из `apps/web` созданы
`relation-context` (ui-unit), `context-canvas` (вложенный ui-unit), внутренние
`context-explorer`, `context-inspector`, `context-list`, `context-node`, `context-edge`.
Команда каждого запуска: `pnpm run create <ui-unit|ui-component> <имя> <каталог владельца>`.
Начальные файлы адаптированы, экран использует только lazy-фасет.

Домен связей объединяет по ID только страницы одной версии, читает исходный узел
отдельно для случая его отсутствия на первой странице. Все области перечитываются
как один согласованный результат; неудачная подгрузка не публикует половину нового
графа. Лимит — восемь страниц по 50; сведения рёбер не заменяются кратчайшими путями.

Кнопка и отдельный URL подключены. Раскладка ELK работает в Worker; первоначальная
камера у корня, последующие добавления сохраняют старые позиции и избегают наложения
новых карточек. У параллельных отношений разные порты, петля проходит вокруг карточки.
Подключены последовательный список и панель полного содержания.

Первый `pnpm run lint:web` прошёл. `pnpm run typecheck:web` выявил пять локальных
ошибок строгой индексации массива/CSS Module; они исправлены без ослабления типов.
Браузерная приёмка и повторная сборка ещё впереди.

## Браузерная проверка и исправления

Временная база `.artifacts/context-visualization-YR7OJJ` создана публичными операциями
Core; при подготовке исправлены входные данные фикстуры (тип приложения, предварительная
реализация фичи, обязательное summary документа). Пользовательские базы не затронуты.
Сервер раздаёт production-сборку на `127.0.0.1:41179`; своя headless-сессия
`tasks-web-context-20260923` использует `apps/web/agent-browser.json` во всех вызовах.

Проверены вход по кнопке, неизвестный тип `checks-contract`, Back после фильтра,
клавиатурный выбор карточки и петли, Markdown/таблица документа, переход на страницу
фичи, восемь разных геометрических линий для восьми ID, сохранение прежних координат
при раскрытии (5/8 → 7/10 узлов/связей). Подгрузка большой области дала ровно 63/62;
REST отдельно подтвердил 50+13 узлов, 50+12 рёбер, единую версию и полный ответ 63/62.

Внешнее добавление диагностической связи на временной базе вызвало HTTP 409 GRAPH_CHANGED:
UI сохранил 63/62 и выбранный DOC-31, явное обновление дало 64/63 с тем же выбором.
Offline сохранил снимок/выбор/режим; reconnect автоматически восстановил чтение.

Первый axe обнаружил контраст подписи продолжения и библиотечной атрибуции. Исправлены
через публичные свойства/CSS-переменную, добавлена роль области; повторно 0 нарушений,
осталась ручная оценка SVG и обрезанных областей. Добавлены русские partial/uninitialized.
На большой выборке обнаружена React #185 из отдельных updateNodeInternals каждого узла:
обновление перенесено в единственный внутренний `context-ports-sync`, созданный генератором.
Повторная проверка исправления ещё требуется.

## Завершение и итоговая приёмка

После пакетного обновления портов проверены большая область 64/63 и достижение
восьми раскрытых страниц: React-ошибок нет. Проверены глубина 2 (9/14), входящее
направление (четыре сущности), Back/Forward, смена корня из мобильного списка,
переход доски по её настоящему slug, изолированный документ и неизвестный ключ.
ArrowRight переместил корень с x=892 на x=897 без смены версии Core и числа рёбер.

Светлая/тёмная темы просмотрены на 1440/1024/768/390. Мобильная кнопка входа
укладывается в ширину, переполнения страницы нет; панель закрывается Escape.
У Drawer Header через публичную композицию установлен role=presentation, убран
дублирующий banner. Итоговый axe диаграммы и мобильной панели — 0 нарушений,
одна категория неполных проверок контраста SVG просмотрена на изображениях.
Итоговые errors/console пусты. ELK Worker поставляется отдельным готовым asset;
финальная production-сборка проходит без прежнего предупреждения IIFE.

`pnpm run lint:web`, `pnpm run typecheck:web` (включая build) и `git diff --check`
успешны. Prettier применён к затронутым исходникам и документам. Общий docs:check
повторно показал те же пять исходных отсутствующих ссылок STORAGE-REFACTORING.
Адресно проверены 11 затронутых документов и 133 ссылки — успешно.

Проверены границы: единственный новый экран и вложенный Canvas; внутренние компоненты
без фасетов; доменные обращения через index.ts; обратных импортов и циклов новых
зависимостей нет. Оформление вида/связи поднято к существующим доменным владельцам
для каталога и диаграммы. Состояние камеры/выбора не смешано с серверным снимком.
GRAPH.md и LINKS-AND-CONTEXT.md остаются верными; API, хранение и предметные записи
не менялись. Канонические документы и индекс актуализированы.

Обе свои headless-сессии закрыты; последний Server PID 1024278 остановлен.
Временная база и изображения остаются в игнорируемой `.artifacts`.
Работа завершена; [матрица проверок](VERIFICATION.md) и точка приёмки сохранены в README.

Окончательные Prettier --check и diff --check прошли. После добавления ссылки на матрицу
адресная проверка охватывает 134 ссылки в 11 документах; остановка сервера подтверждена.

## Замечание пользователя: наложение списка и нечитаемость глубины 3

Пользователь прислал снимок: при активном «Список» поверх строки остаётся крупная
карточка Canvas. Предыдущая проверка клавиатуры/axe не обнаружила дефект визуального
скрытия. `visibility:hidden` родителя допускает `visibility:visible` у дочернего узла;
нужна изоляция всей отрисованной области, сохраняющая отдельно состояние камеры.

Исследование раскладки: ELK получает только размеры и концы рёбер. Порты назначаются
позже, маршруты ELK отбрасываются и заменяются SmoothStep, а при раскрытии новые
координаты смешиваются с прежними вручную. Это разрушает совместную геометрию графа
и ведёт линии через карточки и друг через друга. Плотные подписи усугубляют картину.

Read-only запросы к работающему Playground подтвердили состав API-FI-2:
глубина 1 — 3/2, глубина 2 — 22/30, глубина 3 — 35/99. Данные не изменялись.
Продолжение сохраняет владельцев Canvas/экрана и стандарт чтения сохранённых связей.
План и критерии новой проверки зафиксированы в README до изменений.

## Продолжение: дерево как основной способ чтения

Пользователь предложил древовидный просмотр и подтвердил продолжение. Принято дерево
путей от корня с сохранением единственной карточки сущности; дополнительные связи,
общие родители и циклы доступны через счётчик и сведения. Граф и список сохраняются
отдельными режимами. Поиск пути должен совпадать с деревом и учитывать направление.

Перед правками заново выполнен допуск Web. Рабочее дерево чистое, HEAD `68124da`;
предыдущие изменения сохранены пользователем. Черновой Canvas ещё не подключён
к новой согласованной геометрии. Собственный сервер 1225228 на временной копии доступен.
План, владельцы, совместимость URL и проверки зафиксированы в README.

## Реализация дерева и первая проверка нового среза

Подключён новый контракт Canvas и общие маршруты ELK. Добавлены режимы дерево/граф/список,
совместимый `mode=diagram`, счётчики дополнительных связей и петель, кнопки фокуса выбранной
сущности и восстановления раскладки. Поиск путей поднят в существующий `domains/relations`:
два потребителя получают одинаковый детерминированный путь с учётом направления.
Дерево использует общие порты ветвей, граф — отдельные порты рёбер; направления Core сохранены.

Генератором создан внутренний `context-node-relations` у инспектора. Для выбранного DOC-7
проверены восемь из 12 отношений, продолжение до 12 и открытие дополнительной входящей
связи DOC-1 → DOC-7 с настоящим ID, Markdown и метаданными. Подсветка пути согласована
с деревом. Переключение режима сохраняет снимок 35/99.

На временной копии API-FI-2 после загрузки обеих страниц:
дерево — 35 карточек, 34 уникальных маршрута, 65 дополнительных связей;
граф — 35 карточек и 99 уникальных маршрутов. Сэмплирование отрисованных SVG-путей
не обнаружило прохождения линий через внутренность карточек в обоих режимах.
REST отдельно подтвердил 35/99. При списке opacity=0, inert=true, aria-hidden=true;
hit-test попадает в строку списка. Камера до/после списка совпала в точности.

Typecheck, lint и две production-сборки прошли. Первая навигация после перезапуска
попала до готовности собственного сервера; после подтверждения запуска прошла.
Собственный сервер теперь PID 1294515 на 41179. Проверки тем, размеров, краевых
случаев и окончательных документов ещё продолжаются; прежние результаты их не заменяют.

## Завершение дерева: визуальная и функциональная приёмка

На глубине 3 проверено раскрытие FEATURE-7: 35/99 → 38/102, выбранная карточка
сохранила координаты. Все четыре ширины в обеих темах просмотрены. Список больше
не перекрывается Canvas; исходный баг подтверждён исправленным визуально и hit-test.

На прежней временной фикстуре проверены 5/8 с петлёй и параллельными отношениями,
клавиатурный выбор, Markdown-таблица, ручной перенос и восстановление маршрутов,
входящее/исходящее дерево, прежняя ссылка diagram и пустая область 1/0.
Мобильная панель открывает петлю с точным ID и обоими концами, Escape закрывает её.
Полный REST-контекст отдельно вернул 11/16 и complete=true.

Первый axe нашёл контраст счётчика, исправлена светлая/тёмная палитра; повторные
аудиты дали 0 нарушений. Неполные проверки SVG оценены визуально. Финальные ошибки
JavaScript и console пусты. Offline не дал воспроизводимого отказа в CLI, поэтому
проверен явный abort запросов графа: данные, DOC-7 и режим списка сохранились,
повтор после снятия блокировки восстановил чтение. Конкурентная запись и восемь
областей не перепроверялись: соответствующие доменные обработчики не изменены.

Lint, typecheck и финальная production-сборка прошли. Сессия закрыта, сервер 1325956
остановлен. Канонические документы актуализированы. GRAPH.md и LINKS-AND-CONTEXT.md
сохраняют верность: дерево — проекция чтения тех же рёбер; продуктовых записей, новых
линков, миграции или изменения основного полного API нет. Источники скилла не менялись.
Проверены фасеты и импорты: общий алгоритм доступен через домен, вложенный Canvas
не зависит от родителя; новый список инспектора не создаёт самостоятельного юнита.

Финальные проверки документации: полный `pnpm run docs:check` подтвердил ровно пять
исходных битых ссылок STORAGE-REFACTORING; адресный `checkDocumentation` прошёл
для всех 10 затронутых документов и 91 ссылки. Prettier --check и diff --check успешны.
Остановка собственного сервера подтверждена отсутствием процесса. Досье, навигация
и действующие описания соответствуют новому основному представлению дерева.
