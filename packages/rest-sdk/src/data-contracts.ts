/**
 * Generated by @gromlab/rest-api-codegen.
 * Do not edit manually. Regenerate the SDK from its OpenAPI specification.
 * https://github.com/gromlab-ru/rest-api-codegen
 */

export interface PlansQuery {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы: 12 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 12
   */
  limit?: number;
  /**
   * Версия первой страницы; обязательна для продолжения
   * @maxLength 128
   */
  version?: string;
  /**
   * Поиск по ключу, названию, цели и краткому описанию
   * @maxLength 1024
   */
  q?: string;
  /** Фильтр состояния плана */
  status?: PlansQueryStatusEnum;
}

export interface PlanningCandidatesQuery {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы: 12 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 12
   */
  limit?: number;
  /**
   * Версия первой страницы; обязательна для продолжения
   * @maxLength 128
   */
  version?: string;
  /**
   * Поиск задач по ключу и названию
   * @maxLength 1024
   */
  q?: string;
  /**
   * Доска: ключ, slug или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  board?: string;
  /**
   * Редактируемый этап; его задачи остаются допустимым выбором
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  stage?: string;
  /**
   * Оставить свободные задачи и текущий состав выбранного этапа
   * @default "true"
   */
  availableOnly?: PlanningCandidatesQueryAvailableOnlyEnum;
}

export interface PlanningCandidatesPage {
  /** Записи текущей страницы */
  items: {
    /**
     * Постоянный ID задачи: 8 символов
     * @pattern ^[A-Za-z0-9]{8}$
     */
    id: string;
    /**
     * Текущий ключ задачи с префиксом доски
     * @minLength 1
     * @maxLength 257
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
     */
    key: string;
    /** Постоянный ID текущей доски */
    boardId: string;
    /**
     * Однострочный заголовок без Markdown; может быть пустым
     * @minLength 0
     */
    title: string;
    /**
     * Явные связи «Реализует», максимум 100; пустой массив удаляет все связи
     * @maxItems 100
     * @default []
     */
    productLinks: {
      /** Цель реализации: общая фича, сценарий или контракт приложения */
      kind: PlanningCandidatesPageKindEnum;
      /**
       * ID или ключ продуктовой цели; при записи нормализуется в постоянный ID
       * @minLength 1
       * @maxLength 128
       */
      id: string;
    }[];
    /** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
    column: PlanningCandidatesPageColumnEnum;
    /** Порядок внутри колонки */
    rank: number;
    /**
     * Ревизия задачи, прочитанная перед изменением
     * @exclusiveMin 0
     * @max 9007199254740991
     */
    revision: number;
    /**
     * Прямые зависимости по ID; выполнение требует done, критериев и рекурсивного выполнения обязательств
     * @maxItems 200
     */
    dependencies: string[];
    /**
     * Связанные задачи; обратное представление вычисляется
     * @maxItems 200
     */
    related: string[];
    /** Родительская задача или null; незавершённая подзадача блокирует завершение родителя */
    parentId: string | null;
    /**
     * Время создания
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    createdAt: string;
    /**
     * Время последнего изменения
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    updatedAt: string;
    /**
     * Автор создания
     * @maxLength 128
     */
    createdBy: string;
    /**
     * Автор последнего изменения
     * @maxLength 128
     */
    updatedBy: string;
    /** Прогресс критериев приёмки; отсутствует в старых квитанциях */
    acceptance?: {
      /**
       * Общее число критериев
       * @min 0
       * @max 9007199254740991
       */
      total: number;
      /**
       * Число выполненных критериев
       * @min 0
       * @max 9007199254740991
       */
      completed: number;
    };
    /** Выполнены критерии, подзадачи и зависимости; можно завершить задачу */
    canComplete?: boolean;
    /** Текущий slug доски для навигации */
    boardSlug: string;
    /** Невыполненные прямые зависимости и подзадачи без повторов; их блокеры доступны через чтение связей */
    blockers: string[];
    /** Есть невыполненные зависимости */
    blocked: boolean;
    /** Можно брать в работу: колонка ready и нет блокеров */
    ready: boolean;
    /** Фактическое выполнение задачи */
    completed: boolean;
    /** Текущая принадлежность либо null */
    assignment: {
      /**
       * Постоянный ID записи выбранного проекта
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      planId: string;
      /** Текущий ключ плана */
      planKey: string;
      /** Название плана */
      planTitle: string;
      /**
       * Постоянный ID записи выбранного проекта
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      stageId: string;
      /** Название этапа */
      stageTitle: string;
      /** Состояние плана: черновик, в работе, завершён или отменён */
      status: PlanningCandidatesPageStatusEnum;
      /** Текущее участие в черновом или начатом плане */
      current: boolean;
    } | null;
  }[];
  /**
   * Полное число записей выбранной области
   * @min 0
   * @max 9007199254740991
   */
  total: number;
  /** Смещение следующей страницы либо null */
  nextOffset: number | null;
  /** Версия согласованного состава и фильтров */
  version: string;
}

export interface PlanningPageQuery {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы: 12 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 12
   */
  limit?: number;
  /**
   * Версия первой страницы; обязательна для продолжения
   * @maxLength 128
   */
  version?: string;
}

export interface PlansPage {
  /** Записи текущей страницы */
  items: {
    /**
     * Однострочное название плана
     * @minLength 1
     */
    title: string;
    /**
     * Краткое описание обычным многострочным текстом
     * @default ""
     */
    summary: string;
    /**
     * Цель плана в Markdown
     * @default ""
     */
    goal: string;
    /**
     * Обоснование начала работы в Markdown
     * @default ""
     */
    rationale: string;
    /**
     * Границы изменения в Markdown
     * @default ""
     */
    boundaries: string;
    /**
     * Ожидаемый результат в Markdown
     * @default ""
     */
    expectedResult: string;
    /**
     * Области воздействия по постоянным адресам
     * @maxItems 100
     * @default []
     */
    scope: {
      /** Вид области воздействия; не является целью реализации задачи */
      kind: PlansPageKindEnum;
      /**
       * Постоянный ID записи выбранного проекта
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      id: string;
    }[];
    /**
     * Участники плана; не определяют права исполнения
     * @maxItems 100
     * @default []
     */
    participants: string[];
    /** План работ */
    kind: "work-plan";
    /**
     * Постоянный ID проекта-владельца
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    projectId: string;
    /** Состояние плана: черновик, в работе, завершён или отменён */
    status: PlansPageStatusEnum;
    /** Итог завершения либо причина отмены в Markdown */
    result: string;
    /** Фактическое время начала либо null */
    startedAt: string | null;
    /** Фактическое время завершения или отмены либо null */
    closedAt: string | null;
    /**
     * Постоянный ID записи выбранного проекта
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    id: string;
    /** Читаемый ключ записи */
    key: string;
    /**
     * Ревизия записи
     * @exclusiveMin 0
     * @max 9007199254740991
     */
    revision: number;
    /**
     * Момент времени в UTC
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    createdAt: string;
    /**
     * Момент времени в UTC
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    updatedAt: string;
    /**
     * Автор операции
     * @maxLength 128
     */
    createdBy: string;
    /**
     * Автор операции
     * @maxLength 128
     */
    updatedBy: string;
    /** Подписи выбранной области для человека */
    scopeLabels: {
      /** Постоянный адрес kind:ID */
      ref: string;
      /** Актуальные ключ и название области */
      label: string;
    }[];
    /**
     * Полное количество этапов
     * @min 0
     * @max 9007199254740991
     */
    stageCount: number;
    counts: {
      /**
       * Все уникальные задачи собственного состава
       * @min 0
       * @max 9007199254740991
       */
      total: number;
      /**
       * Фактически выполненные задачи с учётом обязательств
       * @min 0
       * @max 9007199254740991
       */
      completed: number;
      /**
       * Задачи в колонке in-progress
       * @min 0
       * @max 9007199254740991
       */
      active: number;
      /**
       * Задачи в колонке review
       * @min 0
       * @max 9007199254740991
       */
      review: number;
      /**
       * Задачи с невыполненными внешними или дочерними обязательствами
       * @min 0
       * @max 9007199254740991
       */
      blocked: number;
      /**
       * Процент фактического выполнения; пустой состав — 0
       * @min 0
       * @max 100
       */
      percent: number;
    };
    /** Весь непустой состав фактически выполнен */
    ready: boolean;
    /** Ближайший этап либо null */
    nextStage: {
      /**
       * Постоянный ID записи выбранного проекта
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      id: string;
      /** Название ближайшего незавершённого этапа */
      title: string;
    } | null;
    /**
     * До 12 индикаторов этапов для карточки; полный каталог читается отдельно
     * @maxItems 12
     */
    stagePreview: {
      /**
       * Постоянный ID записи выбранного проекта
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      id: string;
      /** Фактическое выполнение непустого этапа */
      completed: boolean;
    }[];
  }[];
  /**
   * Полное число записей выбранной области
   * @min 0
   * @max 9007199254740991
   */
  total: number;
  /** Смещение следующей страницы либо null */
  nextOffset: number | null;
  /** Версия согласованного состава и фильтров */
  version: string;
  /** Полные счётчики состояний проекта без поисковых фильтров, включая all */
  statusCounts: Partial<Record<string, number>>;
}

export interface PlanSummary {
  /**
   * Однострочное название плана
   * @minLength 1
   */
  title: string;
  /**
   * Краткое описание обычным многострочным текстом
   * @default ""
   */
  summary: string;
  /**
   * Цель плана в Markdown
   * @default ""
   */
  goal: string;
  /**
   * Обоснование начала работы в Markdown
   * @default ""
   */
  rationale: string;
  /**
   * Границы изменения в Markdown
   * @default ""
   */
  boundaries: string;
  /**
   * Ожидаемый результат в Markdown
   * @default ""
   */
  expectedResult: string;
  /**
   * Области воздействия по постоянным адресам
   * @maxItems 100
   * @default []
   */
  scope: {
    /** Вид области воздействия; не является целью реализации задачи */
    kind: PlanSummaryKindEnum;
    /**
     * Постоянный ID записи выбранного проекта
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    id: string;
  }[];
  /**
   * Участники плана; не определяют права исполнения
   * @maxItems 100
   * @default []
   */
  participants: string[];
  /** План работ */
  kind: "work-plan";
  /**
   * Постоянный ID проекта-владельца
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
   */
  projectId: string;
  /** Состояние плана: черновик, в работе, завершён или отменён */
  status: PlanSummaryStatusEnum;
  /** Итог завершения либо причина отмены в Markdown */
  result: string;
  /** Фактическое время начала либо null */
  startedAt: string | null;
  /** Фактическое время завершения или отмены либо null */
  closedAt: string | null;
  /**
   * Постоянный ID записи выбранного проекта
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
   */
  id: string;
  /** Читаемый ключ записи */
  key: string;
  /**
   * Ревизия записи
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  revision: number;
  /**
   * Момент времени в UTC
   * @format date-time
   * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
   */
  createdAt: string;
  /**
   * Момент времени в UTC
   * @format date-time
   * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
   */
  updatedAt: string;
  /**
   * Автор операции
   * @maxLength 128
   */
  createdBy: string;
  /**
   * Автор операции
   * @maxLength 128
   */
  updatedBy: string;
  /** Подписи выбранной области для человека */
  scopeLabels: {
    /** Постоянный адрес kind:ID */
    ref: string;
    /** Актуальные ключ и название области */
    label: string;
  }[];
  /**
   * Полное количество этапов
   * @min 0
   * @max 9007199254740991
   */
  stageCount: number;
  counts: {
    /**
     * Все уникальные задачи собственного состава
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /**
     * Фактически выполненные задачи с учётом обязательств
     * @min 0
     * @max 9007199254740991
     */
    completed: number;
    /**
     * Задачи в колонке in-progress
     * @min 0
     * @max 9007199254740991
     */
    active: number;
    /**
     * Задачи в колонке review
     * @min 0
     * @max 9007199254740991
     */
    review: number;
    /**
     * Задачи с невыполненными внешними или дочерними обязательствами
     * @min 0
     * @max 9007199254740991
     */
    blocked: number;
    /**
     * Процент фактического выполнения; пустой состав — 0
     * @min 0
     * @max 100
     */
    percent: number;
  };
  /** Весь непустой состав фактически выполнен */
  ready: boolean;
  /** Ближайший этап либо null */
  nextStage: {
    /**
     * Постоянный ID записи выбранного проекта
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    id: string;
    /** Название ближайшего незавершённого этапа */
    title: string;
  } | null;
  /**
   * До 12 индикаторов этапов для карточки; полный каталог читается отдельно
   * @maxItems 12
   */
  stagePreview: {
    /**
     * Постоянный ID записи выбранного проекта
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    id: string;
    /** Фактическое выполнение непустого этапа */
    completed: boolean;
  }[];
}

export interface StagesPage {
  /** Записи текущей страницы */
  items: {
    /**
     * Однострочное название этапа
     * @minLength 1
     */
    title: string;
    /**
     * Краткое описание этапа обычным текстом
     * @default ""
     */
    summary: string;
    /**
     * Ожидаемый результат этапа в Markdown
     * @default ""
     */
    outcome: string;
    /**
     * Условия завершения в Markdown; не исполняемая формула
     * @default ""
     */
    completionConditions: string;
    /** Этап плана работ */
    kind: "plan-stage";
    /**
     * Постоянный ID записи выбранного проекта
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    projectId: string;
    /**
     * Единственный план-владелец
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    planId: string;
    /**
     * Порядок отображения; не зависимость исполнения
     * @min 0
     * @max 9007199254740991
     */
    rank: number;
    /**
     * Полный набор ID включённых задач, максимум 2000; карточки читаются страницами
     * @maxItems 2000
     */
    taskIds: string[];
    /**
     * Постоянный ID записи выбранного проекта
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    id: string;
    /** Читаемый ключ записи */
    key: string;
    /**
     * Ревизия записи
     * @exclusiveMin 0
     * @max 9007199254740991
     */
    revision: number;
    /**
     * Момент времени в UTC
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    createdAt: string;
    /**
     * Момент времени в UTC
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    updatedAt: string;
    /**
     * Автор операции
     * @maxLength 128
     */
    createdBy: string;
    /**
     * Автор операции
     * @maxLength 128
     */
    updatedBy: string;
    counts: {
      /**
       * Все уникальные задачи собственного состава
       * @min 0
       * @max 9007199254740991
       */
      total: number;
      /**
       * Фактически выполненные задачи с учётом обязательств
       * @min 0
       * @max 9007199254740991
       */
      completed: number;
      /**
       * Задачи в колонке in-progress
       * @min 0
       * @max 9007199254740991
       */
      active: number;
      /**
       * Задачи в колонке review
       * @min 0
       * @max 9007199254740991
       */
      review: number;
      /**
       * Задачи с невыполненными внешними или дочерними обязательствами
       * @min 0
       * @max 9007199254740991
       */
      blocked: number;
      /**
       * Процент фактического выполнения; пустой состав — 0
       * @min 0
       * @max 100
       */
      percent: number;
    };
  }[];
  /**
   * Полное число записей выбранной области
   * @min 0
   * @max 9007199254740991
   */
  total: number;
  /** Смещение следующей страницы либо null */
  nextOffset: number | null;
  /** Версия согласованного состава и фильтров */
  version: string;
  /**
   * Ревизия плана на момент чтения этой страницы этапов
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  planRevision: number;
}

export interface PlanningTasksPage {
  /** Записи текущей страницы */
  items: {
    /**
     * Постоянный ID задачи: 8 символов
     * @pattern ^[A-Za-z0-9]{8}$
     */
    id: string;
    /**
     * Текущий ключ задачи с префиксом доски
     * @minLength 1
     * @maxLength 257
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
     */
    key: string;
    /** Постоянный ID текущей доски */
    boardId: string;
    /**
     * Однострочный заголовок без Markdown; может быть пустым
     * @minLength 0
     */
    title: string;
    /**
     * Явные связи «Реализует», максимум 100; пустой массив удаляет все связи
     * @maxItems 100
     * @default []
     */
    productLinks: {
      /** Цель реализации: общая фича, сценарий или контракт приложения */
      kind: PlanningTasksPageKindEnum;
      /**
       * ID или ключ продуктовой цели; при записи нормализуется в постоянный ID
       * @minLength 1
       * @maxLength 128
       */
      id: string;
    }[];
    /** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
    column: PlanningTasksPageColumnEnum;
    /** Порядок внутри колонки */
    rank: number;
    /**
     * Ревизия задачи, прочитанная перед изменением
     * @exclusiveMin 0
     * @max 9007199254740991
     */
    revision: number;
    /**
     * Прямые зависимости по ID; выполнение требует done, критериев и рекурсивного выполнения обязательств
     * @maxItems 200
     */
    dependencies: string[];
    /**
     * Связанные задачи; обратное представление вычисляется
     * @maxItems 200
     */
    related: string[];
    /** Родительская задача или null; незавершённая подзадача блокирует завершение родителя */
    parentId: string | null;
    /**
     * Время создания
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    createdAt: string;
    /**
     * Время последнего изменения
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    updatedAt: string;
    /**
     * Автор создания
     * @maxLength 128
     */
    createdBy: string;
    /**
     * Автор последнего изменения
     * @maxLength 128
     */
    updatedBy: string;
    /** Прогресс критериев приёмки; отсутствует в старых квитанциях */
    acceptance?: {
      /**
       * Общее число критериев
       * @min 0
       * @max 9007199254740991
       */
      total: number;
      /**
       * Число выполненных критериев
       * @min 0
       * @max 9007199254740991
       */
      completed: number;
    };
    /** Выполнены критерии, подзадачи и зависимости; можно завершить задачу */
    canComplete?: boolean;
    /** Текущий slug доски для навигации */
    boardSlug: string;
    /** Невыполненные прямые зависимости и подзадачи без повторов; их блокеры доступны через чтение связей */
    blockers: string[];
    /** Есть невыполненные зависимости */
    blocked: boolean;
    /** Можно брать в работу: колонка ready и нет блокеров */
    ready: boolean;
    /** Фактическое выполнение с учётом всех обязательств */
    completed: boolean;
  }[];
  /**
   * Полное число записей выбранной области
   * @min 0
   * @max 9007199254740991
   */
  total: number;
  /** Смещение следующей страницы либо null */
  nextOffset: number | null;
  /** Версия согласованного состава и фильтров */
  version: string;
}

export interface PlanMemberships {
  /** Записи текущей страницы */
  items: {
    /**
     * Постоянный ID записи выбранного проекта
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    planId: string;
    /** Текущий ключ плана */
    planKey: string;
    /** Название плана */
    planTitle: string;
    /**
     * Постоянный ID записи выбранного проекта
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    stageId: string;
    /** Название этапа */
    stageTitle: string;
    /** Состояние плана: черновик, в работе, завершён или отменён */
    status: PlanMembershipsStatusEnum;
    /** Текущее участие в черновом или начатом плане */
    current: boolean;
  }[];
  /**
   * Полное число записей выбранной области
   * @min 0
   * @max 9007199254740991
   */
  total: number;
  /** Смещение следующей страницы либо null */
  nextOffset: number | null;
  /** Версия согласованного состава и фильтров */
  version: string;
}

export interface CreatePlan {
  /** Однострочное название плана */
  title: string;
  /**
   * Краткое описание обычным многострочным текстом
   * @default ""
   */
  summary?: string;
  /**
   * Цель плана в Markdown
   * @default ""
   */
  goal?: string;
  /**
   * Обоснование начала работы в Markdown
   * @default ""
   */
  rationale?: string;
  /**
   * Границы изменения в Markdown
   * @default ""
   */
  boundaries?: string;
  /**
   * Ожидаемый результат в Markdown
   * @default ""
   */
  expectedResult?: string;
  /**
   * Области воздействия по постоянным адресам
   * @maxItems 100
   * @default []
   */
  scope?: {
    /** Вид области воздействия; не является целью реализации задачи */
    kind: CreatePlanKindEnum;
    /**
     * Постоянный ID записи выбранного проекта
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    id: string;
  }[];
  /**
   * Участники плана; не определяют права исполнения
   * @maxItems 100
   * @default []
   */
  participants?: string[];
  /** Автор действия; по умолчанию автор интерфейса */
  actor?: string;
  /**
   * Ключ повтора; тот же запрос возвращает первоначальную квитанцию
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
}

export interface UpdatePlan {
  /** Автор действия; по умолчанию автор интерфейса */
  actor?: string;
  /**
   * Ключ повтора; тот же запрос возвращает первоначальную квитанцию
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /**
   * Прочитанная ревизия плана или релиза
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  ifRevision: number;
  /** Однострочное название плана */
  title?: string;
  /** Новое краткое описание обычным текстом */
  summary?: string;
  /** Новая цель в Markdown */
  goal?: string;
  /** Новое обоснование в Markdown */
  rationale?: string;
  /** Новые границы в Markdown */
  boundaries?: string;
  /** Новый ожидаемый результат в Markdown */
  expectedResult?: string;
  /**
   * Новый полный набор областей; пустой массив очищает его
   * @maxItems 100
   */
  scope?: {
    /** Вид области воздействия; не является целью реализации задачи */
    kind: UpdatePlanKindEnum;
    /**
     * Постоянный ID записи выбранного проекта
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    id: string;
  }[];
  /**
   * Новый полный набор участников
   * @maxItems 100
   */
  participants?: string[];
}

export interface TransitionPlan {
  /** Автор действия; по умолчанию автор интерфейса */
  actor?: string;
  /**
   * Ключ повтора; тот же запрос возвращает первоначальную квитанцию
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /**
   * Прочитанная ревизия плана или релиза
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  ifRevision: number;
  /** Начать, завершить либо отменить план */
  action: TransitionPlanActionEnum;
  /**
   * Итог завершения или причина отмены; обязателен для закрытия
   * @default ""
   */
  result?: string;
}

export interface ChangePlanStage {
  /** Автор действия; по умолчанию автор интерфейса */
  actor?: string;
  /**
   * Ключ повтора; тот же запрос возвращает первоначальную квитанцию
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /**
   * Ревизия плана, защищающая весь состав этапов
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  ifRevision: number;
  /** Создать, изменить, удалить пустой этап или переместить */
  action: ChangePlanStageActionEnum;
  /**
   * Ключ или ID этапа; обязателен кроме создания
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  stage?: string;
  /** Полное содержание этапа для создания или изменения */
  fields?: {
    /** Однострочное название этапа */
    title: string;
    /**
     * Краткое описание этапа обычным текстом
     * @default ""
     */
    summary?: string;
    /**
     * Ожидаемый результат этапа в Markdown
     * @default ""
     */
    outcome?: string;
    /**
     * Условия завершения в Markdown; не исполняемая формула
     * @default ""
     */
    completionConditions?: string;
  };
  /** ID следующего этапа; null — конец полного списка */
  before?: string | null;
  /** Переместить на одну позицию полного списка; взаимоисключается с before */
  direction?: ChangePlanStageDirectionEnum;
}

export interface ChangePlanTasks {
  /** Автор действия; по умолчанию автор интерфейса */
  actor?: string;
  /**
   * Ключ повтора; тот же запрос возвращает первоначальную квитанцию
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /**
   * Прочитанная ревизия плана или релиза
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  ifRevision: number;
  /**
   * Ключ или ID этапа выбранного плана
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  stage: string;
  /**
   * Задачи для включения, ключи или ID, максимум 2000; остальные сохраняются
   * @maxItems 2000
   * @default []
   */
  add?: string[];
  /**
   * Задачи для исключения, ключи или ID, максимум 2000
   * @maxItems 2000
   * @default []
   */
  remove?: string[];
}

export interface TransferPlanTask {
  /** Автор действия; по умолчанию автор интерфейса */
  actor?: string;
  /**
   * Ключ повтора; тот же запрос возвращает первоначальную квитанцию
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /**
   * Ревизия исходного плана
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  ifRevision: number;
  /**
   * Переносимая задача, ключ или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  task: string;
  /**
   * Целевой этап, ключ или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  targetStage: string;
  /**
   * Ревизия целевого плана
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  targetRevision: number;
  /**
   * Причина явного переноса в Markdown
   * @minLength 1
   */
  reason: string;
}

export interface PlanningSaved {
  /**
   * Постоянный ID записи выбранного проекта
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
   */
  id: string;
  /** Ключ на момент операции */
  key: string;
  /**
   * Прочитанная ревизия плана или релиза
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  revision: number;
  /** Выполненное предметное действие */
  action: string;
  /**
   * Ключ безопасного повтора операции
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /**
   * ID затронутого этапа
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
   */
  stageId?: string;
  /**
   * Новая ревизия целевого плана после переноса
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  targetRevision?: number;
}

export interface ReleasesQuery {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы: 12 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 12
   */
  limit?: number;
  /**
   * Версия первой страницы; обязательна для продолжения
   * @maxLength 128
   */
  version?: string;
  /**
   * Поиск по ключу, названию, версии и описанию
   * @maxLength 1024
   */
  q?: string;
  /** Собственное состояние: запланирован, выпущен или отменён */
  status?: ReleasesQueryStatusEnum;
}

export interface ReleasesPage {
  /** Записи текущей страницы */
  items: {
    /**
     * Однострочное название релиза
     * @minLength 1
     */
    title: string;
    /**
     * Пользовательское обозначение выпуска; не ревизия записи
     * @minLength 1
     */
    version: string;
    /**
     * Краткое описание обычным многострочным текстом
     * @default ""
     */
    summary: string;
    /**
     * Полное описание выпуска в Markdown
     * @default ""
     */
    description: string;
    /**
     * Планы целиком по постоянным ID, максимум 200
     * @maxItems 200
     * @minItems 1
     */
    planIds: string[];
    /**
     * Плановая дата YYYY-MM-DD либо пустая строка
     * @default ""
     */
    plannedFor: "" | string;
    /** Самостоятельный релиз */
    kind: "release";
    /**
     * Постоянный ID записи выбранного проекта
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    projectId: string;
    /** Собственное состояние: запланирован, выпущен или отменён */
    status: ReleasesPageStatusEnum;
    /** Фактическая дата фиксации выпуска */
    releasedAt: string | null;
    /** Автор фиксации выпуска */
    releasedBy: string | null;
    /** Постоянный ID самодостаточного снимка */
    snapshotId: string | null;
    /**
     * Постоянный ID записи выбранного проекта
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    id: string;
    /** Читаемый ключ записи */
    key: string;
    /**
     * Ревизия записи
     * @exclusiveMin 0
     * @max 9007199254740991
     */
    revision: number;
    /**
     * Момент времени в UTC
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    createdAt: string;
    /**
     * Момент времени в UTC
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    updatedAt: string;
    /**
     * Автор операции
     * @maxLength 128
     */
    createdBy: string;
    /**
     * Автор операции
     * @maxLength 128
     */
    updatedBy: string;
    readiness: {
      /**
       * Все выбранные планы
       * @min 0
       * @max 9007199254740991
       */
      total: number;
      /**
       * Завершённые и фактически готовые планы
       * @min 0
       * @max 9007199254740991
       */
      ready: number;
      /**
       * Недоступные планы
       * @min 0
       * @max 9007199254740991
       */
      missing: number;
      /**
       * Процент готовых планов; пустой состав — 0
       * @min 0
       * @max 100
       */
      percent: number;
      /** Весь состав готов к явному выпуску */
      canRelease: boolean;
    };
  }[];
  /**
   * Полное число записей выбранной области
   * @min 0
   * @max 9007199254740991
   */
  total: number;
  /** Смещение следующей страницы либо null */
  nextOffset: number | null;
  /** Версия согласованного состава и фильтров */
  version: string;
  /** Полные счётчики состояний релизов без поиска, включая all */
  statusCounts: Partial<Record<string, number>>;
}

export interface ReleaseSummary {
  /**
   * Однострочное название релиза
   * @minLength 1
   */
  title: string;
  /**
   * Пользовательское обозначение выпуска; не ревизия записи
   * @minLength 1
   */
  version: string;
  /**
   * Краткое описание обычным многострочным текстом
   * @default ""
   */
  summary: string;
  /**
   * Полное описание выпуска в Markdown
   * @default ""
   */
  description: string;
  /**
   * Планы целиком по постоянным ID, максимум 200
   * @maxItems 200
   * @minItems 1
   */
  planIds: string[];
  /**
   * Плановая дата YYYY-MM-DD либо пустая строка
   * @default ""
   */
  plannedFor: "" | string;
  /** Самостоятельный релиз */
  kind: "release";
  /**
   * Постоянный ID записи выбранного проекта
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
   */
  projectId: string;
  /** Собственное состояние: запланирован, выпущен или отменён */
  status: ReleaseSummaryStatusEnum;
  /** Фактическая дата фиксации выпуска */
  releasedAt: string | null;
  /** Автор фиксации выпуска */
  releasedBy: string | null;
  /** Постоянный ID самодостаточного снимка */
  snapshotId: string | null;
  /**
   * Постоянный ID записи выбранного проекта
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
   */
  id: string;
  /** Читаемый ключ записи */
  key: string;
  /**
   * Ревизия записи
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  revision: number;
  /**
   * Момент времени в UTC
   * @format date-time
   * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
   */
  createdAt: string;
  /**
   * Момент времени в UTC
   * @format date-time
   * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
   */
  updatedAt: string;
  /**
   * Автор операции
   * @maxLength 128
   */
  createdBy: string;
  /**
   * Автор операции
   * @maxLength 128
   */
  updatedBy: string;
  readiness: {
    /**
     * Все выбранные планы
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /**
     * Завершённые и фактически готовые планы
     * @min 0
     * @max 9007199254740991
     */
    ready: number;
    /**
     * Недоступные планы
     * @min 0
     * @max 9007199254740991
     */
    missing: number;
    /**
     * Процент готовых планов; пустой состав — 0
     * @min 0
     * @max 100
     */
    percent: number;
    /** Весь состав готов к явному выпуску */
    canRelease: boolean;
  };
}

export interface SaveRelease {
  /** Однострочное название релиза */
  title: string;
  /** Пользовательское обозначение выпуска; не ревизия записи */
  version: string;
  /**
   * Краткое описание обычным многострочным текстом
   * @default ""
   */
  summary?: string;
  /**
   * Полное описание выпуска в Markdown
   * @default ""
   */
  description?: string;
  /**
   * Планы целиком по постоянным ID, максимум 200
   * @maxItems 200
   * @minItems 1
   */
  planIds: string[];
  /**
   * Плановая дата YYYY-MM-DD либо пустая строка
   * @default ""
   */
  plannedFor?: "" | string;
  /** Автор действия; по умолчанию автор интерфейса */
  actor?: string;
  /**
   * Ключ повтора; тот же запрос возвращает первоначальную квитанцию
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /**
   * Обязательная ревизия при изменении существующего релиза
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  ifRevision?: number;
  /**
   * Собственное состояние: запланирован, выпущен или отменён
   * @default "planned"
   */
  status?: SaveReleaseStatusEnum;
}

export interface UpdateRelease {
  /** Однострочное название релиза */
  title: string;
  /** Пользовательское обозначение выпуска; не ревизия записи */
  version: string;
  /**
   * Краткое описание обычным многострочным текстом
   * @default ""
   */
  summary?: string;
  /**
   * Полное описание выпуска в Markdown
   * @default ""
   */
  description?: string;
  /**
   * Планы целиком по постоянным ID, максимум 200
   * @maxItems 200
   * @minItems 1
   */
  planIds: string[];
  /**
   * Плановая дата YYYY-MM-DD либо пустая строка
   * @default ""
   */
  plannedFor?: "" | string;
  /** Автор действия; по умолчанию автор интерфейса */
  actor?: string;
  /**
   * Ключ повтора; тот же запрос возвращает первоначальную квитанцию
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /**
   * Прочитанная ревизия плана или релиза
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  ifRevision: number;
  /** Явно выбранное состояние; отсутствие не перепланирует отменённый релиз */
  status: UpdateReleaseStatusEnum;
}

export interface ReleaseAction {
  /** Автор действия; по умолчанию автор интерфейса */
  actor?: string;
  /**
   * Ключ повтора; тот же запрос возвращает первоначальную квитанцию
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /**
   * Прочитанная ревизия плана или релиза
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  ifRevision: number;
  /** Перепланировать, отменить либо явно зафиксировать выпуск */
  action: ReleaseActionActionEnum;
}

export interface ReleasePreview {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы: 12 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 12
   */
  limit?: number;
  /**
   * Версия первой страницы; обязательна для продолжения
   * @maxLength 128
   */
  version?: string;
  /**
   * Выбранные планы, ключи или ID; пустой выбор допускается для формы
   * @maxItems 200
   */
  plans: string[];
}

export interface ReleaseComposition {
  /** Записи текущей страницы */
  items: {
    /**
     * Постоянный ID записи выбранного проекта
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    id: string;
    /** План текущего или сохранённого состава; null при недоступности */
    plan: {
      /**
       * Однострочное название плана
       * @minLength 1
       */
      title: string;
      /**
       * Краткое описание обычным многострочным текстом
       * @default ""
       */
      summary: string;
      /**
       * Цель плана в Markdown
       * @default ""
       */
      goal: string;
      /**
       * Обоснование начала работы в Markdown
       * @default ""
       */
      rationale: string;
      /**
       * Границы изменения в Markdown
       * @default ""
       */
      boundaries: string;
      /**
       * Ожидаемый результат в Markdown
       * @default ""
       */
      expectedResult: string;
      /**
       * Области воздействия по постоянным адресам
       * @maxItems 100
       * @default []
       */
      scope: {
        /** Вид области воздействия; не является целью реализации задачи */
        kind: ReleaseCompositionKindEnum;
        /**
         * Постоянный ID записи выбранного проекта
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
         */
        id: string;
      }[];
      /**
       * Участники плана; не определяют права исполнения
       * @maxItems 100
       * @default []
       */
      participants: string[];
      /** План работ */
      kind: "work-plan";
      /**
       * Постоянный ID проекта-владельца
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      projectId: string;
      /** Состояние плана: черновик, в работе, завершён или отменён */
      status: ReleaseCompositionStatusEnum;
      /** Итог завершения либо причина отмены в Markdown */
      result: string;
      /** Фактическое время начала либо null */
      startedAt: string | null;
      /** Фактическое время завершения или отмены либо null */
      closedAt: string | null;
      /**
       * Постоянный ID записи выбранного проекта
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      id: string;
      /** Читаемый ключ записи */
      key: string;
      /**
       * Ревизия записи
       * @exclusiveMin 0
       * @max 9007199254740991
       */
      revision: number;
      /**
       * Момент времени в UTC
       * @format date-time
       * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
       */
      createdAt: string;
      /**
       * Момент времени в UTC
       * @format date-time
       * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
       */
      updatedAt: string;
      /**
       * Автор операции
       * @maxLength 128
       */
      createdBy: string;
      /**
       * Автор операции
       * @maxLength 128
       */
      updatedBy: string;
      /** Подписи выбранной области для человека */
      scopeLabels: {
        /** Постоянный адрес kind:ID */
        ref: string;
        /** Актуальные ключ и название области */
        label: string;
      }[];
      /**
       * Полное количество этапов
       * @min 0
       * @max 9007199254740991
       */
      stageCount: number;
      counts: {
        /**
         * Все уникальные задачи собственного состава
         * @min 0
         * @max 9007199254740991
         */
        total: number;
        /**
         * Фактически выполненные задачи с учётом обязательств
         * @min 0
         * @max 9007199254740991
         */
        completed: number;
        /**
         * Задачи в колонке in-progress
         * @min 0
         * @max 9007199254740991
         */
        active: number;
        /**
         * Задачи в колонке review
         * @min 0
         * @max 9007199254740991
         */
        review: number;
        /**
         * Задачи с невыполненными внешними или дочерними обязательствами
         * @min 0
         * @max 9007199254740991
         */
        blocked: number;
        /**
         * Процент фактического выполнения; пустой состав — 0
         * @min 0
         * @max 100
         */
        percent: number;
      };
      /** Весь непустой состав фактически выполнен */
      ready: boolean;
      /** Ближайший этап либо null */
      nextStage: {
        /**
         * Постоянный ID записи выбранного проекта
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
         */
        id: string;
        /** Название ближайшего незавершённого этапа */
        title: string;
      } | null;
      /**
       * До 12 индикаторов этапов для карточки; полный каталог читается отдельно
       * @maxItems 12
       */
      stagePreview: {
        /**
         * Постоянный ID записи выбранного проекта
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
         */
        id: string;
        /** Фактическое выполнение непустого этапа */
        completed: boolean;
      }[];
    } | null;
  }[];
  /**
   * Полное число записей выбранной области
   * @min 0
   * @max 9007199254740991
   */
  total: number;
  /** Смещение следующей страницы либо null */
  nextOffset: number | null;
  /** Версия согласованного состава и фильтров */
  version: string;
  readiness: {
    /**
     * Все выбранные планы
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /**
     * Завершённые и фактически готовые планы
     * @min 0
     * @max 9007199254740991
     */
    ready: number;
    /**
     * Недоступные планы
     * @min 0
     * @max 9007199254740991
     */
    missing: number;
    /**
     * Процент готовых планов; пустой состав — 0
     * @min 0
     * @max 100
     */
    percent: number;
    /** Весь состав готов к явному выпуску */
    canRelease: boolean;
  };
}

export interface ReleaseSnapshotPage {
  /** Записи текущей страницы */
  items: {
    /** Вид архивированной записи */
    kind: string;
    /**
     * Постоянный ID записи выбранного проекта
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    id: string;
    /** Ключ на момент выпуска */
    key: string;
    /**
     * Ревизия исходной записи
     * @min 0
     * @max 9007199254740991
     */
    revision: number;
    /** Название на момент выпуска */
    title: string;
    /** Основание включения: состав, обязательство, требование или материал */
    reason: string;
    /** Самодостаточное содержание записи в Markdown, включая критерии и реквизиты */
    content: string;
  }[];
  /**
   * Полное число записей выбранной области
   * @min 0
   * @max 9007199254740991
   */
  total: number;
  /** Смещение следующей страницы либо null */
  nextOffset: number | null;
  /** Версия согласованного состава и фильтров */
  version: string;
  /**
   * Постоянный ID записи выбранного проекта
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
   */
  snapshotId: string;
  /**
   * Постоянный ID записи выбранного проекта
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
   */
  releaseId: string;
  /**
   * Момент времени в UTC
   * @format date-time
   * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
   */
  capturedAt: string;
  /**
   * Автор операции
   * @maxLength 128
   */
  capturedBy: string;
}

export interface WorkPlanProgress {
  /** Адрес выбранной сущности */
  entity: {
    /** Вид предметного прогресса */
    kind: WorkPlanProgressKindEnum;
    /** Постоянный ID сущности */
    id: string;
    /** Читаемый ключ, если назначен */
    key: string | null;
    /** Однострочное название */
    title: string;
  };
  /** Фактически выполнен весь обязательный состав */
  completed: boolean;
  /** Версия согласованного чтения для продолжения этого запроса */
  version: string;
  /** Причины невыполнения или отсутствия работ */
  reasons: {
    /** Строки текущей страницы */
    items: {
      /** Машинный код причины */
      code: WorkPlanProgressCodeEnum;
      /** Понятное объяснение причины */
      message: string;
      /** Сущность, чей прогресс следует раскрыть */
      source: {
        /** Вид предметного прогресса */
        kind: WorkPlanProgressKindEnum1;
        /** Постоянный ID сущности */
        id: string;
        /** Читаемый ключ, если назначен */
        key: string | null;
        /** Однострочное название */
        title: string;
      };
      /** ID невыполненного критерия задачи */
      criterionId?: string;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
  /** Прогресс плана работ */
  kind: "work-plan";
  /** Состояние плана: черновик, в работе, завершён или отменён */
  status: WorkPlanProgressStatusEnum;
  counts: {
    /**
     * Все уникальные задачи собственного состава
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /**
     * Фактически выполненные задачи с учётом обязательств
     * @min 0
     * @max 9007199254740991
     */
    completed: number;
    /**
     * Задачи в колонке in-progress
     * @min 0
     * @max 9007199254740991
     */
    active: number;
    /**
     * Задачи в колонке review
     * @min 0
     * @max 9007199254740991
     */
    review: number;
    /**
     * Задачи с невыполненными внешними или дочерними обязательствами
     * @min 0
     * @max 9007199254740991
     */
    blocked: number;
    /**
     * Процент фактического выполнения; пустой состав — 0
     * @min 0
     * @max 100
     */
    percent: number;
  };
  /** Черновик имеет цель и непустой состав */
  canStart: boolean;
  /** Начатый план имеет фактически выполненный непустой состав */
  canComplete: boolean;
  /** Сохранённое завершение расходится с текущим выполнением задач */
  diverged: boolean;
  /** Страница этапов в предметном порядке */
  stages: {
    /** Строки текущей страницы */
    items: {
      /** ID этапа */
      id: string;
      /** Ключ этапа */
      key: string;
      /** Название этапа */
      title: string;
      counts: {
        /**
         * Все уникальные задачи собственного состава
         * @min 0
         * @max 9007199254740991
         */
        total: number;
        /**
         * Фактически выполненные задачи с учётом обязательств
         * @min 0
         * @max 9007199254740991
         */
        completed: number;
        /**
         * Задачи в колонке in-progress
         * @min 0
         * @max 9007199254740991
         */
        active: number;
        /**
         * Задачи в колонке review
         * @min 0
         * @max 9007199254740991
         */
        review: number;
        /**
         * Задачи с невыполненными внешними или дочерними обязательствами
         * @min 0
         * @max 9007199254740991
         */
        blocked: number;
        /**
         * Процент фактического выполнения; пустой состав — 0
         * @min 0
         * @max 100
         */
        percent: number;
      };
      /** Все задачи непустого этапа фактически выполнены */
      completed: boolean;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
  /** Прямые собственные задачи; вложенные обязательства раскрываются через прогресс задачи */
  tasks: {
    /** Строки текущей страницы */
    items: {
      /** Вид предметного прогресса */
      kind: WorkPlanProgressKindEnum2;
      /** Постоянный ID сущности */
      id: string;
      /** Читаемый ключ, если назначен */
      key: string | null;
      /** Однострочное название */
      title: string;
      /** Фактическое выполнение с учётом всех обязательств */
      completed: boolean;
      /** Сохранённая колонка; не заменяет фактическое выполнение */
      column: WorkPlanProgressColumnEnum;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
}

export interface ReleaseProgress {
  /** Адрес выбранной сущности */
  entity: {
    /** Вид предметного прогресса */
    kind: ReleaseProgressKindEnum;
    /** Постоянный ID сущности */
    id: string;
    /** Читаемый ключ, если назначен */
    key: string | null;
    /** Однострочное название */
    title: string;
  };
  /** Фактически выполнен весь обязательный состав */
  completed: boolean;
  /** Версия согласованного чтения для продолжения этого запроса */
  version: string;
  /** Причины невыполнения или отсутствия работ */
  reasons: {
    /** Строки текущей страницы */
    items: {
      /** Машинный код причины */
      code: ReleaseProgressCodeEnum;
      /** Понятное объяснение причины */
      message: string;
      /** Сущность, чей прогресс следует раскрыть */
      source: {
        /** Вид предметного прогресса */
        kind: ReleaseProgressKindEnum1;
        /** Постоянный ID сущности */
        id: string;
        /** Читаемый ключ, если назначен */
        key: string | null;
        /** Однострочное название */
        title: string;
      };
      /** ID невыполненного критерия задачи */
      criterionId?: string;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
  /** Готовность планового либо исторический результат состоявшегося выпуска */
  kind: "release";
  /** Собственное состояние: запланирован, выпущен или отменён */
  status: ReleaseProgressStatusEnum;
  readiness: {
    /**
     * Все выбранные планы
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /**
     * Завершённые и фактически готовые планы
     * @min 0
     * @max 9007199254740991
     */
    ready: number;
    /**
     * Недоступные планы
     * @min 0
     * @max 9007199254740991
     */
    missing: number;
    /**
     * Процент готовых планов; пустой состав — 0
     * @min 0
     * @max 100
     */
    percent: number;
    /** Весь состав готов к явному выпуску */
    canRelease: boolean;
  };
  /** Выбранные планы: текущие до выпуска, архивные после фиксации */
  plans: {
    /** Строки текущей страницы */
    items: {
      /** Вид предметного прогресса */
      kind: ReleaseProgressKindEnum2;
      /** Постоянный ID сущности */
      id: string;
      /** Читаемый ключ, если назначен */
      key: string | null;
      /** Однострочное название */
      title: string;
      /** Фактическое выполнение с учётом всех обязательств */
      completed: boolean;
      /** Состояние плана: черновик, в работе, завершён или отменён */
      status: ReleaseProgressStatusEnum1;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
  /** Результат прочитан из неизменяемого снимка выпуска */
  historical: boolean;
  /** ID снимка либо null */
  snapshotId: string | null;
}

export interface ProgressQuery {
  /**
   * Смещение каждого списка составляющих и причин
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset: number;
  /**
   * Размер страницы каждого списка: 20 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit: number;
  /** Версия первой страницы этого прогресса; обязательна при offset > 0 */
  version?: string;
  /**
   * Ключ, ID или kind:ID сущности выбранного проекта
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
}

export interface ProgressPageQuery {
  /**
   * Смещение каждого списка составляющих и причин
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset: number;
  /**
   * Размер страницы каждого списка: 20 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit: number;
  /** Версия первой страницы этого прогресса; обязательна при offset > 0 */
  version?: string;
}

export interface TaskProgress {
  /** Текущее участие в плане; null — задача свободна, поле отсутствует у старого сервера */
  planning?: {
    /**
     * Постоянный ID записи выбранного проекта
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    planId: string;
    /** Текущий ключ плана */
    planKey: string;
    /** Название плана */
    planTitle: string;
    /**
     * Постоянный ID записи выбранного проекта
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    stageId: string;
    /** Название этапа */
    stageTitle: string;
    /** Состояние плана: черновик, в работе, завершён или отменён */
    status: TaskProgressStatusEnum;
    /** Текущее участие в черновом или начатом плане */
    current: boolean;
  } | null;
  /** Адрес выбранной сущности */
  entity: {
    /** Вид предметного прогресса */
    kind: TaskProgressKindEnum;
    /** Постоянный ID сущности */
    id: string;
    /** Читаемый ключ, если назначен */
    key: string | null;
    /** Однострочное название */
    title: string;
  };
  /** Фактически выполнен весь обязательный состав */
  completed: boolean;
  /** Версия согласованного чтения для продолжения этого запроса */
  version: string;
  /** Причины невыполнения или отсутствия работ */
  reasons: {
    /** Строки текущей страницы */
    items: {
      /** Машинный код причины */
      code: TaskProgressCodeEnum;
      /** Понятное объяснение причины */
      message: string;
      /** Сущность, чей прогресс следует раскрыть */
      source: {
        /** Вид предметного прогресса */
        kind: TaskProgressKindEnum1;
        /** Постоянный ID сущности */
        id: string;
        /** Читаемый ключ, если назначен */
        key: string | null;
        /** Однострочное название */
        title: string;
      };
      /** ID невыполненного критерия задачи */
      criterionId?: string;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
  /** Прогресс задачи */
  kind: "task";
  /** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
  column: TaskProgressColumnEnum;
  /** Выполнены обязательства; колонка может ещё не быть done */
  canComplete: boolean;
  /** Полный состав критериев приёмки */
  acceptance: {
    /**
     * Полный уникальный состав, независимо от страницы
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /**
     * Фактически выполненная часть полного состава
     * @min 0
     * @max 9007199254740991
     */
    completed: number;
  };
  /** Критерии без полного Markdown */
  criteria: {
    /** Строки текущей страницы */
    items: {
      /** ID критерия */
      id: string;
      /** Название критерия */
      title: string;
      /** Отметка выполнения критерия */
      completed: boolean;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
  /** Прямые подзадачи */
  children: {
    /** Строки текущей страницы */
    items: {
      /** Вид предметного прогресса */
      kind: TaskProgressKindEnum2;
      /** Постоянный ID сущности */
      id: string;
      /** Читаемый ключ, если назначен */
      key: string | null;
      /** Однострочное название */
      title: string;
      /** Фактическое выполнение с учётом всех обязательств */
      completed: boolean;
      /** Сохранённая колонка; не заменяет фактическое выполнение */
      column: TaskProgressColumnEnum1;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
  /** Прямые обязательные зависимости любых досок проекта */
  dependencies: {
    /** Строки текущей страницы */
    items: {
      /** Вид предметного прогресса */
      kind: TaskProgressKindEnum3;
      /** Постоянный ID сущности */
      id: string;
      /** Читаемый ключ, если назначен */
      key: string | null;
      /** Однострочное название */
      title: string;
      /** Фактическое выполнение с учётом всех обязательств */
      completed: boolean;
      /** Сохранённая колонка; не заменяет фактическое выполнение */
      column: TaskProgressColumnEnum2;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
}

export interface ImplementationProgress {
  /** Адрес выбранной сущности */
  entity: {
    /** Вид предметного прогресса */
    kind: ImplementationProgressKindEnum;
    /** Постоянный ID сущности */
    id: string;
    /** Читаемый ключ, если назначен */
    key: string | null;
    /** Однострочное название */
    title: string;
  };
  /** Фактически выполнен весь обязательный состав */
  completed: boolean;
  /** Версия согласованного чтения для продолжения этого запроса */
  version: string;
  /** Причины невыполнения или отсутствия работ */
  reasons: {
    /** Строки текущей страницы */
    items: {
      /** Машинный код причины */
      code: ImplementationProgressCodeEnum;
      /** Понятное объяснение причины */
      message: string;
      /** Сущность, чей прогресс следует раскрыть */
      source: {
        /** Вид предметного прогресса */
        kind: ImplementationProgressKindEnum1;
        /** Постоянный ID сущности */
        id: string;
        /** Читаемый ключ, если назначен */
        key: string | null;
        /** Однострочное название */
        title: string;
      };
      /** ID невыполненного критерия задачи */
      criterionId?: string;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
  /** Уникальные задачи собственного продуктового состава; внешние обязательства не добавляются */
  counts: {
    /**
     * Полный уникальный состав, независимо от страницы
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /**
     * Фактически выполненная часть полного состава
     * @min 0
     * @max 9007199254740991
     */
    completed: number;
  };
  /** Прогресс реализации */
  kind: "implementation";
  /** Вклад в фичу либо сценарий */
  implementationKind: ImplementationProgressImplementationKindEnum;
  /** Приложение-владелец */
  application: {
    /** Вид предметного прогресса */
    kind: ImplementationProgressKindEnum2;
    /** Постоянный ID сущности */
    id: string;
    /** Читаемый ключ, если назначен */
    key: string | null;
    /** Однострочное название */
    title: string;
  };
  /** Проектная фича либо сценарий */
  target: {
    /** Вид предметного прогресса */
    kind: ImplementationProgressKindEnum3;
    /** Постоянный ID сущности */
    id: string;
    /** Читаемый ключ, если назначен */
    key: string | null;
    /** Однострочное название */
    title: string;
  };
  /** Участие реализации в текущем составе */
  active: boolean;
  /** Прямые собственные задачи; вложенные обязательства раскрываются через прогресс задачи */
  tasks: {
    /** Строки текущей страницы */
    items: {
      /** Вид предметного прогресса */
      kind: ImplementationProgressKindEnum4;
      /** Постоянный ID сущности */
      id: string;
      /** Читаемый ключ, если назначен */
      key: string | null;
      /** Однострочное название */
      title: string;
      /** Фактическое выполнение с учётом всех обязательств */
      completed: boolean;
      /** Сохранённая колонка; не заменяет фактическое выполнение */
      column: ImplementationProgressColumnEnum;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
  /** Для FI — активные SI той же фичи и приложения; для SI — пустой список */
  implementations: {
    /** Строки текущей страницы */
    items: {
      /** Вид предметного прогресса */
      kind: ImplementationProgressKindEnum5;
      /** Постоянный ID сущности */
      id: string;
      /** Читаемый ключ, если назначен */
      key: string | null;
      /** Однострочное название */
      title: string;
      /** Фактическое выполнение с учётом всех обязательств */
      completed: boolean;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
}

export interface ScenarioProgress {
  /** Адрес выбранной сущности */
  entity: {
    /** Вид предметного прогресса */
    kind: ScenarioProgressKindEnum;
    /** Постоянный ID сущности */
    id: string;
    /** Читаемый ключ, если назначен */
    key: string | null;
    /** Однострочное название */
    title: string;
  };
  /** Фактически выполнен весь обязательный состав */
  completed: boolean;
  /** Версия согласованного чтения для продолжения этого запроса */
  version: string;
  /** Причины невыполнения или отсутствия работ */
  reasons: {
    /** Строки текущей страницы */
    items: {
      /** Машинный код причины */
      code: ScenarioProgressCodeEnum;
      /** Понятное объяснение причины */
      message: string;
      /** Сущность, чей прогресс следует раскрыть */
      source: {
        /** Вид предметного прогресса */
        kind: ScenarioProgressKindEnum1;
        /** Постоянный ID сущности */
        id: string;
        /** Читаемый ключ, если назначен */
        key: string | null;
        /** Однострочное название */
        title: string;
      };
      /** ID невыполненного критерия задачи */
      criterionId?: string;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
  /** Уникальные задачи собственного продуктового состава; внешние обязательства не добавляются */
  counts: {
    /**
     * Полный уникальный состав, независимо от страницы
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /**
     * Фактически выполненная часть полного состава
     * @min 0
     * @max 9007199254740991
     */
    completed: number;
  };
  /** Прогресс сценария */
  kind: "scenario";
  /** Прямые собственные задачи; вложенные обязательства раскрываются через прогресс задачи */
  tasks: {
    /** Строки текущей страницы */
    items: {
      /** Вид предметного прогресса */
      kind: ScenarioProgressKindEnum2;
      /** Постоянный ID сущности */
      id: string;
      /** Читаемый ключ, если назначен */
      key: string | null;
      /** Однострочное название */
      title: string;
      /** Фактическое выполнение с учётом всех обязательств */
      completed: boolean;
      /** Сохранённая колонка; не заменяет фактическое выполнение */
      column: ScenarioProgressColumnEnum;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
  /** Обязательные активные реализации; прогресс каждой читается отдельно */
  implementations: {
    /** Строки текущей страницы */
    items: {
      /** Вид предметного прогресса */
      kind: ScenarioProgressKindEnum3;
      /** Постоянный ID сущности */
      id: string;
      /** Читаемый ключ, если назначен */
      key: string | null;
      /** Однострочное название */
      title: string;
      /** Фактическое выполнение с учётом всех обязательств */
      completed: boolean;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
}

export interface FeatureProgress {
  /** Адрес выбранной сущности */
  entity: {
    /** Вид предметного прогресса */
    kind: FeatureProgressKindEnum;
    /** Постоянный ID сущности */
    id: string;
    /** Читаемый ключ, если назначен */
    key: string | null;
    /** Однострочное название */
    title: string;
  };
  /** Фактически выполнен весь обязательный состав */
  completed: boolean;
  /** Версия согласованного чтения для продолжения этого запроса */
  version: string;
  /** Причины невыполнения или отсутствия работ */
  reasons: {
    /** Строки текущей страницы */
    items: {
      /** Машинный код причины */
      code: FeatureProgressCodeEnum;
      /** Понятное объяснение причины */
      message: string;
      /** Сущность, чей прогресс следует раскрыть */
      source: {
        /** Вид предметного прогресса */
        kind: FeatureProgressKindEnum1;
        /** Постоянный ID сущности */
        id: string;
        /** Читаемый ключ, если назначен */
        key: string | null;
        /** Однострочное название */
        title: string;
      };
      /** ID невыполненного критерия задачи */
      criterionId?: string;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
  /** Уникальные задачи собственного продуктового состава; внешние обязательства не добавляются */
  counts: {
    /**
     * Полный уникальный состав, независимо от страницы
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /**
     * Фактически выполненная часть полного состава
     * @min 0
     * @max 9007199254740991
     */
    completed: number;
  };
  /** Прогресс фичи */
  kind: "feature";
  /** Прямые собственные задачи; вложенные обязательства раскрываются через прогресс задачи */
  tasks: {
    /** Строки текущей страницы */
    items: {
      /** Вид предметного прогресса */
      kind: FeatureProgressKindEnum2;
      /** Постоянный ID сущности */
      id: string;
      /** Читаемый ключ, если назначен */
      key: string | null;
      /** Однострочное название */
      title: string;
      /** Фактическое выполнение с учётом всех обязательств */
      completed: boolean;
      /** Сохранённая колонка; не заменяет фактическое выполнение */
      column: FeatureProgressColumnEnum;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
  /** Активные FI фичи */
  implementations: {
    /** Строки текущей страницы */
    items: {
      /** Вид предметного прогресса */
      kind: FeatureProgressKindEnum3;
      /** Постоянный ID сущности */
      id: string;
      /** Читаемый ключ, если назначен */
      key: string | null;
      /** Однострочное название */
      title: string;
      /** Фактическое выполнение с учётом всех обязательств */
      completed: boolean;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
  /** Все проектные сценарии фичи, включая не выбранные приложениями */
  scenarios: {
    /** Строки текущей страницы */
    items: {
      /** Вид предметного прогресса */
      kind: FeatureProgressKindEnum4;
      /** Постоянный ID сущности */
      id: string;
      /** Читаемый ключ, если назначен */
      key: string | null;
      /** Однострочное название */
      title: string;
      /** Фактическое выполнение с учётом всех обязательств */
      completed: boolean;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
}

export interface ApplicationProgress {
  /** Адрес выбранной сущности */
  entity: {
    /** Вид предметного прогресса */
    kind: ApplicationProgressKindEnum;
    /** Постоянный ID сущности */
    id: string;
    /** Читаемый ключ, если назначен */
    key: string | null;
    /** Однострочное название */
    title: string;
  };
  /** Фактически выполнен весь обязательный состав */
  completed: boolean;
  /** Версия согласованного чтения для продолжения этого запроса */
  version: string;
  /** Причины невыполнения или отсутствия работ */
  reasons: {
    /** Строки текущей страницы */
    items: {
      /** Машинный код причины */
      code: ApplicationProgressCodeEnum;
      /** Понятное объяснение причины */
      message: string;
      /** Сущность, чей прогресс следует раскрыть */
      source: {
        /** Вид предметного прогресса */
        kind: ApplicationProgressKindEnum1;
        /** Постоянный ID сущности */
        id: string;
        /** Читаемый ключ, если назначен */
        key: string | null;
        /** Однострочное название */
        title: string;
      };
      /** ID невыполненного критерия задачи */
      criterionId?: string;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
  /** Уникальные задачи собственного продуктового состава; внешние обязательства не добавляются */
  counts: {
    /**
     * Полный уникальный состав, независимо от страницы
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /**
     * Фактически выполненная часть полного состава
     * @min 0
     * @max 9007199254740991
     */
    completed: number;
  };
  /** Прогресс заявленного состава приложения, не подтверждение поставки */
  kind: "application";
  /** Обязательные активные реализации; прогресс каждой читается отдельно */
  implementations: {
    /** Строки текущей страницы */
    items: {
      /** Вид предметного прогресса */
      kind: ApplicationProgressKindEnum2;
      /** Постоянный ID сущности */
      id: string;
      /** Читаемый ключ, если назначен */
      key: string | null;
      /** Однострочное название */
      title: string;
      /** Фактическое выполнение с учётом всех обязательств */
      completed: boolean;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
  /** Задачи досок приложения с продуктовыми целями */
  businessTasks: {
    /**
     * Полный уникальный состав, независимо от страницы
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /**
     * Фактически выполненная часть полного состава
     * @min 0
     * @max 9007199254740991
     */
    completed: number;
  };
  /** Все задачи досок приложения, включая работу без продуктовой цели */
  allTasks: {
    /**
     * Полный уникальный состав, независимо от страницы
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /**
     * Фактически выполненная часть полного состава
     * @min 0
     * @max 9007199254740991
     */
    completed: number;
  };
}

export interface ProductProgress {
  /** Адрес выбранной сущности */
  entity: {
    /** Вид предметного прогресса */
    kind: ProductProgressKindEnum;
    /** Постоянный ID сущности */
    id: string;
    /** Читаемый ключ, если назначен */
    key: string | null;
    /** Однострочное название */
    title: string;
  };
  /** Фактически выполнен весь обязательный состав */
  completed: boolean;
  /** Версия согласованного чтения для продолжения этого запроса */
  version: string;
  /** Причины невыполнения или отсутствия работ */
  reasons: {
    /** Строки текущей страницы */
    items: {
      /** Машинный код причины */
      code: ProductProgressCodeEnum;
      /** Понятное объяснение причины */
      message: string;
      /** Сущность, чей прогресс следует раскрыть */
      source: {
        /** Вид предметного прогресса */
        kind: ProductProgressKindEnum1;
        /** Постоянный ID сущности */
        id: string;
        /** Читаемый ключ, если назначен */
        key: string | null;
        /** Однострочное название */
        title: string;
      };
      /** ID невыполненного критерия задачи */
      criterionId?: string;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
  /** Уникальные задачи собственного продуктового состава; внешние обязательства не добавляются */
  counts: {
    /**
     * Полный уникальный состав, независимо от страницы
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /**
     * Фактически выполненная часть полного состава
     * @min 0
     * @max 9007199254740991
     */
    completed: number;
  };
  /** Прогресс продукта по его фичам */
  kind: "product";
  /** Фичи продукта с адресами и готовностью */
  features: {
    /** Строки текущей страницы */
    items: {
      /** Вид предметного прогресса */
      kind: ProductProgressKindEnum2;
      /** Постоянный ID сущности */
      id: string;
      /** Читаемый ключ, если назначен */
      key: string | null;
      /** Однострочное название */
      title: string;
      /** Фактическое выполнение с учётом всех обязательств */
      completed: boolean;
    }[];
    /**
     * Полное число строк списка
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /** Следующее смещение или null */
    nextOffset: number | null;
  };
}

export interface EntityDeletionQuery {
  /**
   * Ключ или постоянный адрес удаляемой сущности
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /** Вид удаляемой сущности; проект, паспорт и системные доски не удаляются */
  kind: EntityDeletionQueryKindEnum;
}

export interface EntityDeletionPreview {
  /** Выбранная сущность */
  target: {
    /** Постоянный адрес в выбранном проекте */
    ref: {
      /** Вид основной сущности */
      kind: EntityDeletionPreviewKindEnum;
      /**
       * Постоянный ID; внутренние отношения сохраняют только этот адрес
       * @minLength 1
       * @maxLength 128
       */
      id: string;
    };
    /** Текущий читаемый ключ для человека и агента */
    key: string;
    /** Однострочное название */
    title: string;
    /** Краткое обычное описание без полного Markdown */
    summary: string;
    /**
     * Ревизия записи для следующего изменения
     * @min 0
     * @max 9007199254740991
     */
    revision: number;
    /** Текущее предметное состояние; null, когда неприменимо */
    status: string | null;
    /** Активная запись; снятая реализация сохраняет адрес */
    active: boolean;
    /** Приложение, доска или родитель для различения одинаковых названий */
    context?: string;
    /** Компактные свойства документа без полного Markdown */
    document?: {
      /** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
      kind: EntityDeletionPreviewKindEnum1;
      /** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
      status: EntityDeletionPreviewStatusEnum;
      /** Эффективный раздел; удалённый раздел отображается как null */
      sectionId: string | null;
      /** Закрепление в проекте */
      pinned: boolean;
      /**
       * Момент времени в UTC
       * @format date-time
       * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
       */
      updatedAt: string;
      /**
       * Количество прямых отношений документа
       * @min 0
       * @max 9007199254740991
       */
      linkCount: number;
      /** Фрагмент совпадения полнотекстового поиска */
      excerpt?: string;
    };
  };
  /** Версия состава удаления и связей для подтверждения */
  version: string;
  /**
   * Полный состав удаления, не более 1000 сущностей
   * @maxItems 1000
   */
  deleted: {
    /** Постоянный адрес в выбранном проекте */
    ref: {
      /** Вид основной сущности */
      kind: EntityDeletionPreviewKindEnum2;
      /**
       * Постоянный ID; внутренние отношения сохраняют только этот адрес
       * @minLength 1
       * @maxLength 128
       */
      id: string;
    };
    /** Текущий читаемый ключ для человека и агента */
    key: string;
    /** Однострочное название */
    title: string;
    /** Краткое обычное описание без полного Markdown */
    summary: string;
    /**
     * Ревизия записи для следующего изменения
     * @min 0
     * @max 9007199254740991
     */
    revision: number;
    /** Текущее предметное состояние; null, когда неприменимо */
    status: string | null;
    /** Активная запись; снятая реализация сохраняет адрес */
    active: boolean;
    /** Приложение, доска или родитель для различения одинаковых названий */
    context?: string;
    /** Компактные свойства документа без полного Markdown */
    document?: {
      /** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
      kind: EntityDeletionPreviewKindEnum3;
      /** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
      status: EntityDeletionPreviewStatusEnum1;
      /** Эффективный раздел; удалённый раздел отображается как null */
      sectionId: string | null;
      /** Закрепление в проекте */
      pinned: boolean;
      /**
       * Момент времени в UTC
       * @format date-time
       * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
       */
      updatedAt: string;
      /**
       * Количество прямых отношений документа
       * @min 0
       * @max 9007199254740991
       */
      linkCount: number;
      /** Фрагмент совпадения полнотекстового поиска */
      excerpt?: string;
    };
  }[];
  /**
   * Сохраняемые сущности, у которых снимаются ссылки
   * @maxItems 1000
   */
  detached: {
    /** Постоянный адрес в выбранном проекте */
    ref: {
      /** Вид основной сущности */
      kind: EntityDeletionPreviewKindEnum4;
      /**
       * Постоянный ID; внутренние отношения сохраняют только этот адрес
       * @minLength 1
       * @maxLength 128
       */
      id: string;
    };
    /** Текущий читаемый ключ для человека и агента */
    key: string;
    /** Однострочное название */
    title: string;
    /** Краткое обычное описание без полного Markdown */
    summary: string;
    /**
     * Ревизия записи для следующего изменения
     * @min 0
     * @max 9007199254740991
     */
    revision: number;
    /** Текущее предметное состояние; null, когда неприменимо */
    status: string | null;
    /** Активная запись; снятая реализация сохраняет адрес */
    active: boolean;
    /** Приложение, доска или родитель для различения одинаковых названий */
    context?: string;
    /** Компактные свойства документа без полного Markdown */
    document?: {
      /** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
      kind: EntityDeletionPreviewKindEnum5;
      /** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
      status: EntityDeletionPreviewStatusEnum2;
      /** Эффективный раздел; удалённый раздел отображается как null */
      sectionId: string | null;
      /** Закрепление в проекте */
      pinned: boolean;
      /**
       * Момент времени в UTC
       * @format date-time
       * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
       */
      updatedAt: string;
      /**
       * Количество прямых отношений документа
       * @min 0
       * @max 9007199254740991
       */
      linkCount: number;
      /** Фрагмент совпадения полнотекстового поиска */
      excerpt?: string;
    };
  }[];
  /**
   * Количество отзываемых активных связей графа
   * @min 0
   * @max 9007199254740991
   */
  relations: number;
}

export interface DeleteEntity {
  /**
   * Ключ или постоянный адрес удаляемой сущности
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /** Вид удаляемой сущности; проект, паспорт и системные доски не удаляются */
  kind: DeleteEntityKindEnum;
  /**
   * Версия просмотренного состава удаления
   * @minLength 1
   */
  ifVersion: string;
  /**
   * Ключ безопасного повтора операции
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /**
   * Автор удаления; по умолчанию автор сервера
   * @maxLength 128
   */
  actor?: string;
}

export interface EntityDeleted {
  /** Каскадное удаление завершено */
  action: "delete";
  ref: {
    /** Вид основной сущности */
    kind: EntityDeletedKindEnum;
    /**
     * Постоянный ID; внутренние отношения сохраняют только этот адрес
     * @minLength 1
     * @maxLength 128
     */
    id: string;
  };
  /**
   * Ключ безопасного повтора операции
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /**
   * Количество удалённых сущностей
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  deleted: number;
  /**
   * Количество сохранённых сущностей со снятыми ссылками
   * @min 0
   * @max 9007199254740991
   */
  detached: number;
  /**
   * Количество отозванных связей графа
   * @min 0
   * @max 9007199254740991
   */
  relations: number;
}

export interface TaskActivityQuery {
  /**
   * Размер страницы, по умолчанию 20, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit: number;
  /**
   * Непрозрачный курсор следующей страницы; сохраняйте фильтры
   * @maxLength 2048
   */
  cursor?: string;
  /**
   * Только события после известного последовательного номера
   * @min 0
   * @max 9007199254740991
   */
  after?: number;
  /**
   * Точное имя автора для фильтрации
   * @maxLength 128
   */
  actor?: string;
  /**
   * Тип действия, например update или comment-publish
   * @minLength 1
   */
  action?: string;
}

export interface TaskActivityPage {
  items: {
    /**
     * Постоянный номер записи в ленте задачи
     * @pattern ^[1-9]\d{0,14}$
     */
    id: string;
    /** Постоянный ID задачи */
    taskId: string;
    /**
     * Последовательный номер в ленте задачи
     * @exclusiveMin 0
     * @max 9007199254740991
     */
    sequence: number;
    /**
     * Момент времени в UTC
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    at: string;
    /**
     * Автор операции
     * @maxLength 128
     */
    actor: string;
    /** Роль автора: оператор, оркестратор или воркер */
    actorRole?: TaskActivityPageActorRoleEnum;
    /** Сохранённое действие */
    action: string;
    /** Читаемое название события или заголовок сообщения */
    title: string;
    /** Общий идентификатор составной операции */
    operationId: string;
    /**
     * Ревизия содержания задачи на момент события
     * @exclusiveMin 0
     * @max 9007199254740991
     */
    revision: number;
    /** У старой записи отсутствуют подробности изменений */
    legacy: boolean;
    /** Названия изменённых полей без полных значений */
    fields: string[];
  }[];
  /** Курсор продолжения или null */
  nextCursor: string | null;
  /**
   * Верхняя граница снимка; новые события читаются через after
   * @min 0
   * @max 9007199254740991
   */
  snapshot: number;
}

export interface TaskHistoryEvent {
  /**
   * Постоянный номер записи в ленте задачи
   * @pattern ^[1-9]\d{0,14}$
   */
  id: string;
  /** Постоянный ID задачи */
  taskId: string;
  /**
   * Последовательный номер в ленте задачи
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  sequence: number;
  /**
   * Момент времени в UTC
   * @format date-time
   * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
   */
  at: string;
  /**
   * Автор операции
   * @maxLength 128
   */
  actor: string;
  /** Роль автора: оператор, оркестратор или воркер */
  actorRole?: TaskHistoryEventActorRoleEnum;
  /** Сохранённое действие */
  action: string;
  /** Читаемое название события или заголовок сообщения */
  title: string;
  /** Общий идентификатор составной операции */
  operationId: string;
  /**
   * Ревизия содержания задачи на момент события
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  revision: number;
  /** У старой записи отсутствуют подробности изменений */
  legacy: boolean;
  /** Названия изменённых полей без полных значений */
  fields: string[];
  /** Изменения полей; Markdown отмечается фактом изменения без копий текста */
  changes: {
    /** Постоянный адрес изменённого поля */
    field: string;
    /** Русское название изменения */
    label: string;
    /** Обычный текст или Markdown */
    format: TaskHistoryEventFormatEnum;
    /** Прежнее значение; null при добавлении или сокращённом содержании */
    before: string | null;
    /** Новое значение; null при удалении или сокращённом содержании */
    after: string | null;
    /** Сохранён факт изменения текста; прежняя и новая редакции не записываются в историю */
    contentOmitted?: true;
  }[];
  /** Полный Markdown опубликованного сообщения */
  description?: string;
}

export interface TaskCommentSaved {
  /** Постоянный ID задачи */
  id: string;
  /**
   * Постоянный номер записи в ленте задачи
   * @pattern ^[1-9]\d{0,14}$
   */
  commentId: string;
  /** Сообщение опубликовано */
  action: "comment-publish";
  /**
   * Ревизия ленты, не содержания задачи
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  revision: number;
  /**
   * Ключ безопасного повтора операции
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
}

export interface PublishTaskComment {
  /**
   * Обязательный однострочный заголовок сообщения
   * @minLength 1
   */
  title: string;
  /** Полное сообщение в Markdown, до 256 КиБ */
  description: string;
  /**
   * Имя автора; Web передаёт Оператор, агент задаёт своё имя
   * @maxLength 128
   */
  actor: string;
  /** Роль автора: оператор, оркестратор или воркер */
  actorRole: PublishTaskCommentActorRoleEnum;
  /**
   * Ключ безопасного повтора операции
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
}

export interface EntityPageQuery {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы от 1 до 100
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /**
   * Версия первой страницы; изменение требует начать чтение заново
   * @maxLength 128
   */
  version?: string;
}

export interface EntityTypeQuery {
  /** Вид основной сущности */
  kind: EntityTypeQueryKindEnum;
}

export interface EntityTypes {
  items: {
    /** Вид основной сущности */
    kind: EntityTypesKindEnum;
    /** Название вида по-русски */
    title: string;
    /** Для чего нужна эта сущность */
    description: string;
    /**
     * Версия переносимого определения
     * @exclusiveMin 0
     * @max 9007199254740991
     */
    contractVersion: number;
    /** Правила назначения ключа; фактические префиксы доступны в key-spaces */
    keyPolicy: string;
    /** Поддерживаемые фильтры записей этого вида */
    filters: string[];
    /** Поддерживаемые действия; предметные условия проверяются Core */
    actions: string[];
  }[];
  /**
   * Полное число результатов выбранной области
   * @min 0
   * @max 9007199254740991
   */
  total: number;
  /** Смещение продолжения; null в конце */
  nextOffset: number | null;
  /** Версия согласованного снимка */
  version: string;
}

export interface EntityTypeDetail {
  /** Вид основной сущности */
  kind: EntityTypeDetailKindEnum;
  /** Название вида по-русски */
  title: string;
  /** Для чего нужна эта сущность */
  description: string;
  /**
   * Версия переносимого определения
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  contractVersion: number;
  /** Правила назначения ключа; фактические префиксы доступны в key-spaces */
  keyPolicy: string;
  /** Поддерживаемые фильтры записей этого вида */
  filters: string[];
  /** Поддерживаемые действия; предметные условия проверяются Core */
  actions: string[];
  /** JSON Schema полных данных сущности */
  schema: Partial<Record<string, any>>;
  /** JSON Schema создания; null, если создание выполняет другой сценарий */
  createSchema: Partial<Record<string, any>> | null;
  /** JSON Schema изменения; null для вычисляемого содержания */
  updateSchema: Partial<Record<string, any>> | null;
}

export interface EntitiesQuery {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы от 1 до 100
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /**
   * Версия первой страницы; изменение требует начать чтение заново
   * @maxLength 128
   */
  version?: string;
  /** Вид основной сущности */
  kind?: EntitiesQueryKindEnum;
  /**
   * Поиск по ключам, ID, названию и краткому описанию
   * @maxLength 4096
   */
  q?: string;
  /**
   * Адресное чтение до 100 кратких карточек по ключам или ID
   * @maxItems 100
   */
  refs?: string[];
  /**
   * Доска задач: ключ или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  board?: string;
  /**
   * Приложение доски, реализации или задачи: ключ или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  application?: string;
  /**
   * Фича сценария или реализации: ключ или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  feature?: string;
  /**
   * Сценарий реализации: ключ или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  scenario?: string;
  /**
   * Явная цель задачи, реализации или документа: ключ или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  target?: string;
  /**
   * Родитель задачи: ключ или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  parent?: string;
  /**
   * Предметное состояние выбранного вида
   * @maxLength 128
   */
  status?: string;
  /** Активность реализации; прежние ссылки доступны без фильтра */
  active?: EntitiesQueryActiveEnum;
  /**
   * Раздел документов; none — без раздела
   * @maxLength 64
   */
  section?: string;
  /** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
  documentKind?: EntitiesQueryDocumentKindEnum;
  /** Только закреплённые либо незакреплённые документы */
  pinned?: EntitiesQueryPinnedEnum;
  /** Включить только архив либо исключить архивные документы */
  archived?: EntitiesQueryArchivedEnum;
  /**
   * Сортировка по ключу, названию или последнему обновлению
   * @default "key"
   */
  sort?: EntitiesQuerySortEnum;
}

export interface EntitiesPage {
  /** Страница кратких карточек */
  items: {
    /** Постоянный адрес в выбранном проекте */
    ref: {
      /** Вид основной сущности */
      kind: EntitiesPageKindEnum;
      /**
       * Постоянный ID; внутренние отношения сохраняют только этот адрес
       * @minLength 1
       * @maxLength 128
       */
      id: string;
    };
    /** Текущий читаемый ключ для человека и агента */
    key: string;
    /** Однострочное название */
    title: string;
    /** Краткое обычное описание без полного Markdown */
    summary: string;
    /**
     * Ревизия записи для следующего изменения
     * @min 0
     * @max 9007199254740991
     */
    revision: number;
    /** Текущее предметное состояние; null, когда неприменимо */
    status: string | null;
    /** Активная запись; снятая реализация сохраняет адрес */
    active: boolean;
    /** Приложение, доска или родитель для различения одинаковых названий */
    context?: string;
    /** Компактные свойства документа без полного Markdown */
    document?: {
      /** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
      kind: EntitiesPageKindEnum1;
      /** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
      status: EntitiesPageStatusEnum;
      /** Эффективный раздел; удалённый раздел отображается как null */
      sectionId: string | null;
      /** Закрепление в проекте */
      pinned: boolean;
      /**
       * Момент времени в UTC
       * @format date-time
       * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
       */
      updatedAt: string;
      /**
       * Количество прямых отношений документа
       * @min 0
       * @max 9007199254740991
       */
      linkCount: number;
      /** Фрагмент совпадения полнотекстового поиска */
      excerpt?: string;
    };
  }[];
  /** Счётчики библиотеки без поисковых фильтров: all, draft, pinned, archived, none и section:ID */
  libraryCounts?: Partial<Record<string, number>>;
  /**
   * Полное число результатов выбранной области
   * @min 0
   * @max 9007199254740991
   */
  total: number;
  /** Смещение продолжения; null в конце */
  nextOffset: number | null;
  /** Версия согласованного снимка */
  version: string;
}

export interface EntityGetQuery {
  /**
   * Ключ или постоянный ID сущности; для уточнения вида допустим kind:ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /** Уточнение ожидаемого вида при разрешении адреса */
  kind?: EntityGetQueryKindEnum;
}

export interface EntitySummary {
  /** Постоянный адрес в выбранном проекте */
  ref: {
    /** Вид основной сущности */
    kind: EntitySummaryKindEnum;
    /**
     * Постоянный ID; внутренние отношения сохраняют только этот адрес
     * @minLength 1
     * @maxLength 128
     */
    id: string;
  };
  /** Текущий читаемый ключ для человека и агента */
  key: string;
  /** Однострочное название */
  title: string;
  /** Краткое обычное описание без полного Markdown */
  summary: string;
  /**
   * Ревизия записи для следующего изменения
   * @min 0
   * @max 9007199254740991
   */
  revision: number;
  /** Текущее предметное состояние; null, когда неприменимо */
  status: string | null;
  /** Активная запись; снятая реализация сохраняет адрес */
  active: boolean;
  /** Приложение, доска или родитель для различения одинаковых названий */
  context?: string;
  /** Компактные свойства документа без полного Markdown */
  document?: {
    /** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
    kind: EntitySummaryKindEnum1;
    /** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
    status: EntitySummaryStatusEnum;
    /** Эффективный раздел; удалённый раздел отображается как null */
    sectionId: string | null;
    /** Закрепление в проекте */
    pinned: boolean;
    /**
     * Момент времени в UTC
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    updatedAt: string;
    /**
     * Количество прямых отношений документа
     * @min 0
     * @max 9007199254740991
     */
    linkCount: number;
    /** Фрагмент совпадения полнотекстового поиска */
    excerpt?: string;
  };
}

export interface EntityDetail {
  /** Постоянный адрес в выбранном проекте */
  ref: {
    /** Вид основной сущности */
    kind: EntityDetailKindEnum;
    /**
     * Постоянный ID; внутренние отношения сохраняют только этот адрес
     * @minLength 1
     * @maxLength 128
     */
    id: string;
  };
  /** Текущий читаемый ключ для человека и агента */
  key: string;
  /** Однострочное название */
  title: string;
  /** Краткое обычное описание без полного Markdown */
  summary: string;
  /**
   * Ревизия записи для следующего изменения
   * @min 0
   * @max 9007199254740991
   */
  revision: number;
  /** Текущее предметное состояние; null, когда неприменимо */
  status: string | null;
  /** Активная запись; снятая реализация сохраняет адрес */
  active: boolean;
  /** Приложение, доска или родитель для различения одинаковых названий */
  context?: string;
  /** Компактные свойства документа без полного Markdown */
  document?: {
    /** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
    kind: EntityDetailKindEnum1;
    /** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
    status: EntityDetailStatusEnum;
    /** Эффективный раздел; удалённый раздел отображается как null */
    sectionId: string | null;
    /** Закрепление в проекте */
    pinned: boolean;
    /**
     * Момент времени в UTC
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    updatedAt: string;
    /**
     * Количество прямых отношений документа
     * @min 0
     * @max 9007199254740991
     */
    linkCount: number;
    /** Фрагмент совпадения полнотекстового поиска */
    excerpt?: string;
  };
  /** Полные типизированные данные; Markdown передаётся строками */
  data:
    | {
        /**
         * Однострочное название плана
         * @minLength 1
         */
        title: string;
        /**
         * Краткое описание обычным многострочным текстом
         * @default ""
         */
        summary: string;
        /**
         * Цель плана в Markdown
         * @default ""
         */
        goal: string;
        /**
         * Обоснование начала работы в Markdown
         * @default ""
         */
        rationale: string;
        /**
         * Границы изменения в Markdown
         * @default ""
         */
        boundaries: string;
        /**
         * Ожидаемый результат в Markdown
         * @default ""
         */
        expectedResult: string;
        /**
         * Области воздействия по постоянным адресам
         * @maxItems 100
         * @default []
         */
        scope: {
          /** Вид области воздействия; не является целью реализации задачи */
          kind: EntityDetailKindEnum2;
          /**
           * Постоянный ID записи выбранного проекта
           * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
           */
          id: string;
        }[];
        /**
         * Участники плана; не определяют права исполнения
         * @maxItems 100
         * @default []
         */
        participants: string[];
        /** План работ */
        kind: "work-plan";
        /**
         * Постоянный ID проекта-владельца
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
         */
        projectId: string;
        /** Состояние плана: черновик, в работе, завершён или отменён */
        status: EntityDetailStatusEnum1;
        /** Итог завершения либо причина отмены в Markdown */
        result: string;
        /** Фактическое время начала либо null */
        startedAt: string | null;
        /** Фактическое время завершения или отмены либо null */
        closedAt: string | null;
      }
    | {
        /**
         * Однострочное название этапа
         * @minLength 1
         */
        title: string;
        /**
         * Краткое описание этапа обычным текстом
         * @default ""
         */
        summary: string;
        /**
         * Ожидаемый результат этапа в Markdown
         * @default ""
         */
        outcome: string;
        /**
         * Условия завершения в Markdown; не исполняемая формула
         * @default ""
         */
        completionConditions: string;
        /** Этап плана работ */
        kind: "plan-stage";
        /**
         * Постоянный ID записи выбранного проекта
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
         */
        projectId: string;
        /**
         * Единственный план-владелец
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
         */
        planId: string;
        /**
         * Порядок отображения; не зависимость исполнения
         * @min 0
         * @max 9007199254740991
         */
        rank: number;
        /**
         * Полный набор ID включённых задач, максимум 2000; карточки читаются страницами
         * @maxItems 2000
         */
        taskIds: string[];
      }
    | {
        /**
         * Однострочное название релиза
         * @minLength 1
         */
        title: string;
        /**
         * Пользовательское обозначение выпуска; не ревизия записи
         * @minLength 1
         */
        version: string;
        /**
         * Краткое описание обычным многострочным текстом
         * @default ""
         */
        summary: string;
        /**
         * Полное описание выпуска в Markdown
         * @default ""
         */
        description: string;
        /**
         * Планы целиком по постоянным ID, максимум 200
         * @maxItems 200
         * @minItems 1
         */
        planIds: string[];
        /**
         * Плановая дата YYYY-MM-DD либо пустая строка
         * @default ""
         */
        plannedFor: "" | string;
        /** Самостоятельный релиз */
        kind: "release";
        /**
         * Постоянный ID записи выбранного проекта
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
         */
        projectId: string;
        /** Собственное состояние: запланирован, выпущен или отменён */
        status: EntityDetailStatusEnum2;
        /** Фактическая дата фиксации выпуска */
        releasedAt: string | null;
        /** Автор фиксации выпуска */
        releasedBy: string | null;
        /** Постоянный ID самодостаточного снимка */
        snapshotId: string | null;
      }
    | {
        /**
         * Отображаемое имя проекта, от 1 до 120 символов без переносов строк
         * @minLength 1
         * @maxLength 120
         * @pattern ^[^\p{Cc}]+$
         */
        name: string;
        /**
         * Slug адреса: 2–64 строчные латинские буквы/цифры с одиночными дефисами между частями
         * @minLength 2
         * @maxLength 64
         * @pattern ^[a-z0-9]+(?:-[a-z0-9]+)*$
         */
        slug: string;
        /**
         * Разделы библиотеки в порядке отображения; пустой список допустим
         * @maxItems 100
         */
        documentSections?: {
          /**
           * Постоянный ID раздела библиотеки
           * @pattern ^[a-zA-Z0-9_-]{1,64}$
           */
          id: string;
          /**
           * Название раздела библиотеки
           * @minLength 1
           */
          name: string;
        }[];
        /** Настройки выбранного проекта */
        kind: "project";
      }
    | {
        /** Паспорт продукта */
        kind: "product";
        /**
         * Название продукта; пусто до заполнения паспорта
         * @minLength 0
         */
        name: string;
        /** Краткое многострочное описание обычным текстом */
        summary: string;
        /** Описание продукта в Markdown; пусто до заполнения паспорта */
        description: string;
      }
    | {
        kind: "feature";
        /**
         * Однострочное название
         * @minLength 1
         */
        name: string;
        /** Краткое многострочное описание обычным текстом */
        summary: string;
        /** Полное описание в Markdown */
        description: string;
      }
    | {
        kind: "scenario";
        /**
         * Постоянный ID родительской фичи
         * @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$
         */
        featureId: string;
        /**
         * Однострочное название сценария
         * @minLength 1
         */
        name: string;
        /** Описание поведения сценария в Markdown */
        description: string;
      }
    | {
        kind: "application";
        /**
         * Однострочное название
         * @minLength 1
         */
        name: string;
        /** Краткое многострочное описание обычным текстом */
        summary: string;
        /** Полное описание в Markdown */
        description: string;
        /**
         * Неизменяемый уникальный адрес приложения и доски: 1–64 символа, строчные латинские буквы, цифры и дефисы; product, infrastructure и new зарезервированы
         * @minLength 1
         * @maxLength 64
         * @pattern ^[a-z0-9]+(?:-[a-z0-9]+)*$
         */
        slug: string;
        /**
         * Префикс задач доски; при создании по умолчанию из slug, затем неизменяем
         * @pattern ^[A-Z][A-Z0-9]{1,15}$
         */
        prefix?: string;
        /** Назначение приложения: клиентское, серверное или внутреннее */
        type: EntityDetailTypeEnum;
      }
    | {
        /**
         * Постоянный ID общей фичи
         * @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$
         */
        featureId: string;
        /** Постоянный ID сценария; null для общего вклада в фичу */
        scenarioId: string | null;
        /**
         * Однострочное название реализации
         * @minLength 1
         */
        title: string;
        /** Описание вклада приложения в Markdown */
        description: string;
        /** Состояние вклада приложения */
        status: EntityDetailStatusEnum3;
        active: boolean;
        basis: string;
        kind: "implementation";
        /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
        applicationId: string;
      }
    | {
        /**
         * Адрес доски: 1–64 символа, строчные латинские буквы, цифры и дефисы
         * @minLength 1
         * @maxLength 64
         * @pattern ^[a-z0-9]+(?:-[a-z0-9]+)*$
         */
        slug: string;
        /**
         * Неизменяемый уникальный префикс ключей задач, например WEB или PRODUCT
         * @pattern ^[A-Z][A-Z0-9]{1,15}$
         */
        prefix?: string;
        /** ID приложения; null у системных досок */
        applicationId: string | null;
        /** Доска задач */
        kind: "board";
        /** Область доски: продукт, приложение или инфраструктура */
        scope: EntityDetailScopeEnum;
      }
    | {
        /** Постоянный ID текущей доски */
        boardId: string;
        /**
         * Однострочный заголовок без Markdown; может быть пустым
         * @minLength 0
         */
        title: string;
        /** Описание задачи в Markdown; может быть пустым */
        description: string;
        /**
         * Явные связи «Реализует», максимум 100; пустой массив удаляет все связи
         * @maxItems 100
         * @default []
         */
        productLinks: {
          /** Цель реализации: общая фича, сценарий или контракт приложения */
          kind: EntityDetailKindEnum3;
          /**
           * ID или ключ продуктовой цели; при записи нормализуется в постоянный ID
           * @minLength 1
           * @maxLength 128
           */
          id: string;
        }[];
        /** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
        column: EntityDetailColumnEnum;
        /** Порядок внутри колонки */
        rank: number;
        /**
         * Прямые зависимости по ID; выполнение требует done, критериев и рекурсивного выполнения обязательств
         * @maxItems 200
         */
        dependencies: string[];
        /**
         * Связанные задачи; обратное представление вычисляется
         * @maxItems 200
         */
        related: string[];
        /** Родительская задача или null; незавершённая подзадача блокирует завершение родителя */
        parentId: string | null;
        /** Задача доски */
        kind: "task";
      }
    | {
        kind: "document";
        /**
         * Однострочное название документа
         * @minLength 1
         */
        name: string;
        /** Краткое обычное описание документа */
        summary: string;
        /** Полный текст документа в Markdown */
        body: string;
        /** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
        documentKind: EntityDetailDocumentKindEnum;
        /** Раздел; null — без раздела */
        sectionId?: string | null;
        /** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
        documentStatus?: EntityDetailDocumentStatusEnum;
        /** Закреплён для всех участников проекта */
        pinned?: boolean;
        /**
         * Адресные связи документа; текст и связи сохраняются атомарно
         * @maxItems 100
         */
        relations?: {
          /** Связанная сущность проекта: вид и постоянный ID */
          target: {
            /** Вид связанной сущности проекта */
            kind: EntityDetailKindEnum4;
            /**
             * Постоянный ID связанной сущности
             * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
             */
            id: string;
          };
          /** references — контекст для сущности; documents — документ описывает сущность */
          type: EntityDetailTypeEnum1;
          /** Когда и зачем читать документ: необязательное пояснение Markdown */
          description: string;
        }[];
        /**
         * Явные области применимости документа по постоянным ID
         * @maxItems 1000
         */
        links: (
          | {
              kind: "product";
            }
          | {
              kind: "feature";
              /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
              id: string;
            }
          | {
              kind: "scenario";
              /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
              id: string;
            }
          | {
              kind: "application";
              /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
              id: string;
            }
          | {
              kind: "implementation";
              /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
              applicationId: string;
              /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
              id: string;
            }
        )[];
      };
  /** Краткие карточки прямых предметных ссылок для отображения актуальных ключей */
  references: {
    /** Постоянный адрес в выбранном проекте */
    ref: {
      /** Вид основной сущности */
      kind: EntityDetailKindEnum5;
      /**
       * Постоянный ID; внутренние отношения сохраняют только этот адрес
       * @minLength 1
       * @maxLength 128
       */
      id: string;
    };
    /** Текущий читаемый ключ для человека и агента */
    key: string;
    /** Однострочное название */
    title: string;
    /** Краткое обычное описание без полного Markdown */
    summary: string;
    /**
     * Ревизия записи для следующего изменения
     * @min 0
     * @max 9007199254740991
     */
    revision: number;
    /** Текущее предметное состояние; null, когда неприменимо */
    status: string | null;
    /** Активная запись; снятая реализация сохраняет адрес */
    active: boolean;
    /** Приложение, доска или родитель для различения одинаковых названий */
    context?: string;
    /** Компактные свойства документа без полного Markdown */
    document?: {
      /** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
      kind: EntityDetailKindEnum6;
      /** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
      status: EntityDetailStatusEnum4;
      /** Эффективный раздел; удалённый раздел отображается как null */
      sectionId: string | null;
      /** Закрепление в проекте */
      pinned: boolean;
      /**
       * Момент времени в UTC
       * @format date-time
       * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
       */
      updatedAt: string;
      /**
       * Количество прямых отношений документа
       * @min 0
       * @max 9007199254740991
       */
      linkCount: number;
      /** Фрагмент совпадения полнотекстового поиска */
      excerpt?: string;
    };
  }[];
}

export interface EntityKeysQuery {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы от 1 до 100
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /**
   * Версия первой страницы; изменение требует начать чтение заново
   * @maxLength 128
   */
  version?: string;
  /**
   * Ключ или постоянный ID сущности; для уточнения вида допустим kind:ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /** Уточнение ожидаемого вида при разрешении адреса */
  kind?: EntityKeysQueryKindEnum;
}

export interface EntityKeysPage {
  /** Текущий ключ и прежние алиасы */
  items: {
    ref: {
      /** Вид основной сущности */
      kind: EntityKeysPageKindEnum;
      /**
       * Постоянный ID; внутренние отношения сохраняют только этот адрес
       * @minLength 1
       * @maxLength 128
       */
      id: string;
    };
    /** Разрешимый ключ записи */
    key: string;
    /** Основной ключ для текущего отображения */
    current: boolean;
  }[];
  /**
   * Полное число результатов выбранной области
   * @min 0
   * @max 9007199254740991
   */
  total: number;
  /** Смещение продолжения; null в конце */
  nextOffset: number | null;
  /** Версия согласованного снимка */
  version: string;
}

export interface EntityKeySpacesQuery {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы от 1 до 100
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /**
   * Версия первой страницы; изменение требует начать чтение заново
   * @maxLength 128
   */
  version?: string;
  /** Вид основной сущности */
  kind: EntityKeySpacesQueryKindEnum;
}

export interface EntityKeySpaces {
  /** Актуальные пространства ключей выбранного вида */
  items: {
    /** Владелец нумерации; null для общего пространства вида */
    scope: {
      /** Вид основной сущности */
      kind: EntityKeySpacesKindEnum;
      /**
       * Постоянный ID; внутренние отношения сохраняют только этот адрес
       * @minLength 1
       * @maxLength 128
       */
      id: string;
    } | null;
    /** Назначение пространства ключей */
    title: string;
    /** Префикс назначаемых ключей */
    prefix: string;
    /** Человекочитаемый формат ключа */
    pattern: string;
  }[];
  /**
   * Полное число результатов выбранной области
   * @min 0
   * @max 9007199254740991
   */
  total: number;
  /** Смещение продолжения; null в конце */
  nextOffset: number | null;
  /** Версия согласованного снимка */
  version: string;
}

export interface EntityHistory {
  /** События записи, доступные у её владельца */
  items: {
    /**
     * Ревизия записи
     * @exclusiveMin 0
     * @max 9007199254740991
     */
    revision: number;
    /**
     * Автор операции
     * @maxLength 128
     */
    actor: string;
    /**
     * Момент времени в UTC
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    at: string;
    /** Действие, создавшее ревизию */
    action: string;
    /** Пояснение предметного действия в Markdown, если сохранено владельцем */
    description?: string;
  }[];
  /**
   * Полное число результатов выбранной области
   * @min 0
   * @max 9007199254740991
   */
  total: number;
  /** Смещение продолжения; null в конце */
  nextOffset: number | null;
  /** Версия согласованного снимка */
  version: string;
}

export interface CreateEntity {
  /** Типизированные данные создаваемой сущности */
  data:
    | {
        /** Создать паспорт продукта */
        kind: "product";
        /** Однострочное название */
        name: string;
        /** Краткое многострочное описание обычным текстом */
        summary: string;
        /** Полное описание в Markdown */
        description: string;
      }
    | {
        kind: "feature";
        /** Однострочное название */
        name: string;
        /** Краткое многострочное описание обычным текстом */
        summary: string;
        /** Полное описание в Markdown */
        description: string;
      }
    | {
        kind: "scenario";
        /**
         * Ключ или ID родительской фичи
         * @minLength 1
         * @maxLength 257
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
         */
        featureId: string;
        /** Однострочное название сценария */
        name: string;
        /** Описание поведения сценария в Markdown */
        description: string;
      }
    | {
        kind: "application";
        /** Однострочное название */
        name: string;
        /** Краткое многострочное описание обычным текстом */
        summary: string;
        /** Полное описание в Markdown */
        description: string;
        /**
         * Неизменяемый уникальный адрес приложения и доски: 1–64 символа, строчные латинские буквы, цифры и дефисы; product, infrastructure и new зарезервированы
         * @minLength 1
         * @maxLength 64
         * @pattern ^[a-z0-9]+(?:-[a-z0-9]+)*$
         */
        slug: string;
        /**
         * Префикс задач доски; при создании по умолчанию из slug, затем неизменяем
         * @pattern ^[A-Z][A-Z0-9]{1,15}$
         */
        prefix?: string;
        /** Назначение приложения: клиентское, серверное или внутреннее */
        type: CreateEntityTypeEnum;
      }
    | {
        /** Создать участие приложения в фиче или сценарии */
        kind: "implementation";
        /**
         * Ключ или ID приложения
         * @minLength 1
         * @maxLength 257
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
         */
        application: string;
        /**
         * Ключ или ID фичи либо сценария
         * @minLength 1
         * @maxLength 257
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
         */
        target: string;
        /** Однострочное название вклада */
        title: string;
        /** Описание вклада в Markdown */
        description: string;
        /**
         * Состояние вклада; done подтверждает актуальные требования
         * @default "none"
         */
        status?: CreateEntityStatusEnum;
      }
    | {
        /**
         * Критерии приёмки при создании задачи одной атомарной операцией; все первоначально не выполнены
         * @maxItems 100
         */
        acceptanceCriteria?: {
          /** Обязательный однострочный заголовок критерия */
          title: string;
          /**
           * Краткое описание обычным многострочным текстом, до 4 КиБ
           * @default ""
           */
          summary?: string;
          /**
           * Полное описание критерия в Markdown, до 64 КиБ
           * @default ""
           */
          description?: string;
        }[];
        /** Создать задачу */
        kind: "task";
        /**
         * Ключ или ID доски
         * @minLength 1
         * @maxLength 257
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
         */
        board: string;
        /**
         * Однострочный заголовок без Markdown; может быть пустым
         * @default ""
         */
        title?: string;
        /**
         * Описание задачи в Markdown; может быть пустым
         * @default ""
         */
        description?: string;
        /**
         * Ключи или ID фич, сценариев и реализаций, над которыми работает задача
         * @maxItems 100
         * @default []
         */
        targets?: string[];
        /**
         * Ключи или ID задач, необходимых для завершения
         * @maxItems 100
         * @default []
         */
        dependencies?: string[];
        /**
         * Ключи или ID связанных задач без блокировки
         * @maxItems 100
         * @default []
         */
        related?: string[];
        /**
         * Ключ или ID родительской задачи
         * @default null
         */
        parent?: string | null;
        /**
         * Колонка: inbox, ready, in-progress, review, done; cancelled — отмена
         * @default "inbox"
         */
        column?: CreateEntityColumnEnum;
      }
    | {
        kind: "document";
        /** Однострочное название документа */
        name: string;
        /** Краткое обычное описание документа */
        summary: string;
        /** Полный текст документа в Markdown */
        body: string;
        /** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
        documentKind: CreateEntityDocumentKindEnum;
        /** Раздел; null — без раздела */
        sectionId?: string | null;
        /** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
        documentStatus?: CreateEntityDocumentStatusEnum;
        /** Закреплён для всех участников проекта */
        pinned?: boolean;
        /**
         * Адресные связи документа; текст и связи сохраняются атомарно
         * @maxItems 100
         */
        relations?: {
          /** Связанная сущность проекта: вид и постоянный ID */
          target: {
            /** Вид связанной сущности проекта */
            kind: CreateEntityKindEnum;
            /**
             * Постоянный ID связанной сущности
             * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
             */
            id: string;
          };
          /** references — контекст для сущности; documents — документ описывает сущность */
          type: CreateEntityTypeEnum1;
          /** Когда и зачем читать документ: необязательное пояснение Markdown */
          description: string;
        }[];
        /**
         * Ключи или ID продуктовых областей документа
         * @maxItems 100
         * @default []
         */
        targets?: string[];
      };
  /**
   * Ключ безопасного повтора операции
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /** Автор; по умолчанию автор текущего интерфейса */
  actor?: string;
}

export interface UpdateEntity {
  /**
   * Ключ или постоянный ID сущности; для уточнения вида допустим kind:ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /**
   * Прочитанная ревизия изменяемой записи
   * @min 0
   * @max 9007199254740991
   */
  ifRevision: number;
  /**
   * Ключ безопасного повтора операции
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /** Автор; по умолчанию автор текущего интерфейса */
  actor?: string;
  /** Только изменяемые поля выбранного вида */
  changes:
    | {
        /** Изменить имя проекта */
        kind: "project";
        /**
         * Отображаемое имя проекта, от 1 до 120 символов без переносов строк
         * @minLength 1
         * @maxLength 120
         * @pattern ^[^\p{Cc}]+$
         */
        name?: string;
        /**
         * Разделы библиотеки в порядке отображения; пустой список допустим
         * @maxItems 100
         */
        documentSections?: {
          /**
           * Постоянный ID раздела библиотеки
           * @pattern ^[a-zA-Z0-9_-]{1,64}$
           */
          id: string;
          /** Название раздела библиотеки */
          name: string;
        }[];
      }
    | {
        /** Создать паспорт продукта */
        kind: "product";
        /** Однострочное название */
        name?: string;
        /** Краткое многострочное описание обычным текстом */
        summary?: string;
        /** Полное описание в Markdown */
        description?: string;
      }
    | {
        kind: "feature";
        /** Однострочное название */
        name?: string;
        /** Краткое многострочное описание обычным текстом */
        summary?: string;
        /** Полное описание в Markdown */
        description?: string;
      }
    | {
        kind: "scenario";
        /** Однострочное название сценария */
        name?: string;
        /** Описание поведения сценария в Markdown */
        description?: string;
      }
    | {
        kind: "application";
        /** Однострочное название */
        name?: string;
        /** Краткое многострочное описание обычным текстом */
        summary?: string;
        /** Полное описание в Markdown */
        description?: string;
        /** Назначение приложения: клиентское, серверное или внутреннее */
        type?: UpdateEntityTypeEnum;
      }
    | {
        /** Создать участие приложения в фиче или сценарии */
        kind: "implementation";
        /** Однострочное название вклада */
        title?: string;
        /** Описание вклада в Markdown */
        description?: string;
        status?: UpdateEntityStatusEnum;
      }
    | {
        /** Изменить содержание задачи */
        kind: "task";
        /** Однострочный заголовок без Markdown; может быть пустым */
        title?: string;
        /** Описание задачи в Markdown; может быть пустым */
        description?: string;
        /**
         * Новый набор продуктовых целей; отсутствие сохраняет текущий набор
         * @maxItems 100
         */
        targets?: string[];
      }
    | {
        kind: "document";
        /** Однострочное название документа */
        name?: string;
        /** Краткое обычное описание документа */
        summary?: string;
        /** Полный текст документа в Markdown */
        body?: string;
        /** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
        documentKind?: UpdateEntityDocumentKindEnum;
        /** Раздел; null — без раздела */
        sectionId?: string | null;
        /** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
        documentStatus?: UpdateEntityDocumentStatusEnum;
        /** Закреплён для всех участников проекта */
        pinned?: boolean;
        /**
         * Адресные связи документа; текст и связи сохраняются атомарно
         * @maxItems 100
         */
        relations?: {
          /** Связанная сущность проекта: вид и постоянный ID */
          target: {
            /** Вид связанной сущности проекта */
            kind: UpdateEntityKindEnum;
            /**
             * Постоянный ID связанной сущности
             * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
             */
            id: string;
          };
          /** references — контекст для сущности; documents — документ описывает сущность */
          type: UpdateEntityTypeEnum1;
          /** Когда и зачем читать документ: необязательное пояснение Markdown */
          description: string;
        }[];
        /**
         * Новый набор областей документа
         * @maxItems 100
         */
        targets?: string[];
      };
}

export interface RenameEntity {
  /**
   * Ключ или постоянный ID сущности; для уточнения вида допустим kind:ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /**
   * Прочитанная ревизия изменяемой записи
   * @min 0
   * @max 9007199254740991
   */
  ifRevision: number;
  /**
   * Ключ безопасного повтора операции
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /** Автор; по умолчанию автор текущего интерфейса */
  actor?: string;
  /**
   * Читаемый ключ сущности; назначается ядром, может иметь прежние алиасы
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*$
   */
  key: string;
}

export interface MoveEntityTask {
  /**
   * Ключ или постоянный ID сущности; для уточнения вида допустим kind:ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /**
   * Прочитанная ревизия изменяемой записи
   * @min 0
   * @max 9007199254740991
   */
  ifRevision: number;
  /**
   * Ключ безопасного повтора операции
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /** Автор; по умолчанию автор текущего интерфейса */
  actor?: string;
  /**
   * Ключ или ID новой доски; без него текущая доска
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  board?: string;
  /** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
  column: MoveEntityTaskColumnEnum;
  /**
   * Ключ или ID следующей задачи; null — конец колонки
   * @default null
   */
  before?: string | null;
}

export interface LinkEntityTask {
  /**
   * Ключ или постоянный ID сущности; для уточнения вида допустим kind:ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /**
   * Прочитанная ревизия изменяемой записи
   * @min 0
   * @max 9007199254740991
   */
  ifRevision: number;
  /**
   * Ключ безопасного повтора операции
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /** Автор; по умолчанию автор текущего интерфейса */
  actor?: string;
  /**
   * Ключ или ID второй задачи
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  target: string;
  /** Зависит от, связана с или родитель текущей задачи */
  relation: LinkEntityTaskRelationEnum;
  /**
   * Удалить выбранное отношение
   * @default false
   */
  remove?: boolean;
}

export interface EntitySaved {
  ref: {
    /** Вид основной сущности */
    kind: EntitySavedKindEnum;
    /**
     * Постоянный ID; внутренние отношения сохраняют только этот адрес
     * @minLength 1
     * @maxLength 128
     */
    id: string;
  };
  /** Ключ записи на момент подтверждённой операции */
  key: string;
  /**
   * Ревизия после операции
   * @min 0
   * @max 9007199254740991
   */
  revision: number;
  /**
   * Ключ безопасного повтора операции
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /** Выполненное действие */
  action: EntitySavedActionEnum;
}

export interface GraphPage {
  /** Страница узлов, включая изолированные */
  nodes: {
    /** Адрес сущности */
    ref: {
      /**
       * Расширяемый вид сущности
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      kind: string;
      /**
       * Постоянный ID сущности в выбранном проекте
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      id: string;
    };
    /** Однострочное название сущности */
    title: string;
    /** Читаемый ключ или ID */
    key: string;
    /**
     * Ревизия источника
     * @min 0
     * @max 9007199254740991
     */
    revision: number;
    /** Состояние источника, пустая строка если не применимо */
    status: string;
  }[];
  /** Страница рёбер */
  edges: {
    /**
     * Постоянный ID отношения
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    id: string;
    /**
     * Расширяемый тип отношения, например references или contains
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    type: string;
    /** Начало отношения */
    from: {
      /**
       * Расширяемый вид сущности
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      kind: string;
      /**
       * Постоянный ID сущности в выбранном проекте
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      id: string;
    };
    /** Конец отношения */
    to: {
      /**
       * Расширяемый вид сущности
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      kind: string;
      /**
       * Постоянный ID сущности в выбранном проекте
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      id: string;
    };
    /** Пояснение назначения связи в Markdown */
    description: string;
    /**
     * Ревизия отношения
     * @exclusiveMin 0
     * @max 9007199254740991
     */
    revision: number;
    /** Единственный источник — явно сохранённая связь движка Core */
    source: "graph";
    /**
     * Автор установки связи
     * @maxLength 128
     */
    createdBy: string;
    /**
     * Время установки связи
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    createdAt: string;
  }[];
  /** Карточки концов рёбер этой страницы для независимого рендера */
  endpoints: {
    /** Адрес сущности */
    ref: {
      /**
       * Расширяемый вид сущности
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      kind: string;
      /**
       * Постоянный ID сущности в выбранном проекте
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      id: string;
    };
    /** Однострочное название сущности */
    title: string;
    /** Читаемый ключ или ID */
    key: string;
    /**
     * Ревизия источника
     * @min 0
     * @max 9007199254740991
     */
    revision: number;
    /** Состояние источника, пустая строка если не применимо */
    status: string;
  }[];
  /** Причины включения узлов в обход */
  paths: {
    /** Узел страницы */
    target: {
      /**
       * Расширяемый вид сущности
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      kind: string;
      /**
       * Постоянный ID сущности в выбранном проекте
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      id: string;
    };
    /** Один кратчайший объясняющий путь от корня */
    nodes: {
      /**
       * Расширяемый вид сущности
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      kind: string;
      /**
       * Постоянный ID сущности в выбранном проекте
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      id: string;
    }[];
    /** ID отношений пути; не все возможные пути */
    edges: string[];
    /** Читаемые ключи узлов пути в том же порядке */
    keys?: string[];
  }[];
  /**
   * Число узлов выбранной области
   * @min 0
   * @max 9007199254740991
   */
  totalNodes: number;
  /**
   * Число отношений выбранной области
   * @min 0
   * @max 9007199254740991
   */
  totalEdges: number;
  /** Продолжение обеих выборок, null в конце */
  nextOffset: number | null;
  /** Узлы страницы с ещё не пройденными соседями из-за глубины */
  boundary: {
    /**
     * Расширяемый вид сущности
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    kind: string;
    /**
     * Постоянный ID сущности в выбранном проекте
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    id: string;
  }[];
  /** Есть непройденные узлы за границей глубины */
  depthLimited: boolean;
  /** Версия всего согласованного графа и карточек */
  version: string;
}

export interface GraphQuery {
  /**
   * Ключ или ID корня; допустим kind:ID. Без корня весь граф проекта
   * @maxLength 257
   */
  root?: string;
  /**
   * Оставить только отношения выбранного типа
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
   */
  type?: string;
  /**
   * Направление обхода относительно каждого узла
   * @default "both"
   */
  direction?: GraphQueryDirectionEnum;
  /**
   * Глубина одного обхода 0–100; продолжайте от граничных узлов
   * @min 0
   * @max 100
   * @default 3
   */
  depth?: number;
  /**
   * Полный обход сохранённых связей; context — совместимое имя all без скрытых ограничений по видам
   * @default "all"
   */
  profile?: GraphQueryProfileEnum;
  /**
   * Поиск узлов по ключу, адресу и названию; рёбра только между найденными узлами
   * @maxLength 1024
   */
  q?: string;
  /**
   * Смещение одновременно в списках узлов и рёбер
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер каждой страницы узлов и рёбер, максимум 100
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /**
   * Версия первой страницы; изменение требует повторить чтение
   * @maxLength 128
   */
  version?: string;
}

export interface GraphMutation {
  /**
   * Атомарный пакет до 100 явных изменений
   * @maxItems 100
   * @minItems 1
   */
  operations: (
    | {
        /** Создать отношение */
        action: "add";
        /**
         * Тип создаваемого отношения; не ограничен предметными видами
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
         */
        type: string;
        /** Ключ, ID или постоянный адрес начала связи */
        from:
          | string
          | {
              /**
               * Расширяемый вид сущности
               * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
               */
              kind: string;
              /**
               * Постоянный ID сущности в выбранном проекте
               * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
               */
              id: string;
            };
        /** Ключ, ID или постоянный адрес конца связи */
        to:
          | string
          | {
              /**
               * Расширяемый вид сущности
               * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
               */
              kind: string;
              /**
               * Постоянный ID сущности в выбранном проекте
               * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
               */
              id: string;
            };
        /**
         * Необязательное пояснение в Markdown
         * @default ""
         */
        description?: string;
      }
    | {
        /** Изменить пояснение существующего отношения */
        action: "update";
        /**
         * ID редактируемого отношения
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
         */
        id: string;
        /** Новое пояснение в Markdown */
        description: string;
      }
    | {
        /** Отозвать отношение с сохранением истории */
        action: "remove";
        /**
         * ID отзываемого отношения
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
         */
        id: string;
      }
  )[];
  /**
   * Версия прочитанного графа; конфликт не теряет изменения
   * @minLength 1
   * @maxLength 128
   */
  ifVersion: string;
  /**
   * Ключ безопасного повтора всего пакета
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /** Автор изменения */
  actor?: string;
}

export interface GraphSaved {
  /** ID созданных, изменённых и отозванных отношений в порядке операций */
  ids: string[];
  /**
   * Ревизия хранилища отношений после пакета
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  revision: number;
  /** Версия графа после записи */
  version: string;
  /** Ключ повтора; повтор возвращает первоначальную квитанцию */
  requestId: string;
}

export interface GraphHistory {
  items: {
    /** Выполненное действие */
    action: GraphHistoryActionEnum;
    /** Состояние связи после действия; для отзыва последнее сохранённое состояние */
    edge: {
      /**
       * Постоянный ID отношения
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      id: string;
      /**
       * Расширяемый тип отношения, например references или contains
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      type: string;
      /** Начало отношения */
      from: {
        /**
         * Расширяемый вид сущности
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
         */
        kind: string;
        /**
         * Постоянный ID сущности в выбранном проекте
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
         */
        id: string;
      };
      /** Конец отношения */
      to: {
        /**
         * Расширяемый вид сущности
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
         */
        kind: string;
        /**
         * Постоянный ID сущности в выбранном проекте
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
         */
        id: string;
      };
      /** Пояснение назначения связи в Markdown */
      description: string;
      /**
       * Ревизия отношения
       * @exclusiveMin 0
       * @max 9007199254740991
       */
      revision: number;
      /** Единственный источник — явно сохранённая связь движка Core */
      source: "graph";
      /**
       * Автор установки связи
       * @maxLength 128
       */
      createdBy: string;
      /**
       * Время установки связи
       * @format date-time
       * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
       */
      createdAt: string;
    };
    /**
     * Автор события
     * @maxLength 128
     */
    actor: string;
    /**
     * Время события
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    at: string;
    /**
     * Ревизия пакета
     * @exclusiveMin 0
     * @max 9007199254740991
     */
    revision: number;
  }[];
  /**
   * @min 0
   * @max 9007199254740991
   */
  total: number;
  nextOffset: number | null;
  /**
   * @min 0
   * @max 9007199254740991
   */
  revision: number;
}

export interface GraphHistoryQuery {
  /**
   * История одного отношения; без ID весь журнал графа
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
   */
  id?: string;
  /**
   * Смещение событий
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы событий
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /**
   * Ревизия журнала первой страницы
   * @min 0
   * @max 9007199254740991
   */
  revision?: number;
}

export interface FullContext {
  /** Постоянный адрес исходной сущности */
  root: {
    /**
     * Расширяемый вид сущности
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    kind: string;
    /**
     * Постоянный ID сущности в выбранном проекте
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    id: string;
  };
  /** Версия согласованного снимка карточек и связей */
  version: string;
  /** Все узлы достижимой компоненты */
  nodes: {
    /** Адрес сущности */
    ref: {
      /**
       * Расширяемый вид сущности
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      kind: string;
      /**
       * Постоянный ID сущности в выбранном проекте
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      id: string;
    };
    /** Однострочное название сущности */
    title: string;
    /** Читаемый ключ или ID */
    key: string;
    /**
     * Ревизия источника
     * @min 0
     * @max 9007199254740991
     */
    revision: number;
    /** Состояние источника, пустая строка если не применимо */
    status: string;
  }[];
  /** Все действующие рёбра, включая петли и параллельные отношения */
  edges: {
    /**
     * Постоянный ID отношения
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    id: string;
    /**
     * Расширяемый тип отношения, например references или contains
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
     */
    type: string;
    /** Начало отношения */
    from: {
      /**
       * Расширяемый вид сущности
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      kind: string;
      /**
       * Постоянный ID сущности в выбранном проекте
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      id: string;
    };
    /** Конец отношения */
    to: {
      /**
       * Расширяемый вид сущности
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      kind: string;
      /**
       * Постоянный ID сущности в выбранном проекте
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
       */
      id: string;
    };
    /**
     * Ревизия отношения
     * @exclusiveMin 0
     * @max 9007199254740991
     */
    revision: number;
  }[];
  /** Успешный ответ всегда полный; превышение бюджета возвращает ошибку */
  complete: true;
}

export interface FullContextQuery {
  /**
   * Ключ, ID или kind:ID исходной сущности; обход обоих направлений до конца компоненты
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  root: string;
}

export interface ProjectSettings {
  /**
   * Отображаемое имя проекта, от 1 до 120 символов без переносов строк
   * @minLength 1
   * @maxLength 120
   * @pattern ^[^\p{Cc}]+$
   */
  name: string;
  /**
   * Slug адреса: 2–64 строчные латинские буквы/цифры с одиночными дефисами между частями
   * @minLength 2
   * @maxLength 64
   * @pattern ^[a-z0-9]+(?:-[a-z0-9]+)*$
   */
  slug: string;
  /**
   * Разделы библиотеки в порядке отображения; пустой список допустим
   * @maxItems 100
   */
  documentSections?: {
    /**
     * Постоянный ID раздела библиотеки
     * @pattern ^[a-zA-Z0-9_-]{1,64}$
     */
    id: string;
    /**
     * Название раздела библиотеки
     * @minLength 1
     */
    name: string;
  }[];
  /**
   * Ревизия настроек проекта для защиты от одновременного редактирования
   * @min 0
   * @max 9007199254740991
   */
  revision: number;
}

export interface SaveProjectSettings {
  /**
   * Отображаемое имя проекта, от 1 до 120 символов без переносов строк
   * @minLength 1
   * @maxLength 120
   * @pattern ^[^\p{Cc}]+$
   */
  name: string;
  /**
   * Slug адреса: 2–64 строчные латинские буквы/цифры с одиночными дефисами между частями
   * @minLength 2
   * @maxLength 64
   * @pattern ^[a-z0-9]+(?:-[a-z0-9]+)*$
   */
  slug: string;
  /**
   * Исходная ревизия; конфликт возвращает REVISION_CONFLICT
   * @min 0
   * @max 9007199254740991
   */
  ifRevision: number;
}

export interface BoardTaskView {
  /**
   * Постоянный ID задачи: 8 символов
   * @pattern ^[A-Za-z0-9]{8}$
   */
  id: string;
  /**
   * Текущий ключ задачи с префиксом доски
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  key: string;
  /** Постоянный ID текущей доски */
  boardId: string;
  /**
   * Однострочный заголовок без Markdown; может быть пустым
   * @minLength 0
   */
  title: string;
  /** Описание задачи в Markdown; может быть пустым */
  description: string;
  /**
   * Явные связи «Реализует», максимум 100; пустой массив удаляет все связи
   * @maxItems 100
   * @default []
   */
  productLinks: {
    /** Цель реализации: общая фича, сценарий или контракт приложения */
    kind: BoardTaskViewKindEnum;
    /**
     * ID или ключ продуктовой цели; при записи нормализуется в постоянный ID
     * @minLength 1
     * @maxLength 128
     */
    id: string;
  }[];
  /** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
  column: BoardTaskViewColumnEnum;
  /** Порядок внутри колонки */
  rank: number;
  /**
   * Ревизия задачи, прочитанная перед изменением
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  revision: number;
  /**
   * Прямые зависимости по ID; выполнение требует done, критериев и рекурсивного выполнения обязательств
   * @maxItems 200
   */
  dependencies: string[];
  /**
   * Связанные задачи; обратное представление вычисляется
   * @maxItems 200
   */
  related: string[];
  /** Родительская задача или null; незавершённая подзадача блокирует завершение родителя */
  parentId: string | null;
  /**
   * Время создания
   * @format date-time
   * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
   */
  createdAt: string;
  /**
   * Время последнего изменения
   * @format date-time
   * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
   */
  updatedAt: string;
  /**
   * Автор создания
   * @maxLength 128
   */
  createdBy: string;
  /**
   * Автор последнего изменения
   * @maxLength 128
   */
  updatedBy: string;
  /** Прогресс критериев приёмки; отсутствует в старых квитанциях */
  acceptance?: {
    /**
     * Общее число критериев
     * @min 0
     * @max 9007199254740991
     */
    total: number;
    /**
     * Число выполненных критериев
     * @min 0
     * @max 9007199254740991
     */
    completed: number;
  };
  /** Выполнены критерии, подзадачи и зависимости; можно завершить задачу */
  canComplete?: boolean;
  /** Текущий slug доски для навигации */
  boardSlug: string;
  /** Невыполненные прямые зависимости и подзадачи без повторов; их блокеры доступны через чтение связей */
  blockers: string[];
  /** Есть невыполненные зависимости */
  blocked: boolean;
  /** Можно брать в работу: колонка ready и нет блокеров */
  ready: boolean;
}

export interface CriteriaPage {
  /** Критерии без полного Markdown */
  items: {
    /**
     * Обязательный однострочный заголовок критерия
     * @minLength 1
     */
    title: string;
    /**
     * Краткое описание обычным многострочным текстом, до 4 КиБ
     * @default ""
     */
    summary: string;
    /**
     * Постоянный ID критерия приёмки
     * @pattern ^[A-Za-z0-9]{8}$
     */
    id: string;
    /** Критерий отмечен выполненным */
    completed: boolean;
    /** Время отметки выполнения или null */
    completedAt: string | null;
    /** Автор отметки выполнения или null */
    completedBy: string | null;
  }[];
  /** Общее число критериев */
  total: number;
  /** Смещение продолжения или null */
  nextOffset: number | null;
  /** Версия списка критериев */
  version: string;
  /**
   * Ревизия задачи для изменения критериев
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  revision: number;
}

export interface CriteriaQuery {
  /**
   * Смещение страницы критериев
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы, по умолчанию 20, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /** Версия первой страницы; после изменения перечитайте список */
  version?: string;
}

export interface CriterionView {
  /** Полное содержание критерия */
  criterion: {
    /**
     * Обязательный однострочный заголовок критерия
     * @minLength 1
     */
    title: string;
    /**
     * Краткое описание обычным многострочным текстом, до 4 КиБ
     * @default ""
     */
    summary: string;
    /**
     * Полное описание критерия в Markdown, до 64 КиБ
     * @default ""
     */
    description: string;
    /**
     * Постоянный ID критерия приёмки
     * @pattern ^[A-Za-z0-9]{8}$
     */
    id: string;
    /** Критерий отмечен выполненным */
    completed: boolean;
    /** Время отметки выполнения или null */
    completedAt: string | null;
    /** Автор отметки выполнения или null */
    completedBy: string | null;
  };
  /**
   * Ревизия задачи на момент чтения
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  revision: number;
}

export type ChangeCriterion =
  | {
      /** Обязательный однострочный заголовок критерия */
      title: string;
      /**
       * Краткое описание обычным многострочным текстом, до 4 КиБ
       * @default ""
       */
      summary?: string;
      /**
       * Полное описание критерия в Markdown, до 64 КиБ
       * @default ""
       */
      description?: string;
      /**
       * Прочитанная ревизия задачи
       * @exclusiveMin 0
       * @max 9007199254740991
       */
      ifRevision: number;
      /**
       * Ключ безопасного повтора операции
       * @minLength 1
       * @maxLength 128
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
       */
      requestId: string;
      /** Автор изменения; по умолчанию автор интерфейса */
      actor?: string;
      /** Добавить критерий */
      action: "add";
    }
  | {
      /**
       * Прочитанная ревизия задачи
       * @exclusiveMin 0
       * @max 9007199254740991
       */
      ifRevision: number;
      /**
       * Ключ безопасного повтора операции
       * @minLength 1
       * @maxLength 128
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
       */
      requestId: string;
      /** Автор изменения; по умолчанию автор интерфейса */
      actor?: string;
      /**
       * Постоянный ID критерия приёмки
       * @pattern ^[A-Za-z0-9]{8}$
       */
      criterionId: string;
      /** Обязательный однострочный заголовок критерия */
      title?: string;
      /** Новое краткое описание обычным многострочным текстом */
      summary?: string;
      /** Новое полное описание в Markdown */
      description?: string;
      /** Изменить содержание и сбросить выполнение при изменении текста */
      action: "update";
    }
  | {
      /**
       * Прочитанная ревизия задачи
       * @exclusiveMin 0
       * @max 9007199254740991
       */
      ifRevision: number;
      /**
       * Ключ безопасного повтора операции
       * @minLength 1
       * @maxLength 128
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
       */
      requestId: string;
      /** Автор изменения; по умолчанию автор интерфейса */
      actor?: string;
      /**
       * Постоянный ID критерия приёмки
       * @pattern ^[A-Za-z0-9]{8}$
       */
      criterionId: string;
      /** Явное состояние: true — выполнено, false — снять отметку */
      completed: boolean;
      /** Установить состояние выполнения */
      action: "complete";
    }
  | {
      /**
       * Прочитанная ревизия задачи
       * @exclusiveMin 0
       * @max 9007199254740991
       */
      ifRevision: number;
      /**
       * Ключ безопасного повтора операции
       * @minLength 1
       * @maxLength 128
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
       */
      requestId: string;
      /** Автор изменения; по умолчанию автор интерфейса */
      actor?: string;
      /**
       * Постоянный ID критерия приёмки
       * @pattern ^[A-Za-z0-9]{8}$
       */
      criterionId: string;
      /** Удалить критерий */
      action: "remove";
    };

export interface BoardTaskSaved {
  /**
   * Постоянный ID задачи: 8 символов
   * @pattern ^[A-Za-z0-9]{8}$
   */
  id: string;
  /**
   * ID задачи или ключ, например WEB-24
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  key: string;
  /** ID доски на момент операции */
  boardId: string;
  /**
   * Ревизия задачи, прочитанная перед изменением
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  revision: number;
  /** Выполненное действие */
  action: BoardTaskSavedActionEnum;
  /**
   * ID изменённого критерия приёмки
   * @pattern ^[A-Za-z0-9]{8}$
   */
  criterionId?: string;
  /**
   * Ключ повтора: повтор с тем же содержимым возвращает первоначальную квитанцию
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /** Первоначальное состояние при создании для открытия редактора без дополнительного GET */
  task?: {
    /**
     * Постоянный ID задачи: 8 символов
     * @pattern ^[A-Za-z0-9]{8}$
     */
    id: string;
    /**
     * Текущий ключ задачи с префиксом доски
     * @minLength 1
     * @maxLength 257
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
     */
    key: string;
    /** Постоянный ID текущей доски */
    boardId: string;
    /**
     * Однострочный заголовок без Markdown; может быть пустым
     * @minLength 0
     */
    title: string;
    /** Описание задачи в Markdown; может быть пустым */
    description: string;
    /**
     * Явные связи «Реализует», максимум 100; пустой массив удаляет все связи
     * @maxItems 100
     * @default []
     */
    productLinks: {
      /** Цель реализации: общая фича, сценарий или контракт приложения */
      kind: BoardTaskSavedKindEnum;
      /**
       * ID или ключ продуктовой цели; при записи нормализуется в постоянный ID
       * @minLength 1
       * @maxLength 128
       */
      id: string;
    }[];
    /** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
    column: BoardTaskSavedColumnEnum;
    /** Порядок внутри колонки */
    rank: number;
    /**
     * Ревизия задачи, прочитанная перед изменением
     * @exclusiveMin 0
     * @max 9007199254740991
     */
    revision: number;
    /**
     * Прямые зависимости по ID; выполнение требует done, критериев и рекурсивного выполнения обязательств
     * @maxItems 200
     */
    dependencies: string[];
    /**
     * Связанные задачи; обратное представление вычисляется
     * @maxItems 200
     */
    related: string[];
    /** Родительская задача или null; незавершённая подзадача блокирует завершение родителя */
    parentId: string | null;
    /**
     * Время создания
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    createdAt: string;
    /**
     * Время последнего изменения
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    updatedAt: string;
    /**
     * Автор создания
     * @maxLength 128
     */
    createdBy: string;
    /**
     * Автор последнего изменения
     * @maxLength 128
     */
    updatedBy: string;
    /** Прогресс критериев приёмки; отсутствует в старых квитанциях */
    acceptance?: {
      /**
       * Общее число критериев
       * @min 0
       * @max 9007199254740991
       */
      total: number;
      /**
       * Число выполненных критериев
       * @min 0
       * @max 9007199254740991
       */
      completed: number;
    };
    /** Выполнены критерии, подзадачи и зависимости; можно завершить задачу */
    canComplete?: boolean;
    /** Текущий slug доски для навигации */
    boardSlug: string;
    /** Невыполненные прямые зависимости и подзадачи без повторов; их блокеры доступны через чтение связей */
    blockers: string[];
    /** Есть невыполненные зависимости */
    blocked: boolean;
    /** Можно брать в работу: колонка ready и нет блокеров */
    ready: boolean;
  };
}

export interface BoardTasksPage {
  items: {
    /**
     * Постоянный ID задачи: 8 символов
     * @pattern ^[A-Za-z0-9]{8}$
     */
    id: string;
    /**
     * Текущий ключ задачи с префиксом доски
     * @minLength 1
     * @maxLength 257
     * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
     */
    key: string;
    /** Постоянный ID текущей доски */
    boardId: string;
    /**
     * Однострочный заголовок без Markdown; может быть пустым
     * @minLength 0
     */
    title: string;
    /**
     * Явные связи «Реализует», максимум 100; пустой массив удаляет все связи
     * @maxItems 100
     * @default []
     */
    productLinks: {
      /** Цель реализации: общая фича, сценарий или контракт приложения */
      kind: BoardTasksPageKindEnum;
      /**
       * ID или ключ продуктовой цели; при записи нормализуется в постоянный ID
       * @minLength 1
       * @maxLength 128
       */
      id: string;
    }[];
    /** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
    column: BoardTasksPageColumnEnum;
    /** Порядок внутри колонки */
    rank: number;
    /**
     * Ревизия задачи, прочитанная перед изменением
     * @exclusiveMin 0
     * @max 9007199254740991
     */
    revision: number;
    /**
     * Прямые зависимости по ID; выполнение требует done, критериев и рекурсивного выполнения обязательств
     * @maxItems 200
     */
    dependencies: string[];
    /**
     * Связанные задачи; обратное представление вычисляется
     * @maxItems 200
     */
    related: string[];
    /** Родительская задача или null; незавершённая подзадача блокирует завершение родителя */
    parentId: string | null;
    /**
     * Время создания
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    createdAt: string;
    /**
     * Время последнего изменения
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    updatedAt: string;
    /**
     * Автор создания
     * @maxLength 128
     */
    createdBy: string;
    /**
     * Автор последнего изменения
     * @maxLength 128
     */
    updatedBy: string;
    /** Прогресс критериев приёмки; отсутствует в старых квитанциях */
    acceptance?: {
      /**
       * Общее число критериев
       * @min 0
       * @max 9007199254740991
       */
      total: number;
      /**
       * Число выполненных критериев
       * @min 0
       * @max 9007199254740991
       */
      completed: number;
    };
    /** Выполнены критерии, подзадачи и зависимости; можно завершить задачу */
    canComplete?: boolean;
    /** Текущий slug доски для навигации */
    boardSlug: string;
    /** Невыполненные прямые зависимости и подзадачи без повторов; их блокеры доступны через чтение связей */
    blockers: string[];
    /** Есть невыполненные зависимости */
    blocked: boolean;
    /** Можно брать в работу: колонка ready и нет блокеров */
    ready: boolean;
  }[];
  total: number;
  nextOffset: number | null;
  version: string;
}

export interface BoardTaskLinksPage {
  items: {
    /** Направление связи относительно запрошенной задачи */
    relation: BoardTaskLinksPageRelationEnum;
    task: {
      /**
       * Постоянный ID задачи: 8 символов
       * @pattern ^[A-Za-z0-9]{8}$
       */
      id: string;
      /**
       * Текущий ключ задачи с префиксом доски
       * @minLength 1
       * @maxLength 257
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
       */
      key: string;
      /** Постоянный ID текущей доски */
      boardId: string;
      /**
       * Однострочный заголовок без Markdown; может быть пустым
       * @minLength 0
       */
      title: string;
      /**
       * Явные связи «Реализует», максимум 100; пустой массив удаляет все связи
       * @maxItems 100
       * @default []
       */
      productLinks: {
        /** Цель реализации: общая фича, сценарий или контракт приложения */
        kind: BoardTaskLinksPageKindEnum;
        /**
         * ID или ключ продуктовой цели; при записи нормализуется в постоянный ID
         * @minLength 1
         * @maxLength 128
         */
        id: string;
      }[];
      /** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
      column: BoardTaskLinksPageColumnEnum;
      /** Порядок внутри колонки */
      rank: number;
      /**
       * Ревизия задачи, прочитанная перед изменением
       * @exclusiveMin 0
       * @max 9007199254740991
       */
      revision: number;
      /**
       * Прямые зависимости по ID; выполнение требует done, критериев и рекурсивного выполнения обязательств
       * @maxItems 200
       */
      dependencies: string[];
      /**
       * Связанные задачи; обратное представление вычисляется
       * @maxItems 200
       */
      related: string[];
      /** Родительская задача или null; незавершённая подзадача блокирует завершение родителя */
      parentId: string | null;
      /**
       * Время создания
       * @format date-time
       * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
       */
      createdAt: string;
      /**
       * Время последнего изменения
       * @format date-time
       * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
       */
      updatedAt: string;
      /**
       * Автор создания
       * @maxLength 128
       */
      createdBy: string;
      /**
       * Автор последнего изменения
       * @maxLength 128
       */
      updatedBy: string;
      /** Прогресс критериев приёмки; отсутствует в старых квитанциях */
      acceptance?: {
        /**
         * Общее число критериев
         * @min 0
         * @max 9007199254740991
         */
        total: number;
        /**
         * Число выполненных критериев
         * @min 0
         * @max 9007199254740991
         */
        completed: number;
      };
      /** Выполнены критерии, подзадачи и зависимости; можно завершить задачу */
      canComplete?: boolean;
      /** Текущий slug доски для навигации */
      boardSlug: string;
      /** Невыполненные прямые зависимости и подзадачи без повторов; их блокеры доступны через чтение связей */
      blockers: string[];
      /** Есть невыполненные зависимости */
      blocked: boolean;
      /** Можно брать в работу: колонка ready и нет блокеров */
      ready: boolean;
    };
  }[];
  total: number;
  nextOffset: number | null;
  version: string;
}

export interface BoardTasksQuery {
  /**
   * Только прямые подзадачи родителя с указанным ID или ключом; фильтр до пагинации
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  parentId?: string;
  /**
   * Ключ или ID цели: задачи полного состава, включая дочерние сценарии и активные имплементации приложений
   * @minLength 1
   * @maxLength 128
   */
  productTarget?: string;
  /**
   * Slug, префикс или постоянный ID доски выбранного проекта
   * @minLength 1
   * @maxLength 128
   */
  board?: string;
  /** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
  column?: BoardTasksQueryColumnEnum;
  /** unfinished исключает done и cancelled; finished возвращает только их; без параметра все задачи */
  completion?: BoardTasksQueryCompletionEnum;
  /** title — поиск по ключам, ID и заголовку; all или отсутствие — также по Markdown */
  searchIn?: BoardTasksQuerySearchInEnum;
  /**
   * Поиск по ключу, ID, заголовку и Markdown
   * @maxLength 1024
   */
  q?: string;
  /** Заблокированные задачи либо готовые к выполнению для оркестратора */
  readiness?: BoardTasksQueryReadinessEnum;
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Число задач или связей на странице, максимум 100
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /** Версия первой страницы; изменение требует начать чтение заново */
  version?: string;
}

export interface CreateBoardTask {
  /**
   * Ключ повтора: повтор с тем же содержимым возвращает первоначальную квитанцию
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /** Автор изменения; по умолчанию автор текущего интерфейса */
  actor?: string;
  /**
   * Критерии приёмки при создании задачи одной атомарной операцией; все первоначально не выполнены
   * @maxItems 100
   */
  acceptanceCriteria?: {
    /** Обязательный однострочный заголовок критерия */
    title: string;
    /**
     * Краткое описание обычным многострочным текстом, до 4 КиБ
     * @default ""
     */
    summary?: string;
    /**
     * Полное описание критерия в Markdown, до 64 КиБ
     * @default ""
     */
    description?: string;
  }[];
  /**
   * Slug, префикс или постоянный ID доски выбранного проекта
   * @minLength 1
   * @maxLength 128
   */
  board: string;
  /**
   * Однострочный заголовок без Markdown; может быть пустым
   * @default ""
   */
  title?: string;
  /**
   * Описание задачи в Markdown; может быть пустым
   * @default ""
   */
  description?: string;
  /**
   * Явные связи «Реализует», максимум 100; пустой массив удаляет все связи
   * @maxItems 100
   */
  productLinks?: {
    /** Цель реализации: общая фича, сценарий или контракт приложения */
    kind: CreateBoardTaskKindEnum;
    /**
     * ID или ключ продуктовой цели; при записи нормализуется в постоянный ID
     * @minLength 1
     * @maxLength 128
     */
    id: string;
  }[];
  /**
   * Ключ или ID родителя: создание подзадачи одной атомарной операцией
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  parentId?: string;
  /**
   * Ключи или ID зависимостей при создании одной атомарной операцией
   * @maxItems 100
   */
  dependencies?: string[];
  /**
   * Ключи или ID контекстно связанных задач при создании
   * @maxItems 100
   */
  related?: string[];
  /**
   * Колонка: inbox, ready, in-progress, review, done; cancelled — отмена
   * @default "inbox"
   */
  column?: CreateBoardTaskColumnEnum;
  /**
   * Вернуть первоначальную задачу в квитанции для открытия редактора без GET; по умолчанию компактная квитанция
   * @default false
   */
  includeTask?: boolean;
}

export interface UpdateBoardTask {
  /**
   * Ключ повтора: повтор с тем же содержимым возвращает первоначальную квитанцию
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /** Автор изменения; по умолчанию автор текущего интерфейса */
  actor?: string;
  /**
   * Ревизия задачи, прочитанная перед изменением
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  ifRevision: number;
  /** Однострочный заголовок без Markdown; может быть пустым */
  title?: string;
  /** Описание задачи в Markdown; может быть пустым */
  description?: string;
  /**
   * Явные связи «Реализует», максимум 100; пустой массив удаляет все связи
   * @maxItems 100
   */
  productLinks?: {
    /** Цель реализации: общая фича, сценарий или контракт приложения */
    kind: UpdateBoardTaskKindEnum;
    /**
     * ID или ключ продуктовой цели; при записи нормализуется в постоянный ID
     * @minLength 1
     * @maxLength 128
     */
    id: string;
  }[];
}

export interface MoveBoardTask {
  /**
   * Ключ повтора: повтор с тем же содержимым возвращает первоначальную квитанцию
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /** Автор изменения; по умолчанию автор текущего интерфейса */
  actor?: string;
  /**
   * Ревизия задачи, прочитанная перед изменением
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  ifRevision: number;
  /**
   * Целевая доска; при переносе меняется ключ, но не ID
   * @minLength 1
   * @maxLength 128
   */
  board?: string;
  /** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
  column: MoveBoardTaskColumnEnum;
  /**
   * ID карточки, перед которой вставить; null — конец полной колонки
   * @default null
   */
  beforeId?: string | null;
  /** Версия списка до перетаскивания; защищает порядок от параллельных изменений */
  ifVersion?: string;
}

export interface LinkBoardTask {
  /**
   * Ключ повтора: повтор с тем же содержимым возвращает первоначальную квитанцию
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /** Автор изменения; по умолчанию автор текущего интерфейса */
  actor?: string;
  /**
   * Ревизия задачи, прочитанная перед изменением
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  ifRevision: number;
  /**
   * ID или ключ второй задачи того же проекта
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  target: string;
  /** Зависит от, связана с или родитель текущей задачи */
  relation: LinkBoardTaskRelationEnum;
  /**
   * Удалить связь вместо добавления
   * @default false
   */
  remove?: boolean;
}

export interface ServerContextResponse {
  mode: ServerContextResponseModeEnum;
  configPath: string;
  defaultProject: string | null;
  projects: {
    key: string;
    id: string;
    name: string;
    /** Человекочитаемый адрес проекта; постоянный id не меняется */
    slug?: string;
    configPath: string;
    available: boolean;
    error?: string;
  }[];
}

export interface RegisterProjectRequest {
  path?: string;
  config?: string;
  replace?: boolean;
}

export interface ProductState {
  productId: string;
  version: string;
  records: {
    version: 1;
    /** @minLength 1 */
    productId: string;
    /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
    id: string;
    /**
     * Читаемый ключ сущности; назначается ядром, может иметь прежние алиасы
     * @minLength 1
     * @maxLength 128
     * @pattern ^[A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*$
     */
    key?: string;
    reservedKeys?: string[];
    /**
     * @exclusiveMin 0
     * @max 9007199254740991
     */
    revision: number;
    fields:
      | {
          kind: "passport";
          /**
           * Однострочное название
           * @minLength 1
           */
          name: string;
          /** Краткое многострочное описание обычным текстом */
          summary: string;
          /** Полное описание в Markdown */
          description: string;
        }
      | {
          kind: "feature";
          /**
           * Однострочное название
           * @minLength 1
           */
          name: string;
          /** Краткое многострочное описание обычным текстом */
          summary: string;
          /** Полное описание в Markdown */
          description: string;
        }
      | {
          kind: "scenario";
          /**
           * Постоянный ID родительской фичи
           * @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$
           */
          featureId: string;
          /**
           * Однострочное название сценария
           * @minLength 1
           */
          name: string;
          /** Описание поведения сценария в Markdown */
          description: string;
        }
      | {
          kind: "application";
          /**
           * Однострочное название
           * @minLength 1
           */
          name: string;
          /** Краткое многострочное описание обычным текстом */
          summary: string;
          /** Полное описание в Markdown */
          description: string;
          /**
           * Неизменяемый уникальный адрес приложения и доски: 1–64 символа, строчные латинские буквы, цифры и дефисы; product, infrastructure и new зарезервированы
           * @minLength 1
           * @maxLength 64
           * @pattern ^[a-z0-9]+(?:-[a-z0-9]+)*$
           */
          slug: string;
          /**
           * Префикс задач доски; при создании по умолчанию из slug, затем неизменяем
           * @pattern ^[A-Z][A-Z0-9]{1,15}$
           */
          prefix?: string;
          /** Назначение приложения: клиентское, серверное или внутреннее */
          type: ProductStateTypeEnum;
        }
      | {
          kind: "scope";
          /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
          applicationId: string;
          /** @maxItems 10000 */
          contracts: {
            /**
             * Постоянный ID общей фичи
             * @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$
             */
            featureId: string;
            /** Постоянный ID сценария; null для общего вклада в фичу */
            scenarioId: string | null;
            /**
             * Однострочное название реализации
             * @minLength 1
             */
            title: string;
            /** Описание вклада приложения в Markdown */
            description: string;
            /** Состояние вклада приложения */
            status: ProductStateStatusEnum;
            /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
            id: string;
            /**
             * Читаемый ключ сущности; назначается ядром, может иметь прежние алиасы
             * @minLength 1
             * @maxLength 128
             * @pattern ^[A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*$
             */
            key?: string;
            /**
             * @exclusiveMin 0
             * @max 9007199254740991
             */
            revision?: number;
            active: boolean;
            basis: string;
          }[];
        }
      | {
          kind: "document";
          /**
           * Однострочное название документа
           * @minLength 1
           */
          name: string;
          /** Краткое обычное описание документа */
          summary: string;
          /** Полный текст документа в Markdown */
          body: string;
          /** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
          documentKind: ProductStateDocumentKindEnum;
          /** Раздел; null — без раздела */
          sectionId?: string | null;
          /** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
          documentStatus?: ProductStateDocumentStatusEnum;
          /** Закреплён для всех участников проекта */
          pinned?: boolean;
          /**
           * Адресные связи документа; текст и связи сохраняются атомарно
           * @maxItems 100
           */
          relations?: {
            /** Связанная сущность проекта: вид и постоянный ID */
            target: {
              /** Вид связанной сущности проекта */
              kind: ProductStateKindEnum;
              /**
               * Постоянный ID связанной сущности
               * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
               */
              id: string;
            };
            /** references — контекст для сущности; documents — документ описывает сущность */
            type: ProductStateTypeEnum1;
            /** Когда и зачем читать документ: необязательное пояснение Markdown */
            description: string;
          }[];
          /**
           * Явные области применимости документа по постоянным ID
           * @maxItems 1000
           */
          links: (
            | {
                kind: "product";
              }
            | {
                kind: "feature";
                /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
                id: string;
              }
            | {
                kind: "scenario";
                /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
                id: string;
              }
            | {
                kind: "application";
                /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
                id: string;
              }
            | {
                kind: "implementation";
                /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
                applicationId: string;
                /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
                id: string;
              }
          )[];
        };
    /**
     * Момент времени в UTC
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    createdAt: string;
    /**
     * Момент времени в UTC
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    updatedAt: string;
    /**
     * Автор операции
     * @maxLength 128
     */
    createdBy: string;
    /**
     * Автор операции
     * @maxLength 128
     */
    updatedBy: string;
  }[];
  readiness: {
    /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
    id: string;
    status: ProductStateStatusEnum1;
    /**
     * @min 0
     * @max 9007199254740991
     */
    participants: number;
    /**
     * @min 0
     * @max 9007199254740991
     */
    completed: number;
    /**
     * @min 0
     * @max 9007199254740991
     */
    stale: number;
  }[];
}

export interface ProductEntities {
  items: {
    /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
    id: string;
    /**
     * Читаемый ключ сущности; назначается ядром, может иметь прежние алиасы
     * @minLength 1
     * @maxLength 128
     * @pattern ^[A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*$
     */
    key?: string;
    kind: ProductEntitiesKindEnum;
    /** Однострочное название цели */
    title: string;
    /** Краткий обычный текст без полного Markdown */
    summary: string;
    /**
     * @exclusiveMin 0
     * @max 9007199254740991
     */
    revision: number;
    applicationId: string | null;
    /** Читаемый ключ сущности; назначается ядром, может иметь прежние алиасы */
    applicationKey: string | null;
    applicationName: string | null;
    featureId: string | null;
    scenarioId: string | null;
    /** Читаемый ключ сущности; назначается ядром, может иметь прежние алиасы */
    targetKey: string | null;
    targetName: string | null;
    active: boolean;
    status: ProductEntitiesStatusEnum | null;
  }[];
  total: number;
  nextOffset: number | null;
  version: string;
}

export interface ProductEntitiesQuery {
  /**
   * Поиск по ключу или названию; точное совпадение первым
   * @maxLength 4096
   */
  q?: string;
  /** Тип цели */
  kind?: ProductEntitiesQueryKindEnum;
  /**
   * ID или ключ приложения
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  application?: string;
  /** Только реализации фич или сценариев; фильтр применяется до пагинации */
  implementationTarget?: ProductEntitiesQueryImplementationTargetEnum;
  /**
   * До 100 ID или ключей для краткого чтения существующих связей
   * @maxItems 100
   */
  refs?: string[];
  /** Фильтр участия; прежние ссылки читаются без фильтра */
  active?: ProductEntitiesQueryActiveEnum;
  /**
   * Смещение страницы; продолжение возвращается в nextOffset
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset: number;
  /**
   * Размер страницы от 1 до 100
   * @min 1
   * @max 100
   * @default 30
   */
  limit: number;
}

export type ProductEntity =
  | {
      version: 1;
      /** @minLength 1 */
      productId: string;
      /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
      id: string;
      /**
       * Читаемый ключ сущности; назначается ядром, может иметь прежние алиасы
       * @minLength 1
       * @maxLength 128
       * @pattern ^[A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*$
       */
      key?: string;
      reservedKeys?: string[];
      /**
       * @exclusiveMin 0
       * @max 9007199254740991
       */
      revision: number;
      fields:
        | {
            kind: "passport";
            /**
             * Однострочное название
             * @minLength 1
             */
            name: string;
            /** Краткое многострочное описание обычным текстом */
            summary: string;
            /** Полное описание в Markdown */
            description: string;
          }
        | {
            kind: "feature";
            /**
             * Однострочное название
             * @minLength 1
             */
            name: string;
            /** Краткое многострочное описание обычным текстом */
            summary: string;
            /** Полное описание в Markdown */
            description: string;
          }
        | {
            kind: "scenario";
            /**
             * Постоянный ID родительской фичи
             * @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$
             */
            featureId: string;
            /**
             * Однострочное название сценария
             * @minLength 1
             */
            name: string;
            /** Описание поведения сценария в Markdown */
            description: string;
          }
        | {
            kind: "application";
            /**
             * Однострочное название
             * @minLength 1
             */
            name: string;
            /** Краткое многострочное описание обычным текстом */
            summary: string;
            /** Полное описание в Markdown */
            description: string;
            /**
             * Неизменяемый уникальный адрес приложения и доски: 1–64 символа, строчные латинские буквы, цифры и дефисы; product, infrastructure и new зарезервированы
             * @minLength 1
             * @maxLength 64
             * @pattern ^[a-z0-9]+(?:-[a-z0-9]+)*$
             */
            slug: string;
            /**
             * Префикс задач доски; при создании по умолчанию из slug, затем неизменяем
             * @pattern ^[A-Z][A-Z0-9]{1,15}$
             */
            prefix?: string;
            /** Назначение приложения: клиентское, серверное или внутреннее */
            type: ProductEntityTypeEnum;
          }
        | {
            kind: "scope";
            /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
            applicationId: string;
            /** @maxItems 10000 */
            contracts: {
              /**
               * Постоянный ID общей фичи
               * @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$
               */
              featureId: string;
              /** Постоянный ID сценария; null для общего вклада в фичу */
              scenarioId: string | null;
              /**
               * Однострочное название реализации
               * @minLength 1
               */
              title: string;
              /** Описание вклада приложения в Markdown */
              description: string;
              /** Состояние вклада приложения */
              status: ProductEntityStatusEnum;
              /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
              id: string;
              /**
               * Читаемый ключ сущности; назначается ядром, может иметь прежние алиасы
               * @minLength 1
               * @maxLength 128
               * @pattern ^[A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*$
               */
              key?: string;
              /**
               * @exclusiveMin 0
               * @max 9007199254740991
               */
              revision?: number;
              active: boolean;
              basis: string;
            }[];
          }
        | {
            kind: "document";
            /**
             * Однострочное название документа
             * @minLength 1
             */
            name: string;
            /** Краткое обычное описание документа */
            summary: string;
            /** Полный текст документа в Markdown */
            body: string;
            /** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
            documentKind: ProductEntityDocumentKindEnum;
            /** Раздел; null — без раздела */
            sectionId?: string | null;
            /** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
            documentStatus?: ProductEntityDocumentStatusEnum;
            /** Закреплён для всех участников проекта */
            pinned?: boolean;
            /**
             * Адресные связи документа; текст и связи сохраняются атомарно
             * @maxItems 100
             */
            relations?: {
              /** Связанная сущность проекта: вид и постоянный ID */
              target: {
                /** Вид связанной сущности проекта */
                kind: ProductEntityKindEnum;
                /**
                 * Постоянный ID связанной сущности
                 * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
                 */
                id: string;
              };
              /** references — контекст для сущности; documents — документ описывает сущность */
              type: ProductEntityTypeEnum1;
              /** Когда и зачем читать документ: необязательное пояснение Markdown */
              description: string;
            }[];
            /**
             * Явные области применимости документа по постоянным ID
             * @maxItems 1000
             */
            links: (
              | {
                  kind: "product";
                }
              | {
                  kind: "feature";
                  /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
                  id: string;
                }
              | {
                  kind: "scenario";
                  /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
                  id: string;
                }
              | {
                  kind: "application";
                  /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
                  id: string;
                }
              | {
                  kind: "implementation";
                  /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
                  applicationId: string;
                  /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
                  id: string;
                }
            )[];
          };
      /**
       * Момент времени в UTC
       * @format date-time
       * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
       */
      createdAt: string;
      /**
       * Момент времени в UTC
       * @format date-time
       * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
       */
      updatedAt: string;
      /**
       * Автор операции
       * @maxLength 128
       */
      createdBy: string;
      /**
       * Автор операции
       * @maxLength 128
       */
      updatedBy: string;
      /**
       * Постоянный ID или читаемый ключ сущности в выбранном проекте
       * @minLength 1
       * @maxLength 257
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
       */
      canonicalRef?: string;
    }
  | {
      version: 1;
      /** @minLength 1 */
      productId: string;
      /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
      id: string;
      /**
       * Читаемый ключ сущности; назначается ядром, может иметь прежние алиасы
       * @minLength 1
       * @maxLength 128
       * @pattern ^[A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*$
       */
      key?: string;
      reservedKeys?: string[];
      /**
       * @exclusiveMin 0
       * @max 9007199254740991
       */
      revision: number;
      /**
       * Момент времени в UTC
       * @format date-time
       * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
       */
      createdAt: string;
      /**
       * Момент времени в UTC
       * @format date-time
       * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
       */
      updatedAt: string;
      /**
       * Автор операции
       * @maxLength 128
       */
      createdBy: string;
      /**
       * Автор операции
       * @maxLength 128
       */
      updatedBy: string;
      fields: {
        /**
         * Постоянный ID общей фичи
         * @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$
         */
        featureId: string;
        /** Постоянный ID сценария; null для общего вклада в фичу */
        scenarioId: string | null;
        /**
         * Однострочное название реализации
         * @minLength 1
         */
        title: string;
        /** Описание вклада приложения в Markdown */
        description: string;
        /** Состояние вклада приложения */
        status: ProductEntityStatusEnum1;
        active: boolean;
        basis: string;
        kind: "implementation";
        /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
        applicationId: string;
      };
      /**
       * Постоянный ID или читаемый ключ сущности в выбранном проекте
       * @minLength 1
       * @maxLength 257
       * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
       */
      canonicalRef?: string;
    };

export interface ProductEntityQuery {
  /**
   * Постоянный ID или читаемый ключ сущности в выбранном проекте
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
}

export interface UpdateImplementation {
  /**
   * Постоянный ID или читаемый ключ сущности в выбранном проекте
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /**
   * Ревизия выбранной реализации
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  ifRevision: number;
  /**
   * Однострочный заголовок реализации
   * @minLength 1
   */
  title?: string;
  /** Полное описание вклада в Markdown */
  description?: string;
  /** Состояние реализации; done подтверждает актуальные требования */
  status?: UpdateImplementationStatusEnum;
  /**
   * Свободный ключ для разрешения коллизии; ID сохраняется
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*$
   */
  key?: string;
  /**
   * Ключ повтора записи
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /**
   * Автор изменения
   * @maxLength 128
   */
  actor?: string;
}

export interface ProductMutation {
  action: ProductMutationActionEnum;
  /**
   * Постоянный ID или читаемый ключ сущности в выбранном проекте
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  id?: string;
  /**
   * Новый ключ при явном разрешении конфликта; ID и связи сохраняются
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*$
   */
  key?: string;
  fields:
    | {
        kind: "passport";
        /**
         * Однострочное название
         * @minLength 1
         */
        name: string;
        /** Краткое многострочное описание обычным текстом */
        summary: string;
        /** Полное описание в Markdown */
        description: string;
      }
    | {
        kind: "feature";
        /**
         * Однострочное название
         * @minLength 1
         */
        name: string;
        /** Краткое многострочное описание обычным текстом */
        summary: string;
        /** Полное описание в Markdown */
        description: string;
      }
    | {
        kind: "scenario";
        /**
         * Постоянный ID или читаемый ключ сущности в выбранном проекте
         * @minLength 1
         * @maxLength 257
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
         */
        featureId: string;
        /**
         * Однострочное название сценария
         * @minLength 1
         */
        name: string;
        /** Описание поведения сценария в Markdown */
        description: string;
      }
    | {
        kind: "application";
        /**
         * Однострочное название
         * @minLength 1
         */
        name: string;
        /** Краткое многострочное описание обычным текстом */
        summary: string;
        /** Полное описание в Markdown */
        description: string;
        /**
         * Неизменяемый уникальный адрес приложения и доски: 1–64 символа, строчные латинские буквы, цифры и дефисы; product, infrastructure и new зарезервированы
         * @minLength 1
         * @maxLength 64
         * @pattern ^[a-z0-9]+(?:-[a-z0-9]+)*$
         */
        slug: string;
        /**
         * Префикс задач доски; при создании по умолчанию из slug, затем неизменяем
         * @pattern ^[A-Z][A-Z0-9]{1,15}$
         */
        prefix?: string;
        /** Назначение приложения: клиентское, серверное или внутреннее */
        type: ProductMutationTypeEnum;
      }
    | {
        kind: "document";
        /**
         * Однострочное название документа
         * @minLength 1
         */
        name: string;
        /** Краткое обычное описание документа */
        summary: string;
        /** Полный текст документа в Markdown */
        body: string;
        /** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
        documentKind: ProductMutationDocumentKindEnum;
        /** Раздел; null — без раздела */
        sectionId?: string | null;
        /** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
        documentStatus?: ProductMutationDocumentStatusEnum;
        /** Закреплён для всех участников проекта */
        pinned?: boolean;
        /**
         * Адресные связи документа; текст и связи сохраняются атомарно
         * @maxItems 100
         */
        relations?: {
          /** Связанная сущность проекта: вид и постоянный ID */
          target: {
            /** Вид связанной сущности проекта */
            kind: ProductMutationKindEnum;
            /**
             * Постоянный ID связанной сущности
             * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
             */
            id: string;
          };
          /** references — контекст для сущности; documents — документ описывает сущность */
          type: ProductMutationTypeEnum1;
          /** Когда и зачем читать документ: необязательное пояснение Markdown */
          description: string;
        }[];
        /** @maxItems 1000 */
        links: (
          | {
              kind: "product";
            }
          | {
              kind: "feature";
              /**
               * Постоянный ID или читаемый ключ сущности в выбранном проекте
               * @minLength 1
               * @maxLength 257
               * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
               */
              id: string;
            }
          | {
              kind: "scenario";
              /**
               * Постоянный ID или читаемый ключ сущности в выбранном проекте
               * @minLength 1
               * @maxLength 257
               * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
               */
              id: string;
            }
          | {
              kind: "application";
              /**
               * Постоянный ID или читаемый ключ сущности в выбранном проекте
               * @minLength 1
               * @maxLength 257
               * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
               */
              id: string;
            }
          | {
              kind: "implementation";
              /**
               * Постоянный ID или читаемый ключ сущности в выбранном проекте
               * @minLength 1
               * @maxLength 257
               * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
               */
              applicationId: string;
              /**
               * Постоянный ID или читаемый ключ сущности в выбранном проекте
               * @minLength 1
               * @maxLength 257
               * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
               */
              id: string;
            }
        )[];
      }
    | {
        kind: "scope";
        /**
         * Постоянный ID или читаемый ключ сущности в выбранном проекте
         * @minLength 1
         * @maxLength 257
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
         */
        applicationId: string;
        /** @maxItems 10000 */
        contracts: {
          /**
           * Постоянный ID или читаемый ключ сущности в выбранном проекте
           * @minLength 1
           * @maxLength 257
           * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
           */
          featureId: string;
          /** Постоянный ID или читаемый ключ сущности в выбранном проекте */
          scenarioId: string | null;
          /**
           * Однострочное название реализации
           * @minLength 1
           */
          title: string;
          /** Описание вклада приложения в Markdown */
          description: string;
          /** Состояние вклада приложения */
          status: ProductMutationStatusEnum;
          /**
           * Прочитанный ключ; не меняется операцией замены состава
           * @minLength 1
           * @maxLength 128
           * @pattern ^[A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*$
           */
          key?: string;
          /**
           * Прочитанная ревизия реализации; замена состава проверяет общую версию
           * @exclusiveMin 0
           * @max 9007199254740991
           */
          revision?: number;
        }[];
      }
    | {
        kind: "contract";
        /**
         * Постоянный ID или читаемый ключ сущности в выбранном проекте
         * @minLength 1
         * @maxLength 257
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
         */
        applicationId: string;
        /**
         * Постоянный ID или читаемый ключ сущности в выбранном проекте
         * @minLength 1
         * @maxLength 257
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
         */
        contractId: string;
        status: ProductMutationStatusEnum1;
        /** @minLength 1 */
        title?: string;
        description?: string;
      }
    | {
        /**
         * Ключ или ID общей фичи
         * @minLength 1
         * @maxLength 257
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
         */
        featureId: string;
        /** Ключ или ID сценария; null для общего вклада */
        scenarioId: string | null;
        /**
         * Однострочное название реализации
         * @minLength 1
         */
        title: string;
        /** Описание вклада приложения в Markdown */
        description: string;
        /** Состояние вклада приложения */
        status: ProductMutationStatusEnum2;
        /** Добавить одну реализацию, сохраняя остальные вклады */
        kind: "implementation";
        /**
         * Ключ или ID приложения
         * @minLength 1
         * @maxLength 257
         * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
         */
        applicationId: string;
      };
  /**
   * @min 0
   * @max 9007199254740991
   */
  ifRevision?: number;
  ifVersion?: string;
  /**
   * Ключ безопасного повтора операции
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  requestId: string;
  /**
   * Автор операции
   * @maxLength 128
   */
  actor?: string;
}

export interface ProductSaved {
  /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
  id: string;
  /**
   * Читаемый ключ сущности; назначается ядром, может иметь прежние алиасы
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*$
   */
  key?: string;
  /**
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  revision: number;
}

export interface ProductContext {
  productId: string;
  version: string;
  records: {
    record: {
      version: 1;
      /** @minLength 1 */
      productId: string;
      /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
      id: string;
      /**
       * Читаемый ключ сущности; назначается ядром, может иметь прежние алиасы
       * @minLength 1
       * @maxLength 128
       * @pattern ^[A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*$
       */
      key?: string;
      reservedKeys?: string[];
      /**
       * @exclusiveMin 0
       * @max 9007199254740991
       */
      revision: number;
      fields:
        | {
            kind: "passport";
            /**
             * Однострочное название
             * @minLength 1
             */
            name: string;
            /** Краткое многострочное описание обычным текстом */
            summary: string;
            /** Полное описание в Markdown */
            description: string;
          }
        | {
            kind: "feature";
            /**
             * Однострочное название
             * @minLength 1
             */
            name: string;
            /** Краткое многострочное описание обычным текстом */
            summary: string;
            /** Полное описание в Markdown */
            description: string;
          }
        | {
            kind: "scenario";
            /**
             * Постоянный ID родительской фичи
             * @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$
             */
            featureId: string;
            /**
             * Однострочное название сценария
             * @minLength 1
             */
            name: string;
            /** Описание поведения сценария в Markdown */
            description: string;
          }
        | {
            kind: "application";
            /**
             * Однострочное название
             * @minLength 1
             */
            name: string;
            /** Краткое многострочное описание обычным текстом */
            summary: string;
            /** Полное описание в Markdown */
            description: string;
            /**
             * Неизменяемый уникальный адрес приложения и доски: 1–64 символа, строчные латинские буквы, цифры и дефисы; product, infrastructure и new зарезервированы
             * @minLength 1
             * @maxLength 64
             * @pattern ^[a-z0-9]+(?:-[a-z0-9]+)*$
             */
            slug: string;
            /**
             * Префикс задач доски; при создании по умолчанию из slug, затем неизменяем
             * @pattern ^[A-Z][A-Z0-9]{1,15}$
             */
            prefix?: string;
            /** Назначение приложения: клиентское, серверное или внутреннее */
            type: ProductContextTypeEnum;
          }
        | {
            kind: "scope";
            /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
            applicationId: string;
            /** @maxItems 10000 */
            contracts: {
              /**
               * Постоянный ID общей фичи
               * @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$
               */
              featureId: string;
              /** Постоянный ID сценария; null для общего вклада в фичу */
              scenarioId: string | null;
              /**
               * Однострочное название реализации
               * @minLength 1
               */
              title: string;
              /** Описание вклада приложения в Markdown */
              description: string;
              /** Состояние вклада приложения */
              status: ProductContextStatusEnum;
              /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
              id: string;
              /**
               * Читаемый ключ сущности; назначается ядром, может иметь прежние алиасы
               * @minLength 1
               * @maxLength 128
               * @pattern ^[A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*$
               */
              key?: string;
              /**
               * @exclusiveMin 0
               * @max 9007199254740991
               */
              revision?: number;
              active: boolean;
              basis: string;
            }[];
          }
        | {
            kind: "document";
            /**
             * Однострочное название документа
             * @minLength 1
             */
            name: string;
            /** Краткое обычное описание документа */
            summary: string;
            /** Полный текст документа в Markdown */
            body: string;
            /** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
            documentKind: ProductContextDocumentKindEnum;
            /** Раздел; null — без раздела */
            sectionId?: string | null;
            /** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
            documentStatus?: ProductContextDocumentStatusEnum;
            /** Закреплён для всех участников проекта */
            pinned?: boolean;
            /**
             * Адресные связи документа; текст и связи сохраняются атомарно
             * @maxItems 100
             */
            relations?: {
              /** Связанная сущность проекта: вид и постоянный ID */
              target: {
                /** Вид связанной сущности проекта */
                kind: ProductContextKindEnum;
                /**
                 * Постоянный ID связанной сущности
                 * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
                 */
                id: string;
              };
              /** references — контекст для сущности; documents — документ описывает сущность */
              type: ProductContextTypeEnum1;
              /** Когда и зачем читать документ: необязательное пояснение Markdown */
              description: string;
            }[];
            /**
             * Явные области применимости документа по постоянным ID
             * @maxItems 1000
             */
            links: (
              | {
                  kind: "product";
                }
              | {
                  kind: "feature";
                  /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
                  id: string;
                }
              | {
                  kind: "scenario";
                  /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
                  id: string;
                }
              | {
                  kind: "application";
                  /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
                  id: string;
                }
              | {
                  kind: "implementation";
                  /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
                  applicationId: string;
                  /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
                  id: string;
                }
            )[];
          };
      /**
       * Момент времени в UTC
       * @format date-time
       * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
       */
      createdAt: string;
      /**
       * Момент времени в UTC
       * @format date-time
       * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
       */
      updatedAt: string;
      /**
       * Автор операции
       * @maxLength 128
       */
      createdBy: string;
      /**
       * Автор операции
       * @maxLength 128
       */
      updatedBy: string;
    };
    reasons: string[];
  }[];
  readiness: {
    /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
    id: string;
    status: ProductContextStatusEnum1;
    /**
     * @min 0
     * @max 9007199254740991
     */
    participants: number;
    /**
     * @min 0
     * @max 9007199254740991
     */
    completed: number;
    /**
     * @min 0
     * @max 9007199254740991
     */
    stale: number;
  }[];
}

export interface ProductContextQuery {
  /**
   * Постоянный ID или читаемый ключ сущности в выбранном проекте
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  id?: string;
  /**
   * Постоянный ID или читаемый ключ сущности в выбранном проекте
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  applicationId?: string;
}

export interface ProductOverview {
  productId: string;
  version: string;
  items: {
    /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
    id: string;
    /**
     * Читаемый ключ сущности; назначается ядром, может иметь прежние алиасы
     * @minLength 1
     * @maxLength 128
     * @pattern ^[A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*$
     */
    key?: string;
    revision: number;
    kind: string;
    name: string;
    summary: string;
  }[];
  readiness: {
    /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
    id: string;
    status: ProductOverviewStatusEnum;
    /**
     * @min 0
     * @max 9007199254740991
     */
    participants: number;
    /**
     * @min 0
     * @max 9007199254740991
     */
    completed: number;
    /**
     * @min 0
     * @max 9007199254740991
     */
    stale: number;
  }[];
}

export interface ProductList {
  version: string;
  total: number;
  items: {
    version: 1;
    /** @minLength 1 */
    productId: string;
    /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
    id: string;
    /**
     * Читаемый ключ сущности; назначается ядром, может иметь прежние алиасы
     * @minLength 1
     * @maxLength 128
     * @pattern ^[A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*$
     */
    key?: string;
    reservedKeys?: string[];
    /**
     * @exclusiveMin 0
     * @max 9007199254740991
     */
    revision: number;
    fields:
      | {
          kind: "passport";
          /**
           * Однострочное название
           * @minLength 1
           */
          name: string;
          /** Краткое многострочное описание обычным текстом */
          summary: string;
          /** Полное описание в Markdown */
          description: string;
        }
      | {
          kind: "feature";
          /**
           * Однострочное название
           * @minLength 1
           */
          name: string;
          /** Краткое многострочное описание обычным текстом */
          summary: string;
          /** Полное описание в Markdown */
          description: string;
        }
      | {
          kind: "scenario";
          /**
           * Постоянный ID родительской фичи
           * @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$
           */
          featureId: string;
          /**
           * Однострочное название сценария
           * @minLength 1
           */
          name: string;
          /** Описание поведения сценария в Markdown */
          description: string;
        }
      | {
          kind: "application";
          /**
           * Однострочное название
           * @minLength 1
           */
          name: string;
          /** Краткое многострочное описание обычным текстом */
          summary: string;
          /** Полное описание в Markdown */
          description: string;
          /**
           * Неизменяемый уникальный адрес приложения и доски: 1–64 символа, строчные латинские буквы, цифры и дефисы; product, infrastructure и new зарезервированы
           * @minLength 1
           * @maxLength 64
           * @pattern ^[a-z0-9]+(?:-[a-z0-9]+)*$
           */
          slug: string;
          /**
           * Префикс задач доски; при создании по умолчанию из slug, затем неизменяем
           * @pattern ^[A-Z][A-Z0-9]{1,15}$
           */
          prefix?: string;
          /** Назначение приложения: клиентское, серверное или внутреннее */
          type: ProductListTypeEnum;
        }
      | {
          kind: "scope";
          /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
          applicationId: string;
          /** @maxItems 10000 */
          contracts: {
            /**
             * Постоянный ID общей фичи
             * @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$
             */
            featureId: string;
            /** Постоянный ID сценария; null для общего вклада в фичу */
            scenarioId: string | null;
            /**
             * Однострочное название реализации
             * @minLength 1
             */
            title: string;
            /** Описание вклада приложения в Markdown */
            description: string;
            /** Состояние вклада приложения */
            status: ProductListStatusEnum;
            /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
            id: string;
            /**
             * Читаемый ключ сущности; назначается ядром, может иметь прежние алиасы
             * @minLength 1
             * @maxLength 128
             * @pattern ^[A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*$
             */
            key?: string;
            /**
             * @exclusiveMin 0
             * @max 9007199254740991
             */
            revision?: number;
            active: boolean;
            basis: string;
          }[];
        }
      | {
          kind: "document";
          /**
           * Однострочное название документа
           * @minLength 1
           */
          name: string;
          /** Краткое обычное описание документа */
          summary: string;
          /** Полный текст документа в Markdown */
          body: string;
          /** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
          documentKind: ProductListDocumentKindEnum;
          /** Раздел; null — без раздела */
          sectionId?: string | null;
          /** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
          documentStatus?: ProductListDocumentStatusEnum;
          /** Закреплён для всех участников проекта */
          pinned?: boolean;
          /**
           * Адресные связи документа; текст и связи сохраняются атомарно
           * @maxItems 100
           */
          relations?: {
            /** Связанная сущность проекта: вид и постоянный ID */
            target: {
              /** Вид связанной сущности проекта */
              kind: ProductListKindEnum;
              /**
               * Постоянный ID связанной сущности
               * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
               */
              id: string;
            };
            /** references — контекст для сущности; documents — документ описывает сущность */
            type: ProductListTypeEnum1;
            /** Когда и зачем читать документ: необязательное пояснение Markdown */
            description: string;
          }[];
          /**
           * Явные области применимости документа по постоянным ID
           * @maxItems 1000
           */
          links: (
            | {
                kind: "product";
              }
            | {
                kind: "feature";
                /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
                id: string;
              }
            | {
                kind: "scenario";
                /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
                id: string;
              }
            | {
                kind: "application";
                /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
                id: string;
              }
            | {
                kind: "implementation";
                /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
                applicationId: string;
                /** @pattern ^(?:[A-Za-z0-9]{8}|passport|(?:feature|scenario|application|scope|document|contract)_[a-f0-9]{32})$ */
                id: string;
              }
          )[];
        };
    /**
     * Момент времени в UTC
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    createdAt: string;
    /**
     * Момент времени в UTC
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    updatedAt: string;
    /**
     * Автор операции
     * @maxLength 128
     */
    createdBy: string;
    /**
     * Автор операции
     * @maxLength 128
     */
    updatedBy: string;
  }[];
  nextOffset: number | null;
}

export interface ProductListQuery {
  kind?: ProductListQueryKindEnum;
  /** @maxLength 4096 */
  q?: string;
  /**
   * Постоянный ID или читаемый ключ сущности в выбранном проекте
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  id?: string;
  /**
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset: number;
  /**
   * @min 1
   * @max 100
   * @default 30
   */
  limit: number;
}

export interface ValidationData {
  valid: boolean;
  entities: number;
  tasks: number;
  boards: number;
}

export type ServerEvent =
  | {
      type: "connected";
      data: {
        projectId: string;
      };
    }
  | {
      type: "heartbeat";
      data: {
        timestamp: string;
      };
    }
  | {
      type: "changed";
      data: {
        source: ServerEventSourceEnum;
        version?: string;
      };
    }
  | {
      type: "workspace-error";
      data: {
        code: string;
        message: string;
      };
    };

export interface HealthResponse {
  status: "ok";
  stage: HealthResponseStageEnum;
  contractVersion: 1;
}

export interface ContextResponse {
  capabilities?: string[];
  project: string;
  /** Стабильный идентификатор пути конфигурации проекта */
  projectId: string;
  configPath: string;
  storagePath: string;
  /** Автор изменений, заданный при запуске сервера */
  actor: string;
  config: {
    version: 1;
    /** @default "local" */
    mode: "local";
    projectId?: string;
    projectSettings?: {
      /**
       * Отображаемое имя проекта, от 1 до 120 символов без переносов строк
       * @minLength 1
       * @maxLength 120
       * @pattern ^[^\p{Cc}]+$
       */
      name: string;
      /**
       * Slug адреса: 2–64 строчные латинские буквы/цифры с одиночными дефисами между частями
       * @minLength 2
       * @maxLength 64
       * @pattern ^[a-z0-9]+(?:-[a-z0-9]+)*$
       */
      slug: string;
      /**
       * Разделы библиотеки в порядке отображения; пустой список допустим
       * @maxItems 100
       */
      documentSections?: {
        /**
         * Постоянный ID раздела библиотеки
         * @pattern ^[a-zA-Z0-9_-]{1,64}$
         */
        id: string;
        /**
         * Название раздела библиотеки
         * @minLength 1
         */
        name: string;
      }[];
      /**
       * Ревизия настроек проекта для защиты от одновременного редактирования
       * @min 0
       * @max 9007199254740991
       */
      revision: number;
      version: 1 | 2 | 3;
      /**
       * Читаемый ключ сущности; назначается ядром, может иметь прежние алиасы
       * @minLength 1
       * @maxLength 128
       * @pattern ^[A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*$
       */
      entityKey?: string;
      aliases?: string[];
      requests?: Partial<
        Record<
          string,
          {
            hash: string;
            result: {
              id: string;
              key: string;
              /**
               * @min 0
               * @max 9007199254740991
               */
              revision: number;
            };
          }
        >
      >;
      events?: {
        /**
         * Ревизия записи
         * @exclusiveMin 0
         * @max 9007199254740991
         */
        revision: number;
        /**
         * Автор операции
         * @maxLength 128
         */
        actor: string;
        /**
         * Момент времени в UTC
         * @format date-time
         * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
         */
        at: string;
        /** Действие над записью */
        action: string;
      }[];
    };
    /** @minLength 1 */
    storageDir: string;
    /** @minLength 1 */
    defaultStatus: string;
    /** @minItems 1 */
    readyStatuses: string[];
    statuses: Partial<
      Record<
        string,
        {
          /** @default false */
          terminal: boolean;
          /** @default false */
          satisfiesDependencies: boolean;
          color?: ContextResponseColorEnum;
        }
      >
    >;
    mcp?: {
      /**
       * @min 0
       * @max 65535
       * @default 4710
       */
      port: number;
    };
    /** @default {"port":4700} */
    server: {
      /**
       * @min 0
       * @max 65535
       * @default 4700
       */
      port: number;
      /** @format uri */
      url?: string;
    };
    /** @default {"format":"text","defaultLimit":20,"maxBytes":16384} */
    output: {
      /** @default "text" */
      format: ContextResponseFormatEnum;
      /**
       * @min 1
       * @max 100
       * @default 20
       */
      defaultLimit: number;
      /**
       * @min 1024
       * @max 134217728
       * @default 16384
       */
      maxBytes: number;
    };
  };
}

export interface BoardInfo {
  /** Версия дискового формата доски; v2 сохраняет адреса и историю */
  version: 1 | 2;
  /**
   * Постоянный ID доски
   * @pattern ^(?:[A-Za-z0-9]{8}|board_(?:product|infrastructure|[a-f0-9]{32}))$
   */
  id: string;
  /**
   * Адрес доски: 1–64 символа, строчные латинские буквы, цифры и дефисы
   * @minLength 1
   * @maxLength 64
   * @pattern ^[a-z0-9]+(?:-[a-z0-9]+)*$
   */
  slug: string;
  /**
   * Неизменяемый уникальный префикс ключей задач, например WEB или PRODUCT
   * @pattern ^[A-Z][A-Z0-9]{1,15}$
   */
  prefix: string;
  /**
   * Читаемый ключ сущности; назначается ядром, может иметь прежние алиасы
   * @minLength 1
   * @maxLength 128
   * @pattern ^[A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*$
   */
  key?: string;
  /** Область ответственности доски */
  kind: BoardInfoKindEnum;
  /** ID приложения; null у системных досок */
  applicationId: string | null;
  /**
   * Ревизия записи доски
   * @exclusiveMin 0
   * @max 9007199254740991
   */
  revision: number;
  /**
   * Дата создания доски
   * @format date-time
   * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
   */
  createdAt: string;
  /**
   * Автор создания доски
   * @maxLength 128
   */
  createdBy: string;
  /** Название доски; для приложения — его актуальное название */
  name: string;
}

export interface BoardsQuery {
  /**
   * Смещение в каталоге досок
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset: number;
  /**
   * Размер страницы, не более 100 досок
   * @min 1
   * @max 100
   * @default 50
   */
  limit: number;
  /** Версия каталога первой страницы; защищает продолжение от изменений */
  version?: string;
}

export interface BoardsPage {
  /** Доски: продукт, приложения по времени создания, инфраструктура */
  items: {
    /** Версия дискового формата доски; v2 сохраняет адреса и историю */
    version: 1 | 2;
    /**
     * Постоянный ID доски
     * @pattern ^(?:[A-Za-z0-9]{8}|board_(?:product|infrastructure|[a-f0-9]{32}))$
     */
    id: string;
    /**
     * Адрес доски: 1–64 символа, строчные латинские буквы, цифры и дефисы
     * @minLength 1
     * @maxLength 64
     * @pattern ^[a-z0-9]+(?:-[a-z0-9]+)*$
     */
    slug: string;
    /**
     * Неизменяемый уникальный префикс ключей задач, например WEB или PRODUCT
     * @pattern ^[A-Z][A-Z0-9]{1,15}$
     */
    prefix: string;
    /**
     * Читаемый ключ сущности; назначается ядром, может иметь прежние алиасы
     * @minLength 1
     * @maxLength 128
     * @pattern ^[A-Z][A-Z0-9]*(?:-[A-Z0-9]+)*$
     */
    key?: string;
    /** Область ответственности доски */
    kind: BoardsPageKindEnum;
    /** ID приложения; null у системных досок */
    applicationId: string | null;
    /**
     * Ревизия записи доски
     * @exclusiveMin 0
     * @max 9007199254740991
     */
    revision: number;
    /**
     * Дата создания доски
     * @format date-time
     * @pattern ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z))$
     */
    createdAt: string;
    /**
     * Автор создания доски
     * @maxLength 128
     */
    createdBy: string;
    /** Название доски; для приложения — его актуальное название */
    name: string;
  }[];
  /**
   * Полное число досок проекта
   * @min 0
   * @max 9007199254740991
   */
  total: number;
  /** Смещение продолжения; null в конце */
  nextOffset: number | null;
  /** Версия согласованного каталога */
  version: string;
}

export interface PageMeta {
  hasMore: boolean;
  nextCursor: string | null;
  truncated?: boolean;
}

export interface ApiFailure {
  ok: false;
  error: {
    code: string;
    message: string;
    details?: any;
    /**
     * @min 1
     * @max 255
     */
    exitCode?: number;
  };
}

/** Фильтр состояния плана */
export type PlansQueryStatusEnum =
  | "draft"
  | "active"
  | "completed"
  | "cancelled";

/**
 * Оставить свободные задачи и текущий состав выбранного этапа
 * @default "true"
 */
export type PlanningCandidatesQueryAvailableOnlyEnum = "true" | "false";

/** Цель реализации: общая фича, сценарий или контракт приложения */
export type PlanningCandidatesPageKindEnum =
  | "feature"
  | "scenario"
  | "implementation";

/** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
export type PlanningCandidatesPageColumnEnum =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** Состояние плана: черновик, в работе, завершён или отменён */
export type PlanningCandidatesPageStatusEnum =
  | "draft"
  | "active"
  | "completed"
  | "cancelled";

/** Вид области воздействия; не является целью реализации задачи */
export type PlansPageKindEnum =
  | "project"
  | "product"
  | "application"
  | "feature"
  | "scenario"
  | "implementation";

/** Состояние плана: черновик, в работе, завершён или отменён */
export type PlansPageStatusEnum =
  | "draft"
  | "active"
  | "completed"
  | "cancelled";

/** Вид области воздействия; не является целью реализации задачи */
export type PlanSummaryKindEnum =
  | "project"
  | "product"
  | "application"
  | "feature"
  | "scenario"
  | "implementation";

/** Состояние плана: черновик, в работе, завершён или отменён */
export type PlanSummaryStatusEnum =
  | "draft"
  | "active"
  | "completed"
  | "cancelled";

/** Цель реализации: общая фича, сценарий или контракт приложения */
export type PlanningTasksPageKindEnum =
  | "feature"
  | "scenario"
  | "implementation";

/** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
export type PlanningTasksPageColumnEnum =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** Состояние плана: черновик, в работе, завершён или отменён */
export type PlanMembershipsStatusEnum =
  | "draft"
  | "active"
  | "completed"
  | "cancelled";

/** Вид области воздействия; не является целью реализации задачи */
export type CreatePlanKindEnum =
  | "project"
  | "product"
  | "application"
  | "feature"
  | "scenario"
  | "implementation";

/** Вид области воздействия; не является целью реализации задачи */
export type UpdatePlanKindEnum =
  | "project"
  | "product"
  | "application"
  | "feature"
  | "scenario"
  | "implementation";

/** Начать, завершить либо отменить план */
export type TransitionPlanActionEnum = "start" | "complete" | "cancel";

/** Создать, изменить, удалить пустой этап или переместить */
export type ChangePlanStageActionEnum = "create" | "update" | "remove" | "move";

/** Переместить на одну позицию полного списка; взаимоисключается с before */
export type ChangePlanStageDirectionEnum = "up" | "down";

/** Собственное состояние: запланирован, выпущен или отменён */
export type ReleasesQueryStatusEnum = "planned" | "released" | "cancelled";

/** Собственное состояние: запланирован, выпущен или отменён */
export type ReleasesPageStatusEnum = "planned" | "released" | "cancelled";

/** Собственное состояние: запланирован, выпущен или отменён */
export type ReleaseSummaryStatusEnum = "planned" | "released" | "cancelled";

/**
 * Собственное состояние: запланирован, выпущен или отменён
 * @default "planned"
 */
export type SaveReleaseStatusEnum = "planned" | "released" | "cancelled";

/** Явно выбранное состояние; отсутствие не перепланирует отменённый релиз */
export type UpdateReleaseStatusEnum = "planned" | "released" | "cancelled";

/** Перепланировать, отменить либо явно зафиксировать выпуск */
export type ReleaseActionActionEnum = "plan" | "cancel" | "release";

/** Вид области воздействия; не является целью реализации задачи */
export type ReleaseCompositionKindEnum =
  | "project"
  | "product"
  | "application"
  | "feature"
  | "scenario"
  | "implementation";

/** Состояние плана: черновик, в работе, завершён или отменён */
export type ReleaseCompositionStatusEnum =
  | "draft"
  | "active"
  | "completed"
  | "cancelled";

/** Вид предметного прогресса */
export type WorkPlanProgressKindEnum =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Машинный код причины */
export type WorkPlanProgressCodeEnum =
  | "NOT_DONE"
  | "CRITERION_INCOMPLETE"
  | "CHILD_INCOMPLETE"
  | "DEPENDENCY_INCOMPLETE"
  | "COMPONENT_INCOMPLETE"
  | "NO_WORK"
  | "INACTIVE"
  | "PLAN_NOT_COMPLETED"
  | "PLAN_CANCELLED"
  | "STATE_DIVERGED"
  | "MISSING_PLAN";

/** Вид предметного прогресса */
export type WorkPlanProgressKindEnum1 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Состояние плана: черновик, в работе, завершён или отменён */
export type WorkPlanProgressStatusEnum =
  | "draft"
  | "active"
  | "completed"
  | "cancelled";

/** Вид предметного прогресса */
export type WorkPlanProgressKindEnum2 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Сохранённая колонка; не заменяет фактическое выполнение */
export type WorkPlanProgressColumnEnum =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** Вид предметного прогресса */
export type ReleaseProgressKindEnum =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Машинный код причины */
export type ReleaseProgressCodeEnum =
  | "NOT_DONE"
  | "CRITERION_INCOMPLETE"
  | "CHILD_INCOMPLETE"
  | "DEPENDENCY_INCOMPLETE"
  | "COMPONENT_INCOMPLETE"
  | "NO_WORK"
  | "INACTIVE"
  | "PLAN_NOT_COMPLETED"
  | "PLAN_CANCELLED"
  | "STATE_DIVERGED"
  | "MISSING_PLAN";

/** Вид предметного прогресса */
export type ReleaseProgressKindEnum1 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Собственное состояние: запланирован, выпущен или отменён */
export type ReleaseProgressStatusEnum = "planned" | "released" | "cancelled";

/** Вид предметного прогресса */
export type ReleaseProgressKindEnum2 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Состояние плана: черновик, в работе, завершён или отменён */
export type ReleaseProgressStatusEnum1 =
  | "draft"
  | "active"
  | "completed"
  | "cancelled";

/** Состояние плана: черновик, в работе, завершён или отменён */
export type TaskProgressStatusEnum =
  | "draft"
  | "active"
  | "completed"
  | "cancelled";

/** Вид предметного прогресса */
export type TaskProgressKindEnum =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Машинный код причины */
export type TaskProgressCodeEnum =
  | "NOT_DONE"
  | "CRITERION_INCOMPLETE"
  | "CHILD_INCOMPLETE"
  | "DEPENDENCY_INCOMPLETE"
  | "COMPONENT_INCOMPLETE"
  | "NO_WORK"
  | "INACTIVE"
  | "PLAN_NOT_COMPLETED"
  | "PLAN_CANCELLED"
  | "STATE_DIVERGED"
  | "MISSING_PLAN";

/** Вид предметного прогресса */
export type TaskProgressKindEnum1 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
export type TaskProgressColumnEnum =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** Вид предметного прогресса */
export type TaskProgressKindEnum2 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Сохранённая колонка; не заменяет фактическое выполнение */
export type TaskProgressColumnEnum1 =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** Вид предметного прогресса */
export type TaskProgressKindEnum3 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Сохранённая колонка; не заменяет фактическое выполнение */
export type TaskProgressColumnEnum2 =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** Вид предметного прогресса */
export type ImplementationProgressKindEnum =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Машинный код причины */
export type ImplementationProgressCodeEnum =
  | "NOT_DONE"
  | "CRITERION_INCOMPLETE"
  | "CHILD_INCOMPLETE"
  | "DEPENDENCY_INCOMPLETE"
  | "COMPONENT_INCOMPLETE"
  | "NO_WORK"
  | "INACTIVE"
  | "PLAN_NOT_COMPLETED"
  | "PLAN_CANCELLED"
  | "STATE_DIVERGED"
  | "MISSING_PLAN";

/** Вид предметного прогресса */
export type ImplementationProgressKindEnum1 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Вклад в фичу либо сценарий */
export type ImplementationProgressImplementationKindEnum = "FI" | "SI";

/** Вид предметного прогресса */
export type ImplementationProgressKindEnum2 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Вид предметного прогресса */
export type ImplementationProgressKindEnum3 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Вид предметного прогресса */
export type ImplementationProgressKindEnum4 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Сохранённая колонка; не заменяет фактическое выполнение */
export type ImplementationProgressColumnEnum =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** Вид предметного прогресса */
export type ImplementationProgressKindEnum5 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Вид предметного прогресса */
export type ScenarioProgressKindEnum =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Машинный код причины */
export type ScenarioProgressCodeEnum =
  | "NOT_DONE"
  | "CRITERION_INCOMPLETE"
  | "CHILD_INCOMPLETE"
  | "DEPENDENCY_INCOMPLETE"
  | "COMPONENT_INCOMPLETE"
  | "NO_WORK"
  | "INACTIVE"
  | "PLAN_NOT_COMPLETED"
  | "PLAN_CANCELLED"
  | "STATE_DIVERGED"
  | "MISSING_PLAN";

/** Вид предметного прогресса */
export type ScenarioProgressKindEnum1 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Вид предметного прогресса */
export type ScenarioProgressKindEnum2 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Сохранённая колонка; не заменяет фактическое выполнение */
export type ScenarioProgressColumnEnum =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** Вид предметного прогресса */
export type ScenarioProgressKindEnum3 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Вид предметного прогресса */
export type FeatureProgressKindEnum =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Машинный код причины */
export type FeatureProgressCodeEnum =
  | "NOT_DONE"
  | "CRITERION_INCOMPLETE"
  | "CHILD_INCOMPLETE"
  | "DEPENDENCY_INCOMPLETE"
  | "COMPONENT_INCOMPLETE"
  | "NO_WORK"
  | "INACTIVE"
  | "PLAN_NOT_COMPLETED"
  | "PLAN_CANCELLED"
  | "STATE_DIVERGED"
  | "MISSING_PLAN";

/** Вид предметного прогресса */
export type FeatureProgressKindEnum1 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Вид предметного прогресса */
export type FeatureProgressKindEnum2 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Сохранённая колонка; не заменяет фактическое выполнение */
export type FeatureProgressColumnEnum =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** Вид предметного прогресса */
export type FeatureProgressKindEnum3 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Вид предметного прогресса */
export type FeatureProgressKindEnum4 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Вид предметного прогресса */
export type ApplicationProgressKindEnum =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Машинный код причины */
export type ApplicationProgressCodeEnum =
  | "NOT_DONE"
  | "CRITERION_INCOMPLETE"
  | "CHILD_INCOMPLETE"
  | "DEPENDENCY_INCOMPLETE"
  | "COMPONENT_INCOMPLETE"
  | "NO_WORK"
  | "INACTIVE"
  | "PLAN_NOT_COMPLETED"
  | "PLAN_CANCELLED"
  | "STATE_DIVERGED"
  | "MISSING_PLAN";

/** Вид предметного прогресса */
export type ApplicationProgressKindEnum1 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Вид предметного прогресса */
export type ApplicationProgressKindEnum2 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Вид предметного прогресса */
export type ProductProgressKindEnum =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Машинный код причины */
export type ProductProgressCodeEnum =
  | "NOT_DONE"
  | "CRITERION_INCOMPLETE"
  | "CHILD_INCOMPLETE"
  | "DEPENDENCY_INCOMPLETE"
  | "COMPONENT_INCOMPLETE"
  | "NO_WORK"
  | "INACTIVE"
  | "PLAN_NOT_COMPLETED"
  | "PLAN_CANCELLED"
  | "STATE_DIVERGED"
  | "MISSING_PLAN";

/** Вид предметного прогресса */
export type ProductProgressKindEnum1 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Вид предметного прогресса */
export type ProductProgressKindEnum2 =
  | "task"
  | "implementation"
  | "scenario"
  | "feature"
  | "application"
  | "product"
  | "work-plan"
  | "release";

/** Вид удаляемой сущности; проект, паспорт и системные доски не удаляются */
export type EntityDeletionQueryKindEnum =
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "task"
  | "document";

/** Вид основной сущности */
export type EntityDeletionPreviewKindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
export type EntityDeletionPreviewKindEnum1 =
  | "specification"
  | "description"
  | "rules"
  | "instruction"
  | "proposal"
  | "decision"
  | "research";

/** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
export type EntityDeletionPreviewStatusEnum = "draft" | "active" | "archived";

/** Вид основной сущности */
export type EntityDeletionPreviewKindEnum2 =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
export type EntityDeletionPreviewKindEnum3 =
  | "specification"
  | "description"
  | "rules"
  | "instruction"
  | "proposal"
  | "decision"
  | "research";

/** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
export type EntityDeletionPreviewStatusEnum1 = "draft" | "active" | "archived";

/** Вид основной сущности */
export type EntityDeletionPreviewKindEnum4 =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
export type EntityDeletionPreviewKindEnum5 =
  | "specification"
  | "description"
  | "rules"
  | "instruction"
  | "proposal"
  | "decision"
  | "research";

/** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
export type EntityDeletionPreviewStatusEnum2 = "draft" | "active" | "archived";

/** Вид удаляемой сущности; проект, паспорт и системные доски не удаляются */
export type DeleteEntityKindEnum =
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "task"
  | "document";

/** Вид основной сущности */
export type EntityDeletedKindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Роль автора: оператор, оркестратор или воркер */
export type TaskActivityPageActorRoleEnum =
  | "operator"
  | "orchestrator"
  | "worker";

/** Роль автора: оператор, оркестратор или воркер */
export type TaskHistoryEventActorRoleEnum =
  | "operator"
  | "orchestrator"
  | "worker";

/** Обычный текст или Markdown */
export type TaskHistoryEventFormatEnum = "text" | "markdown";

/** Роль автора: оператор, оркестратор или воркер */
export type PublishTaskCommentActorRoleEnum =
  | "operator"
  | "orchestrator"
  | "worker";

/** Вид основной сущности */
export type EntityTypeQueryKindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Вид основной сущности */
export type EntityTypesKindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Вид основной сущности */
export type EntityTypeDetailKindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Вид основной сущности */
export type EntitiesQueryKindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Активность реализации; прежние ссылки доступны без фильтра */
export type EntitiesQueryActiveEnum = "true" | "false";

/** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
export type EntitiesQueryDocumentKindEnum =
  | "specification"
  | "description"
  | "rules"
  | "instruction"
  | "proposal"
  | "decision"
  | "research";

/** Только закреплённые либо незакреплённые документы */
export type EntitiesQueryPinnedEnum = "true" | "false";

/** Включить только архив либо исключить архивные документы */
export type EntitiesQueryArchivedEnum = "true" | "false";

/**
 * Сортировка по ключу, названию или последнему обновлению
 * @default "key"
 */
export type EntitiesQuerySortEnum = "key" | "title" | "updated";

/** Вид основной сущности */
export type EntitiesPageKindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
export type EntitiesPageKindEnum1 =
  | "specification"
  | "description"
  | "rules"
  | "instruction"
  | "proposal"
  | "decision"
  | "research";

/** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
export type EntitiesPageStatusEnum = "draft" | "active" | "archived";

/** Уточнение ожидаемого вида при разрешении адреса */
export type EntityGetQueryKindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Вид основной сущности */
export type EntitySummaryKindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
export type EntitySummaryKindEnum1 =
  | "specification"
  | "description"
  | "rules"
  | "instruction"
  | "proposal"
  | "decision"
  | "research";

/** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
export type EntitySummaryStatusEnum = "draft" | "active" | "archived";

/** Вид основной сущности */
export type EntityDetailKindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
export type EntityDetailKindEnum1 =
  | "specification"
  | "description"
  | "rules"
  | "instruction"
  | "proposal"
  | "decision"
  | "research";

/** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
export type EntityDetailStatusEnum = "draft" | "active" | "archived";

/** Вид области воздействия; не является целью реализации задачи */
export type EntityDetailKindEnum2 =
  | "project"
  | "product"
  | "application"
  | "feature"
  | "scenario"
  | "implementation";

/** Состояние плана: черновик, в работе, завершён или отменён */
export type EntityDetailStatusEnum1 =
  | "draft"
  | "active"
  | "completed"
  | "cancelled";

/** Собственное состояние: запланирован, выпущен или отменён */
export type EntityDetailStatusEnum2 = "planned" | "released" | "cancelled";

/** Назначение приложения: клиентское, серверное или внутреннее */
export type EntityDetailTypeEnum = "frontend" | "backend" | "internal";

/** Состояние вклада приложения */
export type EntityDetailStatusEnum3 = "none" | "partial" | "done";

/** Область доски: продукт, приложение или инфраструктура */
export type EntityDetailScopeEnum =
  | "product"
  | "application"
  | "infrastructure";

/** Цель реализации: общая фича, сценарий или контракт приложения */
export type EntityDetailKindEnum3 = "feature" | "scenario" | "implementation";

/** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
export type EntityDetailColumnEnum =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
export type EntityDetailDocumentKindEnum =
  | "specification"
  | "description"
  | "rules"
  | "instruction"
  | "proposal"
  | "decision"
  | "research";

/** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
export type EntityDetailDocumentStatusEnum = "draft" | "active" | "archived";

/** Вид связанной сущности проекта */
export type EntityDetailKindEnum4 =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** references — контекст для сущности; documents — документ описывает сущность */
export type EntityDetailTypeEnum1 = "references" | "documents";

/** Вид основной сущности */
export type EntityDetailKindEnum5 =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
export type EntityDetailKindEnum6 =
  | "specification"
  | "description"
  | "rules"
  | "instruction"
  | "proposal"
  | "decision"
  | "research";

/** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
export type EntityDetailStatusEnum4 = "draft" | "active" | "archived";

/** Уточнение ожидаемого вида при разрешении адреса */
export type EntityKeysQueryKindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Вид основной сущности */
export type EntityKeysPageKindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Вид основной сущности */
export type EntityKeySpacesQueryKindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Вид основной сущности */
export type EntityKeySpacesKindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Назначение приложения: клиентское, серверное или внутреннее */
export type CreateEntityTypeEnum = "frontend" | "backend" | "internal";

/**
 * Состояние вклада; done подтверждает актуальные требования
 * @default "none"
 */
export type CreateEntityStatusEnum = "none" | "partial" | "done";

/**
 * Колонка: inbox, ready, in-progress, review, done; cancelled — отмена
 * @default "inbox"
 */
export type CreateEntityColumnEnum =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
export type CreateEntityDocumentKindEnum =
  | "specification"
  | "description"
  | "rules"
  | "instruction"
  | "proposal"
  | "decision"
  | "research";

/** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
export type CreateEntityDocumentStatusEnum = "draft" | "active" | "archived";

/** Вид связанной сущности проекта */
export type CreateEntityKindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** references — контекст для сущности; documents — документ описывает сущность */
export type CreateEntityTypeEnum1 = "references" | "documents";

/** Назначение приложения: клиентское, серверное или внутреннее */
export type UpdateEntityTypeEnum = "frontend" | "backend" | "internal";

export type UpdateEntityStatusEnum = "none" | "partial" | "done";

/** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
export type UpdateEntityDocumentKindEnum =
  | "specification"
  | "description"
  | "rules"
  | "instruction"
  | "proposal"
  | "decision"
  | "research";

/** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
export type UpdateEntityDocumentStatusEnum = "draft" | "active" | "archived";

/** Вид связанной сущности проекта */
export type UpdateEntityKindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** references — контекст для сущности; documents — документ описывает сущность */
export type UpdateEntityTypeEnum1 = "references" | "documents";

/** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
export type MoveEntityTaskColumnEnum =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** Зависит от, связана с или родитель текущей задачи */
export type LinkEntityTaskRelationEnum = "depends-on" | "related" | "parent";

/** Вид основной сущности */
export type EntitySavedKindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Выполненное действие */
export type EntitySavedActionEnum =
  | "create"
  | "update"
  | "rename"
  | "move"
  | "link";

/**
 * Направление обхода относительно каждого узла
 * @default "both"
 */
export type GraphQueryDirectionEnum = "both" | "outgoing" | "incoming";

/**
 * Полный обход сохранённых связей; context — совместимое имя all без скрытых ограничений по видам
 * @default "all"
 */
export type GraphQueryProfileEnum = "all" | "context";

/** Выполненное действие */
export type GraphHistoryActionEnum = "add" | "update" | "remove";

/** Цель реализации: общая фича, сценарий или контракт приложения */
export type BoardTaskViewKindEnum = "feature" | "scenario" | "implementation";

/** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
export type BoardTaskViewColumnEnum =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** Выполненное действие */
export type BoardTaskSavedActionEnum =
  | "create"
  | "update"
  | "move"
  | "link"
  | "rename"
  | "criterion-add"
  | "criterion-update"
  | "criterion-complete"
  | "criterion-remove";

/** Цель реализации: общая фича, сценарий или контракт приложения */
export type BoardTaskSavedKindEnum = "feature" | "scenario" | "implementation";

/** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
export type BoardTaskSavedColumnEnum =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** Цель реализации: общая фича, сценарий или контракт приложения */
export type BoardTasksPageKindEnum = "feature" | "scenario" | "implementation";

/** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
export type BoardTasksPageColumnEnum =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** Направление связи относительно запрошенной задачи */
export type BoardTaskLinksPageRelationEnum =
  | "depends-on"
  | "blocks"
  | "related"
  | "parent"
  | "child";

/** Цель реализации: общая фича, сценарий или контракт приложения */
export type BoardTaskLinksPageKindEnum =
  | "feature"
  | "scenario"
  | "implementation";

/** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
export type BoardTaskLinksPageColumnEnum =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
export type BoardTasksQueryColumnEnum =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** unfinished исключает done и cancelled; finished возвращает только их; без параметра все задачи */
export type BoardTasksQueryCompletionEnum = "unfinished" | "finished";

/** title — поиск по ключам, ID и заголовку; all или отсутствие — также по Markdown */
export type BoardTasksQuerySearchInEnum = "title" | "all";

/** Заблокированные задачи либо готовые к выполнению для оркестратора */
export type BoardTasksQueryReadinessEnum = "blocked" | "ready";

/** Цель реализации: общая фича, сценарий или контракт приложения */
export type CreateBoardTaskKindEnum = "feature" | "scenario" | "implementation";

/**
 * Колонка: inbox, ready, in-progress, review, done; cancelled — отмена
 * @default "inbox"
 */
export type CreateBoardTaskColumnEnum =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** Цель реализации: общая фича, сценарий или контракт приложения */
export type UpdateBoardTaskKindEnum = "feature" | "scenario" | "implementation";

/** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
export type MoveBoardTaskColumnEnum =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** Зависит от, связана с или родитель текущей задачи */
export type LinkBoardTaskRelationEnum = "depends-on" | "related" | "parent";

export type ServerContextResponseModeEnum = "local" | "workspace";

/** Назначение приложения: клиентское, серверное или внутреннее */
export type ProductStateTypeEnum = "frontend" | "backend" | "internal";

/** Состояние вклада приложения */
export type ProductStateStatusEnum = "none" | "partial" | "done";

/** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
export type ProductStateDocumentKindEnum =
  | "specification"
  | "description"
  | "rules"
  | "instruction"
  | "proposal"
  | "decision"
  | "research";

/** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
export type ProductStateDocumentStatusEnum = "draft" | "active" | "archived";

/** Вид связанной сущности проекта */
export type ProductStateKindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** references — контекст для сущности; documents — документ описывает сущность */
export type ProductStateTypeEnum1 = "references" | "documents";

export type ProductStateStatusEnum1 = "none" | "partial" | "done";

export type ProductEntitiesKindEnum =
  | "passport"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "document";

export type ProductEntitiesStatusEnum = "none" | "partial" | "done";

/** Тип цели */
export type ProductEntitiesQueryKindEnum =
  | "passport"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "document";

/** Только реализации фич или сценариев; фильтр применяется до пагинации */
export type ProductEntitiesQueryImplementationTargetEnum =
  | "feature"
  | "scenario";

/** Фильтр участия; прежние ссылки читаются без фильтра */
export type ProductEntitiesQueryActiveEnum = "true" | "false";

/** Назначение приложения: клиентское, серверное или внутреннее */
export type ProductEntityTypeEnum = "frontend" | "backend" | "internal";

/** Состояние вклада приложения */
export type ProductEntityStatusEnum = "none" | "partial" | "done";

/** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
export type ProductEntityDocumentKindEnum =
  | "specification"
  | "description"
  | "rules"
  | "instruction"
  | "proposal"
  | "decision"
  | "research";

/** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
export type ProductEntityDocumentStatusEnum = "draft" | "active" | "archived";

/** Вид связанной сущности проекта */
export type ProductEntityKindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** references — контекст для сущности; documents — документ описывает сущность */
export type ProductEntityTypeEnum1 = "references" | "documents";

/** Состояние вклада приложения */
export type ProductEntityStatusEnum1 = "none" | "partial" | "done";

/** Состояние реализации; done подтверждает актуальные требования */
export type UpdateImplementationStatusEnum = "none" | "partial" | "done";

export type ProductMutationActionEnum = "create" | "update";

/** Назначение приложения: клиентское, серверное или внутреннее */
export type ProductMutationTypeEnum = "frontend" | "backend" | "internal";

/** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
export type ProductMutationDocumentKindEnum =
  | "specification"
  | "description"
  | "rules"
  | "instruction"
  | "proposal"
  | "decision"
  | "research";

/** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
export type ProductMutationDocumentStatusEnum = "draft" | "active" | "archived";

/** Вид связанной сущности проекта */
export type ProductMutationKindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** references — контекст для сущности; documents — документ описывает сущность */
export type ProductMutationTypeEnum1 = "references" | "documents";

/** Состояние вклада приложения */
export type ProductMutationStatusEnum = "none" | "partial" | "done";

export type ProductMutationStatusEnum1 = "none" | "partial" | "done";

/** Состояние вклада приложения */
export type ProductMutationStatusEnum2 = "none" | "partial" | "done";

/** Назначение приложения: клиентское, серверное или внутреннее */
export type ProductContextTypeEnum = "frontend" | "backend" | "internal";

/** Состояние вклада приложения */
export type ProductContextStatusEnum = "none" | "partial" | "done";

/** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
export type ProductContextDocumentKindEnum =
  | "specification"
  | "description"
  | "rules"
  | "instruction"
  | "proposal"
  | "decision"
  | "research";

/** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
export type ProductContextDocumentStatusEnum = "draft" | "active" | "archived";

/** Вид связанной сущности проекта */
export type ProductContextKindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** references — контекст для сущности; documents — документ описывает сущность */
export type ProductContextTypeEnum1 = "references" | "documents";

export type ProductContextStatusEnum1 = "none" | "partial" | "done";

export type ProductOverviewStatusEnum = "none" | "partial" | "done";

/** Назначение приложения: клиентское, серверное или внутреннее */
export type ProductListTypeEnum = "frontend" | "backend" | "internal";

/** Состояние вклада приложения */
export type ProductListStatusEnum = "none" | "partial" | "done";

/** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
export type ProductListDocumentKindEnum =
  | "specification"
  | "description"
  | "rules"
  | "instruction"
  | "proposal"
  | "decision"
  | "research";

/** Состояние публикации: черновик, действующий или архив; не подтверждает истинность текста */
export type ProductListDocumentStatusEnum = "draft" | "active" | "archived";

/** Вид связанной сущности проекта */
export type ProductListKindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** references — контекст для сущности; documents — документ описывает сущность */
export type ProductListTypeEnum1 = "references" | "documents";

export type ProductListQueryKindEnum =
  | "passport"
  | "feature"
  | "scenario"
  | "application"
  | "scope"
  | "document";

export type ServerEventSourceEnum = "api" | "storage";

export type HealthResponseStageEnum = "scaffold" | "ready";

export type ContextResponseColorEnum =
  | "black"
  | "red"
  | "green"
  | "yellow"
  | "blue"
  | "magenta"
  | "cyan"
  | "white"
  | "gray"
  | "none";

/** @default "text" */
export type ContextResponseFormatEnum = "json" | "text";

/** Область ответственности доски */
export type BoardInfoKindEnum = "product" | "application" | "infrastructure";

/** Область ответственности доски */
export type BoardsPageKindEnum = "product" | "application" | "infrastructure";

export type GetHealthOkEnum = true;

export type GetServerContextOkEnum = true;

export type GetProjectsOkEnum = true;

export type RegisterProjectOkEnum = true;

export interface RegisterProjectParams {
  project: string;
}

export type UnregisterProjectOkEnum = true;

export interface UnregisterProjectParams {
  project: string;
}

export type GetContextOkEnum = true;

export type GetProjectSettingsOkEnum = true;

export type SaveProjectSettingsOkEnum = true;

export type ValidateProjectOkEnum = true;

export type GetProductEntitiesOkEnum = true;

export interface GetProductEntitiesParams {
  /**
   * Поиск по ключу или названию; точное совпадение первым
   * @maxLength 4096
   */
  q?: string;
  /** Тип цели */
  kind?: KindEnum;
  /**
   * ID или ключ приложения
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  application?: string;
  /** Только реализации фич или сценариев; фильтр применяется до пагинации */
  implementationTarget?: ImplementationTargetEnum;
  /**
   * До 100 ID или ключей для краткого чтения существующих связей
   * @maxItems 100
   */
  refs?: string[];
  /** Фильтр участия; прежние ссылки читаются без фильтра */
  active?: ActiveEnum;
  /**
   * Смещение страницы; продолжение возвращается в nextOffset
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы от 1 до 100
   * @min 1
   * @max 100
   * @default 30
   */
  limit?: number;
}

/** Тип цели */
export type KindEnum =
  | "passport"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "document";

/** Только реализации фич или сценариев; фильтр применяется до пагинации */
export type ImplementationTargetEnum = "feature" | "scenario";

/** Фильтр участия; прежние ссылки читаются без фильтра */
export type ActiveEnum = "true" | "false";

/** Тип цели */
export type GetProductEntitiesParams1KindEnum =
  | "passport"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "document";

/** Только реализации фич или сценариев; фильтр применяется до пагинации */
export type GetProductEntitiesParams1ImplementationTargetEnum =
  | "feature"
  | "scenario";

/** Фильтр участия; прежние ссылки читаются без фильтра */
export type GetProductEntitiesParams1ActiveEnum = "true" | "false";

export type GetProductEntityOkEnum = true;

export interface GetProductEntityParams {
  /**
   * Постоянный ID или читаемый ключ сущности в выбранном проекте
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
}

export type UpdateProductImplementationOkEnum = true;

export type GetProductStateOkEnum = true;

export type GetProductOverviewOkEnum = true;

export type GetProductRecordsOkEnum = true;

export interface GetProductRecordsParams {
  kind?: KindEnum1;
  /** @maxLength 4096 */
  q?: string;
  /**
   * Постоянный ID или читаемый ключ сущности в выбранном проекте
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  id?: string;
  /**
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * @min 1
   * @max 100
   * @default 30
   */
  limit?: number;
}

export type KindEnum1 =
  | "passport"
  | "feature"
  | "scenario"
  | "application"
  | "scope"
  | "document";

export type GetProductRecordsParams1KindEnum =
  | "passport"
  | "feature"
  | "scenario"
  | "application"
  | "scope"
  | "document";

export type MutateProductOkEnum = true;

export type GetProductContextOkEnum = true;

export interface GetProductContextParams {
  /**
   * Постоянный ID или читаемый ключ сущности в выбранном проекте
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  id?: string;
  /**
   * Постоянный ID или читаемый ключ сущности в выбранном проекте
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  applicationId?: string;
}

export type GetWorkPlanProgressOkEnum = true;

export interface GetWorkPlanProgressParams {
  /**
   * Смещение каждого списка составляющих и причин
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы каждого списка: 20 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /** Версия первой страницы этого прогресса; обязательна при offset > 0 */
  version?: string;
  /**
   * Ключ, ID или kind:ID сущности выбранного проекта
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
}

export type GetReleaseProgressOkEnum = true;

export interface GetReleaseProgressParams {
  /**
   * Смещение каждого списка составляющих и причин
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы каждого списка: 20 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /** Версия первой страницы этого прогресса; обязательна при offset > 0 */
  version?: string;
  /**
   * Ключ, ID или kind:ID сущности выбранного проекта
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
}

export type GetTaskProgressOkEnum = true;

export interface GetTaskProgressParams {
  /**
   * Смещение каждого списка составляющих и причин
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы каждого списка: 20 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /** Версия первой страницы этого прогресса; обязательна при offset > 0 */
  version?: string;
  /**
   * Ключ, ID или kind:ID сущности выбранного проекта
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
}

export type GetImplementationProgressOkEnum = true;

export interface GetImplementationProgressParams {
  /**
   * Смещение каждого списка составляющих и причин
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы каждого списка: 20 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /** Версия первой страницы этого прогресса; обязательна при offset > 0 */
  version?: string;
  /**
   * Ключ, ID или kind:ID сущности выбранного проекта
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
}

export type GetScenarioProgressOkEnum = true;

export interface GetScenarioProgressParams {
  /**
   * Смещение каждого списка составляющих и причин
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы каждого списка: 20 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /** Версия первой страницы этого прогресса; обязательна при offset > 0 */
  version?: string;
  /**
   * Ключ, ID или kind:ID сущности выбранного проекта
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
}

export type GetFeatureProgressOkEnum = true;

export interface GetFeatureProgressParams {
  /**
   * Смещение каждого списка составляющих и причин
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы каждого списка: 20 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /** Версия первой страницы этого прогресса; обязательна при offset > 0 */
  version?: string;
  /**
   * Ключ, ID или kind:ID сущности выбранного проекта
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
}

export type GetApplicationProgressOkEnum = true;

export interface GetApplicationProgressParams {
  /**
   * Смещение каждого списка составляющих и причин
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы каждого списка: 20 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /** Версия первой страницы этого прогресса; обязательна при offset > 0 */
  version?: string;
  /**
   * Ключ, ID или kind:ID сущности выбранного проекта
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
}

export type GetProductProgressOkEnum = true;

export interface GetProductProgressParams {
  /**
   * Смещение каждого списка составляющих и причин
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы каждого списка: 20 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /** Версия первой страницы этого прогресса; обязательна при offset > 0 */
  version?: string;
}

export type GetPlansOkEnum = true;

export interface GetPlansParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы: 12 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 12
   */
  limit?: number;
  /**
   * Версия первой страницы; обязательна для продолжения
   * @maxLength 128
   */
  version?: string;
  /**
   * Поиск по ключу, названию, цели и краткому описанию
   * @maxLength 1024
   */
  q?: string;
  /** Фильтр состояния плана */
  status?: StatusEnum;
}

/** Фильтр состояния плана */
export type StatusEnum = "draft" | "active" | "completed" | "cancelled";

/** Фильтр состояния плана */
export type GetPlansParams1StatusEnum =
  | "draft"
  | "active"
  | "completed"
  | "cancelled";

export type CreatePlanOkEnum = true;

export type GetTaskPlanMembershipsOkEnum = true;

export interface GetTaskPlanMembershipsParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы: 12 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 12
   */
  limit?: number;
  /**
   * Версия первой страницы; обязательна для продолжения
   * @maxLength 128
   */
  version?: string;
  /** ID или ключ задачи */
  reference: any;
}

export type GetPlanningCandidatesOkEnum = true;

export interface GetPlanningCandidatesParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы: 12 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 12
   */
  limit?: number;
  /**
   * Версия первой страницы; обязательна для продолжения
   * @maxLength 128
   */
  version?: string;
  /**
   * Поиск задач по ключу и названию
   * @maxLength 1024
   */
  q?: string;
  /**
   * Доска: ключ, slug или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  board?: string;
  /**
   * Редактируемый этап; его задачи остаются допустимым выбором
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  stage?: string;
  /**
   * Оставить свободные задачи и текущий состав выбранного этапа
   * @default "true"
   */
  availableOnly?: AvailableOnlyEnum;
}

/**
 * Оставить свободные задачи и текущий состав выбранного этапа
 * @default "true"
 */
export type AvailableOnlyEnum = "true" | "false";

/**
 * Оставить свободные задачи и текущий состав выбранного этапа
 * @default "true"
 */
export type GetPlanningCandidatesParams1AvailableOnlyEnum = "true" | "false";

export type GetPlanOkEnum = true;

export interface GetPlanParams {
  /** ID или ключ плана работ */
  reference: any;
}

export type GetPlanStagesOkEnum = true;

export interface GetPlanStagesParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы: 12 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 12
   */
  limit?: number;
  /**
   * Версия первой страницы; обязательна для продолжения
   * @maxLength 128
   */
  version?: string;
  /** ID или ключ плана */
  reference: any;
}

export type ChangePlanStageOkEnum = true;

export interface ChangePlanStageParams {
  /** ID или ключ плана */
  reference: any;
}

export type GetPlanStageTasksOkEnum = true;

export interface GetPlanStageTasksParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы: 12 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 12
   */
  limit?: number;
  /**
   * Версия первой страницы; обязательна для продолжения
   * @maxLength 128
   */
  version?: string;
  /** ID или ключ этапа этого плана */
  stage: any;
  /** ID или ключ плана */
  reference: any;
}

export type UpdatePlanOkEnum = true;

export interface UpdatePlanParams {
  /** ID или ключ плана */
  reference: any;
}

export type TransitionPlanOkEnum = true;

export interface TransitionPlanParams {
  /** ID или ключ плана */
  reference: any;
}

export type ChangePlanTasksOkEnum = true;

export interface ChangePlanTasksParams {
  /** ID или ключ плана */
  reference: any;
}

export type TransferPlanTaskOkEnum = true;

export interface TransferPlanTaskParams {
  /** ID или ключ исходного плана */
  reference: any;
}

export type GetReleasesOkEnum = true;

export interface GetReleasesParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы: 12 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 12
   */
  limit?: number;
  /**
   * Версия первой страницы; обязательна для продолжения
   * @maxLength 128
   */
  version?: string;
  /**
   * Поиск по ключу, названию, версии и описанию
   * @maxLength 1024
   */
  q?: string;
  /** Собственное состояние: запланирован, выпущен или отменён */
  status?: StatusEnum1;
}

/** Собственное состояние: запланирован, выпущен или отменён */
export type StatusEnum1 = "planned" | "released" | "cancelled";

/** Собственное состояние: запланирован, выпущен или отменён */
export type GetReleasesParams1StatusEnum = "planned" | "released" | "cancelled";

export type CreateReleaseOkEnum = true;

export type PreviewReleaseOkEnum = true;

export type GetReleaseOkEnum = true;

export interface GetReleaseParams {
  /** ID или ключ релиза */
  reference: any;
}

export type GetReleasePlansOkEnum = true;

export interface GetReleasePlansParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы: 12 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 12
   */
  limit?: number;
  /**
   * Версия первой страницы; обязательна для продолжения
   * @maxLength 128
   */
  version?: string;
  /** ID или ключ релиза */
  reference: any;
}

export type GetReleaseSnapshotOkEnum = true;

export interface GetReleaseSnapshotParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы: 12 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 12
   */
  limit?: number;
  /**
   * Версия первой страницы; обязательна для продолжения
   * @maxLength 128
   */
  version?: string;
  /** ID или ключ выпущенного релиза */
  reference: any;
}

export type UpdateReleaseOkEnum = true;

export interface UpdateReleaseParams {
  /** ID или ключ релиза */
  reference: any;
}

export type TransitionReleaseOkEnum = true;

export interface TransitionReleaseParams {
  /** ID или ключ релиза */
  reference: any;
}

export type GetBoardsOkEnum = true;

export interface GetBoardsParams {
  /**
   * Смещение в каталоге досок
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы, не более 100 досок
   * @min 1
   * @max 100
   * @default 50
   */
  limit?: number;
  /** Версия каталога первой страницы; защищает продолжение от изменений */
  version?: string;
}

export type GetBoardBySlugOkEnum = true;

export interface GetBoardBySlugParams {
  /**
   * Slug доски выбранного проекта
   * @maxLength 64
   * @pattern ^[a-z0-9]+(?:-[a-z0-9]+)*$
   */
  slug: string;
}

export type GetBoardTasksOkEnum = true;

export interface GetBoardTasksParams {
  /**
   * Только прямые подзадачи родителя с указанным ID или ключом; фильтр до пагинации
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  parentId?: string;
  /**
   * Ключ или ID цели: задачи полного состава, включая дочерние сценарии и активные имплементации приложений
   * @minLength 1
   * @maxLength 128
   */
  productTarget?: string;
  /**
   * Slug, префикс или постоянный ID доски выбранного проекта
   * @minLength 1
   * @maxLength 128
   */
  board?: string;
  /** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
  column?: ColumnEnum;
  /** unfinished исключает done и cancelled; finished возвращает только их; без параметра все задачи */
  completion?: CompletionEnum;
  /** title — поиск по ключам, ID и заголовку; all или отсутствие — также по Markdown */
  searchIn?: SearchInEnum;
  /**
   * Поиск по ключу, ID, заголовку и Markdown
   * @maxLength 1024
   */
  q?: string;
  /** Заблокированные задачи либо готовые к выполнению для оркестратора */
  readiness?: ReadinessEnum;
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Число задач или связей на странице, максимум 100
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /** Версия первой страницы; изменение требует начать чтение заново */
  version?: string;
}

/** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
export type ColumnEnum =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** unfinished исключает done и cancelled; finished возвращает только их; без параметра все задачи */
export type CompletionEnum = "unfinished" | "finished";

/** title — поиск по ключам, ID и заголовку; all или отсутствие — также по Markdown */
export type SearchInEnum = "title" | "all";

/** Заблокированные задачи либо готовые к выполнению для оркестратора */
export type ReadinessEnum = "blocked" | "ready";

/** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
export type GetBoardTasksParams1ColumnEnum =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** unfinished исключает done и cancelled; finished возвращает только их; без параметра все задачи */
export type GetBoardTasksParams1CompletionEnum = "unfinished" | "finished";

/** title — поиск по ключам, ID и заголовку; all или отсутствие — также по Markdown */
export type GetBoardTasksParams1SearchInEnum = "title" | "all";

/** Заблокированные задачи либо готовые к выполнению для оркестратора */
export type GetBoardTasksParams1ReadinessEnum = "blocked" | "ready";

export type CreateBoardTaskOkEnum = true;

export type GetBoardTaskOkEnum = true;

export interface GetBoardTaskParams {
  /** Постоянный ID или текущий/прежний ключ задачи */
  reference: any;
}

export type GetBoardTaskLinksOkEnum = true;

export interface GetBoardTaskLinksParams {
  /**
   * Только прямые подзадачи родителя с указанным ID или ключом; фильтр до пагинации
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  parentId?: string;
  /**
   * Ключ или ID цели: задачи полного состава, включая дочерние сценарии и активные имплементации приложений
   * @minLength 1
   * @maxLength 128
   */
  productTarget?: string;
  /**
   * Slug, префикс или постоянный ID доски выбранного проекта
   * @minLength 1
   * @maxLength 128
   */
  board?: string;
  /** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
  column?: ColumnEnum1;
  /** unfinished исключает done и cancelled; finished возвращает только их; без параметра все задачи */
  completion?: CompletionEnum1;
  /** title — поиск по ключам, ID и заголовку; all или отсутствие — также по Markdown */
  searchIn?: SearchInEnum1;
  /**
   * Поиск по ключу, ID, заголовку и Markdown
   * @maxLength 1024
   */
  q?: string;
  /** Заблокированные задачи либо готовые к выполнению для оркестратора */
  readiness?: ReadinessEnum1;
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Число задач или связей на странице, максимум 100
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /** Версия первой страницы; изменение требует начать чтение заново */
  version?: string;
  /** ID или ключ задачи для чтения графа */
  reference: any;
}

/** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
export type ColumnEnum1 =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** unfinished исключает done и cancelled; finished возвращает только их; без параметра все задачи */
export type CompletionEnum1 = "unfinished" | "finished";

/** title — поиск по ключам, ID и заголовку; all или отсутствие — также по Markdown */
export type SearchInEnum1 = "title" | "all";

/** Заблокированные задачи либо готовые к выполнению для оркестратора */
export type ReadinessEnum1 = "blocked" | "ready";

/** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
export type GetBoardTaskLinksParams1ColumnEnum =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** unfinished исключает done и cancelled; finished возвращает только их; без параметра все задачи */
export type GetBoardTaskLinksParams1CompletionEnum = "unfinished" | "finished";

/** title — поиск по ключам, ID и заголовку; all или отсутствие — также по Markdown */
export type GetBoardTaskLinksParams1SearchInEnum = "title" | "all";

/** Заблокированные задачи либо готовые к выполнению для оркестратора */
export type GetBoardTaskLinksParams1ReadinessEnum = "blocked" | "ready";

export type LinkBoardTaskOkEnum = true;

export interface LinkBoardTaskParams {
  /** ID или ключ исходной задачи */
  reference: any;
}

export type GetTaskCriteriaOkEnum = true;

export interface GetTaskCriteriaParams {
  /**
   * Смещение страницы критериев
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы, по умолчанию 20, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /** Версия первой страницы; после изменения перечитайте список */
  version?: string;
  /** ID или ключ задачи */
  reference: any;
}

export type ChangeTaskCriterionOkEnum = true;

export interface ChangeTaskCriterionParams {
  /** ID или ключ задачи */
  reference: any;
}

export type GetTaskCommentsOkEnum = true;

export interface GetTaskCommentsParams {
  /**
   * Размер страницы, по умолчанию 20, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /**
   * Непрозрачный курсор следующей страницы; сохраняйте фильтры
   * @maxLength 2048
   */
  cursor?: string;
  /**
   * Только события после известного последовательного номера
   * @min 0
   * @max 9007199254740991
   */
  after?: number;
  /** Точное имя автора для фильтрации */
  actor?: string;
  /** Тип действия, например update или comment-publish */
  action?: string;
  /** ID или ключ задачи */
  reference: any;
}

export type PublishTaskCommentOkEnum = true;

export interface PublishTaskCommentParams {
  /** ID или ключ задачи */
  reference: any;
}

export type GetTaskCommentOkEnum = true;

export interface GetTaskCommentParams {
  /** Постоянный номер сообщения в ленте задачи */
  entryId: any;
  /** ID или ключ задачи */
  reference: any;
}

export type GetTaskHistoryOkEnum = true;

export interface GetTaskHistoryParams {
  /**
   * Размер страницы, по умолчанию 20, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /**
   * Непрозрачный курсор следующей страницы; сохраняйте фильтры
   * @maxLength 2048
   */
  cursor?: string;
  /**
   * Только события после известного последовательного номера
   * @min 0
   * @max 9007199254740991
   */
  after?: number;
  /** Точное имя автора для фильтрации */
  actor?: string;
  /** Тип действия, например update или comment-publish */
  action?: string;
  /** ID или ключ задачи */
  reference: any;
}

export type GetTaskHistoryEventOkEnum = true;

export interface GetTaskHistoryEventParams {
  /** Постоянный номер события в ленте задачи */
  entryId: any;
  /** ID или ключ задачи */
  reference: any;
}

export type GetTaskCriterionOkEnum = true;

export interface GetTaskCriterionParams {
  /** Постоянный ID критерия приёмки */
  criterionId: any;
  /** ID или ключ задачи */
  reference: any;
}

export type UpdateBoardTaskOkEnum = true;

export interface UpdateBoardTaskParams {
  /** ID или ключ редактируемой задачи */
  reference: any;
}

export type MoveBoardTaskOkEnum = true;

export interface MoveBoardTaskParams {
  /** ID или ключ перемещаемой задачи */
  reference: any;
}

export type GetGraphOkEnum = true;

export interface GetGraphParams {
  /**
   * Ключ или ID корня; допустим kind:ID. Без корня весь граф проекта
   * @maxLength 257
   */
  root?: string;
  /**
   * Оставить только отношения выбранного типа
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
   */
  type?: string;
  /**
   * Направление обхода относительно каждого узла
   * @default "both"
   */
  direction?: DirectionEnum;
  /**
   * Глубина одного обхода 0–100; продолжайте от граничных узлов
   * @min 0
   * @max 100
   * @default 3
   */
  depth?: number;
  /**
   * Полный обход сохранённых связей; context — совместимое имя all без скрытых ограничений по видам
   * @default "all"
   */
  profile?: ProfileEnum;
  /**
   * Поиск узлов по ключу, адресу и названию; рёбра только между найденными узлами
   * @maxLength 1024
   */
  q?: string;
  /**
   * Смещение одновременно в списках узлов и рёбер
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер каждой страницы узлов и рёбер, максимум 100
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /**
   * Версия первой страницы; изменение требует повторить чтение
   * @maxLength 128
   */
  version?: string;
}

/**
 * Направление обхода относительно каждого узла
 * @default "both"
 */
export type DirectionEnum = "both" | "outgoing" | "incoming";

/**
 * Полный обход сохранённых связей; context — совместимое имя all без скрытых ограничений по видам
 * @default "all"
 */
export type ProfileEnum = "all" | "context";

/**
 * Направление обхода относительно каждого узла
 * @default "both"
 */
export type GetGraphParams1DirectionEnum = "both" | "outgoing" | "incoming";

/**
 * Полный обход сохранённых связей; context — совместимое имя all без скрытых ограничений по видам
 * @default "all"
 */
export type GetGraphParams1ProfileEnum = "all" | "context";

export type MutateGraphOkEnum = true;

export type GetFullContextOkEnum = true;

export interface GetFullContextParams {
  /**
   * Ключ, ID или kind:ID исходной сущности; обход обоих направлений до конца компоненты
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  root: string;
}

export type GetGraphHistoryOkEnum = true;

export interface GetGraphHistoryParams {
  /**
   * История одного отношения; без ID весь журнал графа
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
   */
  id?: string;
  /**
   * Смещение событий
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы событий
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /**
   * Ревизия журнала первой страницы
   * @min 0
   * @max 9007199254740991
   */
  revision?: number;
}

export type ListEntityTypesOkEnum = true;

export interface ListEntityTypesParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы от 1 до 100
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /**
   * Версия первой страницы; изменение требует начать чтение заново
   * @maxLength 128
   */
  version?: string;
}

export type PreviewEntityDeletionOkEnum = true;

export interface PreviewEntityDeletionParams {
  /**
   * Ключ или постоянный адрес удаляемой сущности
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /** Вид удаляемой сущности; проект, паспорт и системные доски не удаляются */
  kind: KindEnum2;
}

/** Вид удаляемой сущности; проект, паспорт и системные доски не удаляются */
export type KindEnum2 =
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "task"
  | "document";

/** Вид удаляемой сущности; проект, паспорт и системные доски не удаляются */
export type PreviewEntityDeletionParams1KindEnum =
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "task"
  | "document";

export type DeleteEntityOkEnum = true;

export type DescribeEntityTypeOkEnum = true;

export interface DescribeEntityTypeParams {
  /** Вид основной сущности */
  kind: KindEnum3;
}

/** Вид основной сущности */
export type KindEnum3 =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Вид основной сущности */
export type DescribeEntityTypeParams1KindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

export type ListEntitiesOkEnum = true;

export interface ListEntitiesParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы от 1 до 100
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /**
   * Версия первой страницы; изменение требует начать чтение заново
   * @maxLength 128
   */
  version?: string;
  /** Вид основной сущности */
  kind?: KindEnum4;
  /**
   * Поиск по ключам, ID, названию и краткому описанию
   * @maxLength 4096
   */
  q?: string;
  /**
   * Адресное чтение до 100 кратких карточек по ключам или ID
   * @maxItems 100
   */
  refs?: string[];
  /**
   * Доска задач: ключ или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  board?: string;
  /**
   * Приложение доски, реализации или задачи: ключ или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  application?: string;
  /**
   * Фича сценария или реализации: ключ или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  feature?: string;
  /**
   * Сценарий реализации: ключ или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  scenario?: string;
  /**
   * Явная цель задачи, реализации или документа: ключ или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  target?: string;
  /**
   * Родитель задачи: ключ или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  parent?: string;
  /**
   * Предметное состояние выбранного вида
   * @maxLength 128
   */
  status?: string;
  /** Активность реализации; прежние ссылки доступны без фильтра */
  active?: ActiveEnum1;
  /**
   * Раздел документов; none — без раздела
   * @maxLength 64
   */
  section?: string;
  /** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
  documentKind?: DocumentKindEnum;
  /** Только закреплённые либо незакреплённые документы */
  pinned?: PinnedEnum;
  /** Включить только архив либо исключить архивные документы */
  archived?: ArchivedEnum;
  /**
   * Сортировка по ключу, названию или последнему обновлению
   * @default "key"
   */
  sort?: SortEnum;
}

/** Вид основной сущности */
export type KindEnum4 =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Активность реализации; прежние ссылки доступны без фильтра */
export type ActiveEnum1 = "true" | "false";

/** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
export type DocumentKindEnum =
  | "specification"
  | "description"
  | "rules"
  | "instruction"
  | "proposal"
  | "decision"
  | "research";

/** Только закреплённые либо незакреплённые документы */
export type PinnedEnum = "true" | "false";

/** Включить только архив либо исключить архивные документы */
export type ArchivedEnum = "true" | "false";

/**
 * Сортировка по ключу, названию или последнему обновлению
 * @default "key"
 */
export type SortEnum = "key" | "title" | "updated";

/** Вид основной сущности */
export type ListEntitiesParams1KindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Активность реализации; прежние ссылки доступны без фильтра */
export type ListEntitiesParams1ActiveEnum = "true" | "false";

/** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
export type ListEntitiesParams1DocumentKindEnum =
  | "specification"
  | "description"
  | "rules"
  | "instruction"
  | "proposal"
  | "decision"
  | "research";

/** Только закреплённые либо незакреплённые документы */
export type ListEntitiesParams1PinnedEnum = "true" | "false";

/** Включить только архив либо исключить архивные документы */
export type ListEntitiesParams1ArchivedEnum = "true" | "false";

/**
 * Сортировка по ключу, названию или последнему обновлению
 * @default "key"
 */
export type ListEntitiesParams1SortEnum = "key" | "title" | "updated";

export type CreateEntityOkEnum = true;

export type GetEntityOkEnum = true;

export interface GetEntityParams {
  /**
   * Ключ или постоянный ID сущности; для уточнения вида допустим kind:ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /** Уточнение ожидаемого вида при разрешении адреса */
  kind?: KindEnum5;
}

/** Уточнение ожидаемого вида при разрешении адреса */
export type KindEnum5 =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Уточнение ожидаемого вида при разрешении адреса */
export type GetEntityParams1KindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

export type ResolveEntityOkEnum = true;

export interface ResolveEntityParams {
  /**
   * Ключ или постоянный ID сущности; для уточнения вида допустим kind:ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /** Уточнение ожидаемого вида при разрешении адреса */
  kind?: KindEnum6;
}

/** Уточнение ожидаемого вида при разрешении адреса */
export type KindEnum6 =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Уточнение ожидаемого вида при разрешении адреса */
export type ResolveEntityParams1KindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

export type GetEntityKeysOkEnum = true;

export interface GetEntityKeysParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы от 1 до 100
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /**
   * Версия первой страницы; изменение требует начать чтение заново
   * @maxLength 128
   */
  version?: string;
  /**
   * Ключ или постоянный ID сущности; для уточнения вида допустим kind:ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /** Уточнение ожидаемого вида при разрешении адреса */
  kind?: KindEnum7;
}

/** Уточнение ожидаемого вида при разрешении адреса */
export type KindEnum7 =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Уточнение ожидаемого вида при разрешении адреса */
export type GetEntityKeysParams1KindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

export type GetEntityKeySpacesOkEnum = true;

export interface GetEntityKeySpacesParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы от 1 до 100
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /**
   * Версия первой страницы; изменение требует начать чтение заново
   * @maxLength 128
   */
  version?: string;
  /** Вид основной сущности */
  kind: KindEnum8;
}

/** Вид основной сущности */
export type KindEnum8 =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Вид основной сущности */
export type GetEntityKeySpacesParams1KindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

export type GetEntityHistoryOkEnum = true;

export interface GetEntityHistoryParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы от 1 до 100
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /**
   * Версия первой страницы; изменение требует начать чтение заново
   * @maxLength 128
   */
  version?: string;
  /**
   * Ключ или постоянный ID сущности; для уточнения вида допустим kind:ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /** Уточнение ожидаемого вида при разрешении адреса */
  kind?: KindEnum9;
}

/** Уточнение ожидаемого вида при разрешении адреса */
export type KindEnum9 =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Уточнение ожидаемого вида при разрешении адреса */
export type GetEntityHistoryParams1KindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

export type UpdateEntityOkEnum = true;

export type RenameEntityKeyOkEnum = true;

export type MoveEntityTaskOkEnum = true;

export type LinkEntityTaskOkEnum = true;

export type GetContextForProjectOkEnum = true;

export interface GetContextForProjectParams {
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetProjectSettingsForProjectOkEnum = true;

export interface GetProjectSettingsForProjectParams {
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type SaveProjectSettingsForProjectOkEnum = true;

export interface SaveProjectSettingsForProjectParams {
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type ValidateProjectForProjectOkEnum = true;

export interface ValidateProjectForProjectParams {
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetProductEntitiesForProjectOkEnum = true;

export interface GetProductEntitiesForProjectParams {
  /**
   * Поиск по ключу или названию; точное совпадение первым
   * @maxLength 4096
   */
  q?: string;
  /** Тип цели */
  kind?: KindEnum10;
  /**
   * ID или ключ приложения
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  application?: string;
  /** Только реализации фич или сценариев; фильтр применяется до пагинации */
  implementationTarget?: ImplementationTargetEnum1;
  /**
   * До 100 ID или ключей для краткого чтения существующих связей
   * @maxItems 100
   */
  refs?: string[];
  /** Фильтр участия; прежние ссылки читаются без фильтра */
  active?: ActiveEnum2;
  /**
   * Смещение страницы; продолжение возвращается в nextOffset
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы от 1 до 100
   * @min 1
   * @max 100
   * @default 30
   */
  limit?: number;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

/** Тип цели */
export type KindEnum10 =
  | "passport"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "document";

/** Только реализации фич или сценариев; фильтр применяется до пагинации */
export type ImplementationTargetEnum1 = "feature" | "scenario";

/** Фильтр участия; прежние ссылки читаются без фильтра */
export type ActiveEnum2 = "true" | "false";

/** Тип цели */
export type GetProductEntitiesForProjectParams1KindEnum =
  | "passport"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "document";

/** Только реализации фич или сценариев; фильтр применяется до пагинации */
export type GetProductEntitiesForProjectParams1ImplementationTargetEnum =
  | "feature"
  | "scenario";

/** Фильтр участия; прежние ссылки читаются без фильтра */
export type GetProductEntitiesForProjectParams1ActiveEnum = "true" | "false";

export type GetProductEntityForProjectOkEnum = true;

export interface GetProductEntityForProjectParams {
  /**
   * Постоянный ID или читаемый ключ сущности в выбранном проекте
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type UpdateProductImplementationForProjectOkEnum = true;

export interface UpdateProductImplementationForProjectParams {
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetProductStateForProjectOkEnum = true;

export interface GetProductStateForProjectParams {
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetProductOverviewForProjectOkEnum = true;

export interface GetProductOverviewForProjectParams {
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetProductRecordsForProjectOkEnum = true;

export interface GetProductRecordsForProjectParams {
  kind?: KindEnum11;
  /** @maxLength 4096 */
  q?: string;
  /**
   * Постоянный ID или читаемый ключ сущности в выбранном проекте
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  id?: string;
  /**
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * @min 1
   * @max 100
   * @default 30
   */
  limit?: number;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type KindEnum11 =
  | "passport"
  | "feature"
  | "scenario"
  | "application"
  | "scope"
  | "document";

export type GetProductRecordsForProjectParams1KindEnum =
  | "passport"
  | "feature"
  | "scenario"
  | "application"
  | "scope"
  | "document";

export type MutateProductForProjectOkEnum = true;

export interface MutateProductForProjectParams {
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetProductContextForProjectOkEnum = true;

export interface GetProductContextForProjectParams {
  /**
   * Постоянный ID или читаемый ключ сущности в выбранном проекте
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  id?: string;
  /**
   * Постоянный ID или читаемый ключ сущности в выбранном проекте
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  applicationId?: string;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetWorkPlanProgressForProjectOkEnum = true;

export interface GetWorkPlanProgressForProjectParams {
  /**
   * Смещение каждого списка составляющих и причин
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы каждого списка: 20 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /** Версия первой страницы этого прогресса; обязательна при offset > 0 */
  version?: string;
  /**
   * Ключ, ID или kind:ID сущности выбранного проекта
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetReleaseProgressForProjectOkEnum = true;

export interface GetReleaseProgressForProjectParams {
  /**
   * Смещение каждого списка составляющих и причин
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы каждого списка: 20 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /** Версия первой страницы этого прогресса; обязательна при offset > 0 */
  version?: string;
  /**
   * Ключ, ID или kind:ID сущности выбранного проекта
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetTaskProgressForProjectOkEnum = true;

export interface GetTaskProgressForProjectParams {
  /**
   * Смещение каждого списка составляющих и причин
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы каждого списка: 20 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /** Версия первой страницы этого прогресса; обязательна при offset > 0 */
  version?: string;
  /**
   * Ключ, ID или kind:ID сущности выбранного проекта
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetImplementationProgressForProjectOkEnum = true;

export interface GetImplementationProgressForProjectParams {
  /**
   * Смещение каждого списка составляющих и причин
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы каждого списка: 20 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /** Версия первой страницы этого прогресса; обязательна при offset > 0 */
  version?: string;
  /**
   * Ключ, ID или kind:ID сущности выбранного проекта
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetScenarioProgressForProjectOkEnum = true;

export interface GetScenarioProgressForProjectParams {
  /**
   * Смещение каждого списка составляющих и причин
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы каждого списка: 20 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /** Версия первой страницы этого прогресса; обязательна при offset > 0 */
  version?: string;
  /**
   * Ключ, ID или kind:ID сущности выбранного проекта
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetFeatureProgressForProjectOkEnum = true;

export interface GetFeatureProgressForProjectParams {
  /**
   * Смещение каждого списка составляющих и причин
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы каждого списка: 20 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /** Версия первой страницы этого прогресса; обязательна при offset > 0 */
  version?: string;
  /**
   * Ключ, ID или kind:ID сущности выбранного проекта
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetApplicationProgressForProjectOkEnum = true;

export interface GetApplicationProgressForProjectParams {
  /**
   * Смещение каждого списка составляющих и причин
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы каждого списка: 20 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /** Версия первой страницы этого прогресса; обязательна при offset > 0 */
  version?: string;
  /**
   * Ключ, ID или kind:ID сущности выбранного проекта
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetProductProgressForProjectOkEnum = true;

export interface GetProductProgressForProjectParams {
  /**
   * Смещение каждого списка составляющих и причин
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы каждого списка: 20 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /** Версия первой страницы этого прогресса; обязательна при offset > 0 */
  version?: string;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetPlansForProjectOkEnum = true;

export interface GetPlansForProjectParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы: 12 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 12
   */
  limit?: number;
  /**
   * Версия первой страницы; обязательна для продолжения
   * @maxLength 128
   */
  version?: string;
  /**
   * Поиск по ключу, названию, цели и краткому описанию
   * @maxLength 1024
   */
  q?: string;
  /** Фильтр состояния плана */
  status?: StatusEnum2;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

/** Фильтр состояния плана */
export type StatusEnum2 = "draft" | "active" | "completed" | "cancelled";

/** Фильтр состояния плана */
export type GetPlansForProjectParams1StatusEnum =
  | "draft"
  | "active"
  | "completed"
  | "cancelled";

export type CreatePlanForProjectOkEnum = true;

export interface CreatePlanForProjectParams {
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetTaskPlanMembershipsForProjectOkEnum = true;

export interface GetTaskPlanMembershipsForProjectParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы: 12 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 12
   */
  limit?: number;
  /**
   * Версия первой страницы; обязательна для продолжения
   * @maxLength 128
   */
  version?: string;
  /** ID или ключ задачи */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetPlanningCandidatesForProjectOkEnum = true;

export interface GetPlanningCandidatesForProjectParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы: 12 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 12
   */
  limit?: number;
  /**
   * Версия первой страницы; обязательна для продолжения
   * @maxLength 128
   */
  version?: string;
  /**
   * Поиск задач по ключу и названию
   * @maxLength 1024
   */
  q?: string;
  /**
   * Доска: ключ, slug или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  board?: string;
  /**
   * Редактируемый этап; его задачи остаются допустимым выбором
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  stage?: string;
  /**
   * Оставить свободные задачи и текущий состав выбранного этапа
   * @default "true"
   */
  availableOnly?: AvailableOnlyEnum1;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

/**
 * Оставить свободные задачи и текущий состав выбранного этапа
 * @default "true"
 */
export type AvailableOnlyEnum1 = "true" | "false";

/**
 * Оставить свободные задачи и текущий состав выбранного этапа
 * @default "true"
 */
export type GetPlanningCandidatesForProjectParams1AvailableOnlyEnum =
  | "true"
  | "false";

export type GetPlanForProjectOkEnum = true;

export interface GetPlanForProjectParams {
  /** ID или ключ плана работ */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetPlanStagesForProjectOkEnum = true;

export interface GetPlanStagesForProjectParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы: 12 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 12
   */
  limit?: number;
  /**
   * Версия первой страницы; обязательна для продолжения
   * @maxLength 128
   */
  version?: string;
  /** ID или ключ плана */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type ChangePlanStageForProjectOkEnum = true;

export interface ChangePlanStageForProjectParams {
  /** ID или ключ плана */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetPlanStageTasksForProjectOkEnum = true;

export interface GetPlanStageTasksForProjectParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы: 12 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 12
   */
  limit?: number;
  /**
   * Версия первой страницы; обязательна для продолжения
   * @maxLength 128
   */
  version?: string;
  /** ID или ключ этапа этого плана */
  stage: any;
  /** ID или ключ плана */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type UpdatePlanForProjectOkEnum = true;

export interface UpdatePlanForProjectParams {
  /** ID или ключ плана */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type TransitionPlanForProjectOkEnum = true;

export interface TransitionPlanForProjectParams {
  /** ID или ключ плана */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type ChangePlanTasksForProjectOkEnum = true;

export interface ChangePlanTasksForProjectParams {
  /** ID или ключ плана */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type TransferPlanTaskForProjectOkEnum = true;

export interface TransferPlanTaskForProjectParams {
  /** ID или ключ исходного плана */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetReleasesForProjectOkEnum = true;

export interface GetReleasesForProjectParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы: 12 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 12
   */
  limit?: number;
  /**
   * Версия первой страницы; обязательна для продолжения
   * @maxLength 128
   */
  version?: string;
  /**
   * Поиск по ключу, названию, версии и описанию
   * @maxLength 1024
   */
  q?: string;
  /** Собственное состояние: запланирован, выпущен или отменён */
  status?: StatusEnum3;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

/** Собственное состояние: запланирован, выпущен или отменён */
export type StatusEnum3 = "planned" | "released" | "cancelled";

/** Собственное состояние: запланирован, выпущен или отменён */
export type GetReleasesForProjectParams1StatusEnum =
  | "planned"
  | "released"
  | "cancelled";

export type CreateReleaseForProjectOkEnum = true;

export interface CreateReleaseForProjectParams {
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type PreviewReleaseForProjectOkEnum = true;

export interface PreviewReleaseForProjectParams {
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetReleaseForProjectOkEnum = true;

export interface GetReleaseForProjectParams {
  /** ID или ключ релиза */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetReleasePlansForProjectOkEnum = true;

export interface GetReleasePlansForProjectParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы: 12 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 12
   */
  limit?: number;
  /**
   * Версия первой страницы; обязательна для продолжения
   * @maxLength 128
   */
  version?: string;
  /** ID или ключ релиза */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetReleaseSnapshotForProjectOkEnum = true;

export interface GetReleaseSnapshotForProjectParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы: 12 по умолчанию, максимум 100
   * @min 1
   * @max 100
   * @default 12
   */
  limit?: number;
  /**
   * Версия первой страницы; обязательна для продолжения
   * @maxLength 128
   */
  version?: string;
  /** ID или ключ выпущенного релиза */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type UpdateReleaseForProjectOkEnum = true;

export interface UpdateReleaseForProjectParams {
  /** ID или ключ релиза */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type TransitionReleaseForProjectOkEnum = true;

export interface TransitionReleaseForProjectParams {
  /** ID или ключ релиза */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetBoardsForProjectOkEnum = true;

export interface GetBoardsForProjectParams {
  /**
   * Смещение в каталоге досок
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы, не более 100 досок
   * @min 1
   * @max 100
   * @default 50
   */
  limit?: number;
  /** Версия каталога первой страницы; защищает продолжение от изменений */
  version?: string;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetBoardBySlugForProjectOkEnum = true;

export interface GetBoardBySlugForProjectParams {
  /**
   * Slug доски выбранного проекта
   * @maxLength 64
   * @pattern ^[a-z0-9]+(?:-[a-z0-9]+)*$
   */
  slug: string;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetBoardTasksForProjectOkEnum = true;

export interface GetBoardTasksForProjectParams {
  /**
   * Только прямые подзадачи родителя с указанным ID или ключом; фильтр до пагинации
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  parentId?: string;
  /**
   * Ключ или ID цели: задачи полного состава, включая дочерние сценарии и активные имплементации приложений
   * @minLength 1
   * @maxLength 128
   */
  productTarget?: string;
  /**
   * Slug, префикс или постоянный ID доски выбранного проекта
   * @minLength 1
   * @maxLength 128
   */
  board?: string;
  /** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
  column?: ColumnEnum2;
  /** unfinished исключает done и cancelled; finished возвращает только их; без параметра все задачи */
  completion?: CompletionEnum2;
  /** title — поиск по ключам, ID и заголовку; all или отсутствие — также по Markdown */
  searchIn?: SearchInEnum2;
  /**
   * Поиск по ключу, ID, заголовку и Markdown
   * @maxLength 1024
   */
  q?: string;
  /** Заблокированные задачи либо готовые к выполнению для оркестратора */
  readiness?: ReadinessEnum2;
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Число задач или связей на странице, максимум 100
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /** Версия первой страницы; изменение требует начать чтение заново */
  version?: string;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

/** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
export type ColumnEnum2 =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** unfinished исключает done и cancelled; finished возвращает только их; без параметра все задачи */
export type CompletionEnum2 = "unfinished" | "finished";

/** title — поиск по ключам, ID и заголовку; all или отсутствие — также по Markdown */
export type SearchInEnum2 = "title" | "all";

/** Заблокированные задачи либо готовые к выполнению для оркестратора */
export type ReadinessEnum2 = "blocked" | "ready";

/** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
export type GetBoardTasksForProjectParams1ColumnEnum =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** unfinished исключает done и cancelled; finished возвращает только их; без параметра все задачи */
export type GetBoardTasksForProjectParams1CompletionEnum =
  | "unfinished"
  | "finished";

/** title — поиск по ключам, ID и заголовку; all или отсутствие — также по Markdown */
export type GetBoardTasksForProjectParams1SearchInEnum = "title" | "all";

/** Заблокированные задачи либо готовые к выполнению для оркестратора */
export type GetBoardTasksForProjectParams1ReadinessEnum = "blocked" | "ready";

export type CreateBoardTaskForProjectOkEnum = true;

export interface CreateBoardTaskForProjectParams {
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetBoardTaskForProjectOkEnum = true;

export interface GetBoardTaskForProjectParams {
  /** Постоянный ID или текущий/прежний ключ задачи */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetBoardTaskLinksForProjectOkEnum = true;

export interface GetBoardTaskLinksForProjectParams {
  /**
   * Только прямые подзадачи родителя с указанным ID или ключом; фильтр до пагинации
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  parentId?: string;
  /**
   * Ключ или ID цели: задачи полного состава, включая дочерние сценарии и активные имплементации приложений
   * @minLength 1
   * @maxLength 128
   */
  productTarget?: string;
  /**
   * Slug, префикс или постоянный ID доски выбранного проекта
   * @minLength 1
   * @maxLength 128
   */
  board?: string;
  /** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
  column?: ColumnEnum3;
  /** unfinished исключает done и cancelled; finished возвращает только их; без параметра все задачи */
  completion?: CompletionEnum3;
  /** title — поиск по ключам, ID и заголовку; all или отсутствие — также по Markdown */
  searchIn?: SearchInEnum3;
  /**
   * Поиск по ключу, ID, заголовку и Markdown
   * @maxLength 1024
   */
  q?: string;
  /** Заблокированные задачи либо готовые к выполнению для оркестратора */
  readiness?: ReadinessEnum3;
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Число задач или связей на странице, максимум 100
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /** Версия первой страницы; изменение требует начать чтение заново */
  version?: string;
  /** ID или ключ задачи для чтения графа */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

/** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
export type ColumnEnum3 =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** unfinished исключает done и cancelled; finished возвращает только их; без параметра все задачи */
export type CompletionEnum3 = "unfinished" | "finished";

/** title — поиск по ключам, ID и заголовку; all или отсутствие — также по Markdown */
export type SearchInEnum3 = "title" | "all";

/** Заблокированные задачи либо готовые к выполнению для оркестратора */
export type ReadinessEnum3 = "blocked" | "ready";

/** Колонка: inbox, ready, in-progress, review, done; cancelled — отмена */
export type GetBoardTaskLinksForProjectParams1ColumnEnum =
  | "inbox"
  | "ready"
  | "in-progress"
  | "review"
  | "done"
  | "cancelled";

/** unfinished исключает done и cancelled; finished возвращает только их; без параметра все задачи */
export type GetBoardTaskLinksForProjectParams1CompletionEnum =
  | "unfinished"
  | "finished";

/** title — поиск по ключам, ID и заголовку; all или отсутствие — также по Markdown */
export type GetBoardTaskLinksForProjectParams1SearchInEnum = "title" | "all";

/** Заблокированные задачи либо готовые к выполнению для оркестратора */
export type GetBoardTaskLinksForProjectParams1ReadinessEnum =
  | "blocked"
  | "ready";

export type LinkBoardTaskForProjectOkEnum = true;

export interface LinkBoardTaskForProjectParams {
  /** ID или ключ исходной задачи */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetTaskCriteriaForProjectOkEnum = true;

export interface GetTaskCriteriaForProjectParams {
  /**
   * Смещение страницы критериев
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы, по умолчанию 20, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /** Версия первой страницы; после изменения перечитайте список */
  version?: string;
  /** ID или ключ задачи */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type ChangeTaskCriterionForProjectOkEnum = true;

export interface ChangeTaskCriterionForProjectParams {
  /** ID или ключ задачи */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetTaskCommentsForProjectOkEnum = true;

export interface GetTaskCommentsForProjectParams {
  /**
   * Размер страницы, по умолчанию 20, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /**
   * Непрозрачный курсор следующей страницы; сохраняйте фильтры
   * @maxLength 2048
   */
  cursor?: string;
  /**
   * Только события после известного последовательного номера
   * @min 0
   * @max 9007199254740991
   */
  after?: number;
  /** Точное имя автора для фильтрации */
  actor?: string;
  /** Тип действия, например update или comment-publish */
  action?: string;
  /** ID или ключ задачи */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type PublishTaskCommentForProjectOkEnum = true;

export interface PublishTaskCommentForProjectParams {
  /** ID или ключ задачи */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetTaskCommentForProjectOkEnum = true;

export interface GetTaskCommentForProjectParams {
  /** Постоянный номер сообщения в ленте задачи */
  entryId: any;
  /** ID или ключ задачи */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetTaskHistoryForProjectOkEnum = true;

export interface GetTaskHistoryForProjectParams {
  /**
   * Размер страницы, по умолчанию 20, максимум 100
   * @min 1
   * @max 100
   * @default 20
   */
  limit?: number;
  /**
   * Непрозрачный курсор следующей страницы; сохраняйте фильтры
   * @maxLength 2048
   */
  cursor?: string;
  /**
   * Только события после известного последовательного номера
   * @min 0
   * @max 9007199254740991
   */
  after?: number;
  /** Точное имя автора для фильтрации */
  actor?: string;
  /** Тип действия, например update или comment-publish */
  action?: string;
  /** ID или ключ задачи */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetTaskHistoryEventForProjectOkEnum = true;

export interface GetTaskHistoryEventForProjectParams {
  /** Постоянный номер события в ленте задачи */
  entryId: any;
  /** ID или ключ задачи */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetTaskCriterionForProjectOkEnum = true;

export interface GetTaskCriterionForProjectParams {
  /** Постоянный ID критерия приёмки */
  criterionId: any;
  /** ID или ключ задачи */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type UpdateBoardTaskForProjectOkEnum = true;

export interface UpdateBoardTaskForProjectParams {
  /** ID или ключ редактируемой задачи */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type MoveBoardTaskForProjectOkEnum = true;

export interface MoveBoardTaskForProjectParams {
  /** ID или ключ перемещаемой задачи */
  reference: any;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetGraphForProjectOkEnum = true;

export interface GetGraphForProjectParams {
  /**
   * Ключ или ID корня; допустим kind:ID. Без корня весь граф проекта
   * @maxLength 257
   */
  root?: string;
  /**
   * Оставить только отношения выбранного типа
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
   */
  type?: string;
  /**
   * Направление обхода относительно каждого узла
   * @default "both"
   */
  direction?: DirectionEnum1;
  /**
   * Глубина одного обхода 0–100; продолжайте от граничных узлов
   * @min 0
   * @max 100
   * @default 3
   */
  depth?: number;
  /**
   * Полный обход сохранённых связей; context — совместимое имя all без скрытых ограничений по видам
   * @default "all"
   */
  profile?: ProfileEnum1;
  /**
   * Поиск узлов по ключу, адресу и названию; рёбра только между найденными узлами
   * @maxLength 1024
   */
  q?: string;
  /**
   * Смещение одновременно в списках узлов и рёбер
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер каждой страницы узлов и рёбер, максимум 100
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /**
   * Версия первой страницы; изменение требует повторить чтение
   * @maxLength 128
   */
  version?: string;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

/**
 * Направление обхода относительно каждого узла
 * @default "both"
 */
export type DirectionEnum1 = "both" | "outgoing" | "incoming";

/**
 * Полный обход сохранённых связей; context — совместимое имя all без скрытых ограничений по видам
 * @default "all"
 */
export type ProfileEnum1 = "all" | "context";

/**
 * Направление обхода относительно каждого узла
 * @default "both"
 */
export type GetGraphForProjectParams1DirectionEnum =
  | "both"
  | "outgoing"
  | "incoming";

/**
 * Полный обход сохранённых связей; context — совместимое имя all без скрытых ограничений по видам
 * @default "all"
 */
export type GetGraphForProjectParams1ProfileEnum = "all" | "context";

export type MutateGraphForProjectOkEnum = true;

export interface MutateGraphForProjectParams {
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetFullContextForProjectOkEnum = true;

export interface GetFullContextForProjectParams {
  /**
   * Ключ, ID или kind:ID исходной сущности; обход обоих направлений до конца компоненты
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  root: string;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetGraphHistoryForProjectOkEnum = true;

export interface GetGraphHistoryForProjectParams {
  /**
   * История одного отношения; без ID весь журнал графа
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
   */
  id?: string;
  /**
   * Смещение событий
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы событий
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /**
   * Ревизия журнала первой страницы
   * @min 0
   * @max 9007199254740991
   */
  revision?: number;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type ListEntityTypesForProjectOkEnum = true;

export interface ListEntityTypesForProjectParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы от 1 до 100
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /**
   * Версия первой страницы; изменение требует начать чтение заново
   * @maxLength 128
   */
  version?: string;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type PreviewEntityDeletionForProjectOkEnum = true;

export interface PreviewEntityDeletionForProjectParams {
  /**
   * Ключ или постоянный адрес удаляемой сущности
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /** Вид удаляемой сущности; проект, паспорт и системные доски не удаляются */
  kind: KindEnum12;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

/** Вид удаляемой сущности; проект, паспорт и системные доски не удаляются */
export type KindEnum12 =
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "task"
  | "document";

/** Вид удаляемой сущности; проект, паспорт и системные доски не удаляются */
export type PreviewEntityDeletionForProjectParams1KindEnum =
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "task"
  | "document";

export type DeleteEntityForProjectOkEnum = true;

export interface DeleteEntityForProjectParams {
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type DescribeEntityTypeForProjectOkEnum = true;

export interface DescribeEntityTypeForProjectParams {
  /** Вид основной сущности */
  kind: KindEnum13;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

/** Вид основной сущности */
export type KindEnum13 =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Вид основной сущности */
export type DescribeEntityTypeForProjectParams1KindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

export type ListEntitiesForProjectOkEnum = true;

export interface ListEntitiesForProjectParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы от 1 до 100
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /**
   * Версия первой страницы; изменение требует начать чтение заново
   * @maxLength 128
   */
  version?: string;
  /** Вид основной сущности */
  kind?: KindEnum14;
  /**
   * Поиск по ключам, ID, названию и краткому описанию
   * @maxLength 4096
   */
  q?: string;
  /**
   * Адресное чтение до 100 кратких карточек по ключам или ID
   * @maxItems 100
   */
  refs?: string[];
  /**
   * Доска задач: ключ или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  board?: string;
  /**
   * Приложение доски, реализации или задачи: ключ или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  application?: string;
  /**
   * Фича сценария или реализации: ключ или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  feature?: string;
  /**
   * Сценарий реализации: ключ или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  scenario?: string;
  /**
   * Явная цель задачи, реализации или документа: ключ или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  target?: string;
  /**
   * Родитель задачи: ключ или ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  parent?: string;
  /**
   * Предметное состояние выбранного вида
   * @maxLength 128
   */
  status?: string;
  /** Активность реализации; прежние ссылки доступны без фильтра */
  active?: ActiveEnum3;
  /**
   * Раздел документов; none — без раздела
   * @maxLength 64
   */
  section?: string;
  /** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
  documentKind?: DocumentKindEnum1;
  /** Только закреплённые либо незакреплённые документы */
  pinned?: PinnedEnum1;
  /** Включить только архив либо исключить архивные документы */
  archived?: ArchivedEnum1;
  /**
   * Сортировка по ключу, названию или последнему обновлению
   * @default "key"
   */
  sort?: SortEnum1;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

/** Вид основной сущности */
export type KindEnum14 =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Активность реализации; прежние ссылки доступны без фильтра */
export type ActiveEnum3 = "true" | "false";

/** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
export type DocumentKindEnum1 =
  | "specification"
  | "description"
  | "rules"
  | "instruction"
  | "proposal"
  | "decision"
  | "research";

/** Только закреплённые либо незакреплённые документы */
export type PinnedEnum1 = "true" | "false";

/** Включить только архив либо исключить архивные документы */
export type ArchivedEnum1 = "true" | "false";

/**
 * Сортировка по ключу, названию или последнему обновлению
 * @default "key"
 */
export type SortEnum1 = "key" | "title" | "updated";

/** Вид основной сущности */
export type ListEntitiesForProjectParams1KindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Активность реализации; прежние ссылки доступны без фильтра */
export type ListEntitiesForProjectParams1ActiveEnum = "true" | "false";

/** Тип: ТЗ, описание, правила, инструкция, проект решения, решение или исследование */
export type ListEntitiesForProjectParams1DocumentKindEnum =
  | "specification"
  | "description"
  | "rules"
  | "instruction"
  | "proposal"
  | "decision"
  | "research";

/** Только закреплённые либо незакреплённые документы */
export type ListEntitiesForProjectParams1PinnedEnum = "true" | "false";

/** Включить только архив либо исключить архивные документы */
export type ListEntitiesForProjectParams1ArchivedEnum = "true" | "false";

/**
 * Сортировка по ключу, названию или последнему обновлению
 * @default "key"
 */
export type ListEntitiesForProjectParams1SortEnum = "key" | "title" | "updated";

export type CreateEntityForProjectOkEnum = true;

export interface CreateEntityForProjectParams {
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type GetEntityForProjectOkEnum = true;

export interface GetEntityForProjectParams {
  /**
   * Ключ или постоянный ID сущности; для уточнения вида допустим kind:ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /** Уточнение ожидаемого вида при разрешении адреса */
  kind?: KindEnum15;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

/** Уточнение ожидаемого вида при разрешении адреса */
export type KindEnum15 =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Уточнение ожидаемого вида при разрешении адреса */
export type GetEntityForProjectParams1KindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

export type ResolveEntityForProjectOkEnum = true;

export interface ResolveEntityForProjectParams {
  /**
   * Ключ или постоянный ID сущности; для уточнения вида допустим kind:ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /** Уточнение ожидаемого вида при разрешении адреса */
  kind?: KindEnum16;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

/** Уточнение ожидаемого вида при разрешении адреса */
export type KindEnum16 =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Уточнение ожидаемого вида при разрешении адреса */
export type ResolveEntityForProjectParams1KindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

export type GetEntityKeysForProjectOkEnum = true;

export interface GetEntityKeysForProjectParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы от 1 до 100
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /**
   * Версия первой страницы; изменение требует начать чтение заново
   * @maxLength 128
   */
  version?: string;
  /**
   * Ключ или постоянный ID сущности; для уточнения вида допустим kind:ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /** Уточнение ожидаемого вида при разрешении адреса */
  kind?: KindEnum17;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

/** Уточнение ожидаемого вида при разрешении адреса */
export type KindEnum17 =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Уточнение ожидаемого вида при разрешении адреса */
export type GetEntityKeysForProjectParams1KindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

export type GetEntityKeySpacesForProjectOkEnum = true;

export interface GetEntityKeySpacesForProjectParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы от 1 до 100
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /**
   * Версия первой страницы; изменение требует начать чтение заново
   * @maxLength 128
   */
  version?: string;
  /** Вид основной сущности */
  kind: KindEnum18;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

/** Вид основной сущности */
export type KindEnum18 =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Вид основной сущности */
export type GetEntityKeySpacesForProjectParams1KindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

export type GetEntityHistoryForProjectOkEnum = true;

export interface GetEntityHistoryForProjectParams {
  /**
   * Смещение страницы
   * @min 0
   * @max 9007199254740991
   * @default 0
   */
  offset?: number;
  /**
   * Размер страницы от 1 до 100
   * @min 1
   * @max 100
   * @default 40
   */
  limit?: number;
  /**
   * Версия первой страницы; изменение требует начать чтение заново
   * @maxLength 128
   */
  version?: string;
  /**
   * Ключ или постоянный ID сущности; для уточнения вида допустим kind:ID
   * @minLength 1
   * @maxLength 257
   * @pattern ^[A-Za-z0-9][A-Za-z0-9._:-]*$
   */
  ref: string;
  /** Уточнение ожидаемого вида при разрешении адреса */
  kind?: KindEnum19;
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

/** Уточнение ожидаемого вида при разрешении адреса */
export type KindEnum19 =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

/** Уточнение ожидаемого вида при разрешении адреса */
export type GetEntityHistoryForProjectParams1KindEnum =
  | "project"
  | "product"
  | "feature"
  | "scenario"
  | "application"
  | "implementation"
  | "board"
  | "task"
  | "document"
  | "work-plan"
  | "plan-stage"
  | "release";

export type UpdateEntityForProjectOkEnum = true;

export interface UpdateEntityForProjectParams {
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type RenameEntityKeyForProjectOkEnum = true;

export interface RenameEntityKeyForProjectParams {
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type MoveEntityTaskForProjectOkEnum = true;

export interface MoveEntityTaskForProjectParams {
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export type LinkEntityTaskForProjectOkEnum = true;

export interface LinkEntityTaskForProjectParams {
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}

export interface WatchEventsForProjectParams {
  /** Slug, имя из реестра или постоянный идентификатор проекта */
  project: string;
}
