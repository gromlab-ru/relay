# Проверка демонабора «БериДело»

Дата: **2026-09-14**. Проверяется набор задач текущего трекера, а не реализация
описываемого в карточках сервиса аренды.

## Данные и воспроизводимость

Пройдены:

- `pnpm --filter @tasks/playground run demo:reset` — материализация и проверка
  подготовленного набора штатным CLI до замены `.tasks`.
- `pnpm --filter @tasks/playground run demo:check` — совпадение с эталоном,
  корректность всех документов, статусов, ссылок и графа.
- 190 карточек, из них 170 рабочих; 55 комментариев и 106 отчётов.
- У рабочих задач уникальные названия, три предметных критерия, результат,
  проверка, продуктовый контекст и источник требований.
- Завершённые задачи замкнуты по зависимостям; активные рабочие задачи не имеют
  незавершённых предпосылок; отменённые карточки не блокируют обязательный объём.
- Форматирование изменённых исходников и документации — Prettier.

## CLI и HTTP на отдельной копии

Поднят собственный сервер на свободном порту с копией данных в
`.artifacts/tasks-beridelo-3NaO4j`. Рабочий playground сохраняет исходный демоснимок.

| Сценарий                                 | Наблюдаемый результат                                   |
| ---------------------------------------- | ------------------------------------------------------- |
| API доски, `limit=40`, все курсоры       | Страницы 40 / 40 / 40 / 40 / 30; 190 уникальных ID      |
| CLI `list --all --limit 40`, все курсоры | 190 уникальных ID без пропусков и дублей                |
| Группа `frontend`                        | 51 карточка                                             |
| Готовые свободные задачи                 | #28, #30, #46, #68, #101, #148, #172                    |
| Дочерние задачи проекта #1               | 16 эпиков                                               |
| Поиск `112038` в описании                | Найдена карточка расчёта #82                            |
| История #82, `limit=20`                  | Прочитаны все 24 комментария и 25 отчётов               |
| CLI `get 82 --full --max-bytes 131072`   | Полный документ с историей помещается в заданный бюджет |
| Захват #101                              | Исполнитель и `in_progress` сохранены                   |
| Завершение #83                           | #84 становится готовой к захвату                        |
| PATCH со старой ревизией #101            | `409 REVISION_CONFLICT`                                 |
| Старый курсор доски после изменения      | `409 BOARD_CHANGED`                                     |
| Изменение через CLI при открытом SSE     | Получено событие `changed`                              |
| `validate` после сценариев изменения     | Хранилище корректно                                     |

Изменяющие сценарии выполнялись только на проверочной копии.

## Браузер

Проверка выполнена через `agent-browser` в собственной headless-сессии
`tasks-web-beridelo-1076751`.

Подтверждено:

- Исходные счётчики: 93 / 18 / 5 / 71 / 3, общий — 190.
- Кнопки подгрузки доводят `todo` до 93, `done` до 71; DOM содержит все 190 карточек.
- Выбор группы `frontend` отображает 51 карточку; сброс возвращает общий набор.
- Поиск по значению из описания открывает #82, включая таблицу сумм, JSON и
  выполненные критерии приёмки.
- Кнопка ранних записей загружает вторую страницу комментариев и отчётов #82.
- Прямые ссылки на #82 и #84 открывают карточки; после завершения #83 у #84
  доступно действие «Взять себе».
- Доска просмотрена на 1440/1024/768/390; на узком экране колонки прокручиваются
  горизонтально, карточка задачи открывается на всю ширину.
- Просмотрены светлая и тёмная темы; длинные названия и многострочные описания читаются.
- `Escape` закрывает карточку, `/` переводит фокус в поиск.
- Необработанных JavaScript-ошибок страницы не обнаружено.

Снимки находятся локально в `.artifacts/tasks-beridelo-3NaO4j/`:
`board-light-1440.png`, `board-dark-{1440,1024,768,390}.png`,
`task-82-light-1440.png`, `task-82-dark-390.png`. Артефакты исключены из Git.

### Замечания к текущему интерфейсу

1. **Доступность блока кода.** Axe-core 4.12.1 на #82 при ширине 390 обнаружил
   `scrollable-region-focusable` для `pre`: горизонтально прокручиваемый блок JSON
   не имеет клавиатурного фокуса. Это воспроизводимый пограничный случай длинного Markdown.
2. **Быстрый посимвольный ввод.** В автоматизированной попытке `keyboard type "101"`
   после `/` поле содержало `1`. Этот способ ввода не отмечен как успешно проверенный;
   требуется отдельная диагностика взаимодействия ввода и перерисовок.
   Полное заполнение поиска через `fill` и поиск по описанию отработали корректно.
3. В консоли есть warning React Router о leaf route `/tasks/...` без `element`.
   Карточки при этом открывались. Аудит также оставил ручную проверку контраста
   ID корневой карточки под открытой панелью как `incomplete`.

Аудит доступности имеет замечание и **не считается полностью пройденным**.
Эти наблюдения относятся к интерфейсу трекера и не добавлены в продуктовый backlog
арендного сервиса, чтобы сохранить целостность легенды.

## Проверки frontend и завершение

- `pnpm run lint:web` — успешно.
- `pnpm run build:web` — успешно; включает TypeScript-проверку frontend.
- Turbo использовал актуальные результаты кеша для неизменённого frontend.
- Проверочная браузерная сессия закрыта, собственный сервер остановлен.

Тестовая инфраструктура разрабатываемого по ТЗ сервиса не создавалась. Его ручные
приёмки представлены задачами, а статусы их выполнения являются частью учебного снимка.
