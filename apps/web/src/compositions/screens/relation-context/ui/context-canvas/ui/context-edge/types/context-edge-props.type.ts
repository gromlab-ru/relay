import type { EdgeProps } from "@xyflow/react";
import type { ContextFlowEdge } from "../../../types/context-flow.type";

/** Контракт линии, выбор которой обрабатывает React Flow. */
export type ContextEdgeProps = EdgeProps<ContextFlowEdge>;
