/** Сигнатура готовой геометрии не зависит от выделения и положения камеры. */
export type ContextPortsSyncProps = {
  /** Адреса узлов и наборы портов, разделённые безопасными для ID разделителями. */
  signature: string;
};
