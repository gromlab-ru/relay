import type { NodeProps } from "@xyflow/react";
import type { ContextFlowNode } from "../../../types/context-flow.type";

/** Контракт карточки, которую размещает и фокусирует React Flow. */
export type ContextNodeProps = NodeProps<ContextFlowNode>;
