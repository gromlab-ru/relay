/** Полный человекочитаемый текст выбранной сущности. */
export type EntityContent = {
  /** Однострочный заголовок. */
  title: string;
  /** Markdown; пустая строка для вида без полного описания. */
  markdown: string;
  /** Адрес доски для перехода на её каноническую страницу, если это доска. */
  boardSlug?: string;
};
