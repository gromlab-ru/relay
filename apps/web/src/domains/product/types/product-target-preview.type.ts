/** Краткая цель реализации для компактного обзора без загрузки полного содержания. */
export type ProductTargetPreview = {
  /** Постоянный ID цели. */
  id: string;
  /** Название фичи, сценария или реализации приложения. */
  title: string;
  /** Читаемое название вида цели. */
  label: string;
  /** Вид исходного требования, включая реализации приложений. */
  requirementKind: "feature" | "scenario";
  /** Активность цели в составе приложения. */
  isActive: boolean;
};
