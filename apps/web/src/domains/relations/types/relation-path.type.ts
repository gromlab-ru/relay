/** Кратчайший путь в уже загруженном снимке, не отдельное сохранённое отношение. */
export type RelationPath = {
  /** Адреса сущностей от корня до цели включительно. */
  nodes: string[];
  /** ID реальных связей между последовательными сущностями. */
  edges: string[];
};
