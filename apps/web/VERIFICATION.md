# Проверка доски, SDK и плавности навигации

Исторический журнал. Снимки в `.artifacts` не входят в Git. Перечисленные ниже снимки
документов, дерева фич и сайдбара от 18 сентября недоступны в текущей рабочей копии;
пути сохранены как метаданные прежней проверки, а не действующие ссылки на файлы.

## Библиотека документов — 18 сентября 2026

Собственная headless-сессия `tasks-web-documents-20260918`, API на `4796`, Vite на `5188`.
Использовано временное хранилище `.artifacts/web/application-scope-review/.relay`.

- «Документы» активны и остаются последним подразделом продукта. Мобильная навигация
  закрывается после перехода по Enter. Два ранее скрытых пункта не появились вновь.
- Проверены карточки шести исходных материалов, чтение Markdown с таблицами и чек-листами,
  поиск по содержимому, фильтры области и типа, сортировка по названию.
- Возврат в библиотеку сохраняет поисковый запрос. Есть пустой каталог и пустая выдача.
- Создан и отредактирован документ; проверены предпросмотр и сохранение через Ctrl+Enter.
  Название и текст обязательны. Ошибка пустого текста возвращает из предпросмотра на
  текстовую вкладку и фокусирует поле.
- Выбраны несколько моковых областей из обеих веток. Выбор сценария не включает фичу
  или приложение автоматически. Поиск находит реализацию сценария вместе с контекстом.
- Проверочная ошибка сохранения сохраняет ввод и выбор. После reload восстановлен
  черновик, повторное сохранение успешно. Проверены уход с черновиком и явная отмена правок.
- Имитировано конкурентное изменение ревизии: черновик восстановлен без перезаписи,
  действие «Оставить мой ввод» позволяет сохранить его явно.
- Старая v3-сессия без библиотеки получает шесть документов; пустой набор остаётся пустым.
  Прежняя пользовательская правка фичи сохранена.
- Каталог просмотрен в светлой и тёмной темах на ширинах 1440 / 1024 / 768 / 390.
  Просмотр документа, редактор и выбор областей проверены на широком и мобильном экране.
  Горизонтального переполнения страницы нет; Escape возвращает фокус из выбора связей.
- Axe-core: ноль нарушений WCAG 2 A/AA в проверенных состояниях каталога, чтения,
  редактора и выбора связей. Отдельные `incomplete` по контрасту текста в модальном окне
  оценены визуально по снимкам; они не отмечены как автоматически пройденные.
- В проверенных сценариях нет необработанных JavaScript-ошибок. Проверены lint,
  TypeScript, production-сборка и `git diff --check`.

Снимки в `.artifacts/web/application-scope-review/`:

- Каталог, светлая тема: `documents-light-1440.png`.
- Каталог, тёмная тема: `documents-dark-1440.png`.
- Чтение документа: `document-reader-light-1440.png`.
- Редактор и предпросмотр: `document-editor-light-1440.png`.
- Множественный выбор областей: `document-scopes-light-1440.png`.
- Мобильный выбор областей: `document-scopes-light-390.png`.

Связи здесь — независимые визуальные моки. Их появление в карточках фич, сценариев
и приложений не реализовывалось и не входило в эту проверку.

## Фичи и сценарии — рабочая итерация 18 сентября 2026

Собственная headless-сессия `tasks-web-feature-scenarios-20260918`, Vite на `5187`,
API на `4797`, временное хранилище `.artifacts/web/feature-scenarios-review/.relay`.

В ходе разработки подтверждены:

- Поиск по сценарию сохраняет родительскую фичу и считает готовность по полному составу.
- Ссылка сценария открывает его подраздел с фокусом и прокруткой ниже общей шапки.
- После переименования сценария сохраняется его постоянный ID и адрес.
- Проверочная ошибка сохранения сохраняет ввод; после reload восстанавливается черновик
  с названием и статусом. Повторное сохранение обновляет сценарий и счётчик родителя.
- Локальные изменения сохраняются при возвращении к дереву, фильтры восстанавливаются.
- Круглые индикаторы визуально просмотрены в обеих схемах. После обратной связи
  промежуточный цвет изменён на приглушённый золотистый.
- Последняя итерация: клик по блоку фичи переключает раскрытие, заголовок остаётся
  отдельной ссылкой; ветки соединены с родительскими индикаторами. Две команды заменены
  одной компактной контекстной кнопкой.
- Фичи по умолчанию свёрнуты. Точечно проверены начальное состояние (10 фич, 0 видимых
  сценариев), сохранение раскрытой ветки после возврата и раскрытие результатов поиска.
  Отметка «Текущая доработка» удалена из списка.
- Lint, typecheck, production-сборка и `git diff --check` проходят.

Широкая браузерная приёмка отложена по просьбе пользователя: продолжается работа над UI.
Последний снимок этой итерации:
дерево фич — `.artifacts/web/feature-scenarios-review/tree-connected-light-1440.png`.
Предыдущие снимки в том же каталоге отражают промежуточные варианты оформления.

## Продукт в общем сайдбаре — 18 сентября 2026

Проверка через agent-browser 0.37.1 в собственной headless-сессии
`tasks-web-sidebar-20260918`. API на `4798` использовал временный проект
`.artifacts/web/sidebar-review/.relay`, Vite работал на `5186`.

- В штатной навигации «Продукт» раскрывает «Паспорт», «Фичи» и «Приложения».
  Проверены сворачивание мышью, Space/Enter, выделение текущего подраздела.
- `/projects/<id>/product/passport` открывает новый паспорт «Напроката» из прототипа.
  Старый `/projects/<id>/passport` проверен отдельно: его экран не перенесён в новый раздел.
- Новый паспорт, фичи, карточка AI-чата и приложение API используют общую шапку
  и сайдбар Relay. Отдельного маршрута `/demo/product` в приложении нет.
- Поиск фичи, переходы фича → приложение → фича, редактор и создание приложения
  работают по вложенным адресам текущего проекта.
- Проверочная ошибка сохранения оставляет ввод в редакторе. После reload восстановился
  черновик; повторное сохранение прошло успешно. Изменение сохранилось после перехода
  в рабочие «Планы» и возвращения в «Фичи».
- На мобильном меню открывается в Drawer, закрывается после выбора и по Escape;
  фокус возвращается на кнопку навигации. Проверено выделение «Приложений» в Drawer.
- Светлая и тёмная темы, ширины 1440 / 1024 / 768 / 390: у страницы нет горизонтального
  переполнения. Выполнена визуальная проверка общего каркаса, нового паспорта и сайдбара.
- Axe-core с `wcag2a,wcag2aa`: ноль нарушений в проверенных состояниях каталога,
  мобильного паспорта и открытого Drawer в обеих темах. Необработанных JS-ошибок нет.
- Lint, typecheck, production-сборка и `git diff --check` проходят.

Скриншоты в `.artifacts/web/sidebar-review/`:

- Паспорт и сайдбар, светлая тема: `passport-light-1440.png`.
- Паспорт и сайдбар, тёмная тема: `passport-dark-1440.png`.
- Мобильная навигация: `sidebar-light-390.png`.
- Мобильная навигация, тёмная тема: `sidebar-dark-390.png`.
- Каталог фич: `features-light-1440.png`.
- AI-чат: `feature-light-1440.png`.
- Приложения: `applications-light-1440.png`.

## Графитовая тема — 17 сентября 2026

Проверка через agent-browser в собственной headless-сессии `tasks-web-graphite-20260917`.
Отдельный API на порту 4797 и Vite на 5187 использовали временное хранилище
`.artifacts/web/graphite-20260917/.relay`: четыре демонстрационных плана и 17 задач.
В ходе проверки создан ещё один план для сценариев черновика и конфликта.

- Планы и доска просмотрены в светлой и тёмной схемах при 1440, 1024, 768 и 390.
  Переполнения страницы по горизонтали нет; прокрутка остаётся внутри навигации и доски.
- Проверены нейтральные основные и второстепенные действия, выбранные пункты,
  прогресс, статусы, Markdown-ссылки, поля, диалоги и карточка задачи.
  Длинные названия больше не сжимают текст статуса в карточках документов.
- Клавиатурный фокус видим в обеих схемах; проверены Tab, выбор в списке,
  захват карточки клавиатурой, переход к соседней колонке и отмена через Escape.
- После закрытия и повторного открытия редактора плана восстановились название и цель.
- Принудительный отказ запроса сохранения показал ошибку и сохранил ввод.
  После снятия блокировки план успешно создан.
- Конкурентное обновление плана через API вызвало предупреждение о новой версии;
  локальное название и цель остались в редакторе.
- Axe-core 4.12.1 с тегами `wcag2a,wcag2aa`: 0 нарушений на планах в обеих схемах,
  на доске и в проверенных диалогах. Частично скрытые колонки проверены после
  горизонтальной прокрутки; текст полей и вкладок дополнительно просмотрен визуально.
- Полный аудит планов также сообщает о переходе заголовков `h1 → h3`
  (`heading-order`, существующая разметка карточек).
- Необработанных JavaScript-ошибок в проверенных сценариях не обнаружено.
- Lint, typecheck, production-сборка, Prettier затронутых файлов и `git diff --check` пройдены.

Снимки: `.artifacts/web/graphite-20260917/`, в том числе `plans-dark-1440.png`,
`board-light-1440.png`, `form-light-390.png`, `form-conflict.png` и `task-dark.png`.

## Relay local/workspace — 15 сентября 2026

Проверка через agent-browser в собственной headless-сессии `tasks-web-relay-2165372`.
Данные — две временные независимые базы с задачей №1 в каждой; отдельные серверы
local и workspace использовали одну и ту же сборку Web.

- Workspace показывает переключатель проектов; выбор мышью и клавиатурой открывает
  правильную базу и сохраняет её UUID в URL.
- При переходе А → Б задача №1 и её содержимое меняются на данные Б.
- Черновик названия задачи А сохранён, отсутствует в задаче Б и восстанавливается после возврата в А.
- Параллельная правка summary через CLI во время редактирования в браузере вызывает
  сравнение версий. После применения выбора сохраняются локальное название и серверное summary.
- Блокировка SSE даёт предупреждение о разрыве; после снятия блокировки подписка
  автоматически восстанавливается до состояния «Синхронизировано».
- Local автоматически открывает единственный проект; переключатель workspace отсутствует.
- Светлая и тёмная темы просмотрены на ширинах 1440, 1024, 768 и 390.
  Горизонтальная прокрутка ограничена доской, переключатель доступен на узком экране.
- Axe-core: 0 нарушений на доске в обеих темах. Колонки, оставленные аудитором
  для ручной проверки контраста за границей узкого экрана, просмотрены в широком виде.
- Необработанных JavaScript-ошибок в проверенных сценариях не обнаружено.

Снимки: `.artifacts/web/relay-2165372-{light,dark}-{1440,1024,768,390}.png`.
Команды генерации ProjectScope и RelayScreen записаны в [GENERATION.md](GENERATION.md).

## Предыдущая проверка

Дата: 14 сентября 2026. Проверка выполнена через agent-browser в отдельной
headless-сессии `tasks-web-groups-20260914`.

Начальные данные — временная копия playground из 190 задач. Для проверки
внешних изменений в копию добавлена одна задача; изменены несколько карточек.
`demo:check` подтвердил эталонные 190 задач, 55 комментариев и 106 отчётов.

## Интерфейс

- Светлая и тёмная темы: ширины 1440, 1024, 768 и 390.
- Колесо над карточкой прокручивает страницу: измерено `scrollY = 750`,
  у всех пяти тел колонок `scrollTop = 0`.
- Горизонтальная прокрутка доски и навигации по группам на узком экране.
- Все десять групп демо и полные счётчики; выбор `backend` показывает 54 задачи.
- Поиск с пустым результатом сохраняет полные счётчики групп.
- Клавиатурный выбор группы, `/` для поиска и `N` для создания.
- Создание подставляет текущую группу, включая длинное название.
- Back и обновление страницы сохраняют восстановленную область; сброс поиска
  сохраняет выбранную группу.
- После внешнего создания автоматически появились «Без группы · 1» и общий
  счётчик 191; область `ungrouped=true` показывает одну правильную карточку.
- Длинное название группы обрезается визуально, сохраняя полный текст в доступном имени
  и доступный счётчик; горизонтального переполнения страницы нет.

Аудит axe-core 4.12.1 доски в двух темах при 1440: **0 нарушений**.
При 390 также **0 нарушений**; аудит оставляет ручную проверку контраста
колонок за горизонтальной границей экрана. Эти колонки проверены в широком виде.

## Плавность подгрузки

Для следующей страницы искусственно задержан ответ GET на 900 мс.
Во время запроса состояние DOM и прокрутки наблюдалось через animation frames.

| Сценарий                                    | Результат                                                         |
| ------------------------------------------- | ----------------------------------------------------------------- |
| 40 → 80 карточек                            | 109 кадров; минимум 40, максимум 80 карточек                      |
| Сохранность старых карточек                 | Все исходные DOM-узлы остались подключены                         |
| Положение страницы                          | Максимальное отклонение `scrollY` — **0 px**                      |
| SSE во время подгрузки                      | **0** переподключений, **0** ошибок                               |
| Последняя страница, 80 → 93                 | Прежние DOM-узлы сохранены, отклонение **0 px**                   |
| Принудительный отказ GET следующей страницы | Остались 40 карточек; SSE подключён, предупреждения о разрыве нет |
| Повтор после снятия отказа                  | Загружены 80 карточек                                             |

## Соединение и конкурентность

- Production-сервер: несколько минут простоя, одна подписка, heartbeat без ошибок.
- Через отдельный Vite-прокси: за 81 секунду получено пять heartbeat,
  соединение установлено один раз, ошибок и предупреждений нет.
- Короткий перезапуск API: тот же экземпляр EventSource восстановил подписку;
  наблюдение за DOM не зафиксировало предупреждения о разрыве.
- Длительное отключение: предупреждение появилось, введённое название сохранилось.
- Пока сервер был отключён, карточка изменена другим клиентом через Core.
  После запуска появились актуальные данные и новая группа, локальное название осталось.
- Сохранение старой ревизии открыло сравнение версий. Выбор своей версии и последующее
  сохранение прошли успешно.

### Отмена SSE в dev-режиме

- Дополнительно проверены именно `http://127.0.0.1:5173/api/v1/events` и прямой
  API на порту 3000. Оба вернули 200 и передавали heartbeat без обрыва
  в течение 35 секунд наблюдения; чтение завершено заданным лимитом времени.
- В браузере отдельно воспроизведён цикл подписка → освобождение → подписка.
  До исправления создавались **2** EventSource и первый немедленно закрывался.
- После исправления тот же цикл создаёт **1** EventSource без раннего закрытия.
- Разрыв между освобождением и повторной подпиской в 40 мс также сохраняет
  единственный поток: **0** закрытий и **0** ошибок.
- После окончательного освобождения закрытие произошло один раз: ресурс очищен.
- Отдельно остановлен backend за Vite-прокси. В ответах SSE зафиксирован 502:
  прежняя реализация оставалась отключённой даже после запуска backend.
- С исправлением зафиксированы повторные запросы после 502 и последующий 200.
  Индикатор вернулся в «Синхронизировано», предупреждение исчезло, 108 загруженных
  карточек остались на доске. Маркер `same-page-after-502` сохранился:
  восстановление произошло без перезагрузки страницы.

## Перенос и ссылки

- Клавиатурный перенос в соседнюю пустую колонку проверен при 1440 и 390.
- Сортировка вниз внутри колонки: порядок `12, 28, 30` стал `28, 12, 30`.
- Мышью перенесена последняя задача длинной колонки в пустую нижнюю область
  другой колонки; сервер подтвердил выбранный статус.
- Markdown-ссылка `/tasks/126` из задачи #12 преобразована в
  `/tasks/126?group=product` и открыта через React Router.
- При переходе сохранены маркер экземпляра приложения, длина истории и
  счётчики EventSource; перезагрузки документа не было.
- Закрытие целевой карточки вернуло на исходную доску `/?group=product`.

## Сборка и контракты

- Полная сборка workspaces и проверки lint/typecheck приложения — успешно.
- Core: **12** тестов; Server Runtime: **26**; standalone/dev-сервер: **3**.
- Проверка типов Contracts и Server Runtime — успешно.
- SDK собран в ESM; все публичные входы загружаются в Node.js, отдельная операция
  `get-board` успешно прочитала реальный API.
- `npm pack --dry-run`: 48 файлов, JavaScript и декларации из `dist`, исходники
  не входят в состав архива.
- Prettier и `git diff --check` — успешно.

Команды генерации новых TSX зафиксированы в [GENERATION.md](GENERATION.md).
Снимки находятся в `.artifacts/web/review-20260914/` от корня репозитория:
`board-{light,dark}-{1440,1024,768,390}.png`, `long-group-390.png`,
`disconnected-draft.png`, `reconnected-conflict.png`.
