# Команды Relay CLI

Справочник сформирован из зарегистрированных команд. Порядок работы и примеры — в [главном руководстве](../SKILL.md) и [сценариях](EXAMPLES.md).

`relay-cli` означает установленную команду или `npx @gromlab/relay-cli`. В workspace указывайте проект префиксом либо `--project`. Конкретный help: `relay-cli <команда> --help`.

## Общие параметры

| Параметр              | Назначение                                                                     |
| --------------------- | ------------------------------------------------------------------------------ |
| `-V, --version`       | Показать версию CLI                                                            |
| `--config <path>`     | Путь к .relay/config.json или relay.workspace.json; приоритет над RELAY_CONFIG |
| `--project <name>`    | Имя проекта из workspace; также relay-cli <проект> <команда>                   |
| `--server-url <url>`  | Адрес сервера; приоритет над RELAY_SERVER_URL и server.url                     |
| `--local`             | Работать напрямую с локальным Core, игнорируя HTTP-настройки                   |
| `--actor <id>`        | Автор записи, например human; приоритет над RELAY_ACTOR                        |
| `--format <format>`   | Формат ответа Значения: json, text.                                            |
| `--color <mode>`      | Цветовая подсветка (по умолчанию auto) Значения: auto, always, never.          |
| `--max-bytes <bytes>` | Максимальный размер ответа UTF-8                                               |

## init

**Синтаксис:** `relay-cli init [options]`.

Создать конфиг и хранилище проекта

| Параметр           | Назначение                                                 |
| ------------------ | ---------------------------------------------------------- |
| `--storage <path>` | Каталог данных относительно конфига По умолчанию: "tasks". |

## validate

**Синтаксис:** `relay-cli validate [options]`.

Проверить продукт, доски, задачи и связи

## config get

**Синтаксис:** `relay-cli config get [options]`.

Показать конфиг, статусы, цвета и пути

## projects init

**Синтаксис:** `relay-cli projects init [options]`.

Создать конфиг проектов

## projects list

**Синтаксис:** `relay-cli projects list [options]`.

Показать зарегистрированные проекты

## projects add

**Синтаксис:** `relay-cli projects add [options] <name> [path]`.

Зарегистрировать или обновить проект

| Аргумент | Назначение                                  |
| -------- | ------------------------------------------- |
| `name`   | Имя проекта в workspace                     |
| `path`   | Путь проекта относительно workspace-конфига |

| Параметр                  | Назначение                           |
| ------------------------- | ------------------------------------ |
| `--project-config <path>` | Конфиг относительно каталога проекта |
| `--replace`               | Заменить существующее подключение    |

## projects remove

**Синтаксис:** `relay-cli projects remove [options] <name>`.

Удалить регистрацию проекта, сохранив его данные

| Аргумент | Назначение                        |
| -------- | --------------------------------- |
| `name`   | Имя удаляемой регистрации проекта |

## product state

**Синтаксис:** `relay-cli product state [options]`.

Полный снимок продукта

## product overview

**Синтаксис:** `relay-cli product overview [options]`.

Компактная карта продукта

## product list

**Синтаксис:** `relay-cli product list [options]`.

Найти записи продукта

| Параметр        | Назначение      |
| --------------- | --------------- |
| `--kind <kind>` | Вид записи      |
| `--q <text>`    | Поиск           |
| `--offset <n>`  | Смещение        |
| `--limit <n>`   | Размер страницы |

## product get

**Синтаксис:** `relay-cli product get [options] <id>`.

Прочитать одну запись

| Аргумент | Назначение                                        |
| -------- | ------------------------------------------------- |
| `id`     | ID или ключ записи, включая реализацию: WEB-FI-12 |

## product entities

**Синтаксис:** `relay-cli product entities [options]`.

Найти цели для связи по ключу или названию

| Параметр              | Назначение                                                                   |
| --------------------- | ---------------------------------------------------------------------------- |
| `--q <text>`          | Ключ или название                                                            |
| `--kind <kind>`       | Тип цели: feature, scenario, application, implementation, passport, document |
| `--application <ref>` | ID или ключ приложения                                                       |
| `--active <value>`    | Участие: true или false                                                      |
| `--offset <n>`        | Смещение                                                                     |
| `--limit <n>`         | Размер страницы                                                              |

## product implementation update

**Синтаксис:** `relay-cli product implementation update [options] <ref>`.

Изменить реализацию или разрешить конфликт ключа

| Аргумент | Назначение             |
| -------- | ---------------------- |
| `ref`    | ID или ключ реализации |

| Параметр                   | Назначение                             |
| -------------------------- | -------------------------------------- |
| `--if-revision <n>`        | Ревизия реализации                     |
| `--title <text>`           | Однострочный заголовок                 |
| `--description <markdown>` | Полное описание Markdown               |
| `--status <status>`        | none, partial или done                 |
| `--key <key>`              | Свободный ключ; ID и связи сохраняются |
| `--request-id <id>`        | Ключ повтора                           |

## product context

**Синтаксис:** `relay-cli product context [options]`.

Собрать связанный контекст

| Параметр             | Назначение        |
| -------------------- | ----------------- |
| `--id <id>`          | Цель контекста    |
| `--application <id>` | Проект-реализатор |

## product validate

**Синтаксис:** `relay-cli product validate [options]`.

Проверить целостность продукта

## product lint

**Синтаксис:** `relay-cli product lint [options]`.

Проверить структуру продуктовых описаний

| Параметр       | Назначение                       |
| -------------- | -------------------------------- |
| `--id <id>`    | Проверить отдельную запись       |
| `--offset <n>` | Смещение предупреждений          |
| `--limit <n>`  | Число предупреждений на странице |

## product migrate

**Синтаксис:** `relay-cli product migrate [options]`.

Перенести продукт в каталоги и многострочный JSON

## product save

**Синтаксис:** `relay-cli product save [options]`.

Создать или изменить запись через JSON

| Параметр                   | Назначение                             |
| -------------------------- | -------------------------------------- |
| `--json <json>`            | JSON операции                          |
| `--description <markdown>` | Многострочный Markdown напрямую        |
| `--body <markdown>`        | Многострочный текст документа напрямую |

## product passport create

**Синтаксис:** `relay-cli product passport create [options]`.

Создать запись

| Параметр                   | Назначение                                                                                  |
| -------------------------- | ------------------------------------------------------------------------------------------- |
| `--name <name>`            | Название                                                                                    |
| `--summary <text>`         | Краткое описание По умолчанию: "".                                                          |
| `--description <markdown>` | Markdown: цель, правила, шаги, ошибки и критерии по смыслу                                  |
| `--body <markdown>`        | Структурированный Markdown документа напрямую                                               |
| `--type <type>`            | Тип приложения: frontend/backend/internal По умолчанию: "frontend".                         |
| `--feature <id>`           | Родительская фича сценария                                                                  |
| `--links <json>`           | Типизированные связи документа По умолчанию: "[]".                                          |
| `--document-kind <kind>`   | Назначение документа: specification/description/rules/decision По умолчанию: "description". |
| `--if-revision <n>`        | Прочитанная ревизия                                                                         |
| `--request-id <id>`        | Ключ безопасного повтора                                                                    |

## product passport update

**Синтаксис:** `relay-cli product passport update [options] <id>`.

Изменить запись

| Аргумент | Назначение    |
| -------- | ------------- |
| `id`     | Постоянный ID |

| Параметр                   | Назначение                                                                                  |
| -------------------------- | ------------------------------------------------------------------------------------------- |
| `--name <name>`            | Название                                                                                    |
| `--summary <text>`         | Краткое описание По умолчанию: "".                                                          |
| `--description <markdown>` | Markdown: цель, правила, шаги, ошибки и критерии по смыслу                                  |
| `--body <markdown>`        | Структурированный Markdown документа напрямую                                               |
| `--type <type>`            | Тип приложения: frontend/backend/internal По умолчанию: "frontend".                         |
| `--feature <id>`           | Родительская фича сценария                                                                  |
| `--links <json>`           | Типизированные связи документа По умолчанию: "[]".                                          |
| `--document-kind <kind>`   | Назначение документа: specification/description/rules/decision По умолчанию: "description". |
| `--if-revision <n>`        | Прочитанная ревизия                                                                         |
| `--request-id <id>`        | Ключ безопасного повтора                                                                    |

## product feature create

**Синтаксис:** `relay-cli product feature create [options]`.

Создать запись

| Параметр                   | Назначение                                                                                  |
| -------------------------- | ------------------------------------------------------------------------------------------- |
| `--name <name>`            | Название                                                                                    |
| `--summary <text>`         | Краткое описание По умолчанию: "".                                                          |
| `--description <markdown>` | Markdown: цель, правила, шаги, ошибки и критерии по смыслу                                  |
| `--body <markdown>`        | Структурированный Markdown документа напрямую                                               |
| `--type <type>`            | Тип приложения: frontend/backend/internal По умолчанию: "frontend".                         |
| `--feature <id>`           | Родительская фича сценария                                                                  |
| `--links <json>`           | Типизированные связи документа По умолчанию: "[]".                                          |
| `--document-kind <kind>`   | Назначение документа: specification/description/rules/decision По умолчанию: "description". |
| `--if-revision <n>`        | Прочитанная ревизия                                                                         |
| `--request-id <id>`        | Ключ безопасного повтора                                                                    |

## product feature update

**Синтаксис:** `relay-cli product feature update [options] <id>`.

Изменить запись

| Аргумент | Назначение    |
| -------- | ------------- |
| `id`     | Постоянный ID |

| Параметр                   | Назначение                                                                                  |
| -------------------------- | ------------------------------------------------------------------------------------------- |
| `--name <name>`            | Название                                                                                    |
| `--summary <text>`         | Краткое описание По умолчанию: "".                                                          |
| `--description <markdown>` | Markdown: цель, правила, шаги, ошибки и критерии по смыслу                                  |
| `--body <markdown>`        | Структурированный Markdown документа напрямую                                               |
| `--type <type>`            | Тип приложения: frontend/backend/internal По умолчанию: "frontend".                         |
| `--feature <id>`           | Родительская фича сценария                                                                  |
| `--links <json>`           | Типизированные связи документа По умолчанию: "[]".                                          |
| `--document-kind <kind>`   | Назначение документа: specification/description/rules/decision По умолчанию: "description". |
| `--if-revision <n>`        | Прочитанная ревизия                                                                         |
| `--request-id <id>`        | Ключ безопасного повтора                                                                    |

## product scenario create

**Синтаксис:** `relay-cli product scenario create [options]`.

Создать запись

| Параметр                   | Назначение                                                                                  |
| -------------------------- | ------------------------------------------------------------------------------------------- |
| `--name <name>`            | Название                                                                                    |
| `--summary <text>`         | Краткое описание По умолчанию: "".                                                          |
| `--description <markdown>` | Markdown: цель, правила, шаги, ошибки и критерии по смыслу                                  |
| `--body <markdown>`        | Структурированный Markdown документа напрямую                                               |
| `--type <type>`            | Тип приложения: frontend/backend/internal По умолчанию: "frontend".                         |
| `--feature <id>`           | Родительская фича сценария                                                                  |
| `--links <json>`           | Типизированные связи документа По умолчанию: "[]".                                          |
| `--document-kind <kind>`   | Назначение документа: specification/description/rules/decision По умолчанию: "description". |
| `--if-revision <n>`        | Прочитанная ревизия                                                                         |
| `--request-id <id>`        | Ключ безопасного повтора                                                                    |

## product scenario update

**Синтаксис:** `relay-cli product scenario update [options] <id>`.

Изменить запись

| Аргумент | Назначение    |
| -------- | ------------- |
| `id`     | Постоянный ID |

| Параметр                   | Назначение                                                                                  |
| -------------------------- | ------------------------------------------------------------------------------------------- |
| `--name <name>`            | Название                                                                                    |
| `--summary <text>`         | Краткое описание По умолчанию: "".                                                          |
| `--description <markdown>` | Markdown: цель, правила, шаги, ошибки и критерии по смыслу                                  |
| `--body <markdown>`        | Структурированный Markdown документа напрямую                                               |
| `--type <type>`            | Тип приложения: frontend/backend/internal По умолчанию: "frontend".                         |
| `--feature <id>`           | Родительская фича сценария                                                                  |
| `--links <json>`           | Типизированные связи документа По умолчанию: "[]".                                          |
| `--document-kind <kind>`   | Назначение документа: specification/description/rules/decision По умолчанию: "description". |
| `--if-revision <n>`        | Прочитанная ревизия                                                                         |
| `--request-id <id>`        | Ключ безопасного повтора                                                                    |

## product application create

**Синтаксис:** `relay-cli product application create [options]`.

Создать запись

| Параметр                   | Назначение                                                                                  |
| -------------------------- | ------------------------------------------------------------------------------------------- |
| `--prefix <prefix>`        | Неизменяемый префикс задач: WEB, API; по умолчанию из slug                                  |
| `--slug <slug>`            | Неизменяемый адрес приложения и доски: латинские строчные буквы, цифры и дефисы             |
| `--name <name>`            | Название                                                                                    |
| `--summary <text>`         | Краткое описание По умолчанию: "".                                                          |
| `--description <markdown>` | Markdown: цель, правила, шаги, ошибки и критерии по смыслу                                  |
| `--body <markdown>`        | Структурированный Markdown документа напрямую                                               |
| `--type <type>`            | Тип приложения: frontend/backend/internal По умолчанию: "frontend".                         |
| `--feature <id>`           | Родительская фича сценария                                                                  |
| `--links <json>`           | Типизированные связи документа По умолчанию: "[]".                                          |
| `--document-kind <kind>`   | Назначение документа: specification/description/rules/decision По умолчанию: "description". |
| `--if-revision <n>`        | Прочитанная ревизия                                                                         |
| `--request-id <id>`        | Ключ безопасного повтора                                                                    |

## product application update

**Синтаксис:** `relay-cli product application update [options] <id>`.

Изменить запись

| Аргумент | Назначение    |
| -------- | ------------- |
| `id`     | Постоянный ID |

| Параметр                   | Назначение                                                                                  |
| -------------------------- | ------------------------------------------------------------------------------------------- |
| `--prefix <prefix>`        | Неизменяемый префикс задач: WEB, API; по умолчанию из slug                                  |
| `--slug <slug>`            | Неизменяемый адрес приложения и доски: латинские строчные буквы, цифры и дефисы             |
| `--name <name>`            | Название                                                                                    |
| `--summary <text>`         | Краткое описание По умолчанию: "".                                                          |
| `--description <markdown>` | Markdown: цель, правила, шаги, ошибки и критерии по смыслу                                  |
| `--body <markdown>`        | Структурированный Markdown документа напрямую                                               |
| `--type <type>`            | Тип приложения: frontend/backend/internal По умолчанию: "frontend".                         |
| `--feature <id>`           | Родительская фича сценария                                                                  |
| `--links <json>`           | Типизированные связи документа По умолчанию: "[]".                                          |
| `--document-kind <kind>`   | Назначение документа: specification/description/rules/decision По умолчанию: "description". |
| `--if-revision <n>`        | Прочитанная ревизия                                                                         |
| `--request-id <id>`        | Ключ безопасного повтора                                                                    |

## product document create

**Синтаксис:** `relay-cli product document create [options]`.

Создать запись

| Параметр                   | Назначение                                                                                  |
| -------------------------- | ------------------------------------------------------------------------------------------- |
| `--name <name>`            | Название                                                                                    |
| `--summary <text>`         | Краткое описание По умолчанию: "".                                                          |
| `--description <markdown>` | Markdown: цель, правила, шаги, ошибки и критерии по смыслу                                  |
| `--body <markdown>`        | Структурированный Markdown документа напрямую                                               |
| `--type <type>`            | Тип приложения: frontend/backend/internal По умолчанию: "frontend".                         |
| `--feature <id>`           | Родительская фича сценария                                                                  |
| `--links <json>`           | Типизированные связи документа По умолчанию: "[]".                                          |
| `--document-kind <kind>`   | Назначение документа: specification/description/rules/decision По умолчанию: "description". |
| `--if-revision <n>`        | Прочитанная ревизия                                                                         |
| `--request-id <id>`        | Ключ безопасного повтора                                                                    |

## product document update

**Синтаксис:** `relay-cli product document update [options] <id>`.

Изменить запись

| Аргумент | Назначение    |
| -------- | ------------- |
| `id`     | Постоянный ID |

| Параметр                   | Назначение                                                                                  |
| -------------------------- | ------------------------------------------------------------------------------------------- |
| `--name <name>`            | Название                                                                                    |
| `--summary <text>`         | Краткое описание По умолчанию: "".                                                          |
| `--description <markdown>` | Markdown: цель, правила, шаги, ошибки и критерии по смыслу                                  |
| `--body <markdown>`        | Структурированный Markdown документа напрямую                                               |
| `--type <type>`            | Тип приложения: frontend/backend/internal По умолчанию: "frontend".                         |
| `--feature <id>`           | Родительская фича сценария                                                                  |
| `--links <json>`           | Типизированные связи документа По умолчанию: "[]".                                          |
| `--document-kind <kind>`   | Назначение документа: specification/description/rules/decision По умолчанию: "description". |
| `--if-revision <n>`        | Прочитанная ревизия                                                                         |
| `--request-id <id>`        | Ключ безопасного повтора                                                                    |

## product scope replace

**Синтаксис:** `relay-cli product scope replace [options] <applicationId>`.

Заменить весь активный состав

| Аргумент        | Назначение             |
| --------------- | ---------------------- |
| `applicationId` | ID или ключ приложения |

| Параметр                 | Назначение                    |
| ------------------------ | ----------------------------- |
| `--json <json>`          | Массив контрактов JSON        |
| `--if-revision <n>`      | Ревизия состава; 0 для нового |
| `--if-version <version>` | Версия прочитанного продукта  |
| `--request-id <id>`      | Ключ повтора                  |

## product contract update

**Синтаксис:** `relay-cli product contract update [options] <id>`.

Изменить или подтвердить контракт

| Аргумент | Назначение   |
| -------- | ------------ |
| `id`     | ID контракта |

| Параметр                   | Назначение                               |
| -------------------------- | ---------------------------------------- |
| `--application <id>`       | Приложение                               |
| `--status <status>`        | Готовность реализации: none/partial/done |
| `--title <text>`           | Заголовок вклада                         |
| `--description <markdown>` | Многострочное описание напрямую          |
| `--if-revision <n>`        | Ревизия состава                          |
| `--if-version <version>`   | Версия прочитанного продукта             |
| `--request-id <id>`        | Ключ повтора                             |

## boards

**Синтаксис:** `relay-cli boards [options]`.

Доски проекта и префиксы задач

| Параметр              | Назначение                                            |
| --------------------- | ----------------------------------------------------- |
| `--offset <n>`        | Смещение страницы                                     |
| `--limit <n>`         | Размер страницы, максимум 100                         |
| `--version <version>` | Версия первой страницы для согласованного продолжения |

## task comment list

**Синтаксис:** `relay-cli task comment list [options] <reference>`.

Прочитать страницу ленты

| Аргумент    | Назначение                         |
| ----------- | ---------------------------------- |
| `reference` | ID или текущий/прежний ключ задачи |

| Параметр            | Назначение                             |
| ------------------- | -------------------------------------- |
| `--limit <n>`       | Размер страницы, 1–100                 |
| `--cursor <cursor>` | Курсор продолжения предыдущей страницы |
| `--after <n>`       | Только после последовательного номера  |
| `--by <name>`       | Точное имя автора записей              |
| `--action <action>` | Тип действия, например update          |

## task comment get

**Синтаксис:** `relay-cli task comment get [options] <reference> <entryId>`.

Прочитать полное содержание записи

| Аргумент    | Назначение             |
| ----------- | ---------------------- |
| `reference` | ID или ключ задачи     |
| `entryId`   | Номер записи из списка |

## task comment publish

**Синтаксис:** `relay-cli task comment publish [options] <reference>`.

Опубликовать сообщение от своего имени

| Аргумент    | Назначение         |
| ----------- | ------------------ |
| `reference` | ID или ключ задачи |

| Параметр                   | Назначение                                  |
| -------------------------- | ------------------------------------------- |
| `--title <text>`           | Обязательный однострочный заголовок         |
| `--description <markdown>` | Полное сообщение в Markdown                 |
| `--role <role>`            | Роль: operator, orchestrator или worker     |
| `--request-id <id>`        | Ключ безопасного повтора; по умолчанию UUID |

## task history list

**Синтаксис:** `relay-cli task history list [options] <reference>`.

Прочитать страницу ленты

| Аргумент    | Назначение                         |
| ----------- | ---------------------------------- |
| `reference` | ID или текущий/прежний ключ задачи |

| Параметр            | Назначение                             |
| ------------------- | -------------------------------------- |
| `--limit <n>`       | Размер страницы, 1–100                 |
| `--cursor <cursor>` | Курсор продолжения предыдущей страницы |
| `--after <n>`       | Только после последовательного номера  |
| `--by <name>`       | Точное имя автора записей              |
| `--action <action>` | Тип действия, например update          |

## task history get

**Синтаксис:** `relay-cli task history get [options] <reference> <entryId>`.

Прочитать полное содержание записи

| Аргумент    | Назначение             |
| ----------- | ---------------------- |
| `reference` | ID или ключ задачи     |
| `entryId`   | Номер записи из списка |

## task criterion list

**Синтаксис:** `relay-cli task criterion list [options] <reference>`.

Список критериев без полного Markdown

| Аргумент    | Назначение         |
| ----------- | ------------------ |
| `reference` | ID или ключ задачи |

| Параметр              | Назначение                                            |
| --------------------- | ----------------------------------------------------- |
| `--offset <n>`        | Смещение страницы                                     |
| `--limit <n>`         | Размер страницы, максимум 100                         |
| `--version <version>` | Версия первой страницы для согласованного продолжения |

## task criterion get

**Синтаксис:** `relay-cli task criterion get [options] <reference> <criterionId>`.

Полное описание и состояние критерия

| Аргумент      | Назначение             |
| ------------- | ---------------------- |
| `reference`   | ID или ключ задачи     |
| `criterionId` | Постоянный ID критерия |

## task criterion add

**Синтаксис:** `relay-cli task criterion add [options] <reference>`.

Добавить критерий

| Аргумент    | Назначение         |
| ----------- | ------------------ |
| `reference` | ID или ключ задачи |

| Параметр                   | Назначение                                                     |
| -------------------------- | -------------------------------------------------------------- |
| `--if-revision <n>`        | Прочитанная ревизия задачи                                     |
| `--request-id <id>`        | Ключ безопасного повтора; по умолчанию создаётся автоматически |
| `--title <title>`          | Обязательный однострочный заголовок                            |
| `--summary <text>`         | Краткое описание обычным многострочным текстом                 |
| `--description <markdown>` | Полное описание в Markdown                                     |

## task criterion update

**Синтаксис:** `relay-cli task criterion update [options] <reference> <criterionId>`.

Изменить критерий и сбросить выполнение при изменении текста

| Аргумент      | Назначение             |
| ------------- | ---------------------- |
| `reference`   | ID или ключ задачи     |
| `criterionId` | Постоянный ID критерия |

| Параметр                   | Назначение                                                     |
| -------------------------- | -------------------------------------------------------------- |
| `--if-revision <n>`        | Прочитанная ревизия задачи                                     |
| `--request-id <id>`        | Ключ безопасного повтора; по умолчанию создаётся автоматически |
| `--title <title>`          | Новый однострочный заголовок                                   |
| `--summary <text>`         | Краткое описание обычным многострочным текстом                 |
| `--description <markdown>` | Полное описание в Markdown                                     |

## task criterion complete

**Синтаксис:** `relay-cli task criterion complete [options] <reference> <criterionId>`.

Отметить критерий выполненным

| Аргумент      | Назначение             |
| ------------- | ---------------------- |
| `reference`   | ID или ключ задачи     |
| `criterionId` | Постоянный ID критерия |

| Параметр            | Назначение                                                     |
| ------------------- | -------------------------------------------------------------- |
| `--if-revision <n>` | Прочитанная ревизия задачи                                     |
| `--request-id <id>` | Ключ безопасного повтора; по умолчанию создаётся автоматически |

## task criterion reopen

**Синтаксис:** `relay-cli task criterion reopen [options] <reference> <criterionId>`.

Снять отметку выполнения

| Аргумент      | Назначение             |
| ------------- | ---------------------- |
| `reference`   | ID или ключ задачи     |
| `criterionId` | Постоянный ID критерия |

| Параметр            | Назначение                                                     |
| ------------------- | -------------------------------------------------------------- |
| `--if-revision <n>` | Прочитанная ревизия задачи                                     |
| `--request-id <id>` | Ключ безопасного повтора; по умолчанию создаётся автоматически |

## task criterion remove

**Синтаксис:** `relay-cli task criterion remove [options] <reference> <criterionId>`.

Удалить критерий

| Аргумент      | Назначение             |
| ------------- | ---------------------- |
| `reference`   | ID или ключ задачи     |
| `criterionId` | Постоянный ID критерия |

| Параметр            | Назначение                                                     |
| ------------------- | -------------------------------------------------------------- |
| `--if-revision <n>` | Прочитанная ревизия задачи                                     |
| `--request-id <id>` | Ключ безопасного повтора; по умолчанию создаётся автоматически |

## task list

**Синтаксис:** `relay-cli task list [options]`.

Найти задачи и блокеры на всех или одной доске

| Параметр                | Назначение                                                         |
| ----------------------- | ------------------------------------------------------------------ |
| `--offset <n>`          | Смещение страницы                                                  |
| `--limit <n>`           | Размер страницы, максимум 100                                      |
| `--version <version>`   | Версия первой страницы для согласованного продолжения              |
| `--board <board>`       | Slug, префикс или ID доски                                         |
| `--column <column>`     | Ключ колонки                                                       |
| `--completion <state>`  | unfinished — без готовых и отменённых; finished — только они       |
| `--search-in <scope>`   | title — ключи, ID и заголовок; all — также Markdown (по умолчанию) |
| `--product-target <id>` | Только задачи, явно реализующие продуктовую цель с этим ID         |
| `--q <text>`            | Поиск по ключу, заголовку и Markdown                               |
| `--readiness <state>`   | blocked — с блокерами, ready — готовые к выполнению                |

## task get

**Синтаксис:** `relay-cli task get [options] <reference>`.

Открыть задачу с полным Markdown и блокерами

| Аргумент    | Назначение                         |
| ----------- | ---------------------------------- |
| `reference` | ID или текущий/прежний ключ задачи |

## task links

**Синтаксис:** `relay-cli task links [options] <reference>`.

Зависимости, блокируемые задачи, родитель и подзадачи

| Аргумент    | Назначение         |
| ----------- | ------------------ |
| `reference` | ID или ключ задачи |

| Параметр              | Назначение                                            |
| --------------------- | ----------------------------------------------------- |
| `--offset <n>`        | Смещение страницы                                     |
| `--limit <n>`         | Размер страницы, максимум 100                         |
| `--version <version>` | Версия первой страницы для согласованного продолжения |

## task create

**Синтаксис:** `relay-cli task create [options]`.

Создать задачу на доске

| Параметр                    | Назначение                                                                                |
| --------------------------- | ----------------------------------------------------------------------------------------- |
| `--request-id <id>`         | Ключ безопасного повтора; если не указан, создаётся автоматически                         |
| `--board <board>`           | Slug, префикс или ID доски                                                                |
| `--title <title>`           | Однострочный заголовок; по умолчанию пустой                                               |
| `--description <markdown>`  | Полное описание Markdown напрямую                                                         |
| `--feature <ids...>`        | ID реализуемых общих фич; вместе с другими целями заменяет набор связей                   |
| `--scenario <ids...>`       | ID реализуемых общих сценариев                                                            |
| `--implementation <ids...>` | ID реализуемых контрактов приложений                                                      |
| `--clear-product-links`     | Удалить все продуктовые связи (не совмещать с целями)                                     |
| `--column <column>`         | Колонка; по умолчанию inbox                                                               |
| `--criteria <json>`         | Массив критериев {title,summary,description} в JSON для атомарного создания; максимум 100 |
| `--parent-id <id>`          | Постоянный ID родителя для атомарного создания подзадачи                                  |

## task update

**Синтаксис:** `relay-cli task update [options] <reference>`.

Изменить содержание или продуктовые связи задачи

| Аргумент    | Назначение                    |
| ----------- | ----------------------------- |
| `reference` | Постоянный ID или ключ задачи |

| Параметр                    | Назначение                                                              |
| --------------------------- | ----------------------------------------------------------------------- |
| `--request-id <id>`         | Ключ безопасного повтора; если не указан, создаётся автоматически       |
| `--if-revision <n>`         | Прочитанная ревизия задачи                                              |
| `--title <title>`           | Новый однострочный заголовок                                            |
| `--description <markdown>`  | Полное описание Markdown напрямую                                       |
| `--feature <ids...>`        | ID реализуемых общих фич; вместе с другими целями заменяет набор связей |
| `--scenario <ids...>`       | ID реализуемых общих сценариев                                          |
| `--implementation <ids...>` | ID реализуемых контрактов приложений                                    |
| `--clear-product-links`     | Удалить все продуктовые связи (не совмещать с целями)                   |

## task move

**Синтаксис:** `relay-cli task move [options] <reference>`.

Перенести задачу в колонку или на другую доску

| Аргумент    | Назначение                    |
| ----------- | ----------------------------- |
| `reference` | Постоянный ID или ключ задачи |

| Параметр                 | Назначение                                                        |
| ------------------------ | ----------------------------------------------------------------- |
| `--request-id <id>`      | Ключ безопасного повтора; если не указан, создаётся автоматически |
| `--if-revision <n>`      | Прочитанная ревизия задачи                                        |
| `--board <board>`        | Целевая доска; новый ключ, прежний ID                             |
| `--column <column>`      | inbox, ready, in-progress, review, done или cancelled             |
| `--before-id <id>`       | Вставить перед указанной задачей; по умолчанию конец колонки      |
| `--if-version <version>` | Версия порядка задач до перемещения                               |

## task link

**Синтаксис:** `relay-cli task link [options] <reference>`.

Добавить или удалить связь задач

| Аргумент    | Назначение                    |
| ----------- | ----------------------------- |
| `reference` | Постоянный ID или ключ задачи |

| Параметр                | Назначение                                                        |
| ----------------------- | ----------------------------------------------------------------- |
| `--request-id <id>`     | Ключ безопасного повтора; если не указан, создаётся автоматически |
| `--if-revision <n>`     | Прочитанная ревизия задачи                                        |
| `--target <reference>`  | ID или ключ второй задачи                                         |
| `--relation <relation>` | depends-on, related или parent                                    |
| `--remove`              | Удалить указанную связь                                           |

## graph migrate

**Синтаксис:** `relay-cli graph migrate [options]`.

Перенести единый JSON графа в раздельное хранилище v2

## graph reindex

**Синтаксис:** `relay-cli graph reindex [options]`.

Восстановить индексы графа и истории из постоянных записей

## graph list

**Синтаксис:** `relay-cli graph list [options]`.

Прочитать граф или выбранный подграф с продолжением

| Параметр                       | Назначение                                                            |
| ------------------------------ | --------------------------------------------------------------------- |
| `--root <address>`             | Ключ или ID корня; без него весь проект                               |
| `--type <type>`                | Фильтр типа отношений                                                 |
| `--direction <direction>`      | both, outgoing или incoming                                           |
| `--profile <profile>`          | all — полный обход; context — совместимое имя all                     |
| `--depth <n>`                  | Глубина обхода 0–100                                                  |
| `--q <text>`                   | Поиск сущностей по ключу, адресу и названию                           |
| `--offset <n>`                 | Смещение страницы                                                     |
| `--limit <n>`                  | Размер страницы 1–100                                                 |
| `--snapshot-version <version>` | Версия первой страницы графа; отличается от глобального --version CLI |

## graph context

**Синтаксис:** `relay-cli graph context [options] <root>`.

Восстановить окружение сущности с объясняющими путями

| Аргумент | Назначение                                      |
| -------- | ----------------------------------------------- |
| `root`   | Ключ или ID исходной сущности; допустим kind:ID |

| Параметр                       | Назначение                                                            |
| ------------------------------ | --------------------------------------------------------------------- |
| `--type <type>`                | Фильтр типа отношений                                                 |
| `--direction <direction>`      | both, outgoing или incoming                                           |
| `--profile <profile>`          | all — полный обход; context — совместимое имя all                     |
| `--depth <n>`                  | Глубина обхода 0–100                                                  |
| `--q <text>`                   | Поиск сущностей по ключу, адресу и названию                           |
| `--offset <n>`                 | Смещение страницы                                                     |
| `--limit <n>`                  | Размер страницы 1–100                                                 |
| `--snapshot-version <version>` | Версия первой страницы графа; отличается от глобального --version CLI |

## graph history

**Синтаксис:** `relay-cli graph history [options]`.

Прочитать историю установленных и отозванных отношений

| Параметр         | Назначение                      |
| ---------------- | ------------------------------- |
| `--id <id>`      | ID отношения                    |
| `--offset <n>`   | Смещение                        |
| `--limit <n>`    | Размер страницы                 |
| `--revision <n>` | Ревизия журнала первой страницы |

## graph link

**Синтаксис:** `relay-cli graph link [options]`.

Установить произвольную направленную связь

| Параметр                   | Назначение                                          |
| -------------------------- | --------------------------------------------------- |
| `--if-version <version>`   | Версия из graph list/context                        |
| `--request-id <id>`        | Ключ безопасного повтора; по умолчанию генерируется |
| `--from <address>`         | Ключ или ID начала связи                            |
| `--to <address>`           | Ключ или ID конца связи                             |
| `--type <type>`            | Произвольный тип отношения                          |
| `--description <markdown>` | Пояснение назначения связи в Markdown               |

## graph update

**Синтаксис:** `relay-cli graph update [options] <id>`.

Изменить пояснение связи

| Аргумент | Назначение                       |
| -------- | -------------------------------- |
| `id`     | ID явно установленного отношения |

| Параметр                   | Назначение                                          |
| -------------------------- | --------------------------------------------------- |
| `--if-version <version>`   | Версия из graph list/context                        |
| `--request-id <id>`        | Ключ безопасного повтора; по умолчанию генерируется |
| `--description <markdown>` | Пояснение назначения связи в Markdown               |

## graph unlink

**Синтаксис:** `relay-cli graph unlink [options] <id>`.

Отозвать связь с сохранением истории

| Аргумент | Назначение                       |
| -------- | -------------------------------- |
| `id`     | ID явно установленного отношения |

| Параметр                 | Назначение                                          |
| ------------------------ | --------------------------------------------------- |
| `--if-version <version>` | Версия из graph list/context                        |
| `--request-id <id>`      | Ключ безопасного повтора; по умолчанию генерируется |

## graph apply

**Синтаксис:** `relay-cli graph apply [options]`.

Применить атомарный пакет изменений

| Параметр                 | Назначение                                          |
| ------------------------ | --------------------------------------------------- |
| `--if-version <version>` | Версия из graph list/context                        |
| `--request-id <id>`      | Ключ безопасного повтора; по умолчанию генерируется |
| `--json <json>`          | Массив операций add/update/remove, максимум 100     |

## entities types

**Синтаксис:** `relay-cli entities types [options]`.

Перечислить виды сущностей и их назначение

| Параметр                       | Назначение                                                |
| ------------------------------ | --------------------------------------------------------- |
| `--limit <n>`                  | Размер страницы 1–100                                     |
| `--offset <n>`                 | Смещение страницы                                         |
| `--snapshot-version <version>` | Версия первой страницы; при изменении перечитайте выборку |

## entities type

**Синтаксис:** `relay-cli entities type [options] <kind>`.

Прочитать контракт вида, поля, фильтры и правила ключей

| Аргумент | Назначение                                     |
| -------- | ---------------------------------------------- |
| `kind`   | Вид, например task, feature или implementation |

## entities list

**Синтаксис:** `relay-cli entities list [options]`.

Найти краткие карточки сущностей

| Параметр                       | Назначение                                                |
| ------------------------------ | --------------------------------------------------------- |
| `--limit <n>`                  | Размер страницы 1–100                                     |
| `--offset <n>`                 | Смещение страницы                                         |
| `--snapshot-version <version>` | Версия первой страницы; при изменении перечитайте выборку |
| `--kind <kind>`                | Вид сущностей                                             |
| `--q <text>`                   | Поиск по ключам, ID, названию и краткому описанию         |
| `--refs <refs...>`             | Ключи или ID выбранных сущностей                          |
| `--board <ref>`                | Доска задач: ключ или ID                                  |
| `--application <ref>`          | Приложение: ключ или ID                                   |
| `--feature <ref>`              | Фича: ключ или ID                                         |
| `--scenario <ref>`             | Сценарий: ключ или ID                                     |
| `--target <ref>`               | Явная продуктовая цель: ключ или ID                       |
| `--parent <ref>`               | Родитель задачи: ключ или ID                              |
| `--status <status>`            | Предметное состояние                                      |
| `--active <value>`             | Участие реализации: true или false                        |
| `--section <id>`               | Раздел документов; none — без раздела                     |
| `--document-kind <kind>`       | Тип документа                                             |
| `--pinned <value>`             | Закрепление документа: true или false                     |
| `--archived <value>`           | Только архив либо исключить архив: true или false         |
| `--sort <field>`               | Сортировка: key, title или updated                        |

## entities get

**Синтаксис:** `relay-cli entities get [options] <ref>`.

Прочитать все данные одной сущности

| Аргумент | Назначение                             |
| -------- | -------------------------------------- |
| `ref`    | Ключ или ID сущности; допустим kind:ID |

| Параметр        | Назначение             |
| --------------- | ---------------------- |
| `--kind <kind>` | Уточнить ожидаемый вид |

## entities resolve

**Синтаксис:** `relay-cli entities resolve [options] <ref>`.

Разрешить ключ или ID в постоянный адрес

| Аргумент | Назначение                             |
| -------- | -------------------------------------- |
| `ref`    | Ключ или ID сущности; допустим kind:ID |

| Параметр        | Назначение             |
| --------------- | ---------------------- |
| `--kind <kind>` | Уточнить ожидаемый вид |

## entities keys

**Синтаксис:** `relay-cli entities keys [options] <ref>`.

Прочитать текущий ключ и прежние алиасы

| Аргумент | Назначение           |
| -------- | -------------------- |
| `ref`    | Ключ или ID сущности |

| Параметр                       | Назначение                                                |
| ------------------------------ | --------------------------------------------------------- |
| `--limit <n>`                  | Размер страницы 1–100                                     |
| `--offset <n>`                 | Смещение страницы                                         |
| `--snapshot-version <version>` | Версия первой страницы; при изменении перечитайте выборку |

## entities history

**Синтаксис:** `relay-cli entities history [options] <ref>`.

Прочитать сохранённые события ревизий

| Аргумент | Назначение           |
| -------- | -------------------- |
| `ref`    | Ключ или ID сущности |

| Параметр                       | Назначение                                                |
| ------------------------------ | --------------------------------------------------------- |
| `--limit <n>`                  | Размер страницы 1–100                                     |
| `--offset <n>`                 | Смещение страницы                                         |
| `--snapshot-version <version>` | Версия первой страницы; при изменении перечитайте выборку |

## entities key-spaces

**Синтаксис:** `relay-cli entities key-spaces [options] <kind>`.

Перечислить актуальные области нумерации и префиксы

| Аргумент | Назначение   |
| -------- | ------------ |
| `kind`   | Вид сущности |

| Параметр                       | Назначение                                                |
| ------------------------------ | --------------------------------------------------------- |
| `--limit <n>`                  | Размер страницы 1–100                                     |
| `--offset <n>`                 | Смещение страницы                                         |
| `--snapshot-version <version>` | Версия первой страницы; при изменении перечитайте выборку |

## entities create

**Синтаксис:** `relay-cli entities create [options] <kind>`.

Создать сущность и её обязательные связи

| Аргумент | Назначение                                                                                  |
| -------- | ------------------------------------------------------------------------------------------- |
| `kind`   | Создаваемый вид: product, feature, scenario, application, implementation, task или document |

| Параметр                     | Назначение                                                                                  |
| ---------------------------- | ------------------------------------------------------------------------------------------- |
| `--name <text>`              | Однострочное название продукта, фичи, сценария, приложения или документа                    |
| `--title <text>`             | Однострочный заголовок задачи или реализации                                                |
| `--summary <text>`           | Краткое многострочное обычное описание                                                      |
| `--description <markdown>`   | Полное описание в Markdown                                                                  |
| `--body <markdown>`          | Полный Markdown документа                                                                   |
| `--feature <ref>`            | Родительская фича сценария: ключ или ID                                                     |
| `--application <ref>`        | Приложение реализации: ключ или ID                                                          |
| `--target <ref>`             | Фича или сценарий реализации: ключ или ID                                                   |
| `--board <ref>`              | Доска задачи: ключ или ID                                                                   |
| `--slug <slug>`              | Адрес создаваемого приложения                                                               |
| `--prefix <prefix>`          | Префикс задач приложения                                                                    |
| `--type <type>`              | Тип приложения: frontend, backend или internal                                              |
| `--document-kind <kind>`     | Тип документа: specification, description, rules, instruction, proposal, decision, research |
| `--document-status <state>`  | Состояние документа: draft, active или archived                                             |
| `--section-id <id>`          | Постоянный ID раздела библиотеки                                                            |
| `--clear-section`            | Оставить документ без раздела                                                               |
| `--pinned <value>`           | Закрепление документа: true или false                                                       |
| `--relations <json>`         | Массив связей документа: target {kind,id}, type references/documents, description Markdown  |
| `--document-sections <json>` | Упорядоченный массив разделов проекта: id и name                                            |
| `--status <status>`          | Состояние реализации: none, partial или done                                                |
| `--column <column>`          | Начальная колонка задачи                                                                    |
| `--parent <ref>`             | Родитель задачи: ключ или ID                                                                |
| `--targets <refs...>`        | Продуктовые цели задачи или документа: ключи или ID                                         |
| `--dependencies <refs...>`   | Зависимости создаваемой задачи: ключи или ID                                                |
| `--related <refs...>`        | Связанные задачи: ключи или ID                                                              |
| `--json <json>`              | Дополнительные типизированные поля объекта; явные параметры имеют приоритет                 |
| `--request-id <id>`          | Ключ безопасного повтора; по умолчанию генерируется                                         |

## entities update

**Синтаксис:** `relay-cli entities update [options] <ref>`.

Изменить поля сущности с проверкой ревизии

| Аргумент | Назначение                      |
| -------- | ------------------------------- |
| `ref`    | Ключ или ID изменяемой сущности |

| Параметр                     | Назначение                                                                                  |
| ---------------------------- | ------------------------------------------------------------------------------------------- |
| `--name <text>`              | Однострочное название продукта, фичи, сценария, приложения или документа                    |
| `--title <text>`             | Однострочный заголовок задачи или реализации                                                |
| `--summary <text>`           | Краткое многострочное обычное описание                                                      |
| `--description <markdown>`   | Полное описание в Markdown                                                                  |
| `--body <markdown>`          | Полный Markdown документа                                                                   |
| `--feature <ref>`            | Родительская фича сценария: ключ или ID                                                     |
| `--application <ref>`        | Приложение реализации: ключ или ID                                                          |
| `--target <ref>`             | Фича или сценарий реализации: ключ или ID                                                   |
| `--board <ref>`              | Доска задачи: ключ или ID                                                                   |
| `--slug <slug>`              | Адрес создаваемого приложения                                                               |
| `--prefix <prefix>`          | Префикс задач приложения                                                                    |
| `--type <type>`              | Тип приложения: frontend, backend или internal                                              |
| `--document-kind <kind>`     | Тип документа: specification, description, rules, instruction, proposal, decision, research |
| `--document-status <state>`  | Состояние документа: draft, active или archived                                             |
| `--section-id <id>`          | Постоянный ID раздела библиотеки                                                            |
| `--clear-section`            | Оставить документ без раздела                                                               |
| `--pinned <value>`           | Закрепление документа: true или false                                                       |
| `--relations <json>`         | Массив связей документа: target {kind,id}, type references/documents, description Markdown  |
| `--document-sections <json>` | Упорядоченный массив разделов проекта: id и name                                            |
| `--status <status>`          | Состояние реализации: none, partial или done                                                |
| `--column <column>`          | Начальная колонка задачи                                                                    |
| `--parent <ref>`             | Родитель задачи: ключ или ID                                                                |
| `--targets <refs...>`        | Продуктовые цели задачи или документа: ключи или ID                                         |
| `--dependencies <refs...>`   | Зависимости создаваемой задачи: ключи или ID                                                |
| `--related <refs...>`        | Связанные задачи: ключи или ID                                                              |
| `--json <json>`              | Дополнительные типизированные поля объекта; явные параметры имеют приоритет                 |
| `--request-id <id>`          | Ключ безопасного повтора; по умолчанию генерируется                                         |
| `--if-revision <n>`          | Прочитанная ревизия записи                                                                  |

## entities rename

**Синтаксис:** `relay-cli entities rename [options] <ref> <key>`.

Изменить ключ, сохранив ID, отношения и алиасы

| Аргумент | Назначение                                |
| -------- | ----------------------------------------- |
| `ref`    | Ключ или ID сущности                      |
| `key`    | Новый читаемый ключ, например TASK-WEB-23 |

| Параметр            | Назначение               |
| ------------------- | ------------------------ |
| `--if-revision <n>` | Прочитанная ревизия      |
| `--request-id <id>` | Ключ безопасного повтора |

## entities move-task

**Синтаксис:** `relay-cli entities move-task [options] <ref>`.

Переместить задачу в колонку или на другую доску

| Аргумент | Назначение         |
| -------- | ------------------ |
| `ref`    | Ключ или ID задачи |

| Параметр            | Назначение                   |
| ------------------- | ---------------------------- |
| `--column <column>` | Целевая колонка              |
| `--board <ref>`     | Ключ или ID целевой доски    |
| `--before <ref>`    | Ключ или ID следующей задачи |
| `--if-revision <n>` | Прочитанная ревизия          |
| `--request-id <id>` | Ключ безопасного повтора     |

## entities link-task

**Синтаксис:** `relay-cli entities link-task [options] <ref> <target>`.

Установить или снять предметную связь задач

| Аргумент | Назначение                 |
| -------- | -------------------------- |
| `ref`    | Ключ или ID текущей задачи |
| `target` | Ключ или ID второй задачи  |

| Параметр                | Назначение                     |
| ----------------------- | ------------------------------ |
| `--relation <relation>` | depends-on, related или parent |
| `--remove`              | Удалить выбранную связь        |
| `--if-revision <n>`     | Прочитанная ревизия            |
| `--request-id <id>`     | Ключ безопасного повтора       |
